# CG-20260919-011 线内独立只读复核报告（B 级）

- 复核批次：**CG-20260919-011**（承接 §11.2 台账 **PL-026**／**PL-027**）
- 复核角色：prefab-standards-reviewer（发起人线内调用的独立只读复核代理）
- 复核日期：2026-09-19
- 被复核改动：`standards-index.md` 头部「最后更新」行回填为 CG-20260919-011（原 CG-20260918-004 行降级为历史快照链）＋ §7.1 :297「替代关系」「影响范围」两列对齐 :296／§三 从属摘要基准 ＋ §八 本批一行；`change-governance.md` §九 新增 -011 行、头部降级、§11.2 PL-026／PL-027 转已闭合；`CHANGELOG.md` v1.12.35。索引五处副本＋治理件三份镜像带平
- 定级：**B 级**（§一「修正错误、不影响安全性的常规变更」＋§4.1 standards-index 名称逐字修正同族；零触标准数值／状态字段／替代关系语义／实施日期／收录总数 69 条，故不升 A；发起人开工前确认「B 级并放行」）
- 取证环境：仓 `D:\Qoder-Files\装配式装修技能开发`（`origin/master`）＋ 运行时根 `C:\Users\<user>\.qoder\skills`
- 授权边界：本线仅本地编辑＋实跑验证，**不含提交与推送**；git commit 须经用户明文指令并点明范围，push 须另发明文指令

---

## 结论：**PASS-with-OBS**

七项逐条核验全部 **PASS**，无 FAIL。登记面（PL-026／PL-027 转已闭合、§九 -011 唯一新行、头部链降级无损、三份镜像带平）、内容面（:297 两列对齐 :296 从属基准、其余列/其余节/收录总数 69/运行时脚本零改动）、判定依据（(c) 的 no-op 否证与「不 fabricate 核验」前提）均经独立现算复核成立。OBS 五条见下，其中 OBS-1（独立性受限）为该类线内复核的固有约束。

---

## ① PL-026／PL-027 收口正确性 — PASS

`git diff -U0 -- _专题_技能合集策划/change-governance.md` 仅 3 个 hunk（`@@ -7 +7,2 @@`、`@@ -373,0 +375 @@`、`@@ -438,2 +440,2 @@`）。逐格比对（HEAD 版 vs 工作区版，7 列）：PL-026／PL-027 的**编号／事项／来源登记／登记日期／到期日五列 byte-identical**（事项 154／228 字、来源 27／30 字、日期与到期日全等），状态列 `待处置 → 已闭合`，处置依据列纯末尾追加（`new.strip().startswith(old.strip())` 前缀全等，153→374 字、173→615 字）。台账状态分布 HEAD `待处置 7／已闭合 19` → 工作区 `待处置 5／已闭合 21`，恰翻转 2 行；检查 7 实跑同值。

## ② §九 新行结构 — PASS

`change-governance.md:374`＝`CG-20260919-010`、`:375`＝`CG-20260919-011`（紧邻其后，均在 §九 区间内）。ASCII 竖线计数 7（6 列），与表头及 -010 行一致；单元内竖线用全角｜（375 行 4 个 U+FF5C、0 个多余 ASCII 竖线）。`CG-20260919-011` 在 §九 内唯一：`git show HEAD:...` grep 计数 = 0、`git log -S"CG-20260919-011"` 为空 → 未被历史占用。§九 104 条 CG 行 ID 去重零重复。

## ③ 头部回填 — PASS

`standards-index.md:7`＝`> **最后更新**：2026-09-19（CG-20260919-011，**B 级**…）`；`:9`＝`> **此前更新（历史快照）**：2026-09-18（CG-20260918-004，**A 级**…`。内容零丢失：新版 :9 前缀逐字等于 HEAD :7 去前缀正文（1457 字），HEAD :9（`CG-20260917-009` 链）仍包含于其后。:1-6、:8、:10-13 与 HEAD 逐行全等（含 :11 `收录标准总数：69 条`）。`change-governance.md` 头部同理（-010 那条原文降级为历史快照，未改写）。

## ④ §7.1 :296／:297 内容面 — PASS

`:296` 整行与 HEAD byte-identical（本批零改动）。`:297`：cell3 `新增` → `新增（无直接替代旧标准）`（与 :296 cell3 全等）；cell4 `耐火试验结果应用指南` → `隔墙/吊顶耐火试验结果应用指南`（基准 :296 cell4 `隔墙/吊顶耐火试验方法`）；cell0 编号、cell1 名称、cell2 实施日期 `2026-10-01`、cell5 建议行动四格与 HEAD 全等。两行仍 6 列。名称格为上一批 CG-20260919-010 所改，本批 diff 未触。

## ⑤ 跨副本互等（原始字节直读、无 eol 归一） — PASS

- `standards-index.md` 五处（`_专题_技能合集策划/`＋运行时根＋运行时 `shared/`＋`技能仓备份/` 根＋`技能仓备份/shared/`）：全部 `3f439768513649d6`，95,909 B，**CR=0**（LF 446）。
- `change-governance.md` 三处（L1＋运行时 `shared/`＋`技能仓备份/shared/`）：全部 `135bb77b07a57169`，610,286 B，**CR=0**（LF 453）。
- HEAD 前像 CR 亦 0 → 检查 6 (C)「纯 LF」断言与独立计数一致。副本集合与 `sync_skill_backup.py` 的 `ROOT_FILES`＋`SHARED_FILES` 活口全集一致；门禁旁证 validate (A) 段同值。
- 控制台 GBK 乱码字形已按要求排除，仅取哈希/计数结构判读。

## ⑥ 独立否证 PL-027 (c) 断言 — PASS（本批「不改动 §二 :188 区间」判定成立）

静态读路（不采信自述）：`validate_governance.py` 检查 3 取数按列名「标准编号」＋`核验日期/核验状态`（`i_vid = find_col(ID_COL_ALIASES)`），**序号列从不作值消费**，且 `STD_ID_SHAPE` 过滤使 `"11-13"` 与「多项推荐性/…」编号格皆不入台账；核验记录表整表 `in_main_table=False`。`sre_reasoner.py` `_classify_verify_table`／`_load_standards_index` 仅取 ID／核验日期／核验状态三列名，序号列根本不读。目标行唯一定位：全文件仅 `standards-index.md:188` 序号格为区间形。

非破坏内存差分实测（未写盘）：`check_standards()` 基线 PASS/WARN/FAIL = (2,1,0)；改序号格为任意值、乃至整行删除，仍 (2,1,0) 且 std_id 集合与 names 字典全等；唯把**编号格**改成真实标准号才扰动消费面。SRE `STANDARD_VERIFY` 基线 29 键，序号格编辑后全等。**不 fabricate 前提独立成立**：§三 :142/:143 状态列＝`即将实施`，两编号在 `STANDARD_VERIFY` 中不存在核验记录，故把 :188 区间改为「已核验」将虚报未做的核验。→ 改 :188 区间文本对检查 3 通过/警告数与 SRE 载入均为严格 no-op，PL-027 依据列「区间联动检查 3 计数」经实证为**假**，本批「不改动 :188 并在依据列据实否证」处置成立。

## ⑦ 不动面自证 — PASS

`git diff --numstat`（相对 HEAD `fff0e02`）：本批改动＝CHANGELOG（+12/0）、change-governance.md（L1＋镜像，5/3）、standards-index.md（L1＋两处镜像，4/3）。difflib 对齐实测 `standards-index.md` 真实改动仅 4 处（replace 7、replace 9、replace 297、insert 357），`change-governance.md` 仅 3 处。据此 §三 主表、核验记录表与 :188 聚合行、§7.1 其余列、收录总数 69（:11）、§八／§九 全部历史行、§11.2 其余 25 行零改动；`程序文件/` 全目录 git-clean（`validate_governance.py` 未改，版本面 v1.11.0）；运行时 `sre_reasoner.py` 与仓内被跟踪副本字节全等、mtime 早于本批写入窗 → 本批未触。

## ⑧ 七门禁独立复跑（只读）

validate `PASS｜通过 225｜失败 0｜警告 1｜exit 0`；verification_policy `Ran 6 OK`；ace `Ran 90 OK`；pending_ledger `Ran 39 OK`；cross_layer_eol `Ran 25 OK`；sre `Ran 66 OK / exit 0 / 缺口信号 0 / 跨轮指纹 4f53cda18c2baa0c 不漂移`；ic07 `Ran 20 OK`。七者 exit 全 0。收尾 `sync_skill_backup.py` 判据「范围 122＋新增 0＋移除 0」。

---

## OBS（独立复核固有局限与本轮观察）

- **OBS-1｜独立性受限（必示）**：本复核与发起人共享同一工作区真值（`_专题_技能合集策划/` 即写面本身，`~/.qoder/skills/` 与 `技能仓备份/` 皆其镜像），「副本互等」只证明**同步无漂移**，不证明内容外部为真；亦无法接触 openstd／std.samr 官方渠道，故 :296/:297 措辞、「§7.1 属 §三 从属摘要表」的裁定基准、以及 -009/-010 名称逐字凭据均沿用发起人裁定，本代理仅复算其内部一致性。
- **OBS-2｜报告前向引用（随本报告落盘消解）**：§九 -011 行、`:7` 头部与 CHANGELOG v1.12.35 均把本文件作为「PASS-with-OBS」凭据引用；报告落盘前该引用是悬空指针，落盘后可见为 `记录/` 未跟踪文件（`记录/` 未被 .gitignore 覆盖）。属复核后置写法，非虚假。
- **OBS-3｜§ 标号与行锚称谓滑移**：`:188` 聚合行物理位于 **§三**「推荐性标准抽查核验记录」表（表头 :178、数据 :180-188），治理散文（PL-027 原述 → -011 行 → CHANGELOG）一律称「§二 :188」。因 :188 序号格区间形在全文件唯一，目标无歧义；PL-027 原述属快照不可改写，滑移由前批继承。**建议后续批在新增文字里统一为「§三 抽查核验记录表 :188」**——本批 :188 未改动，故不属本批写面，仅登记为观察。
- **OBS-4｜PL-027 三合一行的闭合粒度**：(a)(b) 实改、(c) 明写「不改动、据实记账」，而整行状态翻为 `已闭合`。按 §11.1 三值枚举，(c) 更接近「已裁定不做」或另拆一条 PL 承载；现行做法合规（依据列非空、检查 7 绿），但因 §11.3 第 3 条声明不做「§九 散文遗留 → 台账」反查，(c) 的口径滞后此后只在散文里可发现、`待处置` 计数不再提示，存在「闭合掩盖残余」的复发面。
- **OBS-5｜只读运行的写入侧限制**：为守只读，未运行 `sre_regression_test.py` 主入口（避免覆写机器本地 `sre_regression_report.json`），其 66/0/指纹由内存内复刻同段代码路径所得（同值，不含落盘 `generated_at` 侧链）；只读门禁刷新 `程序文件/__pycache__/*.pyc`（已 gitignore、非治理件），未创建/修改任何 `.md` 或脚本。

---

## 处置裁定

七项全 PASS，结论 PASS-with-OBS，放行成立。OBS-3（称谓滑移）与 OBS-4（(c) 闭合粒度）均为**观察**而非本批写面缺陷：OBS-3 因 :188 本批不改而落为后续批新增文字的统一建议，OBS-4 属台账粒度取舍、当前合规。二者均**未据此新开 PL**（若要追办须另行申请登记，不属本线授权）。本批提交／推送待用户明文指令。
