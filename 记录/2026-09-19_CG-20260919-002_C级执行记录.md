# CG-20260919-002（C 级）执行记录 — §九 三条已提交历史行形制订正 ＋ PL-016／PL-019 闭合 ＋ PL-021 新登记

- 批次：`CG-20260919-002`｜日期：2026-09-19｜定级：**C 级**
- 仓库：`D:\Qoder-Files\装配式装修技能开发`（L1 开发源）
- 开线 brief 中的 **P1** 项（PL-016 ＋ PL-019）即本批；与 v1.12.26（`CG-20260919-001`，改校验器检查面＝B 级）**不混批**

---

## 一、定级依据与授权

| 判据 | 取值 |
|---|---|
| §一定义表 | 「环境路径／BOM／换行与形制类修正属 C 级」 |
| §二 第 3 步 | C 级＝发起人直接执行并登记、无需复核，但须记录变更日志（本文件＋§九行＋CHANGELOG 即日志载体） |
| 先例 | `CG-20260918-006`／`-007`／`-008`（同为登记面形制与状态句订正，不触校验器代码、不触运行时技能文件、不触标准数值） |
| 前置承接 | `PL-016` 登记时自设前置「断行修复属 C 级换行类修正，但会改历史行字节，故须另批裁定」——本批即该另批 |
| 授权 | 用户 2026-09-19「按你建议在本线继续跑」（路径 A 为本线向用户明示后采信的裁定路径） |
| 边界 | 本线只做本地文件编辑与实跑验证；commit 须明文授权并点明范围、push 须另发明文指令（选项式或单词授权不算） |

**一处自觉披露**：本批对 `程序文件/validate_governance.py` 有字节改动，但改动面是 `collect_cg_log_ids()` 的 docstring 文字，属**说明句**不属**检查面**。以 AST 比较自证（见 §四 取证 6）：剥 docstring 后前后 AST 逐字相同、`compile()` 通过。若采更严格口径（C 级批零触校验器文件），该一句可拆入下批，本批选择同批处理以免登记面留过期陈述（PL-018 同族失效模式）。

---

## 二、开工现查（全部当场实跑，不采信转述）

| 项 | 读数 |
|---|---|
| `git log --oneline -1` | `cc78ba5` |
| `git status --porcelain` | 8 件（6 M ＋ 2 ??），全属 `CG-20260919-001` 的未提交态，本批不并入其提交叙事 |
| `git rev-list --left-right --count HEAD...origin/master` | 领先 2 ／ 落后 0 |
| `validate_governance.py`（开场） | PASS｜通过 225｜失败 0｜警告 1｜exit 0 |
| §九 形态（订正前实读） | 表级可见数据行 **72**（止于 `CG-20260916-008`）／逐行首列读法 **94** 个编号／异常行 5 处（:274 列数 7、:331 列数 4、:332 非管行、:337 列数 14、:356 系节末 `---` 属正常） |

---

## 三、三处订正的取证与判据

### 1）`CG-20260916-008` 断行合并（PL-016）

**根因追溯（结论与登记面原述不同）**：`git log --reverse -- _专题_技能合集策划/change-governance.md` 得 74 个触及该文件的提交，逐提交 `git cat-file blob <sha>:<path>` 取原始字节、检索锚串「字节 802」：

- 自该 CG 行**首次入库的提交 `f3271db`** 起，该处字节序列即为「反引号 ＋ 真实 LF（`0x0A`） ＋ 反引号」；
- 即断行**不是**登记后被脚本、换行翻译或子代理改坏，而是登记时把「行内码内的分隔符」写成了真实换行字节；
- 订正＝两物理行并一，并把该 LF 改写为其两字符字面（反斜杠加 n）。语义与原句一致：该行同句自证「CR 字节 802 → 按换行符切得 803 段＝表头 1 ＋ 数据 801 ＋ 末尾空段 1」。

**判据**：合并后列数 6（订前 4 ＋ 3）、反引号计数 30（偶）、`"``"` 不出现、竖线总数＝订前两行之和。

### 2）`CG-20260806-005` 行尾多余分隔竖线（PL-019 的一半，**原述不成立**）

- 该行**行内码 0 处**（反引号计数 0），故 PL-019 原述「1 处未转义竖线」不成立；
- 实测末两字符为「空格 ＋ 竖线」，即 `… | 已发布 | |` —— 多打了一个**分隔符**；
- 删除末两字符即列数 7 → 6，竖线总数 8 → 7，**与内容字面无关**（不是转义、不是改写）。

### 3）`CG-20260917-003` 行内码内 8 处裸竖线转义（PL-019 的另一半）

- 竖线总数 13：5 处为列分隔符（行首、三处列间、列间 2 处），8 处在行内码内（正则字面 `(?=\d|\b)` 一族的交替运算符）；
- 逐字符扫描、按反引号翻转码内／码外状态，仅在码内前置反斜杠 → 转义 8 处，列数 14 → 6；
- **保真判据**：`订正后.replace("\\|", "|") == 订正前` 逐字符全等。

**据实披露（不可兼得面）**：GFM 在行内码内**不剥反斜杠**，故该 8 处码内字面现在渲染为带反斜杠形态。取舍＝取「列形正确」（否则按列位取数会把「状态」列错读到内容中段），弃「码内逐字」，已写入 PL-019 处置依据列。

---

## 四、写入面清单（含未触碰项）

| # | 文件 | 写入面 | 取证 |
|---|---|---|---|
| 1 | `_专题_技能合集策划/change-governance.md` | §九 三条历史行订正（净 −1 物理行） | `p1_apply_002.py`／`p1_verify_002.py`（fid1／fid2／fid3 ＝ True） |
| 2 | 同上 | §九 新增 `CG-20260919-002` 一行（6 列、单元内零竖线） | `p1_register_002.py` 锚点唯一＋列数断言 |
| 3 | 同上 | 头部快照链：新增本批「最后更新」，原 `-001` 行降级「此前更新（历史快照）」 | 同上 |
| 4 | 同上 | §11.2 PL-016／PL-019 → 已闭合（事项／来源／日期列零改写，订正写入依据列）＋ PL-021 新登记 | 同上（台账每行竖线数＝8、`md_cells` 7 列） |
| 5 | 同上 | 运行时镜像 `~/.qoder/skills/shared/` ＋ 仓内镜像 `技能仓备份/shared/` 带平 | 三份 sha256 前 16 互等 |
| 6 | `程序文件/validate_governance.py` | 仅 `collect_cg_log_ids()` docstring 一句 | 剥 docstring 后 AST 逐字相同 |
| 7 | `CHANGELOG.md` | 新增 `## v1.12.27` 节（①—⑩） | 锚点 `## v1.12.26 — 2026-09-19` 唯一命中 |
| 8 | `AGENTS.md` | 门禁表内「§九 断行」描述句按事实刷新（活基线头不刷新，读数未变） | 裸竖线总数不变＝50（表格结构未破） |
| 9 | `记录/2026-09-19_CG-20260919-002_C级执行记录.md` | 本文件（新增） | CR 计数见文末自检 |

**未触及**：`standards-index.md` 及其五处副本、任何技能本体（`SKILL.md`／`reference.md`／`examples.md`／`scripts`）、运行时 `sre_reasoner.py`、`sync_skill_backup.py`（PL-011＝P3）、`md_cells()` 的切格行为（PL-021 另批）、台账其余 19 行、§九 其余 91 行、`程序文件/` 其余脚本。

---

## 五、终态读数（全部当场实跑）

### 1）治理件形态

| 项 | 读数 |
|---|---|
| `change-governance.md` 三份互等（L1／运行时 shared／仓内 `技能仓备份/shared`） | sha256 前 16 ＝ `096d0f73be86d49a`／527,782 B／0 个 CR（配方＝对文件原始字节直读，无 eol 归一） |
| §九 数据行（**表级解析** `iter_md_tables`） | **95**（订正前 72） |
| §九 数据行（**逐行首列** `collect_cg_log_ids`） | **95** —— 两口径自本批起归一（此前差 22） |
| §九 转义感知列数非 6 的行 | **0**；§九 内非表非空非分隔行 **0** |
| §11.2 台账 | 21 行＝待处置 10／已闭合 10／已裁定不做 1；列数集合 [7]；编号唯一 |
| `validate_governance.py` | sha256 前 16 ＝ `91bbaa78453f69ef`／100,744 B／0 个 CR |

### 2）门禁（终态复跑，`CHANGELOG.md`／`AGENTS.md` 写入之后，逐条当场取 exit 码）

| 命令 | exit | 读数 |
|---|---|---|
| `python -B 程序文件/validate_governance.py` | 0 | 总评 **PASS**｜通过 **225**｜失败 **0**｜警告 **1**——与 v1.12.26 终态逐位同值（警告仍＝检查 3 的 38 条无有效核验日期聚合 WARN，不计 `fail_count`）；检查 7 键闭合真值源实读 **95** 个 |
| `python -B 程序文件/verification_policy_test.py` | 0 | Ran **6** tests，OK |
| `python -B 程序文件/ace_regression_test.py` | 0 | Ran **90** tests，OK |
| `python -B 程序文件/pending_ledger_test.py` | 0 | Ran **36** tests，OK（含真实仓 clean 态断言与 PL-016 断行免疫守卫） |
| `python -B 程序文件/cross_layer_eol_test.py` | 0 | Ran **25** tests，OK |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py` | 0 | Ran **66** tests，OK；缺口信号 0 条、跨轮指纹 `4f53cda18c2baa0c` |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py` | 0 | Ran **20** tests，OK |

**七门禁全绿且 validate 读数与上批逐位同值 → `AGENTS.md` 活基线头（`CG-20260919-001`／v1.10.0／225）不刷新**，只按事实订正其内文关于 §九 断行的描述句。

### 3）`sync_skill_backup.py`

「范围 **122**｜一致 121｜新增 **0**｜更新 1｜移除 **0**」——唯一更新件＝机器本地 `prefab-standards-reviewer/sre_regression_report.json`（本批复跑 SRE 门禁所致）。**判据「范围 122 ＋ 新增 0 ＋ 移除 0」满足**。首跑前实测两层 `__pycache__/*.pyc` 计 0 个，无需手工清理（PL-011 缺口本批未触发，仍待处置）。

---

## 六、PL-021（本批新发现，取证非推测）

- 事实：`md_cells()` 以 `line.strip().strip("|").split("|")` 切格，**不认转义**；
- 冲突：本仓登记纪律要求「表格单元内裸竖线一律写成转义形」，该写法用于 §11.2 台账单元即被检查 7 判「行列数 8 ≠ 表头列数 7」，并连带状态列／日期列错位的两条 FAIL；
- 取证法：合成文本内存内直调 `check_pending_ledger`（借用 `pending_ledger_test` 的 `doc/row/run`），不落盘、不改治理件——对照组（单元内零竖线）fail_count＝0，注入组（单元内写转义竖线）fail_count＝3；
- 现态：**未触发**（终态台账 21 行单元内零转义竖线）；
- 前瞻性：`change-governance.md` §九 自本批起**已含**转义竖线（`CG-20260917-003` 行），故将来若给 §九 加列数机算守卫，须先解本项，否则合法转义会反成 FAIL；
- 修复二选一（须另批裁定并各自定级）：解析器改转义感知＝触校验器行为面＝**B 级**；或纪律措辞改为「台账单元内禁写任何竖线（含转义形）」＝**C 级**。

---

## 七、OBS 与结论

| 编号 | 观察 | 定性 |
|---|---|---|
| OBS-1 | 本批为 C 级，依 §二 第 3 步**不经复核**，全部关键读数由线内脚本自证（脚本列于 §四取证列），**未启用独立只读子代理** | 与定级一致，非偏离；若后续要求 B 级以上复核，独立性受限这一点须显式登记 |
| OBS-2 | 登记面 PL-016／PL-019 的**原述两处不实**（「未转义竖线」定性、『仅渲染缺陷』定性），本批按快照纪律保留原述、把订正写入处置依据列 | 已订正，属他批登记质量而非本批引入 |
| OBS-3 | `CG-20260916-008` 断行自**首个提交**即存在 → 「历史 CG 行是当时事实快照」这条纪律在此案中不阻止订正：断行是**载体缺陷**不是**事实陈述**，订正不改写任何事实 | 据此判 A 路径成立，与用户质询后的复核结论一致 |
| OBS-4 | 本批改了校验器文件一处 docstring（见 §一 披露） | 行为面零改动已机算自证；更严格口径下可拆批 |
| OBS-5 | 开线 brief 记「台账开场 20 行：待处置 12／已闭合 7／已裁定不做 1」，与本批反推的开工态（11／8／1）差 1 行 | 差值＝`CG-20260919-001` 在 P0 批闭合 PL-020 所致；两处不作同值引用，本批 CHANGELOG 不写未实测的开工台账分布 |

**结论：PASS。** 三处形制订正落盘且逐条保真自证；PL-016／PL-019 闭合、PL-021 取号登记；七门禁与 sync 终态读数见下；未 commit、未 push（授权边界内）。
