# 2026-09-24 尾巴 1 整改执行记录 — workspace_health 根目录白名单 FAIL

## 一、事项与根因

- 来源：CG-20260923-003／CG-20260924-001 两批收尾报告认定的「尾巴 1」——`_工具/workspace_health.py` 检查项 `check_root_dir_whitelist` 持续 FAIL，是各批「技术面绿但整体验收不放行」的唯一阻断项。
- 根因：该检查以根仓 `_管理规范/文件与项目管理规范.md` §一 顶层目录职责表为白名单单源，根目录下三个 `_` 前缀目录未在该表登记：
  - `_archive-20260921-traces`（CG-20260921-005 镜像层留痕件仓外归档，4.0 MB／67 件）
  - `_preimage-20260922-batchA`（CG-20260922-003 批前像快照，401 KB／9 件）
  - `_tmp-scripts`（各门禁测试临时作业面，305 MB／940 件）
- 授权：发起人 2026-09-24 明示开线处理（超出本仓工作区边界的部分＝根仓规范与目录迁移，经该授权覆盖）。

## 二、处置（分堆：常驻登记／终态归档迁移）

1. **`_tmp-scripts/` 补登记 §一**（常驻判定＝被本仓 4 个门禁测试硬编码引用：`skill_cross_layer_test.py:31`、`cross_layer_eol_test.py:42`、`storage_anchor_guard_test.py:27`、`wetzone_construction_guard_test.py:34`，改名即断链，故命名按 C4 系统兼容豁免登记）。规范文件版本头 v2.7→v2.8、生效日期 2026-09-24。
2. **两个归档目录迁入已登记的 `_归档/`**（终态归档容器，职责相符）：`_归档/_archive-20260921-traces`、`_归档/_preimage-20260922-batchA`，目录名原样保留以维持历史记录可 grep。两目录在根仓 git 均为未跟踪件（`git status` 前像 `??`），迁移不触及任何 git 面；无任何活脚本引用其路径（引用面仅 `_tmp-scripts` 内历史批一次性脚本）。

## 三、验证（同口径复跑，可复算）

| 证据 | 改前（12:54） | 改后（15:36） |
|---|---|---|
| `python -B _工具/定期体检.py --run-now` | `overall_status=FAIL`、`can_proceed=false`、唯一 FAIL＝`check_root_dir_whitelist`（列三目录） | `overall_status=WARN`、**0 FAIL**、`can_proceed=true`、25 OK／6 WARN，exit 0 |
| `check_root_dir_whitelist()` 直调 | — | `(True, '根目录仅含规范登记目录与项目目录', '')` |
| 报告落点 | `_整理与清理/2026-09-24/体检报告/体检报告_2026-09-24_1254.json` | 同目录 `体检报告_2026-09-24_1536.json` |

WARN 6 项均非本线阻断面（台账行长、出口裁定、发布回执、推送就绪、模块膨胀等，属各线渐进整改既有登记）。

## 四、边界（本线不做项）

- **未清理 `_tmp-scripts` 内 305 MB 历史一次性脚本**：删除不可逆、须逐件判定，另批裁定；本批只解除白名单阻断。
- **未提交、未推送任何 git 仓**（根仓 `文件与项目管理规范.md` 的修改与本记录文件均留工作区）。
- 历史 CG 行与两批执行记录中的旧路径记载属当时事实快照，不改写。
- 本仓 `validate_governance.py` 复跑无回归（本批未触碰声明表治理文件与运行时技能件）。
