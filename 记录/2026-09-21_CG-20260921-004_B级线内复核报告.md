# CG-20260921-004 B 级线内复核报告

> **批次**：CG-20260921-004（`walk_files` 留痕判据订正与冻结基准升 v2 批，B 级）
> **复核方式**：`change-governance.md` §二 第 3 步 B 级复核＝发起人线内只读子代理独立复算（`Explore` 代理；禁写盘、禁 git 写操作；要求一切计数用 Python `pathlib.rglob` 而非 `find`，`git check-ignore` 加 `-c core.quotePath=false`）
> **复核日期**：2026-09-21
> **判定**：**首轮 14 项中 11 项真、2 项部分真（第 8、11 项）、1 项为代理自查新增观测（第 14 项，内含三处）→ 批内处置 5 项 → 定向复算全绿**
> **首轮结论如实入表、不抹**：第 8 项与第 11 项的「判假」部分一条是登记面配方未钉（值本身可复算），一条是**主代理交给复核代理的指令文字有误述**，二者均批内订正，不改写本表判定。
> **结论**：本批可本地入库（不含推送）；PL-031 维持待处置不闭合；另因复核揭出两处缺口，本批台账净增 1 行（PL-036）。

---

## 一、首轮 14 项复算结果

| # | 声明 | 首轮判定 | 代理取证（实测读数） |
|---|---|---|---|
| 1 | 旧判据字面 `"_pre_"` 对真实留痕命名命中 0；镜像层 50 件留痕件全为 gitignored 且未跟踪 | 真 | 运行时全库 385 件：字面命中 **0**（文件名与任一段两口径皆 0）、文法命中 文件名 **62**／任一段 **73**；镜像全库 123 件：字面命中 0、文法命中 50。该 50 件 `git check-ignore --stdin` 命中 50/50、`git ls-files 技能仓备份` 交叉命中 **0**（该目录跟踪件恰 72） |
| 2 | 新判据非恒真（有牙） | 真 | 同进程 `collect_scope()` 两态并算：旧字面 **122**、现判据 **72**，两值不等；`--check` 现判据读数「范围 72｜一致 72｜新增 0｜移除 0」 |
| 3 | 未做不可逆删除（镜像层 50 件原地驻留） | 真 | 50 件现仍在盘且可枚举；sync 读数「移除 0」；`git status --porcelain -- 技能仓备份` 删除行 **0**（仅 2 条 `M`） |
| 4 | 检查 9 不受影响 | 真 | validate 检查 9 节现读「运行时∪镜像 **120**／gitignored **58**／纳入判据 **62**、漂移 0／单层缺失 0」，与 v1 全同 |
| 5 | 九门禁全绿 | 真 | validate 总评 PASS／通过 **236**／失败 **0**／警告 **1**／exit 0；verification 6／ace 90／sre 66／ic07 20／pending 39／cross_layer 25／index_ref 26／skill_cross 21 全部 OK |
| 6 | 三层 `change-governance.md` 互等 | 真（时点值） | 复算时三件 sha256 全等 `765687bfe2254a08…`／725,082 B；本批收尾因登记面又增订，终态值见 §三（`4b7d08aadc461c51…`／727,707 B） |
| 7 | v1 冻结件零改写 | 真 | sha256 `d2fba82085cf1a598ad40564312d55c87c761eb9d671c3ee86d4195501c17c73`、**27,028 B**、CR 计数 0；`git diff --stat` 该件 0 行、`git status` 未列 |
| 8 | v2 结构（纯 LF／§二 15 项／§三—§五 与 v1 逐字节全等／§六 增补） | **部分真（值对而配方未钉）** | 真＝纯 LF（CR 0）、§二 表数据行 15、§六 第 5 条 v2 注＋新立第 6 条两处 diff、§三—§五 切片在两版间逐字节相同；**判假＝注册值 `e04d6648…` 按其常用切片口径（自 `## 三、` 起、不含前导换行）复算不得，代理得 `fe854bdf…`，另试 12 种切片变体均未命中，遂判「自证失效」** |
| 9 | v2 §二 内容包指纹 71 件 `7f80ab70…475d` 可按配方复算，且件集全为已跟踪件 | 真 | 逐件 `git cat-file blob 7506107:技能仓备份/<rel>` → `sha256(原始字节)` → `"<rel>:<hex>"` 排序 `\n` join → 整体 sha256 ＝ **`7f80ab701d1757b3e348e4aee5152c67ef44b0cccbd732609166c52de196475d`** 逐字命中；71 件对 `git ls-tree -r 7506107` 与 `git ls-files` 差集均 **∅**（唯一不在树内的正是被剔除的 `sre_regression_report.json`）→ v1「仅本机可复算」限制确已消除 |
| 10 | v1 两枚指纹按各自口径可复算（配方反向验证） | 真 | 冻结基 `db2a37d`：121 件态（50 件未跟踪取现字节＋71 件取 blob）＝ **`701dd7ebd2ae7a966c343d94793e1862a749084c53786aba409046bef065c3d0`** MATCH；71 件全 blob 态＝ **`30de3347e9e39aa7a186948e48d6ab24a731843b80ae21806cf47ac3aecb5e8c`** MATCH |
| 11 | 登记面（§九 115 行／本批行单物理行 6 列／台账 35 行／PL-031 状态未改写／diff 5 增 2 删／历史 CG 行零改写） | **部分真（主代理指令文字误述）** | 真＝逐行 CG 数据行 **115**、CG-20260921-004 行系单物理行且 `md_cells` 解析 **6 列**（竖线 7）、§11.2 台账 35 行含 PL-035 已闭合、PL-031 仍「待处置」未改写、`git diff -U0 --numstat` ＝ 5 增 2 删且两条 `-` 行正是头部「最后更新」行与 PL-031 行、历史 CG 行零改写；**判假＝主代理在复核指令里写的「用本仓 `iter_md_tables` 同法解析＝115`」不成立，表级解析实测只见 104 行**（`## 九` 区间内第 387 行为空行，表级解析在空行处把其后 11 条近期 CG 行判为「无表头块」整块丢弃） |
| 12 | AGENTS／CHANGELOG 面 | 真 | `AGENTS.md:59` 现读「本行判据是『范围 **72** ＋ 新增 0 ＋ 移除 0』（CG-20260921-004 起…以下所有『范围 122』历史读数按快照纪律不改写）」，旧 122 读数多处原地存留；CHANGELOG diff ＝ **15 增 0 删**、新节 `## v1.13.6`（第 5 行）、历史版本节零改写 |
| 13 | 本批未推送 | 真 | `git log -1 --format=%H` ＝ `7506107535e3ba29aca6466c17580d369b0c4407`（本批尚未 commit，HEAD 仍为上批）；`git rev-list --count origin/master..HEAD` ＝ **1**（该 1 系上批 CG-20260921-003 的本地提交，与 v2 §二「本地领先 1 件」互印证）；按指令未联网 `ls-remote` |
| 14 | 代理自查观测（超出主代理声明面） | 三处，其中两处为真缺口 | (a) `sync_skill_backup.py:222` 把 `同步说明.md` 头部版本字面硬编码为 **`v1.3`**，与本文件头部 v1.4／v1.5 脱钩，且错值已随产物发布（实测产物第 3 行「…（sync_skill_backup.py v1.3）」）；(b) §九 表级／逐行双口径分裂 104 vs 115，`git show` 逐版本回查确认断块自 **CG-20260920-001** 批即存在、非本批引入，且九门禁无一条机算双口径一致；(c) 其余无异常——`TRACE_ARTIFACT_RE` 与 docstring／注释／清单模板措辞一致，旧 `"_pre_"` 字面分支仍在（覆盖无日期形态），清单数据行 72 与 `collect_scope()` 双向差集 ∅、0 件留痕件混入 |

## 二、批内处置

| 缺陷 | 根因 | 处置 | 复算证据 |
|---|---|---|---|
| ① v2 §七 分节 `sha256` 未钉切片口径（第 8 项） | 登记文本只写「该三节合并文本 sha256」，未写切片边界与前导换行是否入算；值由生成脚本按 `re.search(r'\n## 三、')`→`re.search(r'\n## 六、')` 口径算出 | 在 v2 §七 补钉口径全文（**含前导换行、至 `\n## 六、` 止不含**），并同时列出两个易错口径的实算值供复算者自查 | 补钉后 v1／v2 两版该切片均 `e04d6648e602e5ff7061dabdc5740e6f170ad40e4baaea99f87651ab875e3812`（与注册值逐字同）；不含前导换行 → `fe854bdf…`；含前导再 `rstrip()` → `071270c4…`。**定性：值无误、缺陷在配方未随值入库**，与既有约定「哈希须附归一配方」同型 |
| ② 清单头版本字面硬编码 `v1.3`（第 14(a) 项） | 版本字面在文件头与写盘模板两处手抄、无单源 | 新增常量 `SCRIPT_VERSION = "1.5"`，模板改读该常量 | 收尾产物 `技能仓备份/同步说明.md` 第 3 行现读「…（sync_skill_backup.py **v1.5**）」；清单数据行 72 |
| ③ §九 表内空行致表级解析少读 11 条近期 CG 行、双口径分裂无守卫（第 11、14(b) 项） | 登记面历史区某批写入时把空行落进表区；检查 7 的 §九 真值源走**逐行**口径，故表级口径何时分裂均不报红 | 新登 **PL-036**（待处置，常规 TTL 2026-12-20，来源登记 CG-20260921-004）；本批**不删该空行**（属登记面历史区字节，须发起人裁定另批执行）；主代理指令中的「115＝`iter_md_tables` 同法」误述在 §九 行与 `CHANGELOG.md` v1.13.6 第 ⑨(c) 项就地订正 | 两读数同报：逐行 **115**／表级 **104**；§11.2 台账 35→**36** 行；检查 7 与其余门禁全绿 |
| ④ 留痕件计数表述口径混用 | 主代理把运行时**文件名**口径的 62 写成「两层全库 62 件」，未标口径，致复核代理按任一段口径报 73、两方数字看似冲突 | `CHANGELOG.md` v1.13.6 第 ①③ 项与 §九 行 col3 改为两口径并列（运行时文件名 62／任一段 73、镜像 50，**实际进范围者 50 件**）并说明多出的 11 件在三个根级 `_pre…` 留痕目录内、本不属采集范围 | 同进程三口径实测复算，与 §一 表第 1 项代理读数逐字一致 |
| ⑤ v2 字节数因 ① 增订而变 | 补钉配方属文字增订 | `CHANGELOG.md` 第 ⑤ 项与 §九 行同步为 **30,622 B**，并注明「复核批内补钉切片口径后由 29,984 增至此值」；v1 留档值不受影响 | v2 现读 30,622 B、CR 0；因 §七 在 §六 之后、不入切片，§三—§五 分节哈希仍 `e04d6648…` |

## 三、终态读数（收尾顺序＝编辑→带平→复算→最后跑 sync）

- `validate_governance.py`：**PASS／通过 236／失败 0／警告 1／exit 0**（本批零改校验器、零新增检查项，通过数 no-op）。
- 八测试门禁：verification 6／ace 90／sre 66／ic07 20／pending 39／cross_layer 25／index_ref 26／skill_cross 21，全部 `OK`。
- `sync_skill_backup.py` 末次跑读数「范围 **72**｜一致 71｜新增 0｜更新 1｜移除 0」（唯一更新件＝机器本地 `sre_regression_report.json`，系随后复跑门禁所致），其后 `--check` 复跑＝「范围 72｜一致 **72**｜新增 0｜更新 0｜移除 0」。
- 三层 `change-governance.md`：sha256 全等 `4b7d08aadc461c51…`、**727,707 B**、纯 LF；`技能仓备份/同步说明.md` 清单头 v1.5、数据行 72。
- v1 冻结件：`d2fba820…`／27,028 B 零改写；v2：30,622 B、§二 指纹表 **15 项**、内容包 71 件 `7f80ab70…475d`（可克隆复现）。
- 登记面：§九 逐行 **115** 行（本批 +1、单物理行 6 列）；§11.2 台账 **36** 行（PL-035 补登并闭合、PL-036 新登待处置）；PL-031 状态仍「待处置」未改写。
- 本批写面：`程序文件/sync_skill_backup.py`（v1.5）＋`_专题_技能合集策划/change-governance.md` 三层带平＋`技能仓备份/同步说明.md`＋`AGENTS.md`＋`CHANGELOG.md`（v1.13.6）＋新建 v2＋新建本报告。零改 15 技能件／契约文件／校验器 v1.13.0／九门禁件／`standards-index.md` 本体／§九 历史 CG 行。

## 四、未决事项（各带触发条件，互不依赖，非编号队列）

1. **镜像层 50 件留痕件的清理**——须发起人裁定；该 50 件 gitignored 且未跟踪，删除不可逆，本批只使其「两侧不可见」而不删。
2. **PL-036**——两半消解须另批：①删 §九 表内空行属改登记面历史区字节，须发起人裁定；②新增「§九 表级行数＝逐行行数」机算守卫属校验器检查面，须另批且定级按 §一（改校验器检查面为 B 级）。
3. **PL-031 阶段 1 宿主实跑批**——触发＝发起人决定投入实跑工时；记录须显式标注所依版本（v2）并引用该版 §二 指纹值，不得跨版引用范围件数与内容包哈希。
4. **盲评责任人指派／ACE 脱敏检测报告**——外部输入，本线不产出；拿不到则 D-04 记「未执行」，禁止以理论测试替代。
5. **推送**——本批**不含 push**，沿发起人 2026-09-21 明文「只本地提交、推送随阶段 1 那批一起做」。
