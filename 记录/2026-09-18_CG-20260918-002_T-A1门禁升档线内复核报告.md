# CG-20260918-002 T-A1 名称双源差分门禁 WARN→FAIL 升档 — B 级线内独立复核报告

- **复核日期**：2026-09-18
- **变更编号／级别**：CG-20260918-002／**B 级**（校验器检查面变更＝B 级，先例 CG-20260916-010）
- **复核形式**：线内**独立只读子代理**（用户 2026-09-18 AskUserQuestion 第三项裁定；先例 CG-20260916-010／CG-20260918-001）
- **复核约束**：只读取证——仓内零写入、运行时 `~/.qoder/skills/` 零写入、不执行 git 写操作；允许实跑门禁（SRE 回归重写机器本地 `sre_regression_report.json` 属既有登记豁免的副作用）
- **总判**：**PASS with OBS**（12 项逐条 PASS；OBS 6 项，其中 3 项当场闭合／当场补登记，3 项程序性·独立性局限如实留痕）

## 一、复核范围（发起人声称面）

把 `程序文件/validate_governance.py`（v1.7.0→**v1.8.0**）检查 5「SRE 静态体检」的信号 **T-A1（SRE 硬编码标准名称 vs 索引主表名称 双源差分）** 由 `report.warn` 升为 `report.fail`（计 `fail_count`、可致 exit 1），其余 T-A2—T-A5 维持首轮 WARN；声称六处改动（尾环落报告分支／版本头／头部检查 5 口径／v1.8.0 变更块／`check_sre_static` docstring 与小节标题／main 调用处注释），登记侧同批改 `AGENTS.md` 活基线行、`CHANGELOG.md` v1.12.17、设计方案 §6.3.3 ③(b)＋§9.2 T-A1 行注记、`change-governance.md` §九 行＋头部并三层带平。

## 二、12 项逐条核验

| # | 核验项 | 判定 | 关键证据 |
|---|---|---|---|
| 1 | 改动面真伪 | **PASS** | `git status` 仅 7 件（AGENTS.md／CHANGELOG.md／change-governance.md L1＋`技能仓备份/shared/`／`技能仓备份/同步说明.md`／设计方案／validate_governance.py，numstat 22/6）；运行时 `sre_reasoner.py` sha16 `9d38750c04c07677`、`sre_regression_test.py` `2eb60898ac19d611` 与仓内镜像互等且零改动；`standards-index.md` 三层同哈希 `bcdf96283daa8f5e`（＝-001 登记值）零改动 |
| 2 | 升档实现正确性 | **PASS** | `validate_governance.py:1319-1325` 仅 `sig["id"]=="T-A1"` 走 `report.fail`、else `report.warn`；`Report.fail`（:131-133）置 FAIL 项、`print_report` 返回 `total_fail`（:168）、main `:1568-1569` `sys.exit(1 if fail_count>0 else 0)`；T-A1 差分判据本体（:1055-1090 双源取数／kw_div、:1303-1318 锚定命中）无 hunk |
| 3 | 判据零外溢 | **PASS** | diff 恰 6 hunk（:5／:16／:75／:997／:1319／:1561），全在声称范围；T-A2—T-A5 与检查 1—4／6 零 hunk |
| 4 | clean 态读数复算 | **PASS** | 独立实跑 exit 0、`总评：PASS｜通过 196｜失败 0｜警告 1`，T-A1 行为 `+ T-A1：SRE 硬编码名称与索引主表名称零冲突` |
| 5 | 升档后守卫仍能失败 | **PASS（独立复现）** | 复核者另建仓外目录 `D:\Temp\CG-20260918-002-REVIEW\rt\`、拷运行时真件、注入 `08J931` 名称加 `Z` 后缀（注入锚 `assert count==1`），`--runtime-dir` 实跑 **exit 1、FAIL｜194｜1｜2**，唯一 FAIL＝`x T-A1 [DRIFT] …差分 1 处…｜anchor=true（命中 08J931…）`；原登记副本 `D:\Temp\CG-20260918-002\rt\sre_reasoner.py:271`＝`"建筑隔声与吸声构造X"`，运行时真件 `:271` 无 `X`（`diff --strip-trailing-cr` 唯一差异即该行）＝注入态属实、真件干净 |
| 6 | 两案互不掩盖 | **PASS** | 检查 6 层路径取模块常量（:109-115、:1390-1391），:1489 明示不受 `--runtime-dir` 影响；注入态实跑检查 6 仍 `[OK]` |
| 7 | 三层镜像一致性 | **PASS** | 三份 `change-governance.md` sha256 前 16 位均 `5ce601db90477f1f`，与登记逐字一致 |
| 8 | 登记一致性交叉核对 | **PASS** | AGENTS 活基线行／CHANGELOG v1.12.17／CG §九 行＋头部／设计方案两处注记，五处 196-0-1、194-1-2、v1.8.0、B 级、Ran 66/90/20/6、指纹 `4f53cda18c2baa0c`、注入细节互核一致、无矛盾；**如实标注**：本报告文件在复核时点尚未落盘＝预写引用（沿 CG-20260917-006 OBS-1 先例，落盘即闭合） |
| 9 | 四门禁复现 | **PASS** | SRE `Ran 66 OK` exit 0、缺口信号 0、指纹 `4f53cda18c2baa0c`（连击 81 轮属机器本地、不钉数字）；ACE `Ran 90 OK`；IC-07 `Ran 20 OK`；`verification_policy_test.py` `Ran 6 OK` |
| 10 | 遗留未擅自处置 | **PASS** | (b)(c) floor-system 两文件与索引本体零改动；(d) 运行时 `sre_reasoner.py:436` 仍为 `STANDARD_NAMES.setdefault(std_no, row[i_name])`；(e) `sre_regression_test.py` 哈希与镜像互等未动；(f) 改动面无 P1/P2/P3 相关文件 |
| 11 | 风险项登记如实 | **PASS** | `D:\Temp\` 在仓外；注入副本不在生效路径（默认 `runtime_dir=~/.qoder/skills/prefab-standards-reviewer`，仓内三层均无注入值）；「误回拷会被 FAIL 档即刻阻断」的结构性兜底声称已由第 5 项实证 |
| 12 | 定级成立 | **PASS** | `change-governance.md` §一 B 级＝不影响安全性的常规变更；「校验器检查面变更＝B 级」先例确系 CG-20260916-010（§九 CG-20260917-004 行明文引用）；本批未触推理算法／接口契约／标准数据／红线注册，无 A/S 触发条件 |

**独立复算与登记不符之处**：逐项相符（196/0/1、194/1/2、各 exit code、指纹、三层哈希 `5ce601db90477f1f`、注入态后缀、`:436` setdefault 全部复算吻合）。唯二非矛盾差异：① 复核者注入字符用 `Z`（登记用 `X`），行为等价；② `技能仓备份/同步说明.md` 未列入声称改动面清单（见 OBS-3）。

## 三、OBS 清单与处置

| 编号 | 类别 | 内容 | 处置 |
|---|---|---|---|
| OBS-1 | 当场闭合 | 本报告文件名在 AGENTS／CHANGELOG／设计方案／CG 行四处系**预写引用**，复核时点尚未落盘 | 本报告落盘即闭合（沿 CG-20260917-006 OBS-1 先例） |
| OBS-2 | 当场闭合 | 复核实跑 SRE 门禁两次重写机器本地 `sre_regression_report.json`（运行时 sha16 由 `38d01886…` 转 `61f200fe297a14f8`） | 收尾复跑 `sync_skill_backup.py` 带平「更新 1」，属预期（沿 CG-20260918-001 OBS-1 先例） |
| OBS-3 | 当场补登记 | `技能仓备份/同步说明.md` 的改动（sync 工具副产物：时间戳＋2 行哈希）未列入声称改动面清单，非缺陷但归因缺失 | 已在本报告与 CHANGELOG／CG 行的影响范围补归因登记 |
| OBS-4 | 程序性·独立性局限 | 复核子代理由发起人编排、取证范围由任务书给定 | 如实登记（沿 -001/-002/-003/-004/-006 先例），不改判 |
| OBS-5 | 程序性·覆盖面局限 | 复核者未复跑 `sync_skill_backup.py`（会写仓内镜像、违只读约束），改以哈希互等间接验证「范围 122＋一致＋移除 0」状态 | 如实登记；主线程收尾实跑补齐直接证据 |
| OBS-6 | 程序性·覆盖面局限 | 正向对照（未注入副本＋`--runtime-dir`→exit 0）未由复核者单独复现，以默认目录 clean 跑＋注入跑等效覆盖 | 如实登记；发起人侧已有该项实测（exit 0、196/0/1） |
| OBS-7 | 另案·风险项 | 复核者自建 `D:\Temp\CG-20260918-002-REVIEW\rt\`（含 `Z` 后缀注入副本）留在原处，与原登记目录 `D:\Temp\CG-20260918-002\` 同为仓外非生效路径 | 两处临时目录合并登记为**待用户处置风险项**：auto-mode 策略拦截本代理的仓外删除与覆写中和（依纪律不换调用方式绕过）；两副本均不在生效路径，误回拷会被本批 FAIL 档即刻阻断（结构性兜底，已由第 5 项实证） |

## 四、复核实跑命令与 exit code

| 命令 | exit |
|---|---|
| `git status`／`git diff --stat`／`--numstat`（只读） | 0 |
| `python -B 程序文件/validate_governance.py`（clean 态） | 0 |
| `python -B 程序文件/validate_governance.py --runtime-dir D:\Temp\CG-20260918-002-REVIEW\rt`（注入态） | 1（预期） |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py`（×2） | 0 |
| `python -B 程序文件/ace_regression_test.py` | 0 |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py` | 0 |
| `python -B 程序文件/verification_policy_test.py` | 0 |
| `sha256sum` 三层／运行时哈希比对（只读） | 0 |

## 五、复核结论

本批为**门禁档位归属变更**，判据逻辑、标准数据、推理运行时与接口契约零改动；clean 态硬判据「失败 0＋exit 0」读数与上一批基线同值，升档只在名称差分非空时生效，且已由发起人（`X` 后缀）与复核者（`Z` 后缀）**两次独立注入**证实守卫仍能失败、FAIL 归因唯一、检查 6 不被掩盖。**B 级线内复核判定 PASS with OBS**，无功能阻塞项；OBS-1／OBS-2／OBS-3 当场闭合或补登记，OBS-4—OBS-6 为程序性局限如实留痕，OBS-7 为仓外临时目录待用户处置。

本报告不构成对标准最新状态的重新核验，也不作最终工程合规放行。

## 六、发起人补记（复核时点之后，不改写上方快照）

本报告第 7 项与「独立复算」段所载三层哈希 `5ce601db90477f1f` 系**复核时点值**。复核结论回填登记面时，L1 `change-governance.md` 又被编辑（§九 CG 行 ⑥ 复核终态／⑦ sync 实测读数／影响范围列补 `技能仓备份/同步说明.md` 与 `sre_regression_report.json`／状态列补 PASS with OBS 12-12、头部同批补复核结论与两处风险目录、④ 风险项补复核者侧目录），三层一致性一度被打破，已**第二次带平**：

| 项 | 复核时点 | 补记时点（终态） |
|---|---|---|
| 三份 `change-governance.md` sha256 前 16 位 | `5ce601db90477f1f` | **`c52c13353f4e899f`**（L1／RT_SHARED／REPO_SHARED 互等） |
| 字节数／CR 字节 | — | 380,677 B／CR 0（三层同值，`python` 原字节复算） |

带平后**复跑门禁同值**：`validate_governance.py` PASS｜通过 196｜失败 0｜警告 1｜exit 0，检查 6 (A) 14 项／(B) 9 项全绿于新哈希（含 `change-governance.md：RT_SHARED = REPO_SHARED（c52c13353f4e899f）`）；`sync_skill_backup.py` 跑前 `.pyc` 只读复检命中 0 件，读数「范围 122｜一致 121｜新增 0｜更新 1（`sre_regression_report.json`）｜移除 0」，判据成立。本补记为发起人侧记录，非复核者结论；上方 12 项判定与 OBS 七项处置均不受影响（改动面仅限登记文字，`validate_governance.py` 与运行时零改动）。
