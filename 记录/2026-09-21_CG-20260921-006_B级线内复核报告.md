# CG-20260921-006 B 级线内独立复算报告（探针批登记面）

> **一句话定位**：本批（PL-031 阶段 1 探针批实跑取证与 G-1—G-4／F-1—F-8 登记）的自述结论，交一个**线内只读子代理**独立复算。首轮 15 项声明全部实跑／实读取证，结论 **PASS-with-OBS**；本报告**不抹首轮结论**，批内处置与收尾复算差额单列在 §二，端到端终态在 §三。
>
> **代理侧约束（指令原文摘）**：严格只读（禁写／禁改／禁 commit／禁 sync 写态，临时件只准落 `D:\Qoder-Files\_tmp-scripts\`）；不得相信主代理任何数字，须自己算出来才算「真」；主动找反例（断言是否恒真、行号是否指对、比值口径是否混用）。
>
> **本报告的判据地位**：与冻结基准 §一 第 5 条同口径——复核为真只证明「登记面自述可复算」，**不构成 PL-031 的行为证据面**，也不改写台账状态。

---

## 一、首轮复算结果（15 项，原样入库）

| # | 主代理声明（缩写） | 代理实测 | 判定 |
|---|---|---|---|
| 1 | L1 终态字节／CR／LF／sha16 ＋ 三层字节全等 | 逐项复算相同，三层 sha256 全等 | **真** |
| 2 | 本批零改 `AGENTS.md`：diff 空＋工作树与 HEAD 第 54 行同为运行时技能路径＋实跑 Ran 20／OK → F-8「路径指错」证伪 | `git diff HEAD --stat -- AGENTS.md` 空；两读同为 `~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py`；实跑 Ran 20／OK | **真** |
| 3 | 错路径串首见于执行方自己发出的命令、先于任何工具结果；注入快照不落转录 | 逐行 json 解析：该串首现于 line 1509 的 assistant Bash `input.command`，其前 0 命中；`验收门禁` 首现于 line 1218 的 tool_result（非注入记录） | **真** |
| 4 | 登记面只改头部行＋§九 加一行（6 列单物理行）＋§11.2 加四行＋PL-031 仅依据列尾部追加；历史 CG 行零改写 | diff 仅 2 条 `-` 内容行（头行＋PL-031）、3 hunk；CG-006 行 7 个未转义竖线 | **真**（附注 a：口径须精确到列级） |
| 5 | 台账 41 行、PL-038—041 四行 7 列齐备、日期可解析、待处置有到期日、来源 CG 键闭合 | PL 行 41／唯一 41；四行 7 列、登记 2026-09-21、到期 2026-12-20、来源 CG-20260921-006 键闭合（§九 实读 117） | **真** |
| 6 | CHANGELOG v1.13.8 已插入且为纯新增（删除 0 行），v1.13.7 及以下不动 | diff `+14/-0`；HEAD 无 v1.13.8 节；v1.13.7 作为未变上下文出现 | **真** |
| 7 | 九门禁读数＝237／0／1／exit 0、双口径 117＝117、6·90·43·25·26·21 各 OK、SRE 66 OK 指纹一致 | 逐门禁实跑复现全部读数 | **真** |
| 8 | 取证件字节／CR／LF／sha256、10 个真题段、评分三字段全空 | 全中；70 行维度表的分列非空者＝0；10 行评分人均为「待指派」、10 行分歧裁定均空 | **真**（首轮读数为 169,191 B／`54477a4e…`，本件 §二 述其变更） |
| 9 | 契约请求／响应与输出全文真源于转录（抽 A-01／B-08） | A-01 段 31 个时间戳全在对应 jsonl、B-08 段 27 个同；探针 60 字符为转录 text 的 verbatim 子串 | **真** |
| 10 | 成本与体量：净 64.3／墙钟 29.0／均 6.4／最长 23.1（A-03）／最短 0.3（B-03）／转录 3,265,836 B／可读 74,600 B／1∶44 | 时间戳复算全中（64.28→64.3、28.95→29.0、avg 6.43→6.4）；八件字节和逐件匹配；比值 43.8 | **真**（附注 b：口径披露不足） |
| 11 | G-1＝ACE M5 公式与算例／区间互斥（36.6↔≈21、36.0↔≈20、28.4↔≈15，五档区间无一含公式值） | 公式两处均在（`:232`／`:592`）；`18·lg108 − 10·lg10 + 10 = 36.6` 而 `:980` 印 ≈21；五档区间 18-23／23-28／13-18／17-22／19-24 均不含各自公式值 | **真** |
| 12 | G-2＝缝隙 1% 损失按墙级 15／25／30 dB 对不分墙级 8—12 dB，Rw≥50 低估 18—22 dB | PW 表 Rw35→15、45→25、≥50→30；ACE `:297` 单行 8—12；Rw50 代入复算损失 30，差 18—22 成立 | **真** |
| 13 | G-3＝12mm 石膏板吻合频率同文件两表互斥（1950—2450 对 ~2960），敏感性说明只释硅酸钙板 | `:259`＝1950—2450；`:278-281` fc1≈2960；`:285` 只论硅酸钙板 E（5—8 GPa） | **真** |
| 14 | G-4＝运行时层留痕目录存在且被技能加载器命中，10 题中 5 题命中、B-04 只命中该副本 | 目录存在（同级另有 `_pre20260918CG008`／`CG010`）；命中段＝A-01／A-02／A-03／A-04／B-04；B-04 加载列表仅 1 条＝该副本 | **真** |
| 15 | 实跑未污染：`--check` 72／72／0／0／0 ＋ `git status` 仅空目录 `素材/` | 复跑呈「范围 72｜一致 **71**｜更新 **1**｜移除 0」（唯一更新件＝机器本地 `sre_regression_report.json`，系代理自身跑 SRE 门禁所致、被检查 9 排除、不入判据）；`git status` 实为 **5 路径**，主代理「四类」漏计 `技能仓备份/shared/change-governance.md` | **部分真**（附注 c） |

**总体结论（首轮）＝PASS-with-OBS**：15 项中 14 项判真、1 项（#15）判部分真；无一项判假，无需回退任何登记结论。

---

## 二、批内处置（三项 OBS ＋ 收尾复算两项新发现，逐条落地、不静默吸收）

**附注 d（收尾复算新发现，不属首轮三项 OBS）**——本件 §二 各项处置落地后复算 `git diff HEAD --numstat -- _专题_技能合集策划/change-governance.md`＝**8 增／2 删**、`git diff HEAD --unified=0` 现读 **4 hunk**（头部行 `-7 +7,2`、§九 `+401`、PL-031 `-470`、§11.2 四行 `+477,4`），与首轮表内 #4 所记「3 hunk」不符。差额根因＝首轮计数把头部行的改写形态记作 1（该行拆为「新最后更新行＋被降级为历史快照的原行」两条后 hunk 内行数变化，`--unified=0` 仍只占 1 hunk，故差额系**计数口径未附配方**而非有一行内容被多改）。#4 的**承重判定**——「删除行仅 2 条＝头行与 PL-031 行、§九 全部历史 CG 行零改写」——收尾复算仍为真（上列删除行原文逐条即此两行）。按快照纪律**不改写首轮表**，只在此登记差额与可复算配方（hunk 数须随 `--unified=0` 的整段输出报出，不得裸记数字）。

**附注 c（#15，git status 漏计一项 ＋ 「一致 72」时点性）**——主代理把「探针结束时点」的两个读数写成了无时限定语的句子，读起来像本件入库时点的终态声明。处置＝在取证件 §一 补入时点限定与**判据集合**：① 明写两读数＝探针结束时点快照、不作本件入库时点终态声明；② 收尾变更面不写固定计数，改写**判据集合**＝L1 `change-governance.md` ＋其仓内镜像 ＋ `CHANGELOG.md` ＋ `sync` 自产的 `技能仓备份/同步说明.md` ＋ `记录/` 交付件 ＋ 既有空目录 `素材/`（代理实测的「5 路径」只是该集合在某一时刻的枚举，随门禁复跑与交付件增删而浮动，故不取为判据）；③ 明写 `--check` 在同一收尾内呈两态（门禁复跑前「一致 72」／复跑后「一致 71｜更新 1」），唯一更新件系机器本地 `sre_regression_report.json`、属检查 9 排除面、**不入判据**；④ 把承重判据从这两个浮动读数**移开**——「运行时技能目录与仓内镜像内容未被这 10 题实跑改写」这一判定改由「真实外部写面 3 次全部落 `D:\Qoder-Files\_tmp-scripts\`」承担。定级不变（文档面订正，未触任何判据数值）。

**附注 b（#10，分钟列口径）**——代理的子观测「A-04 显示 9.5 系截断、其余行四舍五入」经主代理复算**不成立**：A-04 精确跨度 9.5495 分，四舍五入到 0.1 即 9.5，十行用的是同一口径（代理自己复算出的 truncate1 列显示 A-05／A-06／A-02／B-04 若截断会变成 2.7／0.7／6.1／3.5，与表内值不符，恰证表内为四舍五入）。**成立的部分**＝逐行相加 64.2 ≠ 合计 64.3（合计由未舍入跨度 64.3214 得来），且墙钟 29.0 与净执行 64.3 是两个口径。处置＝附录二 增 ※※ 口径脚注：单行四舍五入到 0.1、合计与均值由未舍入值算得、差 0.1 属十次舍入累积非数据错误；同时钉住「转录量与输出口均按字节计、3.1 MB＝3.11 MiB（1 MiB＝1,048,576 B）」，消除字节／MiB 混读。

**附注 a（#4，前缀口径不精确）**——代理称 PL-031 行严格 `startswith` 为 False。实况＝注记插在末列闭合竖线之前，故整行不是逐字符前缀；**该口径本来就不该用整行前缀**。主代理复算改用列级比对：`git show HEAD:` 前像与现读各切 7 列，**仅第 7 列（依据列）不同，且新值以旧值整段为前缀；含状态列在内的其余 6 列字节相同**。故登记面所述「只在依据列追加进度注记、状态列不改写」为真，本报告把它固化为**列级判据**而非行级前缀判据（行级前缀判据在末列追加场景下必然假红）。取证件与登记面正文不因此改动。

**记录件四次重生成的连带面**：①② 两项处置都要改取证件正文，②的判据化改写又改一次，收尾复算发现 `素材/` 描述不实再改一次，故记录件共重生成四次，字节与哈希随每次浮动——首轮复算读数 169,191 B／LF 1745／sha256 `54477a4e727bc31f…`；第二次（时点限定＋终态枚举）170,724 B／LF 1747／`ab7e483b1b6b9dd1…`；第三次（②改为判据集合、去掉固定计数）171,109 B／CR 0／LF 1747／`dc7f6811115e4647…`；本批终态（附注 e 的 `素材/` 订正）**173,745 B／CR 0／LF 1,749／sha256 `a72307fab25e0ac2999492f5723748442987f8538800bfd582d22fda9cc2ae8a`**。登记面（头部行、§九 本批行、PL-031 进度注记）**三处**该件的字节与 sha 字面已同步为终态值，旧值（`171109 B`／`170724 B`／`169191 B`／`dc7f6811…`／`ab7e483b…`／`54477a4e…`）残留复算各＝0；第三次替换因字节宽度相同故总字节未动，本次因订正文段较长故 L1 由 753,295 B 增至 **753,735 B**（LF 496 不变）、sha16 浮动为 `13b063fd3eb4d7d6`，三层带平后字节全等。**同批连带面（如实记）**：`CHANGELOG.md` v1.13.8 ① 内该件的落盘读数由首轮值改指终态、首轮与前两次读数转为该条内的快照注保留，本文件由 352,158 B 经 352,374 B 增至 **352,737 B**（LF 906 不变）；因改动落在本批新增行内部，`git diff HEAD --numstat` 仍为 **+14/−0**（纯新增形态未破），表内 #6「纯新增、删除 0 行」的判定不因此翻转。

**附注 e（收尾复算新发现，不属首轮三项 OBS）——`素材/` 判作「空目录」不实**：取证件 §一 与登记面（头部行、§九 本批行）三处均把收尾 `git status` 的未跟踪项写作「空目录 `素材/`」。收尾复算现读＝该目录内含 `素材/GB 50118-2010 《民用建筑隔声设计规范》.pdf`，**8,632,623 B、`%PDF-1.6` 头、sha256 前 16 位 `46d9d97675862ef7`、`st_mtime` 2020-11-25（复制保留源时间戳）、`st_ctime` 2026-09-21 15:21:58（本地）**，故「空目录」为**载体描述错**。**三条实证 ＋ 一条推定**：① 非探针会话写入——该时刻落在 B-08 会话 07:17:34Z 发出「4.2.1 条原文用哪个通道取？本地无该标准任何文本载体」的 `AskUserQuestion` 之后、07:22:06Z 收到「你提供标准 PDF」作答之前，且十路会话转录内 `Invoke-WebRequest`／`curl`／`wget`／`DownloadFile`／`WebClient` 命中 **0**；② 它不在本批 17 次带写面命令内，真实外部写面仍为 3 次且全落 `D:\Qoder-Files\_tmp-scripts\`，**故「实跑未污染运行时层与仓内镜像」的承重判据未动**；③ 该件**未跟踪但也不被忽略**（`git check-ignore -v` exit 1，`.gitignore` 无覆盖 `素材/` 的条目），本批只按显式路径 `git add`、不纳该件——由此暴露一项**误纳风险**：后续任何 `git add -A`/`git add .` 会把 8.6 MB 国家标准文本带上 GitHub Public 仓，属版权与暴露面双重判断，须另批决定「加 `.gitignore` 条目／迁出仓外／由发起人裁定入库」三择一（与 PL-030 残余公开面同族，本批不代作）；④ 写入者身份本仓侧无独立记录，最合理解释＝发起人手工放入（其本地标准库 `D:\资料\中装科技\2-政策及规范\0-建筑通用规范\` 有同名件，2026-09-15 会话转录 line 2485 现读可见），记作**推定**不作实证。**处置**＝三处字面按本批新增行可订正口径就地改写（原措辞以引号形保留于该目录条内），并把该 PDF 登记为 PL-038／附录三 T-5 的**第一份实际到达的输入载体**（标准文本可得性自此不再是缺口，缺的是官方身份核验）。首轮表内 #15 的措辞含「仅空目录 `素材/`」，按快照纪律**不改写**，读作首轮时点的错误描述，其后判定为部分真已成立。

---

## 三、复算后端到端终态（批内处置后重跑）

| 面 | 读数 |
|---|---|
| `validate_governance.py` | 总评 **PASS**｜通过 **237**｜失败 **0**｜警告 **1**｜exit **0**；「§九 双口径取数一致（逐行＝表级＝**117**）」 |
| 其余八门禁 | verification **6**／ace **90**／sre **66**（缺口 0、跨轮指纹 `4f53cda18c2baa0c`、连续一致 186 轮＝终态复跑读数、系本机 JSON 递增计数每跑 +1、只作同值证据不作判据）／ic07 **20**／pending **43**／cross_layer **25**／index_ref **26**／skill_cross **21**，各 OK |
| 三层 `change-governance.md` | L1 ＝ 运行时 `shared/` ＝ `技能仓备份/shared/`，753,735 B／CR 0／LF 496／sha16 `13b063fd3eb4d7d6` 字节全等 |
| 记录件（交付面） | `记录/2026-09-21_PL-031阶段1试跑记录_v1_探针批.md` 173,745 B／CR 0／LF 1,749／sha256 `a72307fab25e0ac2…`，与登记面三处字面同值 |
| `AGENTS.md` | 工作树与 HEAD 零差异（本批未触） |
| 台账 | 41 行（PL-038—041 为本批新增，均待处置、常规 TTL 2026-12-20） |

**本批不闭合任何既登记项**：PL-038—041 四行为新登待处置，PL-031 维持待处置（探针件评分字段全空，行为证据面未建立）。

**授权边界（本批收尾时点）**：发起人 2026-09-21 前序明文「根据你的专业判断及合理性推进，执行，如要开新线直接告诉我」覆盖本地文件写入与三层带平；收尾时点另发六项裁定——**① 本批「提交」＝含本地 commit、不含 push（push 仍须另发整句）**；② PL-040 冻结指纹浮动口径由本仓 AI 协作者按必要性与项目实际状态自定；③ 授权 PL-039 运行时层 `_pre*` 留痕件清理（L2 写面，须先按 PL-037 归档法：仓外归档＋源↔归档 sha256 集合全等＋逐件 MANIFEST，再删）；④ 授权 SRE／ACE 反例真注入（L2 写面、可回退，用前像还原）；⑤ 盲评责任人（PL-041）另线探讨、本批不动；⑥ ACE 实测（T-5）核验通道由本仓 AI 协作者自行开启（本地 `GB 50118-2010` PDF 与发起人标准库已使「文本可得性」不再是缺口，缺口收窄为官方身份核验）。②—⑥ 各自另开 CG 批登记，不并入本批。
