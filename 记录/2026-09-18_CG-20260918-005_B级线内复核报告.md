# CG-20260918-005（B 级）线内独立只读复核报告

- **复核日期**：2026-09-18（实跑时刻 16:41，Git Bash）
- **复核对象**：变更批次 CG-20260918-005「遗留存活性台账（PENDING-TTL 本仓化）＋ validate_governance.py v1.8.0→v1.9.0 新增检查 7 ＋ 第六门禁 pending_ledger_test.py ＋ PL-016 既有缺陷定性」
- **复核范围**：改动面四项的规范符合性（定级/流程）与实现正确性（检查 7 分档、列定位、日期解析、键闭合真值源、常量↔散文一致、门禁负向有效性、台账 16 行内容、越界副作用）
- **复核方式**：**只读 ＋ 实跑**。全程未修改任何被测文件；唯一写入＝本报告。取证输出重定向至 `.tmp-verify/`（未跟踪临时目录，提交前须删）。
- **复核者独立性局限**：子代理由发起人编排、取证范围由任务书给定；本报告不构成对运行时技能文件的写核验（见 F 项以哈希/grep 间接证明）。

---

## 一、A—F 逐项结论

### A. 定级与流程合规 — **PASS**

- **B 级站得住**：§一 定级定义表（`change-governance.md:29`）B 级＝「新增内容、修正错误、优化表述等不影响安全性的常规变更」；本批写面＝治理文件新增 §十一、校验器新增检查 7、新增专项测试，**未触碰任何标准数值、未触碰运行时技能文件**（见 F），不落入 A 级「推荐性标准被废止/替代」触发面，无「应升 A 而未升」情形。
- **先例真实存在且同型**：§九 `change-governance.md:320` 有 `CG-20260916-010 | 2026-09-16 | B | 治理文件跨层一致性检查（检查 6）落地…`——即「新增校验器检查面＝B 级独立批」的直接先例；`CG-20260918-002`（`:331`）亦明文援引「校验器检查面变更＝B 级，CG-20260916-010 先例」。本批沿同一判据，自洽。
- **§二 第 3 步「B 级须经复核」**：本次线内独立只读复核即履行该步（`change-governance.md:52` 流程定义）。
- **结论**：定级 B 合规，流程第 3 步由本报告闭合。

### B. 检查 7 实现正确性 — **PASS**

证据：`程序文件/validate_governance.py:1522-1780`（检查 7）、`:1784-1866`（main 接线）。

1. **FAIL/WARN 分档与口径一致**（`:1699-1775`）：结构性违法全部走 `report.fail`——章节缺失/表缺失/缺列/真值源为空（`:1585,1595,1607,1612`）、行列数不符（`:1701`）、编号非法/重复（`:1707,1711`）、状态出枚举（`:1717`）、日期不可解析（`:1723`）、待处置缺到期日（`:1729`）、登记晚于到期（`:1735`）、终态缺依据（`:1741`）、来源为空（`:1747`）、键闭合悬空（`:1754`）。单纯到期＝聚合一条 `report.warn`＋逐条 `report.info`（`:1761-1764`），TTL 超上限亦仅 WARN（`:1770`）。
2. **`fail_count` 只在结构性违法增加**：`Report.fail` 追加 `("FAIL",…)`（`:159-161`），`print_report` 的 `total_fail` 只数 `lv=="FAIL"`（`:173`），WARN/INFO 不计（`:174,168`）；`main` 以 `total_fail>0` 决定 exit（`:1864`）。故到期只 WARN 不影响 exit，与口径一致。
3. **列定位用表头名不硬编码列号**：`LEDGER_COLS`（`:1531-1539`）为「列名→别名元组」，经 `find_col`（`:905-912`）按表头文本匹配取索引；`iter_md_tables`（`:862-887`）表头感知，无表头块不产表（docstring 明示「宁可漏检也不猜列号」）。取数用 `cols[name]` 动态索引（`:1636-1638`），符合项目纪律。
4. **日期解析对空值/非法值安全**：`ledger_date`（`:1547-1555`）先正则形状校验再 `strptime`，`ValueError` 兜底返回 `None`，不抛异常；空值经 `ledger_blank`（`:1543-1544`）判 `—/-/无/N/A` 等，与不可解析分别处理（`:1662-1669`）。
5. **键闭合真值源对断行免疫且只读 §九**：`collect_cg_log_ids`（`:1558-1576`）用 `find_section(r"^九、变更日志")` 限定区间（`:1566`），逐行取「行首第一列含 CG 编号」（`:1571-1575`），不经 `iter_md_tables`——对跨物理行断行免疫（续行不以 `|` 开头被跳过）。实测真值源 **87 个** CG 编号（validate 输出 `L283`），与登记「原表读法 72、逐行读法 87」一致。其他章节散文 CG 编号不入真值源（专项用例 `test_ids_outside_section_not_counted` 守卫）。
6. **常量与 §11.1 散文同值**：`LEDGER_TTL_DEFAULT=90`/`LEDGER_TTL_EVENT=180`（`:1523-1524`）对应散文 `change-governance.md:361-362`「登记日期 ＋ **90 天**」「＋ **180 天**」；`LEDGER_SOON_DAYS=30`（`:1525`）为实现侧预警窗口（散文未钉数字，属实现选择，不构成不一致）。双守卫用例 `test_ttl_constants_match_section_11_1`／`test_status_enum_matches_section_11_1`（`pending_ledger_test.py:249-260`）现读散文断言，散文改而常量未改即复红。

### C. 门禁「修净后仍能失败」 — **PASS**

证据：`程序文件/pending_ledger_test.py`（36 例）。

- **结构违法注入逐类独立成例**：`TestStructuralFailInjection`（`:142-215`）20 例，每例注入一个真实非法值并断言特定判据关键词（`assert_fail` `:145-148` 同时校验 `fail_count>0` 与关键词命中，非恒真）。逐例核过无「注入合法值」的空跑：缺章节/缺表/缺表头行/缺列/编号形制（PL-1、OBS-2 两例）/编号重复/状态出枚举/登记日期不可解析/到期日不可解析/非法历日 2026-02-30/待处置缺到期/登记晚于到期/已闭合缺依据/已裁定不做缺依据/来源为空/来源悬空 CG/依据悬空 CG/行列数不符/真值源为空——各注入面与被测代码同法取数（合成文本直调 `check_pending_ledger`，同表头字面 `HEADER` `:34-35`、同列名）。
- **真实治理件 clean 态断言**：`test_real_repo_ledger_is_clean`（`:96-100`）读本仓 `change-governance.md` 断言 `fail_count==0`——**当前 FAIL（预期，见 D）**，其失败面恰为待写入的 §九 行，属「有约束力、能失败」的活断言，非空跑。
- **TTL 边界三例**：`test_overdue_is_warn_not_fail`（`due=d(-1)`→WARN 非 FAIL，`:104-109`）、`test_due_today_not_overdue`（`d(0)`→零超期，`:111-115`）、`test_due_tomorrow_overdue_boundary`（`d(-2)`→INFO「已过 2 天」，`:117-119`）；另有 soon 窗口内/外两例与 `test_over_ttl_cap_is_warn_not_fail`（`TTL_EVENT+20`→WARN，`:130-134`）、`test_closed_row_overdue_not_counted`（终态不计超期，`:136-139`）——边界两侧均被覆盖。
- **常量↔散文双守卫**：见 B.6，两例现读散文，成立。
- **PL-016 断行免疫守卫**：`test_wrapped_history_row_does_not_hide_later_ids`（`:219-231`）合成「历史行＋续行（不以 `|` 开头）＋断点之后行」，直调 `collect_cg_log_ids` 断言断点前后编号均可见且整体 `fail_count==0`；`test_bold_id_cell_still_parsed`（`:233-237`）守卫粗体单元。真值源免疫有独立正向证明。
- **结论**：负向注入逐类有效、边界两侧覆盖、无恒真空跑。

### D. 实跑取证 — **PASS（含两处预期中间态，均已定性，非缺陷）**

6 条门禁实跑读数见下表。两处已知情形均如实复现且已核对：

- **检查 6 报 FAIL（预期带平前状态）**：validate 输出 `L245-246` 两条 (A) `change-governance.md：RT_SHARED/REPO_SHARED 282d1545c4b6ec14 ≠ L1 4305c210d59698b6 → 跨层落后，须由 prefab-governance-sync 按 L1→下游带平`。跨层哈希实测：L1＝`4305c210d59698b6`（含 §十一），运行时 shared 与仓内镜像 shared 均＝`282d1545c4b6ec14`（`grep -c 十一、遗留存活性台账`＝0，即两层副本尚无 §十一）。**定性：属预期的带平前状态**——L1 已写入而下游副本尚未带平；(B) 运行时↔仓内镜像同层全等（两者皆 `282d…`），故 (B) 不报。**带平后（prefab-governance-sync 按 L1→下游）两份副本哈希将等于 L1，检查 6 应转 [OK]**。
- **pending_ledger_test 1 例失败（预期）**：`test_real_repo_ledger_is_clean` FAIL，`AssertionError: 2 != 0`，失败信息**恰好只有两条键闭合 FAIL**：`L385 PL-013：处置批次／依据 引用的 'CG-20260918-005' 不在 §九 变更日志内`、`L388 PL-016：来源登记 引用的 'CG-20260918-005' 不在 §九 变更日志内`（真值源 87 个）。原因＝台账 PL-013/PL-016 引用 `CG-20260918-005`，而该编号的 §九 变更日志行尚未写入（grep 确认 `CG-20260918-005` 仅现于 §十一与散文，§九表内无此行）。**定性：属预期的登记时序中间态**——检查 7 的键闭合真值源缺该键。**写入 §九 的 CG-20260918-005 行后，两条悬空键即闭合，`test_real_repo_ledger_is_clean` 与检查 7 均应转 OK**（检查 7 其余 13 项断言本次已全 OK，见 validate `L272-282,286-288`）。
- 其余门禁（G2/G3/G4/G5）全绿 exit 0，与既有基线同值。控制台中文有 GBK 乱码，已按要求重定向至 `.tmp-verify/g*.out` 后读取判据行。

### E. 台账内容正确性 — **PASS**

证据：`change-governance.md:371-388`（16 行）＋检查 7 实跑读数（validate `L272-282`）。

- **编号**：PL-001—PL-016 全合规 `PL-\d{3}`、零重复（`L275-276`「编号形制全部合法 16 个／编号零重复」）。
- **状态**：全落三值枚举（`L278`）；分布＝待处置 13／已闭合 2／已裁定不做 1（`L273`）。
- **日期链**：登记日期 ≤ 到期日全合法（`L281`）；待处置 13 行到期日全非空（`L280`）；日期字面全可解析（`L279`）。
- **终态依据**：已闭合/已裁定不做 3 行（PL-012/PL-013/PL-015）依据全非空（`L282`）。
- **来源非空且键闭合**：来源列 16 行全非空（`L282` 下一行）；引用 CG 编号除 PL-013/PL-016 指向待写入的 `CG-20260918-005` 外，其余（CG-20260918-001/-002/-003/-004、CG-20260916-004/-006）均实测存在于 §九。
  - **PL-013→CG-20260918-004**：来源实为「CG-20260918-003 R4-4／梳理报告 §四.1 裁定 3」（存在于 §九 `:332`），处置依据列引用 CG-20260918-005（待写入）；任务书所述「PL-013→CG-20260918-004」系对该行处置关系的概述，实测来源列指 -003、依据列指 -005，均属本批登记时序，非缺陷。
  - **PL-015→用户裁定（例外情形）**：来源＝「梳理报告 裁定 4／用户 2026-09-18 AskUserQuestion『确认不做』」，**不含 CG 编号**，故不触发键闭合检查（`collect`/`dangling` 只对 CG_ID_SHAPE 命中生效）；状态＝已裁定不做、依据非空（`init_instance.py:require_empty_root()` 硬拒）。此为「用户裁定型」合法例外，符合 §11.1「改判不做须写依据」。
- **PL-014 限期 TTL**：到期日 2026-09-30，实跑为「预警 12 天内到期」INFO（validate `L287`），非超期非 WARN，符合限期明写口径。
- **11.3 覆盖面披露如实**：4 条限制（`:392-395`）逐条核对——第 3 条明确披露「反向覆盖未机算：只做『台账→§九』方向键闭合，不反查 CG 行散文声明的遗留……『新遗留漏登记进台账』仍靠人工，与 PL-010 同族」，如实点出本批失效模式；第 2 条披露「新遗留须取 PL 编号由登记纪律承担、非机算面」。**无夸大、无隐瞒**；检查 7 亦以 INFO 复述该限制（validate `L289`）。

### F. 越界与副作用 — **PASS**

- **未修改运行时技能文件**：`git status` 全部改动落在仓库侧；运行时 `~/.qoder/skills/shared/change-governance.md` 哈希＝`282d1545c4b6ec14`、`grep 十一`＝0，证明 §十一未写入运行时，即本批**未对运行时下手**（带平为登记之后的独立步骤）。G4 SRE `Ran 66 OK`、G5 IC-07 `Ran 20 OK` 与基线同值，间接证明 `sre_reasoner.py`/`sre_regression_test.py`/`shared/` 各件未受影响。
- **校验器改动纯增量**：`git diff --numstat 程序文件/validate_governance.py`＝`300/2`；删除的两行仅为版本头 `v1.8.0`（→v1.9.0）与「校验六项」（→七项），检查 1—6 判据逻辑零改动。
- **改动面与四项声明相符**：本批核心写面＝`change-governance.md`（§十一）、`validate_governance.py`（检查 7）、新增 `pending_ledger_test.py`、PL-016 定性（在 §十一 内）。工作树同时含 CG-20260918-003/-004 的未提交改动（`standards-index.md`、`隔墙技能参考规范标准.md`、`成果/…20260916.md`、PW `reference.md`、设计方案文档、CHANGELOG、各镜像副本），系前序批次尚未入库所致，**非本批越界**（末次提交为 CG-20260918-002，`git log` 确认）。
- **未跟踪临时件**：`.tmp-verify/`（本复核取证目录，含 `g1—g6.out` 等）为未跟踪临时目录，**允许存在但须在提交前删除**；`程序文件/pending_ledger_test.py` 为本批交付物（应入库）；`记录/…-004…md`、`记录/…agent-wiki…md` 属前序批次产物。

---

## 二、6 条门禁实跑读数表

| # | 命令 | 读数 | exit | 定性 |
|---|------|------|------|------|
| G1 | `python -B 程序文件/validate_governance.py` | 总评 **FAIL｜通过 207｜失败 4｜警告 1** | **1** | **预期中间态**：4 FAIL＝检查 6 两条 (A) 跨层落后（change-governance.md RT_SHARED/REPO_SHARED `282d…`≠L1 `4305…`）＋检查 7 两条键闭合悬空（PL-013/PL-016→CG-20260918-005 未入 §九）。带平＋写 §九 行后应转 PASS。警告 1＝检查 3 既有 41 条无核验日期聚合，不计 fail。检查 7 其余 13 项断言全 OK。 |
| G2 | `python -B 程序文件/verification_policy_test.py` | **Ran 6 tests，OK** | 0 | 绿，与基线同值 |
| G3 | `python -B 程序文件/ace_regression_test.py` | **Ran 90 tests，OK** | 0 | 绿，与基线同值 |
| G4 | `python -B "…\prefab-standards-reviewer\sre_regression_test.py"` | **Ran 66 tests，OK**（缺口信号 0、跨轮指纹 `4f53cda18c2baa0c`） | 0 | 绿，与基线同值；report.json 被复跑重写属预期（机器本地、gitignore） |
| G5 | `python -B "…\prefab-standards-reviewer\sr_ic07_compliance_test.py"` | **Ran 20 tests，OK** | 0 | 绿，与基线同值 |
| G6 | `python -B 程序文件/pending_ledger_test.py` | **Ran 36 tests，FAILED (failures=1)** | **1** | **预期中间态**：唯一失败＝`test_real_repo_ledger_is_clean`（`2 != 0`，恰为 PL-013/PL-016 两条 CG-20260918-005 键闭合）；其余 35 例全 ok。写 §九 行后应转 OK。 |

---

## 三、发现的问题清单（按严重性排序）

| # | 严重性 | 问题 | 建议处置 |
|---|--------|------|----------|
| P1 | 低·预期中间态（非缺陷） | 检查 6 两条 (A) FAIL：L1 §十一未带平到运行时/仓内镜像 | 登记后由 `prefab-governance-sync` 按 L1→下游带平；带平后两份副本哈希须＝L1 `4305c210d59698b6`，检查 6 转 [OK] |
| P2 | 低·预期中间态（非缺陷） | 检查 7 两条键闭合 FAIL ＋ G6 `test_real_repo_ledger_is_clean` FAIL：CG-20260918-005 的 §九 变更日志行尚未写入 | 在 §九 补写 CG-20260918-005（B 级）行；写入后真值源含该键，检查 7 与 G6 均转 OK（本复核已验证其余断言全绿） |
| P3 | 提示·非本批引入 | `.tmp-verify/` 未跟踪临时目录（本复核取证产物） | 提交前删除；不入版本库 |
| P4 | 观察·已如实披露 | 11.3 第 3 条：反向覆盖（§九散文遗留→台账）未机算，新遗留漏登记仍靠人工 | 本批已显式披露且与 PL-010 同族登记；后续如欲机算须先解决散文字面无稳定形态问题，非本批范围 |

> 说明：P1/P2 是「登记 → 带平/写 §九 行」正常时序造成的中间态，任务书已预告，本复核逐条核对确认其失败面**恰为且仅为**这两类，无第三类潜在结构违法混入（检查 7 对真实台账其余 13 项断言全 OK）。故不构成阻断性缺陷。

---

## 四、总结论

**PASS（有条件）**——A—F 六项：A/B/C/E/F 判 **PASS**，D 判 **PASS（含两处已定性的预期中间态）**。

本批改判 **B 级成立**（先例 CG-20260916-010 真实存在且同型；未触碰标准数值与运行时技能文件，无应升 A 而未升情形）；检查 7 的分档/列定位/日期解析/键闭合真值源/常量↔散文一致性均实现正确；36 例门禁负向注入逐类有效、无恒真空跑；台账 16 行内容与 11.3 披露如实。

当前 validate（exit 1）与 pending_ledger_test（exit 1）的两处转红，经核对**恰为且仅为**「§十一未带平」与「CG-20260918-005 的 §九 行未写入」两类登记时序中间态，非实现缺陷。

**同意按 B 级继续**：先补写 §九 CG-20260918-005 行 → 由 `prefab-governance-sync` 带平 §十一至运行时/仓内镜像 → 复跑 6 门禁应全绿（validate 转 PASS｜失败 0、pending_ledger 转 Ran 36 OK）→ 删除 `.tmp-verify/` → 单提交入库。提交与推送须用户另行明文确认。
