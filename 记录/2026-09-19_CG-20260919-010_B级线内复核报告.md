# CG-20260919-010 线内独立只读复核报告（B 级）

- 复核批次：**CG-20260919-010**（承接 §11.2 台账 **PL-025**）
- 复核角色：prefab-standards-reviewer（独立只读复核代理）
- 复核日期：2026-09-19
- 被复核改动：`standards-index.md` §7.1 第 296／297 行「标准名称」单元由截断简写补全为 §三 主表官方全称逐字值，并对索引五处副本带平
- 取证环境：仓 `D:\Qoder-Files\装配式装修技能开发`（`origin/master`，工作区 3 件 M）＋ 运行时根 `C:\Users\聂卫华\.qoder\skills`
- 复核用脚本：全部置于仓外 `D:\Qoder-Files\_tmp-scripts\`（`cg010_review_a.py`—`cg010_review_g.py` 与门禁输出 `g1_validate.txt`、`g_*.txt`），不落 C 盘、不进运行时技能目录、不进 `技能仓备份/`，不会被 `sync_skill_backup.py` 当新增件带入镜像；**按 brief「跑完删除」于复核收尾清除**，故本报告 §④ 的复算配方写成自包含命令

---

## 结论

**PASS-with-OBS**：本批内容改动逐字符正确、最小、五处副本字节带平且行尾形态零漂移，对运行时载入面与七道门禁均为可复算的 no-op（225／0／1／exit 0 等读数全部实跑吻合），判级由 A 改判 **B 级成立且正确**；OBS 全部落在「brief 断言口径缺陷」「登记面尚未回填的时序中态」「§7.1 其余列与 §二 :188 的他行残余」「PL-025 判级原述失实须据实订正」四处，**无一属本批写入的内容缺陷**。

---

## ① 逐项取证与实测读数

> 口径声明：所有等值判定一律用 Python 内 `str ==`／原始字节 `sha256`／`count` 完成；控制台中文因 GBK 代码页显示为乱码，**不作为任何判据**（brief 第 9 项）。所有断言均可用本报告所列命令复算。

### 1. 改动面最小性 —— **通过**

| 取证动作 | 实测读数 | 判定 |
|---|---|---|
| `git status --porcelain`（复核结束时） | 恰 3 件 ` M`：`_专题_技能合集策划/standards-index.md`、`技能仓备份/standards-index.md`、`技能仓备份/shared/standards-index.md`；**零未跟踪、零其他改动件** | ✓ 写面无越界 |
| `git diff --numstat` | 三件各 `2 2`；`--stat` 合计 `3 files changed, 6 insertions(+), 6 deletions(-)` | ✓ 每处副本恰 2 行 |
| L1 逐行差分（HEAD 前像 444 行 vs 工作区 444 行） | 差异行号 **仅 [296, 297]**；行数增量 `len(new)-len(old) = 0` | ✓ 全文件仅两物理行 |
| 差异行的逐列比对（转义感知切格） | 296／297 行**只有 col2（标准名称）变**；col1 编号、col3 实施日期、col4 替代关系、col5 影响范围、col6 建议行动 **六列中五列 same**；两行改前改后列数均 **6** | ✓ 仅名称单元 |
| 字节增量核对 | HEAD 前像 91,912 B → 工作区 91,984 B，**Δ = 72 B**；与「新增 `：结构体系的特殊要求`（10 字 ×3 B = 30）＋新增 `：试验方法和试验结果应用指南`（14 字 ×3 B = 42）」的算术和 30+42 = **72** 精确吻合 | ✓ 无隐藏字节变化 |
| 头部声明「收录标准总数：69 条」字面（:11） | 改前改后均在同一位置、字面全等（old/new 同现，`69 条（强制性国标 15 + 推荐性标准 24 + 图集 5 + 产品标准 21 + 其他相关标准 4）`） | ✓ 零改动 |
| §三 全表、§二 聚合行（:188）、§八 变更日志（:326 起）、§7.2／§7.3、§十 接口节 | 由「全文件差异行号仅 [296, 297]」**结构性推定零改动**（这些节均在 :296 之外） | ✓ 零改动 |

**结论**：无越界改动。发起人自述的改前／改后哈希（`2f7ff23f12fcf667` → `abdf525fc61b8d08`）与 0 个 CR、444 行、恰两物理行变化，**全部独立复算成立**。

### 2. 逐字全等 —— **通过**

| 比对 | 实测 |
|---|---|
| §三 :142 名称单元（7 列行的 col3） vs §7.1 :296 名称单元（6 列行的 col2） | `==` → **True**；UTF-8 字节全等 → **True**；长度均 **26 码点／72 字节**；两侧 `sha256[:16]` 同为 `2624f62586f72a65` |
| §三 :143 名称单元 vs §7.1 :297 名称单元 | `==` → **True**；UTF-8 字节全等 → **True**；长度均 **29 码点／83 字节**；两侧 `sha256[:16]` 同为 `d46d09e3ce5143f7` |
| 空格与冒号形制审计 | 两行均为：idx10 = U+0020（词间单空格，非 U+3000）；分隔冒号 = **U+FF1A 全角「：」**（:296 在 idx16、:297 在 idx15），§三 与 §7.1 位置与码点完全一致 |
| Markdown 列数 | §7.1 两行改后仍 **6 列**；§三 两行仍 **7 列**（本批未触 §三） |
| 与发起人给定「目标字面」的关系 | §7.1 现值与 §三 现值**同时**等于 brief 所给两条字面（`cb==target_literal: True`、`ca==target_literal: True`），即基准与结果三方同值 |

### 3. 五处副本一致性与行尾形态 —— **通过**

实算（原始字节直读、`sha256` 不做 eol 归一）：

| 副本 | 路径 | sha256 前 16 | 字节 | CR | LF | 行数 | 形态 |
|---|---|---|---|---|---|---|---|
| L1 | `_专题_技能合集策划/standards-index.md` | `abdf525fc61b8d08` | 91,984 | 0 | 444 | 444 | pure_lf |
| 运行时根 | `~/.qoder/skills/standards-index.md` | `abdf525fc61b8d08` | 91,984 | 0 | 444 | 444 | pure_lf |
| 运行时镜像 | `~/.qoder/skills/shared/standards-index.md` | `abdf525fc61b8d08` | 91,984 | 0 | 444 | 444 | pure_lf |
| 仓内根 | `技能仓备份/standards-index.md` | `abdf525fc61b8d08` | 91,984 | 0 | 444 | 444 | pure_lf |
| 仓内镜像 | `技能仓备份/shared/standards-index.md` | `abdf525fc61b8d08` | 91,984 | 0 | 444 | 444 | pure_lf |

- **五处 sha256 前 16 互等**：`distinct sha16 across 5 copies: 1`；完整 64 位哈希同为 `abdf525fc61b8d086885049499d484fff14399a2b4b12dd6b4cccbe85c6044bd`（即**字节全等**，非仅前 16 位巧合）。
- **CR 计数全为 0、行数全为 444** ✓。

git 可见三处副本 vs `git show HEAD:<path>` 前像的**行尾形态**比对：

| 副本 | HEAD 前像形态 | 工作区形态 | 形态一致 | 字节一致 |
|---|---|---|---|---|
| L1 | pure_lf（CR 0 / LF 444，sha16 `2f7ff23f12fcf667`） | pure_lf（CR 0 / LF 444，`abdf525fc61b8d08`） | **True** | False（本批真实内容编辑） |
| 技能仓备份/ | 同上 | 同上 | **True** | False |
| 技能仓备份/shared/ | 同上 | 同上 | **True** | False |

→ 本批内容编辑**未触发**行尾形态漂移，与 `validate_governance.py` 检查 6 实跑读数一致：**(C) 断言 [OK] 恰 15 项、FAIL/WARN 0 项**（含 `standards-index.md@L1 / @REPO_ROOT / @REPO_SHARED` 三条均为「行尾形态与 HEAD 前像一致（纯 LF）」）。

**带平完整性（超出索引本身的全面反查）**：`python -B 程序文件/sync_skill_backup.py --check`（检查模式、未写盘）读数 **范围 122｜一致 121｜新增 0｜更新 1｜移除 0**，唯一更新件为机器本地 `prefab-standards-reviewer/sre_regression_report.json`——**由本复核实跑 SRE 门禁重写所致**（brief 已豁免的既有行为）。**新增 0／移除 0** ⇒ 本批未漏带平任何一件、未在镜像内引入越界新件。

### 4. 门禁耦合独立取证 —— **通过（但 brief 的一项断言口径须订正，见 OBS-1）**

**(a) 运行时载入面（`sre_reasoner.py`，源码直读＋实调用）**

- `_classify_status_table(header)`（:347）判据顺序：① 命中 `_VERIFY_HEADER_KEYS` 或「锚定ID」→ None；② `"状态" not in header`（**列表精确成员判断，非子串**）→ None；③ 无 `_ID_COLS` 成员 → None；④ `header[0] == "序号"` → main，否则 register。
- §7.1 表头实值 `['标准编号','标准名称','实施日期','替代关系','影响范围','建议行动']` **不含精确的「状态」列**（「替代关系」不等于「状态」）→ 第 ② 步即返回 `None`。**实调用读数：`_classify_status_table(§7.1表头) → None`、`_classify_verify_table(§7.1表头) → False`**。
- 对照组（证明判据不是恒真）：§三 表头 → `main`；§7.2 表头（含精确「状态」列）→ `register`。
- `_load_standards_index()`（:372）另按 `^##` 切块，§7.1 位于 `## 七、标准状态变更预警` 块内、该块标题不含 `_INDEX_L1_HEADING = "第一层级"`，故亦不进 `INDEX_L1_IDS`。

**(b) 门禁侧区间（`validate_governance.py`，源码直读＋实调用）**

- 检查 2 `check_standards()` 计数区间：`body_start = ^##\s+二、`、`body_end = ^##\s+七、`（**§七 及其全部子节 7.1/7.2/7.3 在区间之外**，:509—:519）。
- 区间内仍按表头分流：`classify_index_table()`（:959）对 §7.1 表头实调用 → **`"other"`**（非 main／非 verify／非 register）；`check_standards` 侧另需 `cells[0].isdigit()`（序号列）才计数，而 §7.1 行首列是编号 → **三重排除**。
- T-A1 索引侧比对面：`idx_names` 来自 `check_standards` 产出的 `std_names`（:1147），其构造点 :575—:576 只在 `in_main_table` 且 `cells[0].isdigit()` 时写入，故 **T-A1 比对面不含 §7.1**；`region_tables = iter_md_tables(lines, body_start, body_end)`（:1127）同样止于 §七 之前。
- 实调用对照读数：`classify_index_table`：§三→`main`、§7.1→`other`、§7.2→`register`、§7.3→`register`、§八→`other`。

**(c) 改前／改后「双载入差分」（决定性证据，不采信任何自述）**

在 `D:\Qoder-Files\_tmp-scripts\cg010-sandbox\` 下镜像 `<root>/prefab-standards-reviewer/sre_reasoner.py` ＋ `<root>/shared/standards-index.md`（该函数取 `parent.parent/shared/standards-index.md` 为第一优先），分别注入 **HEAD 前像**与**工作区改后件**并 `exec_module` 触发导入期载入：

| 桶 | OLD（改前） | NEW（改后） | 逐键差分 |
|---|---|---|---|
| `STANDARD_NAMES` | 79 | 79 | **0** |
| `STANDARD_STATUS` | 79 | 79 | **0** |
| `STANDARD_VERIFY` | 29 | 29 | **0** |
| `INDEX_L1_IDS` | 15 | 15 | **0** |
| `STANDARD_PROVINCE` | 8 | 8 | **0** |

`TOTAL DELTA = 0` ⇒ **§7.1 编辑对运行时载入面是可复算的 no-op**。

同一差分在门禁侧亦已实算：`check_standards(OLD)` 与 `check_standards(NEW)` 的三元返回值全等——返回 [0] `True == True`、返回 [1]（主表行集）**69 行 = 69 行、identical True**、返回 [2]（`std_names`，即 T-A1 索引侧比对面）**68 键 = 68 键、identical True**；其中 `GB/T 9978.10-2026`／`GB/T 9978.3-2026` 两键的**改前后值均已等于官方全称**（说明 §三 自 CG-20260918-004 起就是全称、本批未触，本批是让 §7.1 追平 §三，方向正确）。

**⚠ 据实订正 brief 的一项断言**：brief 第 4 项要求确认「两编号在 `STANDARD_NAMES`／`STANDARD_STATUS`／`STANDARD_VERIFY` 中**不存在**」。实测：

| 编号 | in STANDARD_NAMES | in STANDARD_STATUS | in STANDARD_VERIFY | in INDEX_L1_IDS |
|---|---|---|---|---|
| GB/T 9978.10-2026 | **True**（值＝官方全称） | **True**（值＝`即将实施`） | False | False |
| GB/T 9978.3-2026 | **True**（值＝官方全称） | **True**（值＝`即将实施`） | False | False |

即两编号**确实存在**于前两个桶——但**来源是 §三 主表（`kind == "main"`），与 §7.1 无关**。按 brief 字面判会误报 FAIL；本复核改按「OLD/NEW 双载入差分＝0」这一**实质判据**取证，结论不变且更强（不仅 §7.1 不入面，且改后面值一字未动）。已登记为 OBS-1。

### 5. 判级复核 —— **同意改判：本批 = B 级**（专节见 ②）

### 6. 凭据链充分性 —— **成立（本批无须新增联网核验）**

| 取证动作 | 实测读数 | 判定 |
|---|---|---|
| 两条 openstd 凭据串是否**逐字**登于 `_专题_技能合集策划/change-governance.md` | `newGbInfo?hcno=F61CE03ABBE22EE57D6B7C4892D76F29` 与 `hcno=1F9E4AE630C575FB238E92990AB3EC73` 均命中，各 **4 次**（§九 -009 行＋§11.2 PL-022 行＋PL-025 依据列等） | ✓ 既立 |
| 两条凭据是否亦登于 -009 复核报告 | 两条均命中（`记录/2026-09-19_CG-20260919-009_B级线内复核报告.md:198` 独立性条） | ✓ |
| -009 所记「官方逐字全称」字面与索引 §三 :142／:143 现值 | `T10 in change-governance.md: True`、`in -009 report: True`，且 `§三:142 == T10 → True`、`§三:143 == T3 → True`、`§7.1:296 == T10 → True`、`§7.1:297 == T3 → True` | ✓ 四方自洽（登记面／-009 报告／§三 现值／§7.1 新值同字面） |
| §三 :142／:143 是否被本批改动 | **未改**（差异行号仅 [296, 297]，§三 在 :142／:143） | ✓ 基准未被本批自改自证 |
| 体例推定风险 | §三 与 §7.1 两行的「第 N 部分」均为**无词间空格**形制（`第10部分`／`第3部分`），与 §三 :156 GB/T 9978.1-2008 `建筑构件耐火试验方法 第1部分：通用要求` 同族体例一致；未出现「第 10 部分」带空格旧形（仅 :9 头部历史快照句与 :349 §八 历史行内提及旧体例，属快照纪律不改写面） | ✓ 非体例推定，逐字取自既有官方全称字段 |

**判断**：本批**只做「索引内部两表名称一致性」的对齐**，不提出任何新的外部事实声称（不改状态、不改实施日期、不改替代关系、不改收录总数），其基准「§三 :142／:143 现值」自身已由 CG-20260919-009 的 openstd 逐字取证背书且本批未触。故「不新增联网核验、以既立凭据＋§三 现值为唯一基准」**成立且足够**。本复核亦**未联网**（brief 未要求，且见 OBS-7 的同机共盲披露：离线代理无法复算官方现值，此为固有限制而非本批缺陷）。若业务侧要收敛该限制，唯一路径是按 §1.1A 走一次官方平台复读并补 §二 核验记录行（另批，见 OBS-5）。

### 7. 残留与不扩面（只报告不处置）—— **无真实残留；命中共 17 处全为假阳性／历史快照／回退保险**

扫描范围与排除：仓（`D:\Qoder-Files\装配式装修技能开发`）＋ 运行时根（`~/.qoder/skills`）全量 `.md/.py/.json/.txt/.yaml/.yml/.csv/.pyi`；排除 `.git`、`__pycache__`、`.qoder`、文件名含 `_pre`／`.bak`、路径含 `记录/备份归档/`。判据＝正则 `耐火试验方法\s*第\s*(10|3)\s*部分` 命中后，检查其后是否紧接全角「：」＋官方后缀；同一处先判 FULL 再判 TRUNC。命中总数 70，其中 **TRUNC 17、FULL 53**。

| 命中载体 | 处数 | 定性 |
|---|---|---|
| `_专题_技能合集策划/change-governance.md:371`（§九 CG-20260919-009 ⑤「§7.1 两行单列」句）＋ `:435`（§11.2 PL-025 事项列）——连同其两镜像 `技能仓备份/shared/change-governance.md`、`~/.qoder/skills/shared/change-governance.md` 各同 2 处 | **6** | **假阳性／历史快照**：均为「登记改前值」的引用句。三份 change-governance.md 现互等 `sha16 06c26841717da26f／587,684 B／CR 0`（本批复算），说明是**镜像带平后的一致引用句**，非漂移。AGENTS.md 明定历史 CG 行不改写 |
| `记录/2026-09-19_CG-20260919-009_B级线内复核报告.md:66`（2 处）＋`:189`（1 处，`记录/备份归档/2026-08-06/backup_skills_shared_20260806/standards-index.md:121/:122` 带词间空格旧体的披露句） | **3** | **假阳性／执行记录快照**：-009 复核报告对其 OBS-4 已明文「按纪律不改写，此处仅登记读数以免后续复核重复误判为新错」 |
| `~/.qoder/skills/_pre20260918CG001/standards-index.md:293/:294`、`_pre20260918CG010/standards-index.md:294/:295`、`_pre20260918CG010/root__standards-index.md:294/:295`、`_pre20260918CG010/shared__standards-index.md:294/:295` | **8** | **假阳性／回退保险**：属 brief 明定排除的 `_pre*`；且 AGENTS.md 门禁表「反向缺口」条已定：仓内镜像侧不是 `_pre*` 留存位置，运行时侧备份件仅机器本地保险 |
| **合计** | **17** | **真实残留 0 处** |

**索引五处副本的定向复扫**（只查含 `9978.(3|10)-2026` 的行、判「第 N 部分」缺官方后缀）：L1／运行时根／运行时镜像／仓内根／仓内镜像 **residual truncated rows 均为 `[]`**，§三 :142／:143 与 §7.1 :296／:297 四行现值互等 ⇒ **截断简写在索引面上已净**。另复扫 PW `reference.md:107/:108`（运行时＋仓内镜像两处均 FULL，随 CG-20260919-009 收口）、PW `SKILL.md:421`（FULL）、`_专题_隔墙技能策划/隔墙技能参考规范标准.md:47/:49`（FULL）——**9978 族技能面与索引面已全一致，本线可宣告收口**。

**「其余列是否还有其它内部不一致」——如实报告，不处置（见 OBS-4）**：

| 列 | §7.1 :296（9978.10） | §7.1 :297（9978.3） | §三 :142／:143 对应值 | 判定 |
|---|---|---|---|---|
| 实施日期 | `2026-10-01` | `2026-10-01` | `2026-10-01`／`2026-10-01` | **一致** |
| 状态／替代关系 | `新增（无直接替代旧标准）` | `新增` | 两行 §三 状态均 `即将实施`（§三 无替代关系列） | §7.1 **两行措辞不对称**（同为无前版的新增标准，一行标注「无直接替代旧标准」、一行未标），属真实体例残余；本批未触 |
| 影响范围 | `隔墙/吊顶耐火试验方法` | `耐火试验结果应用指南` | `隔墙/吊顶（耐火试验方法）`／`隔墙/吊顶（耐火试验指南）` | **两处不一致**：:296 缺 §三 的全角括号体例；**:297 整体丢掉「隔墙/吊顶」范围限定**（§三 有、§7.1 无），语义面差异比体例面更实质；本批未触（brief 限定只改名称单元） |
| 建议行动 | `关注新旧试验方法差异，更新验证协议引用` | `关注结果应用方法的变化` | §三 无该列 | 不可比，非缺陷 |

### 8. 门禁实跑读数复核 —— **七条全部与发起人自述吻合**

| 命令 | 发起人自述 | **本复核实跑读数** | 吻合 |
|---|---|---|---|
| `python -B 程序文件/validate_governance.py` | PASS｜225｜0｜1｜exit 0 | `总评：PASS \| 通过 225 失败 0 警告 1`、`EXIT=0`；主表 `69 行`、T-A1 `[OK] SRE 硬编码名称与索引主表名称零冲突`、检查 6 (A)(B)(C) 全 [OK] 且 **(C) [OK] 恰 15 项／(C) 无 FAIL 无 WARN**、检查 7 `[OK] 待处置项零超期（6 项）`；唯一 WARN ＝ `无有效「核验日期」记录 38 条（强制性国标 3／其余 35）` | ✓ |
| `python -B 程序文件/verification_policy_test.py` | 6 OK | `Ran 6 tests`／`OK`／`EXIT=0` | ✓ |
| `python -B 程序文件/ace_regression_test.py` | 90 OK | `Ran 90 tests`／`OK`／`EXIT=0` | ✓ |
| `python -B 程序文件/pending_ledger_test.py` | 39 OK | `Ran 39 tests`／`OK`／`EXIT=0` | ✓ |
| `python -B 程序文件/cross_layer_eol_test.py` | 25 OK | `Ran 25 tests in 4.646s`／`OK`／`EXIT=0` | ✓ |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py` | 66 OK，指纹 `4f53cda18c2baa0c` | `Ran 66 tests`／`OK`／`EXIT=0`；尾行 `缺口信号 0 条｜本轮指纹 4f53cda18c2baa0c（上轮指纹 4f53cda18c2baa0c）｜连击一致 128 轮` | ✓（连击轮数属机器本地状态，不钉数字） |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py` | 20 OK | `Ran 20 tests`／`OK`／`EXIT=0` | ✓ |

- **无检查 6 跨层红**，故无需区分「本批漂移」与「登记时序中间态」；`sync_skill_backup.py --check` 的新增 0／移除 0 亦佐证五处带平已到位（发起人收尾若复跑 sync，预期唯一更新件仍是机器本地 `sre_regression_report.json`）。
- 复核 hygiene：`python -B` 全程，`~/.qoder/skills` 下 `.pyc` 计数 **0**；本复核对仓内**零既有文件写入**。

---

## ② 判级意见（本批最要害一项）

**同意改判：CG-20260919-010 应为 B 级，PL-025 原述「须走 A 级并单独审批」不成立。** 三条子问逐一回答：

### (a) 本批落 B 级

判据链（全部现读，不采信转述）：

1. **§一 定义表（`change-governance.md:51—56`）**：A 级 ＝「**标准废止**」——定义文本是「推荐性标准或行业标准被废止/替代，技能引用了已失效的条文」，典型场景是「推荐性国标废止、产品标准被新版替代、地标更新」。本批**不废止任何标准、不新增任何替代关系、不改任何状态字段／实施日期／数值**（① 表实测：仅 col2 变，col3/col4 五列 same，§三 :142/:143 未触，§二 :188 未触，收录总数 69 不动）。A 级的**定义触发子句缺失**。
   B 级 ＝「新增内容、**修正错误**、优化表述等不影响安全性的常规变更」——本批是把索引内部两表互相矛盾的同一编号名称对齐为官方全称逐字值，正是「修正错误」。
2. **§4.1（:128—135）**：四行专项规则分别是「新增标准」「标准废止/替代」「标准状态变更」「标准编号/年份修正」，其中**唯一与名称／编号字面相关的第四行明文写「B 级变更，须全文扫描确认所有引用该标准的文件均已同步修正」**。§4.1 **不存在**「写索引本体即 A 级」这类按文件身份定级的规则。
3. **§二 第 3 步（:75—:79）**：A 与 B 在流程上的差**只有**「S/A 级须 …**＋变更发起人确认**」这一项，复核要求（prefab-standards-reviewer 复核）两档相同。也就是说 §二 根本没有把「触本体」映射到 A 级的通道。
4. **就高原则不适用**：本批不并存任何 A 级面（无状态改判、无日期订正），故无从「就高」。

→ **本批＝B 级，须 prefab-standards-reviewer 复核（本报告的履行即是），无须 §二 第 3 步的 A 级发起人确认**。

### (b) 「触索引本体 ⇒ A 级」被明确否证

| 先例 | 是否写了 `standards-index.md` 本体 | 定级列原文（现读 §九） |
|---|---|---|
| **CG-20260918-009**（:361） | **是**（§二 官方核验记录表补序号 68 行） | **B 级**，且明写「定级依据＝**写面触 `standards-index.md` 本体**＝运行时技能文件读取的数据源＋四层副本，须 `prefab-standards-reviewer` 线内复核（§二 第 3 步 B 级）」＋「**未升 A 级的判据**＝本批不新增/不改动任何标准数值、状态字段与替代关系，收录总数仍 69 条」 |
| **CG-20260918-010**（:362） | **是**（§二 推荐性标准抽查核验记录表补 15／18 两行） | **B 级**，同一判据句式「写面触 `standards-index.md` 本体 … 须线内复核（§二 第 3 步 B 级）」 |
| **CG-20260918-004**（:356，§八 :353 亦载影响面含「**§7.1 两行**」） | 是，且**写过 §7.1 两行** | **A 级**——但其定级列把 A 的理由明确记为「§一定义表 A 级典型场景『推荐性标准被废止或替代』」＋「发现**序号 18 状态与两处实施日期**共三项官方真值冲突后 … 按**就高原则**升 A 级」；而该批的**名称面（＝与本批同类的工作）在原开线时即定 B 级**（「原开线为遗留 (c)『索引全量词间空格体例统一』（B 级）」） |

→ 命题**被否证**：两批（0918-009／-010）都写了索引本体且定 B 级；更直接的，**CG-20260918-004 亲自写过 §7.1 两行**，其名称／体例面当时就按 B 级开线，A 级只由「状态＋实施日期」冲突就高带来。三批合起来把判据钉在**「改了哪一类事实」而非「碰了哪一个文件」**。

### (c) PL-025 的推导确构成逆命题误用

CG-20260919-006（:368）定级列原句：「**不升 A** ＝ 不触任何标准数值、状态字段、替代关系与收录总数（索引 69 条不动），**亦不改 `standards-index.md` 本体**」——这是一组**充分条件合取式**（满足全部则不升 A），不是「不写本体 ⇔ 不升 A」的充要式。CG-20260919-009（:371）⑤ 与其 PL-025 转写（:435 事项列）把它读成「写本体 ⇒ A 级」，属**肯定后件／否定前件式的逆命题误用**：由「P ∧ ¬Q ⇒ B」推不出「Q ⇒ A」。

且该误用还有一处内部矛盾可机算指认：CG-20260919-009 的**定级列自身**写的是「不升 A ＝ …零触碰 `standards-index.md` 本体」，而同批 ⑤ 把「须走 A 级」写进**变更内容列**——同一批内对同一先例逻辑的两种用法，且前者与 0918-009／-010 的先例（写本体仍 B）直接冲突。

**建议的闭合写法（不代改，供发起人在 §九 新行与 PL-025 处置依据列采用）**：

- 依「历史快照纪律」，PL-025 **事项列原述（含「须走 A 级并单独审批」）与 CG-20260919-009 ⑤ 句均不改写**；
- 在 PL-025 **处置依据列**据实记：本批改判为 B 级，判据＝§一 定义表（A 级触发子句为「废止/替代」，本批不触任何状态／日期／数值／替代关系／收录总数）＋§4.1 第 4 行「编号/名称字面修正＝B 级，须全文扫描同步」＋先例 CG-20260918-004（曾写 §7.1 两行、其名称面按 B 开线，A 由状态与日期冲突就高而来）＋CG-20260918-009／-010（两批均写索引本体且定 B 级）；并**显式标注 PL-025 原述的「写本体 ⇒ A 级」系逆命题误用**，同族判断已在 CG-20260919-006 定级列以合取式而非充要式表述。
- 若发起人认为「触本体须 A」这一成文化需求真实存在，那是**新立规则**（改 §一 或 §4.1），属 S/A 级治理件变更、须另行裁定，不能以「先例如此」的名义在 B 级批内既改判又改规则。本批只作**改判**（B），未改规则文件，**这一点已实测确认**：§一／§4.1／§4.x／§二 全部零改动（`git status` 仅 3 件索引 M，三份 change-governance.md 仍互等 `06c26841717da26f`）。

---

## ③ OBS 清单（不粉饰）

- **OBS-1（brief 断言口径缺陷·中）**：brief 第 4 项「确认两编号在 `STANDARD_NAMES`／`STANDARD_STATUS`／`STANDARD_VERIFY` 中**不存在**」与数据面矛盾——两编号在 `STANDARD_NAMES`（79 键）与 `STANDARD_STATUS`（79 键）中**确实存在**，其来源是 §三 主表（`_classify_status_table → "main"`）而非 §7.1；在 `STANDARD_VERIFY`（29 键）与 `INDEX_L1_IDS`（15 元）中不存在。照 brief 字面执行会误报 FAIL。本复核改用「OLD/NEW 双载入逐键差分＝0」作实质判据（`STANDARD_NAMES/STATUS/VERIFY/INDEX_L1_IDS/STANDARD_PROVINCE` 差分 **0/0/0/0/0**）。建议后续 brief 把该判据统一表述为「运行时载入面逐键零变化」。
- **OBS-2（交付不完整·时序中态，非内容缺陷·高优先须闭合）**：本批复核时**登记面尚未回填**——① `standards-index.md` 头部「最后更新」（:7）仍指 `2026-09-18（CG-20260918-004，A 级）`，未指向本批；② 索引 §八 变更日志 **2026-09-19 行数＝0**（末行为 :355 CG-20260918-010）；③ §九 无 `CG-20260919-010` 行；④ PL-025 状态仍「待处置」。§二 第 4 步明定「更新变更日志（**各文件自身的变更日志** ＋ 本文件第九节）」，若批次在此停止则索引本体已变而自身日志未变，属**未完成交付**并会制造新的内部不一致（正是 CG-20260919-003 闭合 PL-023 时「改态同批回填」的做法）。**本报告不构成放行，须待 ①—④ 回填后按 §二 收尾。**
- **OBS-3（判级原述失实须订正·中）**：PL-025 事项列与 CG-20260919-009 ⑤ 的「属 `standards-index.md` 本体、须走 **A 级**并单独审批」是逆命题误用（详见 ②(c)），且与 CG-20260918-009／-010 两个「写本体而判 B」的在案先例正面冲突。按快照纪律**不改写原述**，但须在处置依据列点名订正，否则该误用会作为先例被后续批次继续引用（本仓已有「先例即判据」的引用习惯，扩散风险真实）。
- **OBS-4（§7.1 其余列真实残余·低，属他行携办，本批未触）**：:296／:297 两行的「替代关系」措辞不对称（`新增（无直接替代旧标准）` vs `新增`）；「影响范围」与 §三「核心关联技能」体例不一致（:296 缺全角括号），**且 :297 完全丢失 §三 :143 的「隔墙/吊顶」范围限定**。本批 brief 明确只改名称单元，故未扩面正确；但这些是**可指认的内部不一致**，建议另登 PL 携 TTL，勿在本批内顺手改（一改即触 §一「影响范围」语义面，判级讨论会复杂化）。
- **OBS-5（依据链披露不完整·低，非本批写面）**：索引 §二 `:188` 聚合行仍把「序号 **11-13**、19-30、32-54、57-59 …」标为「待核验／待补充官方来源」，而序号 12／13 的**逐字名称字段**已由 CG-20260919-009 从 openstd 取回并背书（凭据 hcno 两条）。即「名称已有据、状态行仍标待核验」，PL-022 事项列自己也点出了这一点。若为 12／13 补 §二 核验记录行，须预期**检查 3 的「无有效核验日期 38 条」WARN 计数联动下降**（先例：CG-20260918-009 补 1 行 41→40、-010 补 2 行 40→38），并同步 AGENTS.md 门禁表该句的档内分布描述——属另批工作，本批不涉及。
- **OBS-6（假阳性口径披露·程序性）**：第 7 项 17 处 TRUNC 命中**全部**属登记面引用句（`change-governance.md:371/:435` × 三副本）、执行记录快照（-009 报告 :66/:189）、`_pre*` 回退前像（8 处）。特此披露口径以免后续复核把它们误计为「零残留断言未过」。索引五处副本的定向复扫结果为 `[]`。
- **OBS-7（独立性受限·程序性，须与结论同读）**：① 本复核为**线内**——与成果同机、同盘、同 git 仓，能独立复算结构与字面一致性（现读、哈希、行列数、双载入差分、门禁重跑），**不能独立复算「openstd 逐字名称字段是否为真」**（沿 CG-20260919-009 报告 §⑤ 第 2 条同一披露，本批未联网）；② 五层哈希互等与 `--check` 读数均取自同一磁盘状态，若同一脚本把各层同时写坏则与检查 6 (A)(B) 共盲，唯一带外部真值源的是检查 6 (C)（HEAD 对象），本批该断言 **15 项全 [OK]**，本代理另手工比对三处 git 可见副本形态；③ 复核者与发起人同会话，沿 CG-20260918-002 用户裁定与 -003/-004/-005/-009 先例，以本条 OBS 显式登记。
- **OBS-8（本复核自身的最小副作用·如实披露）**：本复核实跑 `sre_regression_test.py` 重写了机器本地 `~/.qoder/skills/prefab-standards-reviewer/sre_regression_report.json`（既有行为、brief 已豁免、`.gitignore` 收录），这使 `sync_skill_backup.py --check` 读数由「一致 122」变为「一致 121｜更新 1」；未跑 `sync_skill_backup.py` 写盘、未 commit、未 push、`~/.qoder/skills` 下 `.pyc` 计数仍 0；除本报告外对仓内零写入（`git status --porcelain` 在本报告落盘前恰为 3 件 M、零未跟踪）。临时脚本置于仓外 `D:\Qoder-Files\_tmp-scripts\`，用毕删除。
- **OBS-9（工具回显噪声·非缺陷）**：复核期间 Write/Edit 工具三次回显「cg010_review_*.py 已被用户或 linter 修改」，逐字比对显示回显内容即我所写内容，判为工具级回显而非外部篡改；为消除该疑虑，第 1／2／3／4 项关键读数均在回显事件之后**重跑一次**（差异行号 [296,297]、五处 sha16 `abdf525fc61b8d08`、双载入差分 0、门禁 225/0/1）且两次一致。

---

## ④ 复算配方（每个数字怎么来的；自包含，不依赖已删除的复核脚本）

复核期用的 7 份脚本（`cg010_review_a—g.py`）与门禁输出（`g1_validate.txt`、`g_*.txt`）已按 brief「跑完删除」在仓外 `D:\Qoder-Files\_tmp-scripts\` 清除，故本节的复算配方写成**自包含命令**，逐条可直接跑出本报告读数。

```bash
# ---- 1) 改动面最小性 ----
git -C "D:\Qoder-Files\装配式装修技能开发" status --porcelain     # 期望：恰 3 件 M ＋ 本报告 1 件 ??
git -C "D:\Qoder-Files\装配式装修技能开发" diff --numstat         # 期望：三件各 2 2
```

```python
# 差异行号＝仅 [296,297]、逐列只有 col2 变、五副本哈希、形态比对（仓外临时 .py 或直接 REPL）
import hashlib, subprocess, os
R = r"D:\Qoder-Files\装配式装修技能开发"; T = os.path.expanduser("~/.qoder/skills")
CP = {"L1": R+r"\_专题_技能合集策划\standards-index.md", "RT_ROOT": T+r"\standards-index.md",
      "RT_SHARED": T+r"\shared\standards-index.md", "BK_ROOT": R+r"\技能仓备份\standards-index.md",
      "BK_SHARED": R+r"\技能仓备份\shared\standards-index.md"}
raw = {k: open(p, "rb").read() for k, p in CP.items()}
print({k: (hashlib.sha256(b).hexdigest()[:16], len(b), b.count(b"\r"), b.count(b"\n")) for k, b in raw.items()})
# 期望：五处同为 abdf525fc61b8d08 / 91984 B / CR 0 / LF 444
print(sorted({hashlib.sha256(b).hexdigest() for b in raw.values()}))   # 期望：1 个（字节全等）
gp = "_专题_技能合集策划/standards-index.md"
head = subprocess.run(["git","-C",R,"show","HEAD:"+gp], capture_output=True).stdout
print(len(head), hashlib.sha256(head).hexdigest()[:16], head.count(b"\r"), head.count(b"\n"))
# 期望：91912 / 2f7ff23f12fcf667 / 0 / 444  →  Δbytes = 91984-91912 = 72 = 30+42（两后缀的 UTF-8 长度）
o, n = head.decode("utf-8").splitlines(), raw["L1"].decode("utf-8").splitlines()
print(len(o), len(n), [i+1 for i in range(len(o)) if o[i] != n[i]])     # 期望：444 444 [296, 297]
def cells(l):
    s=l.strip().strip("|"); out=[]; cur=""; esc=False
    for ch in s:
        if esc: cur+=ch; esc=False; continue
        if ch=="\\": cur+=ch; esc=True; continue
        if ch=="|": out.append(cur.strip()); cur=""
        else: cur+=ch
    out.append(cur.strip()); return out
for ln in (296, 297):
    a, b = cells(o[ln-1]), cells(n[ln-1])
    print(ln, len(a), len(b), [i+1 for i in range(len(a)) if a[i]!=b[i]])   # 期望：6 6 [2]
# 行尾「形态」比对（五类）——工作区与 HEAD 前像均应为 pure_lf
form = lambda b: "pure_lf" if b.count(b"\r")==0 and b.count(b"\n") else ("cr_only" if not b.count(b"\n") else "mixed")
print({k: (form(v), form(head) if k.startswith(("L1","BK")) else "-") for k, v in raw.items()})
```

```python
# ---- 2) 逐字全等（§三 名称是 col idx2、§7.1 名称是 col idx1）----
i = raw["L1"].decode("utf-8").splitlines()
t10 = "建筑构件耐火试验方法 第10部分：结构体系的特殊要求"; t3 = "建筑构件耐火试验方法 第3部分：试验方法和试验结果应用指南"
print(cells(i[141])[2] == cells(i[295])[1] == t10, cells(i[142])[2] == cells(i[296])[1] == t3)   # True True
print([hex(ord(c)) for c in cells(i[295])[1] if c in " :："])                                      # 0x20 0xff1a
```

```python
# ---- 4) 运行时载入面 OLD/NEW 双载入差分 ----
# 沙箱镜像 <root>/prefab-standards-reviewer/sre_reasoner.py ＋ <root>/shared/standards-index.md
# （_load_standards_index 第一优先取 parent.parent/"shared"/standards-index.md），分别注入 HEAD 前像与工作区件
import importlib.util, shutil, sys, os
def load(text, tag):
    root = r"D:\Qoder-Files\_tmp-scripts\cg010-sandbox"; root = os.path.join(root, tag)
    pkg = root + r"\prefab-standards-reviewer"; sh = root + r"\shared"
    shutil.rmtree(root, ignore_errors=True); os.makedirs(pkg); os.makedirs(sh)
    shutil.copyfile(T + r"\prefab-standards-reviewer\sre_reasoner.py", pkg + r"\sre_reasoner.py")
    open(sh + r"\standards-index.md", "wb").write(text.encode("utf-8"))
    sp = importlib.util.spec_from_file_location("r"+tag, pkg + r"\sre_reasoner.py")
    m = importlib.util.module_from_spec(sp); sys.modules["r"+tag] = m; sp.loader.exec_module(m); return m
mo, mn = load(head.decode("utf-8"), "old"), load(raw["L1"].decode("utf-8"), "new")
for b in ("STANDARD_NAMES","STANDARD_STATUS","STANDARD_VERIFY","INDEX_L1_IDS","STANDARD_PROVINCE"):
    A, B = getattr(mo,b), getattr(mn,b)
    print(b, len(A), len(B), sorted(set(A)^set(B)) or "keys-same",
          all(A.get(k)==B.get(k) for k in set(A)|set(B)))          # 期望：79/79/29/15/8，全部 True、对称差 0
print(mn._classify_status_table(['标准编号','标准名称','实施日期','替代关系','影响范围','建议行动']))  # None
print(mn._classify_verify_table(['标准编号','标准名称','实施日期','替代关系','影响范围','建议行动']))   # False
shutil.rmtree(r"D:\Qoder-Files\_tmp-scripts\cg010-sandbox", ignore_errors=True)
```

```python
# ---- 4b) 门禁侧区间：classify_index_table 五表头 + check_standards 改前后返回值全等 ----
sys.path.insert(0, R + r"\程序文件")
sp = importlib.util.spec_from_file_location("vg", R + r"\程序文件\validate_governance.py")
vg = importlib.util.module_from_spec(sp); sys.modules["vg"] = vg; sp.loader.exec_module(vg)
print(vg.classify_index_table(['标准编号','标准名称','实施日期','替代关系','影响范围','建议行动']))   # other
a = vg.check_standards(head.decode("utf-8"), vg.Report()); b = vg.check_standards(raw["L1"].decode("utf-8"), vg.Report())
print(a[1] == b[1], len(b[1]), a[2] == b[2], len(b[2]))            # 期望：True 69 True 68
```

```bash
# ---- 3) 带平完整性（只读）----
python -B "D:\Qoder-Files\装配式装修技能开发\程序文件/sync_skill_backup.py" --check
# 期望：范围 122｜新增 0｜移除 0；「一致」数随机器本地 sre_regression_report.json 是否被复跑而 121/122 浮动
git -C "D:\Qoder-Files\装配式装修技能开发" check-ignore -v "记录/2026-09-19_CG-20260919-010_B级线内复核报告.md"
# 期望：未被忽略（rc=1），故本报告随本批入库

# ---- 6/7) 凭据字面自洽与全仓截断简写扫描（判据：耐火试验方法\s*第\s*(10|3)\s*部分 之后是否紧接全角：＋官方后缀）----
# 排除 .git / __pycache__ / .qoder / 文件名含 _pre 或 .bak / 路径含 记录/备份归档/
# 期望：真实残留 0；TRUNC 命中 17 全部落在 change-governance.md:371/:435（×3 副本）、-009 复核报告 :66/:189、_pre* 前像 8 处

# ---- 8) 七条门禁（一律 -B，避免新生 .pyc）----
python -B 程序文件/validate_governance.py              # 总评 PASS｜通过 225｜失败 0｜警告 1｜exit 0；(C) [OK] 恰 15
python -B 程序文件/verification_policy_test.py         # Ran 6 tests OK
python -B 程序文件/ace_regression_test.py              # Ran 90 tests OK
python -B 程序文件/pending_ledger_test.py              # Ran 39 tests OK
python -B 程序文件/cross_layer_eol_test.py             # Ran 25 tests OK
python -B ~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py      # Ran 66 OK｜缺口信号 0｜指纹 4f53cda18c2baa0c
python -B ~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py  # Ran 20 OK
```

> 本文件以二进制方式写入（`open(p,'wb')` ＋ UTF-8），行尾为纯 LF（0 个 CR），与 `记录/` 下既有复核报告形态一致。
