# 2026-09-26 CG-20260926-002 执行记录 — 总单 ↔ 台账 ↔ 执行记录逐项对账批（C 级登记批）

## 一、事项、线的四件与定级

- 主线来源＝`成果/2026-09-25_项目全面审视与下一阶段执行方案.md` §五「总单与实际成果对账」线（本仓当前优先级 **P1**；P0＝同族的内部闭合批 CG-20260926-001 已随 `06987f6` 入库；P2＝宿主实跑线，须外部授权与输入，本批不触碰）。
- 该线四件（逐字取自报告 §五，本批按件验收）：
  - **责任与必要输入**＝「项目执行者；总单、当前台账、对应文件和执行证据」——四者均为本仓既有载体，无外部输入缺口。
  - **最小交付**＝「在现有载体上形成逐项对应关系」——本批不新建台账、不新建任务单，只在 `文档/专业整改任务总单_v1.md` 上追加。
  - **验收标准**＝「每项有原目标、已做动作、证据、剩余条件；未覆盖项明确列出」——见 §十八 七列表与「未覆盖项 A—F」段。
  - **停止条件**＝「无法找到原验收条件或证据冲突时，不给确定性完成结论」——本批未触发（十组原验收条件均可现读），但同批声明**不给「整套技能已验证」总结论**，该结论不属于本线交付面。
- 定级 **C 级**：零技能件正文改动、零改台账状态、零改判据本体；写面全部为登记面与既有任务单的指针补登。§一 C 级「可由变更发起人直接执行，但须记录变更日志」，§二 四步流程不约束本级。同族先例＝CG-20260925-005（PL-038 闭合登记批）。
- 授权字面：发起人 2026-09-25 指令「请通读…基于优先级及依赖关系，给我明示下一步优先事项并持续推进」——本批系该指令下 P1 线的实施批，属「持续推进」授权面内的登记动作，未扩至提交／推送之外的面。

## 二、对账方法（三通道机算口径，行号不作真值）

| 通道 | 取数口径 | 本批现读 |
|---|---|---|
| 状态 | L1 `change-governance.md` §11.2 逐行匹配台账行首列（`^\| PL-\d{3} \|`），列 6 状态由 `validate_governance.md_cells()` 转义感知切格后取值 | 台账 82 行；待处置 20／已闭合 61／已裁定不做 1（**与批前同值＝本批零改台账**） |
| 变更日志 | `validate_governance.collect_cg_log_ids(lines)`（§九 逐行取首列，不用表级解析） | 145→**146**（＋本批 CG-20260926-002 一行） |
| 证据可现读性 | 对每个指针目标做在库存在性读取：`记录/CG*.md` 文件、`成果/标准核验/` 核验报告、`CHANGELOG.md` 版本号、§九 行 | 十组指针节所列目标全部命中；组3 补登后 §十七 所列两记录＋两报告＋§九 两行＋v1.13.22／23 全命中 |

- **判据说明（为何不钉行号）**：历批已反复观测正文增行使引用行号机械下移（如 `A_VERIFY_DATE` 由 :708/:709 → :737/:738），行号在本仓只作定位提示、不作真值；本批 §十七／§十八 的证据锚一律取「文件＋可 grep 字面」，不取行号。
- **状态唯一来源**：PL 状态仍以 §11.2 为准，总单 §十八 表内的「台账现读状态」列是本批时刻的**转述读数**，不构成第二处状态定义，后续批不得在该列就地改判。

## 三、写面清单（全部为登记面／指针面）

| 件 | 改前→改后 | 内容 |
|---|---|---|
| `文档/专业整改任务总单_v1.md` | 369→**421 行**，97,904→**116,097 B**，纯 LF／CR 0，`git diff --stat`＝**52 增 0 删** | 纯追加两节：**§十七**＝第三批（组3 坡度）实施批验收证据指针补登（CG-20260924-002 结构面＋CG-20260924-003 条文面，11 验收面行）；**§十八**＝总单↔台账↔执行记录逐项对账终态（七列十组表＋未覆盖项 A—F＋对账方法与可复算判据）。**不重排既有节号**（历史节号属当时快照，追加而非插入） |
| `程序文件/脚本/cg20260926_002_governance_patch.py` | 新建 **11,333 B**（124 行，纯 LF，sha256 前 12 `d8035ad8bb3f`） | 治理登记面写入件（可复跑凭据）：唯一锚命中数＝1 断言、幂等退出、CR 计数不变自检、纯 LF 写回；仓根由 `parents[2]` 派生 |
| `_专题_技能合集策划/change-governance.md` | 1,078,814→**1,087,190 B**，LF 605→**608**，CR 0，sha256 前 12 **`e14aa65a9f52`** | 头部「最后更新」快照轮换（CG-20260926-001 降为历史快照）＋§九 CG-20260926-002 行。**§11.2 台账 82 行零改**；§九 历史行零改 |
| `_专题_技能合集策划/数据分类复核清单.json` | records 8,713→**8,716**／baseline 3,780 **不变** | 检查 10 重登记（`cg20260926_001_mirror_register.py`，幂等）：摘失效 1／新登 4（头部快照两行判 N、§九 新行八列逐列判 N、裸「>」结构行）。扫描面 57 键**不含 `文档/`、`记录/`、`CHANGELOG.md`**，故总单与本记录编辑不触发残量登记（已现读键集证伪） |
| `CHANGELOG.md` | 新增 `## v1.13.36 — 2026-09-26`（现读 521,607 B／1,315 行） | 本批十项自述（含过程偏差⑦） |
| `技能仓备份/`＋`同步说明.md` | 由 `sync_skill_backup.py` 带平 | 治理文件三层带平（见 §六） |

- **零改面**：15 技能目录全部正文件（SKILL／reference／examples／scripts）、标准限值／检测数值／经验域／公式常数、契约／红线注册表／索引本体／分类判据本体、`validate_governance.py` 与十二门禁判据、§九 历史 CG 行、冻结基准 v1—v3、`素材/`。

## 四、对账结果与未覆盖项（A—F）

- 十组对应关系已在 §十八 成文：组1／2／3／4／5／6／7／8／9 各有实施批与指针节，组10 三条（PL-053 经验域、PL-055 精度声明、PL-057 现场关系）均未实施故无节。
- 状态混合情况**只有一处**：组9（PL-068 已闭合，同组的 PL-053／PL-055 仍待处置）——本批如实登记，不因组内某项闭合而把整组读作闭合。
- **未覆盖项逐条判据**（登记于 §十八，本处补判断）：
  - **A 组3 缺证据指针节**：登记时点遗漏（§十 起以「第四批」编号而第三批节位空缺）。本批以 §十七 补登闭合该项，属**载体补齐**，非新缺陷。
  - **B 组10 无节**：未实施即无证据可指，补空节会把任务单写成愿望清单——判为**符合预期**，不补。
  - **C PL-069—PL-082（十四条）不回写总单**：这些项在总单 §二 覆盖表中无对应组（属后续批次新观测），回写即在任务单上另立一份状态清单，与「状态唯一来源＝§11.2」直接冲突。判据取§五「任务单仅补验收证据指针」纪律。
  - **D 复核形制不齐**：CG-20260924-002／-003 两批无独立 Critic／Evaluator 记录（前者执行记录 §五 自述「本批未做」，并写「如发起人要求 L2 同级背书，另批补做」）。判为**不实质削弱、不重开、不新立 PL**，三条判据：① -003 走 `scanned-standard-clause-verify` 官方逐字核验，证据类强于同级专业背书；② 该组定性已由带 SRE 只读独立复核的 CG-20260926-001 订正（坡度「非规范规定值」判为不成立并改判 A／S1）；③ 发起人未要求补做 L2 背书，主动补做属扩面。
  - **E 授权字面留存不齐**：部分批次的发起人明文只存于当轮会话、未逐字入档。属登记面欠缺，不影响状态真值（状态真值在台账、结论真值在被指向的核验报告与执行记录）。
  - **F 停止条件未触发**：未出现「原验收条件不可得」或「证据冲突」，故 §十八 各组可给逐项结论；同时明确**不给总结论**，剩余条件按末列如实承接。
- **两处易误判的跨批冲突已在 §十七 取定论**：① `-003` 曾把 AC `:85` 坡度标为「非规范规定值」，后经 GB 55030-2022 第4.6.3条判为不成立，由 CG-20260925-008／CG-20260926-001 订正——现读凭据 AC `:78`／`:85`、BK `:58`／`:62`／`:63`；② FL `examples.md` 术语与 107→95 未披露修正（CG-20260925-003 SRE 建议项）读上下文判为口径说明的**否定式披露**（原「ΔLw 估算」与「L'nT,w 达标可行性」两列已撤除并由守卫禁行内回潮），非 PL-056 措辞回流。

## 五、过程偏差（本批自产，据实登记）

1. **登记脚本首落仓外被执行面拦下**：首版写在 `D:/Qoder-Files/_整理与清理/2026-09-26/总单对账批-CG-20260926-002/patch_governance_002.py`，随后的 Edit 与 Bash 执行均被自动模式以「工作区外」为由拦下。根因＝把「可复跑凭据」误放在取证目录。修复＝按《_管理规范/文件与项目管理规范》§3.7（可复用 `.py` → `程序文件/脚本/`）＋C2 程序文件隔离迁入仓内并实跑通过；仓外草稿件批尾删除前先与仓内终版逐字符 diff，确认仓内为超集、草稿无独有内容后处置（空目录一并回收）。
2. **首版脚本硬编码本机盘符**（`Path(r'D:\...')`），与近期提交 `98f5df3` 刚消除的同型缺陷相悖（换克隆位置即断）：迁入时改为 `Path(__file__).resolve().parents[2]` 派生＋`assert CG.exists()`。
3. **脚本自述打印的预期的行数增量错标**：「(预期 +1)」与实际 +3 不符——头部快照一条在文件里占两个物理行（内容行＋裸「>」续行）。已订正为「+3＝头部新增两物理行＋§九 新行一行」，实际读数与订正后一致。

## 六、门禁终态（实跑）

| 门禁 | 读数 |
|---|---|
| `validate_governance.py` | 总评 **PASS**｜通过 **243**／失败 **0**／警告 **3**，exit 0（警告构成与基线相同＝检查 3 聚合 1 条＋`A_VERIFY_DATE` 2 条 `acoustic-calculation-engine/reference.md:737`／`:738`） |
| 十二门禁（11 个 unittest 套件） | 全 **exit 0**：verification_policy 6／ace_regression **176**／pending_ledger 43／cross_layer_eol 25／index_ref 26／skill_cross_layer 21／data_class 70／storage_anchor 19／wetzone **16**／sre_regression 66（指纹 `4f53cda18c2baa0c`）／sr_ic07 20 ＝ 合计 **488 例全绿** |
| 检查 10 残量 | `--data-class-only` 通过（未判定单元 0）；摘失效 1／新登 4 后 records 8,716／baseline 3,780 |
| 三层带平 | L1 ↔ 运行时 `~/.qoder/skills/shared/` ↔ `技能仓备份/shared/` 三处 `sha256` 前 12 均 **`e14aa65a9f52`**、1,087,190 B、CR 0（检查 6 (A)(B) 绿） |
| `sync_skill_backup.py` 读数序列 | 带平首跑「范围 73｜一致 72｜新增 0｜更新 1｜移除 0」（更新件＝`shared/change-governance.md`＝本批写面）→ 门禁复跑后「更新 1」＝机器本地 `sre_regression_report.json`（复跑机械后果）→ **末次 `--check`「范围 73｜一致 73｜新增 0｜更新 0｜移除 0」＝终态带平** |
| 登记面拼接自证 | `git diff --stat` 对 `change-governance.md`＝4 增 1 删，删除行仅头部「最后更新」那一条，无他行漂移；脚本幂等复跑打印「已登记（§九 CG-20260926-002 行存在），幂等退出」 |
| 机算复核 | §九 CG 行 145→**146**；台账 **82 行不变**、状态分布 待处置 20／已闭合 61／已裁定不做 1（＝本批零改台账的直接证据） |

## 七、提交态

- **commit 随本批**（T1，免逐次授权）；**push 不随批**（T2，须发起人当轮命名整句明文，面板选项不算授权）。
- 未推送枚次不钉固定数，以 `git rev-list --left-right --count origin/master...HEAD` 现读为准。
- 发布＝治理发布（三层带平＋登记面齐），不等于对外发布；对外发布另须重签回执。
