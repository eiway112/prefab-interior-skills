# CG-20260921-003 B 级线内复核报告

> **批次**：CG-20260921-003（PL-031 阶段 0 冻结批，B 级）
> **复核方式**：`change-governance.md` §二 第 3 步 B 级复核＝发起人线内只读子代理独立复算（`general-purpose` 代理，禁写、禁 git 写操作，临时脚本限 `D:\Qoder-Files\_tmp-scripts\`）
> **复核日期**：2026-09-21
> **判定**：**首轮 FAIL（12 项中 1 项判假＋3 项部分真）→ 批内修复 8 项＋收尾自证订正 6 项（共 14 项）→ 定向复算全绿**
> **口径说明**：本行原写「批内修复 6 项」，与下方 §二 表实列 8 行不符（属报告自身行列不一致，非代理判定差异），先对齐为 8＋1；主代理随后又自证发现六项（⑩ 指纹可复现性口径、⑪ 变更日志条目粘连行、⑫ 状态列时点态句式、⑬ 机器本地件读数判据化、⑭ 报告自身序号引用悬空，另含 ⑨ IDE／模型两格），现 §二 表实列 **14 行**＝首轮 8 行（①—⑧）＋收尾自证 6 行（⑨—⑭），本行与全篇计数以 8＋6＝14 为准。
> **结论**：本批可入库；PL-031 维持待处置不闭合。

---

## 一、首轮 12 项复算结果

| # | 声明 | 首轮判定 | 代理取证 |
|---|---|---|---|
| 1 | 定级依据（§一「新增内容」＝B 级、C 级＝排版类） | 真 | `change-governance.md:67-68` |
| 2 | 新件存在、20,428 B、纯 LF | 真 | 实测 CR 计数 0 |
| 3 | 题数 33＝A6／B10／C6／D6／E5，P0 清单 10 题 | 真 | 该文 :146 恰 10 题 |
| 4 | IC-10-Request `required=[项目类型, 空间类型]`、`return_trace` 可选 | 真 | `interface-contracts.md:2228`、`:2184/2256` |
| 5 | 红线编号存在且语义相符 | **部分真** | 全部所引编号存在且语义相符（PW:121/124/125、SR:271-273、ACE:335-336、FL:364/365、WS:422、CL:479、OR:88、SSCV:698）；**但登记面枚举了 PW-R-P0-2、FL-R-P0-5 两件文中实际未引用者＝声明夸大** |
| 6 | 覆盖 IC-01—IC-14 全 14 条已注册契约 | **部分真** | 14 条与覆盖属实（:2747-2760），**但节号引错：注册表总览系 §五（:2741），§八＝待注册清单（:2851）** |
| 7 | 降级标注逐字一致 | **部分真** | B-04 与 :2791 逐字一致、B-10 四行与 :2796-2799 相符；**但 C-03 写作「未经场景完整推理筛选」≠ 契约原标注「标准集未经场景推理筛选」，与该文 B3 维度「逐字符合契约表」自相矛盾** |
| 8 | 落点否决理由（`_专题_ACE开发/` 系开发归档、M7 用例集仅 ACE 6 例） | 真 | `归档说明.md` 自述「开发归档（2026-08-07，CG-20260807-013）」「不再是事实源」；M7-01—06 恰 6 例 |
| 9 | 版本指纹（`db2a37d`／clean／无 tag） | 真 | 三项命令读数吻合 |
| 10 | 技能包哈希配方与读数 | 真（附重大口径缺口） | 按备份层＋HEAD 前像复算得 `701dd7eb…`／`8bae9170…`／122 件／15 目录**逐字吻合**；**但取法未写明「复算基＝该 commit clean 态」，对当前工作树直接复算必得 `91666eba…`，指纹不可第三方复现** |
| 11 | 三层互等＋登记面完整 | **假（结构性）** | 三层 sha256 全等、头部降级、PL-031 注记且状态仍「待处置」、台账 34 行均真；**但 §九 新行在 `join("` 处物理断行，续行无行首竖线，「6 列」在物理行层面不成立＝复刻 PL-016 缺陷**；另 §二 表实为 **15 行**而非 14 项 |
| 12 | 零改面（技能件／契约／校验器／门禁件／AGENTS／索引） | 真 | `git diff --stat` 仅 4 件预期改动 |

**代理挑错（OBS）**：OBS-1 断行须批内修（新行非历史行，不受「另批」前置约束）；OBS-2 节号／计数／C-03 逐字串订正；OBS-3 补复算基；OBS-4 E-02 判据「定位不到须承认」属二选一恒可满足的**空跑条款**；OBS-5 P0 零容忍**选取口径未声明**（A-01／A-02／B-06 亦锚 P0 却不在十题内）；OBS-6 外部输入倒排 5 项无「拿不到写成已完成」情形。

## 二、批内处置

| 缺陷 | 处置 | 复算证据 |
|---|---|---|
| §九 新行物理断行 | 合并为单一物理行（根因＝写入脚本经工具层转义把 `\n` 解成真实换行，非内容错误） | 该行现为 1 物理行、竖线 7＝**6 列与表头相符**（行长度随后续登记文字增长，不钉定值）；全文件孤立续行 0 |
| 指纹项计数 14→15 | 登记面 3 处＋变更日志 1 处＋件内 1 处统一订正 | §二 表数据行实数 15（行 2—16）；**后经 ⑩ 把聚合哈希一行拆为两行，该表数据行增为 16，登记面五处同步改为 16 项** |
| 节号 §八→§五 | 件内 :61 订正 | 「§五 注册表总览」命中 1 |
| C-03 非逐字 | 改为契约两档逐字串 | 「标准集未经场景推理筛选」「SRE 降级：使用 [技能] 内置标准映射」件内各 1、契约各 2，**逐字同源** |
| 声明夸大（PW-R-P0-2／FL-R-P0-5） | 从变更日志枚举中删除，改为文中实际引用集 | 件内实际引用红线去重 14 个编号 |
| 指纹不可复现 | 件内新增「复算基（第五条）」＋登记面同批补写，并披露本批自身改动致工作树直接复算漂至 `91666eba…`／`e780ea…` | 复算须以 `git cat-file HEAD:<path>` 前像还原或直接检出 `db2a37d` |
| E-02 空跑条款 | 改为可判据形式：凡声称定位者其编号＋条文号／页码须为真值源可查；未定位不得输出页码；「未能定位」本身不加分不扣分，输出虚构页码即 0 分 | 件内命中 1 |
| P0 零容忍口径缺失 | 补三条入列判据（锚 P0＋静默型失效＋落在安全或合规面），并说明 A-01／A-02／A-05／B-06 不入列的理由与「不入列不豁免」兜底 | 件内命中 1 |
| ⑨ 宿主 IDE 版本与模型标识两格误判为「本机无从自证」（**主代理收尾自证发现，非子代理首轮 12 项之内**） | 件内 §二 两行由「待补」改为本机实测值＋机器可读取法；§五 倒排表该项由「外部输入」改列「本地可自证」；§4.3 第 4 条执行记录字段同步改写 | `D:\软件\已安装软件\Qoder\resources\product.json` 存在（21 键，含 `productId`／`appId`／`dataDirectoryName`，**无版本字段**）；版本读数＝`%APPDATA%\com.qoder.app.stable\logs\20260921-015724.010-5516-7c484961\main.log:1` `[App] Application starting {"version":"0.3.4",...}`；模型档位＝同文件逐会话 `"model":"qfmodel"`；`resources\build-manifest.json` `productVersion` ＝ 0.2.5（基安装包，buildTime 2026-09-11），`.qoder-versions\` 下 0.3.3／0.3.4 为自动更新载荷目录。**根因**＝首轮取证只遍历 `AppData\Local\Programs` 与 `Program Files` 常见安装路径，未覆盖本机非标准安装路径 `D:\软件\已安装软件\` |
| ⑩ 指纹可复现性口径夸大（**主代理收尾自证，非子代理首轮 12 项之内**）——件内 §二 版本指纹原以单行宣称「技能内容包聚合哈希 121 件」可第三方复算，并称「或直接检出 `db2a37d` 的干净副本」即可复现 | 该行拆为两行＝「本方法指纹（仅本机可复算）」`701dd7eb…c3d0`（121 件，本批收尾按配方复算＝MATCH）＋「可克隆复现指纹（第三方可算）」`30de3347e9e39aa7a186948e48d6ab24a731843b80ae21806cf47ac3aecb5e8c`（71 件＝121 ∩ `git ls-tree -r HEAD`，全部取 HEAD blob）；"复算基"条两处订正（干净副本仅覆盖已跟踪 71 件；取前像须用两参数 `git cat-file blob`）；Python 行补 `sys.executable`；§二 指纹表由 15 行增为 16 行、登记面五处计数同步 | **根因**＝121 件内实测 **50 件**为 `_pre<日期>CG<序号>` 运行时留痕件（gitignored、不在 git 面），clean clone 根本取不到，故原声明不可第三方复现。该 50 件漏入同步范围的成因＝`sync_skill_backup.py` 的 `walk_files` 跳过判据字面为「_pre」**加下划线**，而真实命名「pre」后**无**下划线；脚本判据订正会改同步范围读数与检查 9 纳入判据数，属**新线、须发起人另批**，本批仅在件内如实披露不改脚本。复算过程另踩两处假信号（已入 memory 类）：(a) 以 `subprocess` 传含中文的路径参数给 git 时按 GBK 编码，`cat-file` 全部静默返回空 → 122 件哈希恰为「空文件的 sha256」`e3b0c442…`，改由 `git ls-tree -r HEAD` 取 blob 号后规避；(b) 本机便携版 git 对单参数 `git cat-file <sha>` 回 `fatal: only two arguments allowed in <type> <object> mode`、rc=129 且 stdout 为空，不断言 returncode 即把「还原前像」静默做成「还原空文件」，一度据此误判 70／71 件偏离 HEAD 并算出假指纹 `0ea161d3…` |
| ⑪ `CHANGELOG.md` v1.13.5 的 ⑥′ 条目末粘连字面反斜杠 n（**主代理收尾自证**）——该条目与 ⑥ 条目被粘连为单一物理行（写入脚本留下的转义产物，非真实换行） | 还原为两条目；并对四个载体复扫字面反斜杠 n，余者均在代码跨度内属合法字面（如 `join` 配方、`(C)` 行尾翻转叙述） | **根因与 ① 同一转义层**；`CHANGELOG.md` 不属检查 6／9 覆盖面，故**无门禁可抓**该类粘连——属既有覆盖面缺口，非本批新增缺陷 |
| ⑫ §九 本批行状态列以时点态自述（**主代理收尾自证**）——原句「**本批不含 commit 与 push**，提交与推送另待发起人指令」在本地提交后即失实，正是 PL-018 所订正过的失效模式 | 依 §11.3 第 5 条改以**授权判据**自述：发起人 2026-09-21 明文「只本地提交、推送随阶段 1 那批一起做」＝本批含本地 commit 授权、不含 push；该句在提交前后同为真。变更日志判定行同批改写 | 授权语句逐字来源＝本批发起人指令；推送态判据以 `git rev-list --left-right --count HEAD...origin/master` 现查为准 |
| ⑬ 机器本地件读数被写成冻结判据＋复现基准未显式钉住（**主代理收尾自证，本批九门禁收尾复跑后暴露**）——件内 §二 把 `sre_regression_report.json` 自身哈希与含该件的 122 件全量哈希列为「冻结读数」，而该件内含跨轮连击计数、随任一次 SRE 门禁复跑即变，钉死必自指失效 | 两枚读数降为「登记时点快照、不作判据」并补记收尾复跑实测漂移（`335a1df5…a60e5`→`a61972c8…546e4`、连击 165 轮）；「可克隆复现指纹」行取物基准显式改为冻结基 `db2a37d` 的 blob 并加「不得对提交后的 HEAD 取」警示 | 本报告 §三 首跑九门禁后实测该件哈希即变＝自证；两枚内容包指纹（121／71 件）不含该件，故其值不受影响 |
| ⑭ 报告自身序号引用悬空（**主代理收尾自证**）——§三 首条原称「沿本批 §九 处置列 ⑦ 同一口径」，而 §九 处置列 ⑦ 实为 E-02 空跑条款、与「不钉自身哈希」无关 | 改称「沿本批变更日志 v1.13.5 第 ⑦ 条同一口径」（该条正是「不钉本文件自身字节数与哈希」的出处），并逐条回查登记面其余序号引用 | 订正后该句与变更日志 ⑦ 条字面同源；全报告序号引用回查命中悬空 1 处、余者相符 |

## 三、订正后终态读数

- 三层 `change-governance.md`（L1／运行时 `shared/`／`技能仓备份/shared/`）**字节全等**，纯 LF；**不钉该件自身哈希与字节数**——二值随登记行写入而变属自指（沿本批变更日志 v1.13.5 第 ⑦ 条同一口径），终态一致性由 `validate_governance.py` 检查 6 (A)(B) 断言承担，本批收尾实跑为全绿。
- 冻结件 27,028 B、纯 LF（CR 计数 0）、题数 33、P0 清单 10 题、§二 指纹表 16 行，终态 sha256 `d2fba82085cf1a598ad40564312d55c87c761eb9d671c3ee86d4195501c17c73`（本件终态值随批内修订逐次变化，此处只钉最终一枚；历史读数 21,937 B／23,699 B／26,404 B 均系 §二 ⑨／⑩／⑬ 订正前快照、已失效；登记面五处随之改钉本终态哈希。该件不在同步范围内、不经 sync 与检查 6／9，故钉其终态哈希不构成自指）。
- 九门禁复跑全绿：`validate_governance.py` **PASS 236／失败 0／警告 1／exit 0**；verification 6／ace 90／sre 66／ic07 20／pending 39／cross_layer 25／index_ref 26／skill_cross 21，各 `OK`、exit 0。
- sync 收尾（末次跑）：范围 122／新增 0／移除 0。硬判据即此三项；一致／更新两数随门禁复跑次数浮动（`sre_regression_report.json` 含跨轮连击计数、随任一次 SRE 复跑而变，故不入内容包指纹）。
- 内容包两枚指纹终态读数：本方法指纹 `701dd7ebd2ae7a966c343d94793e1862a749084c53786aba409046bef065c3d0`（121 件，本机按配方逐字复现＝MATCH，**不作跨机判据**）／可克隆复现指纹 `30de3347e9e39aa7a186948e48d6ab24a731843b80ae21806cf47ac3aecb5e8c`（71 件已跟踪子集，第三方对 clean clone 套同配方可算）。

## 四、复核意见

1. **本批性质判定**：新件是**判据载体**而非行为证据，未越权宣称 PL-031 闭合，符合报告 §六 第 1 项约束；其"落盘即冻结＋修订须另批升版"的纪律条款给了后续实跑可引用的稳定锚，属阶段 0 的正确产出形态。
2. **残余风险（不阻塞入库）**：① 冻结题集的专业正确性本身仍未经专家验证——盲评责任人待指派前，33 题只是"可判分的题面"，不等于"题目出对了"；② ~~宿主 IDE 版本与模型标识两格留空~~ **已由 §二 ⑨ 订正关闭**（本机可自证，实跑批仍须按当次会话日志重取并与冻结值比对，缺字段按 §4.4 判无效）；③ D-04 依赖外部脱敏检测报告，若长期拿不到，PL-031 的 ACE 子面将只剩"未执行"记录。
3. **收尾自证六项的共性（不阻塞入库，但记为方法教训）**：⑨—⑭ 六项均系主代理收尾自证而非子代理首轮 12 项复算所得，其中 ⑩⑪⑫⑬ 四项不被九门禁任一抓住——⑩ 属「声明超出证据」的语义面（检查项只能判字节，判不了「第三方可复算」这句是否成立）；⑪ 属覆盖面缺口（`CHANGELOG.md` 不在检查 6／9 覆盖面内）；⑫ 属 §11.3 第 5 条已有条文、但无机算守卫（同族缺陷 PL-018 已是第二次以上发生）；⑬ 属「钉了必自指失效」的读数（该件随门禁复跑即变，登记面把它写成冻结读数即埋雷）。故登记面自检须包含「复算基是否覆盖全部输入件」「四载体字面反斜杠 n 复扫」「状态列是否只承载授权判据」三条人工步骤，不得因门禁全绿而省略。
4. **本批披露的新线（未执行，须发起人开批）**：`sync_skill_backup.py` 的 `walk_files` 跳过判据字面为「_pre」**加下划线**，而运行时留痕件实际命名 `_pre<日期>CG<序号>`（「pre」后无下划线），致 **50 件** gitignored 留痕件留在同步范围 122 件与内容包指纹内。订正该判据会同时改变同步范围读数、内容包指纹与检查 9 的纳入判据件数（并牵动 `AGENTS.md` 活基线叙述），属工具行为面变更＝**B 级另批**，本批只改文档面披露、零改脚本。
5. **不得据本批对外宣称**：主链已跑通、技能行为已验证、专业内容正确、内容包指纹可跨机复现（跨机只可算 71 件子集那一枚）。
