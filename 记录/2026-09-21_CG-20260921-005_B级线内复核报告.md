# CG-20260921-005 B 级线内独立复算复核报告

- 批次：CG-20260921-005（B 级）——§九 双口径机算守卫、空行断块订正与镜像层留痕清理（闭合 PL-036，补登并闭合 PL-037）
- 流程依据：`change-governance.md` §二 第 3 步（B 级须发起人线内只读子代理独立复算）
- 复核人：发起人线内只读子代理（`Agent` 子代理，未使用主代理指令中的既有结论文件，独立取数）
- 执行时刻：2026-09-21（主代理收尾前）
- 复核边界：全程只读。临时件仅写 `D:\Qoder-Files\_tmp-scripts\`（`qa_dual_caliber.py`／`qa_reg.py`／`qa_archive2.py`／`blobs.bin`），未增删仓内文件，未执行任何 git 写操作。
- **判定：PASS-with-OBS**（14 项声明＝11 真／3 部分真／0 假；3 条观察，其中 2 条批内处置、1 条作为不覆盖面登记）

## 一、逐条判定（首轮原文，不抹）

1) **真** —— `validate_governance.py` 第 5 行版本头＝v1.14.0；`check_pending_ledger` 内 1884—1903 行确有该断言：`tbl_ids != cg_ids` → `report.fail`（`Report.fail` 第 267 行，同检查 7 其余 FAIL 一样计 fail_count），`cg_ids=collect_cg_log_ids`（逐行首列）、`tbl_ids` 走 `iter_md_tables`。取证：直读源码＋AST 外独立 import 模块复算。
2) **真** —— 自写内存直调（`D:\Qoder-Files\_tmp-scripts\qa_dual_caliber.py`，不落盘）：表块内插 1 空行 → 复红「逐行 116／表级 3」；表块内跨物理行断行 → 复红「116／7」；标题与表之间（表块外）插空行 → 仍产 [OK] 零 fail。守卫判的是两口径相等，非绝对规范。
3) **真** —— 自算逐行＝表级＝**116**，集合相等 True；实跑 validate＝总评 PASS／通过 237／失败 0／警告 1／exit 0；检查 7 小节该 [OK] 行全文仅 1 条（输出第 288 行）。
4) **真** —— `pending_ledger_test.py` 实跑 **Ran 43 tests OK**；`TestDualCaliberConsistency`（256 行起）含且仅含四例：clean 双口径 [OK]／表块内空行复红／断行复红／表块外空行控制例复绿。PL-016 原守卫 `test_wrapped_history_row_does_not_hide_later_ids`（221 行）未删未注释：仍断言断点后 CG 编号对逐行口径可见＋「除双口径外零 fail」，断块复红半则由新类 `test_wrapped_row_turns_dual_caliber_red` 承担。
5) **真** —— 现读 736,596 B、CR 0（LF 490）；三层（L1／`C:\Users\聂卫华\.qoder\skills\shared\`／`技能仓备份/shared/`）sha256 前缀均 `8543a7b236602efa`、字节数三者同为 736596，全等。
6) **真** —— `CG-20260921-005` 在 §九 区间（1-based 281—401）仅命中 **1 个物理行**（第 399 行），本仓 `md_cells` 解析＝**6 列**、含 0 处 `\|`（故无裸竖线虚增列）；现读 §九 区间内空行仅 281/401（表块外）；PL-036 状态＝**已闭合**（到期 2026-12-20）、PL-037 已登记且＝**已闭合**；§11.2 自算 37 行 × 7 列、分布待处置 9／已闭合 27／已裁定不做 1、行列数零异常。
7) **真** —— `git diff --numstat`＝**5 增 3 删**；三条删除行＝①头部「最后更新」行 ②§九 表块内第 387 行空行（`git show HEAD:` 前像 1-based 387 确为空白行且落在表区间）③PL-036 旧行；HEAD 旧头行正文与新第 8 行「此前更新（历史快照）」正文**逐字相同**（仅前缀变，逐字符比对 True）；删除行中无任何 §九 历史 CG 行＝零改写。
8) **部分真** —— 留痕文法 `_pre_?\d{8}` 命中路径任一段：镜像层残留 **0**；`技能仓备份/` 文件总数 **73**（＝范围 72 ＋ `同步说明.md`）；空目录 **0**、无 `.pyc`；**但** `sync_skill_backup.py --check` 实跑＝「范围 72｜**一致 71｜更新 1**｜新增 0｜移除 0」，唯一更新件 `prefab-standards-reviewer/sre_regression_report.json`（运行时 mtime 14:00 vs 镜像 13:28，即末次 sync 后又跑过 SRE 门禁）。
9) **部分真** —— 归档目录（含 15 子目录）除 MANIFEST 外**恰 50 件**、总字节 **1,600,786** 全等；MANIFEST 50 条 sha256 与实算**50/50 一致**、字节列 50/50 一致；**但**第 4 列「内容另存处」50 行**全为 `runtime-copy`**，全文件 `git-history` 字面出现 **0 次**，「42 标 git-history／8 标 runtime-copy」不成立（42/8 这一事实本身见第 10 条，属实但未被清单逐行承载）。
10) **真** —— 50 件在 `C:\Users\聂卫华\.qoder\skills\`（扫 385 件）sha256 字节全等命中 **50/50**；`git cat-file --batch-all-objects --batch` 全量导出（blob **800** 枚，另有 tree 710／commit 145／tag 3）逐枚算 sha256，命中 **42/50**；未命中 8 件名单与自述**逐项相同**（ACG `_pre20260820CG001F1` 两件＋`sre_reasoner.py_pre20260916CG001/003/004/005`＋`sre_regression_test.py_pre20260916CG004/005`）。
11) **真** —— validate 输出实读：检查 9「运行时∪镜像 120／gitignored 58／纳入判据 62」；检查 8 ① 含 ①′ **38**／② **35**／③ **24**（＝97）、NO-ANCHOR **10 值／4 位置**、EXEMPT **E3 4＋E2 11＋E6 1**。
12) **真** —— 实跑：verification **6**／ace **90**／pending **43**／cross_layer **25**／index_ref **26**／skill_cross **21**／sre **66**（指纹 4f53cda18c2baa0c、信号 0）／ic07 **20**，全部 OK。
13) **部分真** —— AGENTS.md 第 50 行含「CG-20260921-005 起 237、v1.14.0、通过 **237**」＝实跑一致；第 55 行「Ran 43 tests」＝一致；第 59 行末确有 CG-20260921-005 留痕处置注记（载归档路径 `D:\Qoder-Files\_archive-20260921-traces\` 与「文件数 123→73」，其 `--check` 措辞只钉「范围 72｜新增 0｜移除 0」，与实测相符）。CHANGELOG v1.13.7 中「5 增 3 删」✓、「800 枚 blob」✓（实测恰 800）、「台账 36→37」✓（HEAD 自算 36 行，分布 25/10/1）；**但**该节称 `--check` 复跑「一致 72」，现态不可复现（见第 8 条）。
14) **真** —— §一 第 69 行 B 级＝「新增内容、修正错误、优化表述等**不影响安全性**的常规变更」；第 70 行 C 级＝「文档结构优化、措辞改善、格式统一等**不影响功能和准确性**的变更｜典型场景＝排版统一、章节重组、说明文字润色」；§4.1 第 147 行另有「标准编号/年份修正＝B 级变更」。「改动校验器检查面属 B 级独立批」先例可现读 ≥3 条：§九 第 377 行 **CG-20260919-001**（检查 6 增 (C)）、第 391 行 **CG-20260920-004**（新增检查 8）、第 392 行 **CG-20260920-005**（新增检查 9），且这些行内还引用 **CG-20260918-005**、**CG-20260916-010**。

## 二、未决与观察（首轮原文）＋ 批内处置

### ① 归档清单第 4 列标注缺失（原第 9 条）→ 批内重生成清单（载体缺陷已订正）

- **首轮结论**：`MANIFEST.txt`（7,532 B）第 4 列对 50 件一律写 `runtime-copy`，未区分 42 件「git 历史亦存」；登记面（AGENTS 第 59 行／CHANGELOG ④）却称清单承载该审计。事实层无误（代理独立复算 42/8），属**逐行标注缺失**。
- **主代理批内处置**：清单已按 `--batch-check` 配方重算并重生成（**8,498 B**、50 行、第 4 列两值 `git-history+runtime-copy` 42 行／`runtime-copy-only` 8 行、头部载取物配方与本缺陷说明），AGENTS／CHANGELOG／PL-037 依据列的 8,498 B 与两值标签随之对齐。
- **根因（记入 CHANGELOG v1.13.7 第 ⑥′ 项）**：`git rev-list --objects --all` 第二字段是**路径**而非对象类型，首版脚本按 `== "blob"` 过滤类型 → 命中 0 → blob 比对集合为空 → 成员判据在空集上恒不成立，脚本照跑照出、无一处报错。**与 CG-20260921-003 第 ⑥″ 项（便携版 git 单参数 `cat-file` 回 rc=129 空输出、不断言 `returncode` 即把「还原前像」静默做成「还原空文件」）同族**：两次都是取数面空集不报错。收口判据＝任何「集合成员／全量比对」型断言必须先断言被比对集合**非空**（空集即 FAIL），已写入本批审计脚本。
- **删除决策不受影响**：其判据是「50 件在运行时层有字节全等孪生」（首版即为真、经主代理与复核代理两侧独立复算 50/50），42/8 标签只作额外保险，不作为可删性的必要条件。

### ② 终态磁盘 `--check` 读数与「一致 72」自述不符（原第 8／13 条）→ 批内订正措辞并钉死终态顺序

- **首轮结论**：终态 `--check` 为一致 71／更新 1，漂移源＝机器本地 `sre_regression_report.json`（14:00 复跑门禁所写，晚于 13:28 末次 sync）。依 CG-20260921-003「机器本地件不作判据」不构成功能缺陷，但批内自述的「最后跑 sync」在磁盘态上未成立，「一致 72」这一读数当时不可复现。
- **主代理批内处置**：CHANGELOG ④ 与 AGENTS 同步器行的 `--check` 措辞统一改为只钉「范围 72｜新增 0｜移除 0」（与代理实测相符），并如实登记复核时点的「一致 71／更新 1」及其来源；同批新立 CHANGELOG v1.13.7 第 ⑥″ 项，把终态收尾顺序显式钉为 **编辑 → 带平 → 复算 → 最后跑 `sync_skill_backup.py`（写态）→ 只读 `--check`，此后不得再跑任何门禁**（`同步说明.md` 无机器守卫）。

### ③ 双口径断言的位置（观察，不作批内改动）

- **首轮结论**：双口径断言只在 `check_pending_ledger` 内机算，且位于 §十一 台账表存在性／必需列检查**之后**——若那两项先 FAIL 并 `return`，本断言当轮不执行。
- **主代理判定**：**不构成功能缺口，本批不改位置**。理由是同一轮已计 FAIL 且 exit 非 0，不存在「结构违法时本断言静默放过」的路径（失败面已由前序检查承担）；该观察随本报告与 CHANGELOG §11.3 第 6 条同族的「不覆盖面须披露而非机算」纪律登记为**不覆盖面**，而非登记为遗留项（不新增 PL 编号，避免把「位置优化」伪装成缺陷）。

### ④ §九 表块内空行余量（观察，证实断言无假红）

- clean 态实测 §九 区间内空行仅剩 281（标题与表之间）与 401（表后、`---` 前），均在表块外，与新增断言不冲突——即第 2 条控制例所判「表块外空行复绿」在真实文件面上同为真。

## 三、批内处置汇总与终态

| 项 | 首轮判定 | 处置 | 落点 |
|---|---|---|---|
| 1—7、10—12、14 | 真 | 无需处置 | — |
| 8 | 部分真 | `--check` 措辞订正＋终态顺序成文 | CHANGELOG ④／⑥″、AGENTS 同步器行 |
| 9 | 部分真 | 归档清单重生成（42/8 两值标签）＋根因收口判据 | `MANIFEST.txt` 8,498 B、CHANGELOG ⑥′、PL-037 依据列 |
| 13 | 部分真 | 同 8（「一致 72」读数改为不判该数） | 同上 |
| 观察③ | 披露 | 不覆盖面登记，本批不改断言位置 | 本报告 §二 ③ |

- 处置后 L1 登记面为 737,355 B（复核时点 736,596 B），三层须重新带平后方为终态；本报告的字节／哈希读数均标注为**复核时点快照**，不作终态判据。
- 报告本身首次入库不抹首轮结论（沿 CG-20260921-003／-004 先例）。
