# CG-20260920-002 线内独立只读复核报告（B 级）

- 复核批次：**CG-20260920-002**（承接 §11.2 台账 **PL-028**；登记批＝CG-20260920-001）
- 复核角色：发起人线内调用的独立只读复核代理（内容面与登记面分离核验；终核两处窄口径判据由发起人复算，见 OBS-1）
- 复核日期：2026-09-20
- 被复核改动：运行时 `~/.qoder/skills/prefab-wall-surface-system/` 三件共 **50 处** standards-index 序号引用订正（散文 11 处带「原序号 N」注＋表列 25 格＋内联 `索引#N` 14 处，两处《尺寸指南》行＝「54、69」并列）＋ `技能仓备份/prefab-wall-surface-system/` 同名三件（sync 派生）＋ `change-governance.md`（头部新增、§九 -002 行、§11.2 PL-028 转已闭合与读数订正条款）三份镜像 ＋ `CHANGELOG.md` v1.12.37 ＋ 本报告
- 定级：**B 级**（§一「修正错误、不影响安全性的常规变更」＋§4.1「标准编号/年份修正＝B 级、须全文扫描同步」同族；零触索引本体、标准数值、状态字段、替代关系与校验器检查面，故不升 A；发起人开工前确认「B 级」，范围扩至 50 处时二次确认）
- 取证环境：仓 `D:\Qoder-Files\装配式装修技能开发`（`origin/master`）＋ 运行时根 `C:\Users\聂卫华\.qoder\skills`
- 授权边界：本线仅只读实测＋本地编辑＋实跑验证，**不含提交与推送**；commit 须用户明文指令并点明范围，push 须另发明文

---

## 结论：**PASS-with-OBS**（前置＝本批两处 FAIL 项已按项订正）

复核**连抓两轮 FAIL**，两轮均已订正，不是回退内容：

| 轮次 | 结论 | FAIL 项 | 处置 |
|---|---|---|---|
| 第一轮（内容面） | **FAIL** | 登记批 CG-20260920-001 自述「真漂 11 处、**全部**」为过称：其扫描只匹配散文形「序号 N」，漏表列头「索引序号」的数值单元与内联「索引#N」两类载体 | 发起人改写审计器（三类载体独立取数＋strip「原序号 N」免自匹配）独立证实＝**50 处**，报范围扩至全 39 余格经发起人裁定，本批全部订正 |
| 第二轮（登记面） | **FAIL** | ① 复核报告路径在三处登记面被引用时**文件尚未落盘**；② PL-028 的无漂对照「吊顶族序号引用 13 处」计数过称（实为 19 处） | ① 报告随本轮订正真实落盘（本文件），登记面措辞改述为「两轮 FAIL＋终轮订正」而不预称「已核」；② PL-028 与 -002 行 ② 就地订正为 19 处并附行号明细 |
| 终核（两处窄口径） | **PASS** | — | 报告存在性＝本文件在盘；19 处＝发起人独立脚本枚举复核（见 §②′） |

---

## ① 值正确性 — PASS

复核代理自建真值表（`standards-index.md` 主表 :76/:139/:194/:216/:227/:238/:274，58 个编号，最大序号 69），逐格比对三件，**零不等格**：

- `reference.md:125—:141` ＝ 2, 5, 3, 8, 31, 19, 46, 47, 45, 51, 53, 59, 55, 56, 57, 58, 「54、69」；`:145`＝9/10/32/33/34、`:149`＝10、`:150`＝32、`:151`＝9、`:152`＝33、`:168`＝34、`:169`＝40、`:170`＝41、`:606`＝47、`:894`＝47
- `SKILL.md:526—:541` 全 16 行；`:12`＝59、`:569`＝59、`:570`＝53
- `examples.md` 内联 24 处 `索引#N` 全部等值（:31=31、:32=46、:33=2/5、:39=8、:40=55、:198=51、:472=46/45、:604=59、:608=2 等）

## ② 穷尽性与排除面 — PASS（该族残留漂移 0）

对全 `~/.qoder/skills/*.md`（排除 `standards-index.md` 自身与 `*_pre*` 回退件）按四类载体（表列「索引序号」／内联「索引#N」／散文「(standards-)索引序号 N」／裸「序号 N」）独立取数：205 候选、严格就近配对 61 组，**无一残留漂移**。排除面逐条判读：

- `GB 50222-2017 表 5.1.1 序号 17` 四处（ceiling `examples.md:65`、`reference.md:206/:399`、`SKILL.md:122`）＝标准内部表行号，不属索引序号族；
- `skill-qa-tester/reference.md:127` 的「序号44-1」＝用例条目号；
- `standards-reasoning-rules.md:576`（SJG＝58）位于其 §七 变更日志＝历史快照，按快照纪律不改。

**发起人自报的两处假阳性经独立确认非漂移**：`prefab-ceiling-system/reference.md:197`「序号 3」实指 GB 50016-2014（索引真值 3，审计器错配到同段前置的 GB 55037）、`prefab-wall-surface-system/reference.md:380`「索引序号 2、5」＝GB 55037(2)／GB 50222(5) 并列，均正确不改动。

### ②′ 终核：吊顶族无漂对照计数 13 → **19**（发起人独立复算）

散文形「序号」枚举实测 `prefab-ceiling-system` 命中 23 处，扣 4 处「表 5.1.1 序号 17」得 **19 处**＝`reference.md:83—:86/:197/:214/:282/:286/:291/:345/:346/:457/:470×3` ＋ `SKILL.md:447—:450`；值域 60/61/62/63/42/32/9/34/8/19/3 逐条与索引一致（含 `:470` 一行三编号按 61/63/62 非单调并列，逐格现查无误）。故 -001 ③ 的「零漂」结论成立、仅计数过称，订正随 PL-028 与 -002 行 ② 承载，不改判。

### ②″ 新发现两处**不构成漂移**的解析险口（不改动、不立 PL）

1. `prefab-ceiling-system/reference.md:470`：非单调并列使机算就近配对易误判（本批审计器即以「018」截断 JC/T 564.1-2018）；
2. `prefab-wall-surface-system/reference.md:149`：「代替 GB 18580-2017」系替代关系注记，同行已自证「2017 版不单独收录」，不属序号引用族。

## ③ 口径落实 — PASS（一处字面偏差已订正）

逐行「数字位掩蔽后比对」：`examples.md` 12 行非数字改动 0；表列 `reference.md` 13 格、`SKILL.md` 12 格仅数值变（第 13 格＝53→「54、69」并列）；散文 11 处均带注。**偏差**：`reference.md:145` 五值集合声明初稿写「原记 9/10/31/32/33」而裁定措辞为「原序号 N」，已订正为「原序号 9/10/31/32/33」以求字面一致（订正后该件 sha256 前 16 位由 `9ad73bb9f94fcd94` 变 `4b539ee4a29fd673`，登记面同步回填）。

## ④ 写面纪律与带平 — PASS

三件 runtime↔`技能仓备份/` 字节互等（终态 `4b539ee4a29fd673`／`4e0eb03874c206ae`／`73b7b46cc1ba7c09`）；CR 字节 0；`\n` 计数 reference 1024／SKILL 601／examples 729 未变；`git status --porcelain` 恰 7 件＝授权清单，无越界；改前三件快照哈希 `7e713ca9ec74d5e0`／`9d560ba30c6a5297`／`c1d1960082a5be5d` 在 `D:\Qoder-Files\_pre-backup\2026-09-20_CG-20260920-002\` 实测命中；`sync_skill_backup.py --check`（只读）＝范围 122｜新增 0｜移除 0。

## ⑤ 登记面自述可核性 — 第二轮 FAIL 后 PASS

经核与实测相符者：50＝11＋25＋14（表列行号、内联 12 行含 :472/:607 各两处，逐行吻合）；索引现读行号 `:149/:161/:231—:233/:243/:245—:248/:276—:279`；收录总数 69 条；validate 225／0／1；台账 28 行、状态分布 待处置 5／已闭合 22／已裁定不做 1；检查 7 键闭合真值源实读 **106**（-001 落位时 105）；sync「更新 4」。两条过称（报告路径预先引用、吊顶族 13 处）见上表处置。

## ⑥ 门禁独立复跑 — 全 PASS

validate **PASS｜通过 225｜失败 0｜警告 1｜exit 0**；verification_policy Ran 6 OK；ace Ran 90 OK；pending_ledger Ran 39 OK；cross_layer_eol Ran 25 OK；sre Ran 66 OK（缺口信号 0、`signal_fingerprint` 与 `signal_previous_fingerprint` 均 `4f53cda18c2baa0c` 不漂移）；ic07 Ran 20 OK。**内容面对七门禁为 no-op**：技能件不在检查 6 声明表 7 件内（PL-010），故全绿不构成序号正确性的证据，该证据仅由 ①② 的独立复算提供。

## ⑦ 未触碰面 — PASS

`standards-index.md` 五处副本哈希全等（前 16 位 `3f4397685136`）且不在 `git diff` 内；`程序文件/*.py`、`AGENTS.md`、其余技能目录零改动；`_专题_墙面技能策划/` 策划稿同漂件（`combined_reference_v2.md`、`SKILL_draft_v2.md:567`）经发起人裁定只记不追改。

---

## OBS（逐条留痕）

1. **独立性受限**：三轮复核均由同一发起人调度、与改动共享同一工作区真值，非跨人独立；终核两处窄口径（报告存在性、19 处计数）由发起人独立脚本复算而未启第三轮子代理，理由＝判据可机算且范围窄。
2. `技能仓备份/同步说明.md` 的 `shared/change-governance.md` 行在复算时点仍为登记前态（`64a4d834…` vs 现值 `49b344f2…`）＝该清单系 sync 自产、无机器守卫，故收尾顺序固定为「编辑→带平→复算→最后跑 sync」，终跑后自愈；检查 6 不以其为真值源。
3. runtime 两层不在任何 git 仓内，其未被本批改动的件只经 (A)(B) 字节全等传递成立（§11.3 第 6 条①）。
4. 序号所指标准的**官方真值本身**（如 54／69 两指南归属）不在本次复核面内——本批复核只做「技能侧 ↔ 索引现值」的文件层键闭合，索引现值的官方凭据沿索引 §二 核验记录。
5. **结构缺口 PL-010 不因本批闭合**：本批把内容改净，但同类漂移仍无门禁承担者；且第一轮与第二轮的漏扫均出在人工枚举载体的环节，说明「靠人工记住全部载体形态」不可靠。机算守卫（把技能侧序号/编号引用纳入检查面）须另批设计，属新增校验器检查面。

## 放行判据

内容面 50 处订正无需回工；登记面两处 FAIL 项已订正。本批终态以「七门禁 exit 0＋失败 0」＋「三载体独立复算该族漂移 0」＋「三份治理镜像与三件技能副本字节互等」三条为判据，不以通过数固定值判回归。
