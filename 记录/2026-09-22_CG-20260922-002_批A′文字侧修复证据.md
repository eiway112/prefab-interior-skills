# CG-20260922-002 批 A′｜PL-047／048／050／051 文字侧修复证据

- 批次：`CG-20260922-002`（B 级）
- 日期：2026-09-22
- 授权原文（发起人）：「授权开批 A′，写面＝PL-047／048／050／051 的文字侧，B 级，含本地 commit。」
- 写面边界：运行时 L2 层 `~/.qoder/skills/` 的三件技能正文（`prefab-floor-system/examples.md`、`acoustic-calculation-engine/reference.md`、`acoustic-calculation-engine/SKILL.md`）＋登记面＋本证据件。**不含**任何标准限值／材料参数／经验域区间／公式常数的数值改动，**不含** `shared/` 契约文件与 `standards-index.md` 本体，**不含** push。

---

## 一、改前取证（取物面＝`git show HEAD:技能仓备份/<相对路径>`）

| 件 | 改前 B | 改前 sha256 前 16 | CR | LF | 改后 B | 改后 sha256 前 16 | CR | LF | 净增 |
|---|---|---|---|---|---|---|---|---|---|
| `prefab-floor-system/examples.md` | 63,235 | `e70ee54e1875dc7b` | 0 | 1391 | 65,051 | `57bb2c26ff21a4bf` | 0 | 1392 | +1,816 |
| `acoustic-calculation-engine/reference.md` | 39,986 | `438aa8eb054cc494` | 0 | 716 | 40,151 | `36d11764048f6731` | 0 | 716 | +165 |
| `acoustic-calculation-engine/SKILL.md` | 11,107 | `0074fddf985e7792` | 0 | 197 | 11,448 | `90960e455fcdb5d4` | 0 | 199 | +341 |

- 逐件净增相加 = 1,816 + 165 + 341 = **+2,322 B**（总净增取该相加值，非另测）。
- 三件均无 BOM 且改后仍无 BOM；CR 计数改前改后均为 0；LF 增量与新增物理行数一致（examples +1＝E23 追加的注行，SKILL +2＝E19 的空行与「承载形态」声明行，reference 0＝同段扩写）。

## 二、逐处编辑清单（23 处；行号＝本批终态，可现读复核）

改法一律为 Python 字节级 `read → count(old)==1 断言 → replace → write`，每处的唯一命中断言在脚本执行时即失败即停；下列「追加串」为逐字写入内容，可用其对点位做 grep 复算。

编辑对总数＝A 组 12 ＋ B 组 7 ＋ C 组 1 ＋ D 组 3 ＝ **23 处**，与脚本 `EDITS`／`PAIRS` 的条目数一致（`a8_fix_l2.py` 20 对＋`a8_fix_l2_round2.py` 3 对）。

### A. PL-047 达标结论侧（12 处：E01—E12）

- **E01** `examples.md:63` 自评表「评价量匹配」行追加：`；L'nT,w 由 Ln,w 区间加宽而得、两量间无换算式，已在 Step 3 披露`
- **E02** `:117` 括注由「（考虑现场侧向传声等因素，可能略高于实验室值）」改为「（现场侧向传声等因素使现场值高于实验室值；两量间无换算式，此处为区间加宽而非换算结果）」
- **E03** `:119` `结论：达标，且有较大余量（约 17-23 dB 余量）` → `结论：不作达标判定——估算区间低于 ≤ 65 dB 限值，达标须以现场 L'nT,w 检测判定（现场值系区间加宽、无换算式，不列余量数值）`
- **E04** `:128` 表行 `| 隔声 | D1 | ✓ | …达标余量充足 |` → `| 隔声 | D1 | ⚠ 待现场检测 | …两量间无换算式，达标须现场 L'nT,w 检测判定 |`
- **E05** `:194` `- **达标判断：达标**（…余量约 17-23 dB）` → `- **达标判断：须现场 L'nT,w 检测判定**（估算值低于 65 dB 限值；现场值系区间加宽、无换算式，不列余量数值）`
- **E06** `:208` `| D1-隔声 | ✓ | …≪ 65 dB，达标余量充足 |` → `| D1-隔声 | ⚠ 待现场检测 | …< 65 dB；两量间无换算式，达标须现场检测判定 |`
- **E07** `:241` 「即使隔声余量充足」→「即使隔声估算值看似远低于限值」（原措辞预设了 E03 已撤销的余量结论）
- **E08** `:757` 示例 4 自评表「评价量匹配」行，同 E01 追加串
- **E09** `:806` 「（考虑现场因素）」→「（现场因素使现场值高于实验室值；两量间无换算式，此处为区间加宽而非换算结果）」
- **E10** `:813` `结论：达标，余量充足（约 17-25 dB）` → 同 E03 形态（限值同 ≤ 65 dB）
- **E11** `:896` `- **达标判断：达标**（余量约 17-25 dB）` → 同 E05 形态
- **E12** `:918` `| D1-隔声 | ✓ | …≪ 65 dB，达标余量充足 |` → 同 E06 形态

### B. PL-048 越域披露与取消越域分级（7 处：E13—E16、E21—E23）

- **E13** `:191` ΔLw ≈ 33.8 dB 追加：`（公式外推值，超出本件「常规浮筑地面改善量：18-25 dB」经验域上界，属越域使用，不作承诺性改善量）`
- **E14** `:893` ΔLw ≈ 35.6 dB 追加同上串（示例 4 同型面，登记时未列，本批按「同一失效模式在同文件全量清扫」纪律补出）
- **E15** `:1064` `    ≈ 43.9 Hz → 优秀` → `    ≈ 43.9 Hz（第二级属 MSM 双叶系统，不适用浮筑地面 f₀ 分级表，见本件「本 MSM 近似的限制」第 1 条）`
- **E16** `:1074` `           ≈ 46 dB` 追加：`（越域：超出本件「复合构造（浮筑+架空）：理论可达 30-40 dB」经验域上界 6 dB，属理想叠加外推）`
- **E21** `:1160` 示例 5 摘要 `ΔLw₁ ≈ 36 dB` 追加 `（越域，见下注）`（其 `f₀₁ ≈ 50 Hz（优秀）` 系第一级浮筑、在 M5 域内，保留分级）
- **E22** `:1161` `f₀₂ ≈ 44 Hz（优秀）` → `f₀₂ ≈ 44 Hz（第二级属 MSM 双叶系统，不适用浮筑地面 f₀ 分级表，见本件「本 MSM 近似的限制」第 1 条）`
- **E23** `:1162` 复合 `ΔLw ≈ 40-46 dB（含耦合折减）` → 追加 `；越域，见下注`，并新增注行 `:1163`：`注：ΔLw₁ ≈ 36 dB 与复合 ΔLw ≈ 40-46 dB 分别超出本件「常规浮筑地面改善量：18-25 dB」与「复合构造（浮筑+架空）：理论可达 30-40 dB」经验域上界，属公式外推而非经验域内值，不作承诺性改善量。`

### C. PL-051 精度证据状态标注（1 处：E17）

- **E17** `acoustic-calculation-engine/reference.md:209` 精度声明行尾追加：`该精度值系经验判断、属待独立验证区间，不作可核验精度证据对外引用（证据状态见本件 M7「该精度值的证据状态」条）。`——采指针回指 `:390` 真值注，不手抄长注（单源纪律）。

### D. PL-050 定位与承载形态澄清＋PL-051 第二处（3 处：E18—E20）

- **E18** `acoustic-calculation-engine/SKILL.md:15` 定位行 → `独立补充技能——通用隔声计算基础设施（公式与判据库，无可执行程序内核）`
- **E19** `:19` 段后新增两行（空行＋声明）：`> **承载形态**：本技能以 `SKILL.md`／`reference.md` 承载公式、判据与分级表，目录内无 `.py` 程序内核；「引擎」指规则集而非可执行进程，内核缺席不属故障。`
- **E20** `:116`（原 `:114`，因 E19 新增两行而下移）精度表「Rw（基准校准模式）」行追加：`；该精度系经验判断、待独立验证，不作可核验精度证据引用`

## 三、改后复验读数（脚本现算）

- `examples.md`：`余量充足` = **0**、`余量约` = **0**、`结论：达标` = **0**、`达标判断：达标` = 0；`越域` 披露 = **7** 处；`无换算式` = **11** 处；`待现场检测` = **3** 处。
- MSM 越域分级残留 = **0**（`含「优秀」的行` 现读 6 处：`:93`、`:190`、`:788`、`:892`、`:1047`、`:1160`，逐条现读均为浮筑单级 f₀（49.6—63 Hz）对 `prefab-floor-system/reference.md:211—216` 分级表 `f₀ < 80 Hz → 优秀`，属域内合法用法）。
- `±2-3 dB` 技能正文分布（排除式口径＝排除路径任一段命中 `_pre_?\d{8}` 的留痕件与文件名为 `change-governance.md` 的治理件）＝**4 处／2 件**（ACE reference `:209`／`:388`／`:390`，ACE SKILL `:116`）。改前后处数不变（本批追加串不复述该字面），标注覆盖由 1 处升为 **3 处**，第 4 处 `:388` 由其紧邻 `:390` 的证据状态注覆盖。

## 四、越界自证

以「运行时现字节 ↔ `git show HEAD:技能仓备份/<相对路径>` 前像」对 `git ls-files 技能仓备份` 的 72 件（按留痕文法过滤后仍 72 件）逐件比对，命中 **4** 项＝本批 3 件 L2＋`shared/change-governance.md`（登记面经带平，属授权写面）；`同步说明.md` 在运行时层无对应件，故不属本比对集，只出现在 `git diff --stat -- 技能仓备份` 的 **5** 件件集里（sync 派生件，非写面）。两口径同判＝本批未触碰其余 68 件技能件、7 件治理文件中的其余 6 件、`素材/` 与 `成果/`。

## 五、对上一批（CG-20260922-001）登记面两处夸大的现读订正

登记面 PL-048／PL-051 的「事项」列按快照纪律**不改写**，订正记于本件与两行的「处置依据」列：

1. **PL-051 未标注位点数被高估**：原述「除 `:390` 外未逐处标注证据状态」在技能正文 4 处口径下读作 3 处未标注。现读 `acoustic-calculation-engine/reference.md:388` 的精度声明由紧邻的 `:390`「该精度值的证据状态」注（同段、间隔一空行）覆盖，故改前真实未标注 = **2 处**（`:209`、SKILL `:114`），非 3 处。本批把这 2 处标注到位。
2. **PL-048 越域分级点数错指**：原述「`:1047`／`:1064` 两处 f₀ 直接给「→ 优秀」，即把浮筑地面 f₀ 分级表用于架空第二级（MSM）」中，`:1047` 现读为 `f₀₁ = (1/2π) × √(10×10⁶ / 103) ≈ 49.6 Hz → 优秀`，系**第一级浮筑**、在 M5 分级表域内，属合法用法。真实越域分级只有 `:1064`（MSM 第二级）一处；本批另在同文件扫出同型面 `:1161`（示例 5 摘要段的第二级 MSM），故取消的越域分级 = **2 处**（`:1064`、`:1161`），其中 1 处系本批新发现而非上批所登。

## 六、跨件同族面：只登记不动手

批 A′ 授权写面限于上述四行的文字侧所指文件，故以下面**只登记判据、本批零改动**（新登 PL-056）：

- `prefab-floor-system/reference.md:1049` 选型表「L'nT,w 达标可行性」列值「高（余量充足）」、`:1062` 选型建议「隔声余量充足」——与本批在 `examples.md` 撤除的「余量充足」措辞同族。二者语义是构造可行性分级而非个案达标结论，是否须统一改判为「达标须现场 L'nT,w 检测判定」属口径扩面，不在本批授权写面。
- `prefab-partition-wall-solution/examples.md:92`、`:149` 的「安全余量充足」经按 CG-20260922-001 单源化后的 §4.B.1 余量规则复算**判为非缺陷**：`:149` 目标 Rw+C≥35 dB、估算 45-50 → 中值 47.5 ≥ 35＋3、下限 45 ≥ 35，两条件均成立；`:92` 目标 Rw+C>50 dB、估算 57-62 → 中值 59.5 ≥ 53、下限 57 ≥ 50，成立且余量 9.5 dB。故不列入 PL-056 待改面，仅在此登记判据以免后续批误改。

## 七、台账归宿（四行转已闭合＋五行新登）

采「主项转已闭合＋残余另登新行」拆分，未把部分动作写成整项闭合：

| 行 | 归宿 | 闭合承重 | 残余 → 新行 |
|---|---|---|---|
| PL-047 | 已闭合 | 达标结论与余量数值在 `examples.md` 全量撤除（三类字面复归 0），并披露两量间无换算式 | PL-057（Ln,w ↔ L'nT,w 无换算式真值源） |
| PL-048 | 已闭合 | 越域披露 7 处、越域分级 2 处取消，域内「优秀」用法逐条现读保留 | PL-053（18-25／30-40 两个域值本身须外部真值） |
| PL-050 | 已闭合 | 定位行与「承载形态」声明入正文（SKILL.md） | PL-054（内核新建 或 名称／description 改述，二者须发起人裁定且后者触契约手抄面） |
| PL-051 | 已闭合 | 未标注 2 处标注到位，且 `:388` 经现读判为已被 `:390` 覆盖 | PL-055（±2-3 dB 数值保留抑或撤回须外部真值） |

新登五行均：登记日期 2026-09-22、到期日 2026-12-21（常规 TTL 90 天）、状态 待处置、来源列引 `CG-20260922-002`。

台账行数 52 → **57**；状态分布＝待处置 19 → **20**、已闭合 32 → **36**、已裁定不做 1 不变（36＋20＋1＝57）。

## 八、本批不闭合与批 B 三项

- 不闭合：PL-038（M5 公式侧／区间侧须官方真值）、PL-046（降级判据行为终证须注入复验）、PL-031（T-4 盲评责任人未指派）、PL-044（pre-push 阻断仍活）、PL-049、PL-052（后者缺的是设计意图输入：83-85mm 是漏计项还是把地暖管按另一口径计入，无输入则只登记不猜改）。
- 批 B 三项（M5 改公式侧还是区间侧、现场 L'nT,w 换算、±2-3 dB 数值取舍）一律须外部真值通道，不并入本批；本批所有改动均为口径、披露与证据状态标注，零改上述三项的数值本体。
- 推送态：`.git/hooks/pre-push` 在 PL-044 未裁定的 FAIL 出口裁定下 exit 1，本仓当前不可推送；本批授权含本地 commit、不含 push。
