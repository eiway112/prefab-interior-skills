# CG-20260924-005 专业整改五批-组5 M2 吻合频率整改执行记录

- 执行日期：2026-09-24
- 变更级别：**B 级技术修订**（定级依据见 `change-governance.md` §九 CG-20260924-005 行；级别不继承登记批 CG-20260923-004 的 C 级任务定义，按总单§五纪律独立定级）
- 授权：发起人组5指令「开『组5 M2 吻合频率批』，目标＝PL-065 误差说明与公式一致＋G-3 三表同源、绝对结论收窄或全域验证，全程内部复算可闭合、不等外部通道」；方法论两项经发起人 `AskUserQuestion` 确认＝三表同源采「全声明域包络」、绝对结论采「收窄＋披露」
- 提交态：**不提交、不推送**（授权面不含；`.git/hooks/pre-push` 仍受 PL-044 阻断）。提交态以 `git log`、推送态以 `git rev-list --left-right --count HEAD...origin/master` 为准。
- 取证目录：`D:\Qoder-Files\_整理与清理\2026-09-24\专业整改五批-组5M2-CG-20260924-005\`（原像／m2_recalc 复算脚本与输出／门禁日志／登记与残量）
- 闭合方式：**全程内部复算闭合，不走外部官方核验通道**——fc 属 C 类派生计算量（先复算）、参数域属 B 类材料物理参数（复算区间自洽），依 `shared/data-classification.md` §四均不以官方核验为合格前置。

## 一、写面清单（运行时 2 件＋守卫 1 件，均纯 LF）

| 件 | 改前 | 改后 | sha256（改前→改后前16位） |
|---|---|---|---|
| `~/.qoder/skills/acoustic-calculation-engine/reference.md` | 41,902 B | 42,703 B | 09304639cd9a948d → 1ec7de5b4ea99c93 |
| `~/.qoder/skills/prefab-partition-wall-solution/reference.md` | 106,767 B | 107,932 B | e9f745740aca8495 → d1b49359862a1c8a |
| `程序文件/ace_regression_test.py`（守卫） | — | v1.4.0→v1.5.0 | 新增 `TestM2CoincidenceDocConsistency` 10 例（116→126） |

合计写面净 +1,966 B。改前原像两件存取证目录 `原像/`（`before_hashes.txt` 全哈希，与本记录写面表逐值全等自证）。**未在运行时根新建 `_pre20260924CG005` 备份**（避免增 PL-042 残留面），回退载体＝原像两件 sha256 留存，未 commit 前可逐件 `git show HEAD:<path>` 还原。

## 二、逐 PL 写面明细

### PL-065（ACE 吻合频率修正因子省略误差说明不符公式）→ 已闭合
- `acoustic-calculation-engine/reference.md:72` 原「σ=0.2-0.3 及简化式误差约 3-8%」与公式不符，按薄板弯曲波理论复算订正。吻合频率完整式 `fc = c²/(2π·h)·√(12(1-σ²)·ρ/E)`（c=343 m/s），弯曲刚度修正因子 `√(12(1-σ²))` 分两项：
  - **主导项 `√12≈3.464`**：整体省略该因子退化为简化版 `√(ρ/E)` 时 fc 偏低约 70%（简化式仅完整式的 29.8%）；端点反例 12mm 石膏板 ρ800/E2.5GPa/σ0.25 由完整式 **2961 Hz** 误算为简化式 **883 Hz**，比值 **3.354**。
  - **次级项 `√(1-σ²)`**：σ 从 0.2→0.3 仅使因子变化约 **2.6%**（3.394→3.305，−2.64%）。
- 原「误差约 3-8%」字面撤净（守卫 `assert_ace_correction_factor_text` 钉住：「3-8%」须消失且 70%／29.8%／2961→883／2.6% 须在场）。结论＝简化版不可用于定量计算、ACE 始终用完整修正公式。
- 总单§三闭合条件满足：误差说明与公式一致、省略因子主误差与泊松比次级修正分清、端点反例可复算。

### PL-038 G-3（PW 三表同源＋绝对结论收窄）→ 局部处置，母项仍待处置
- **三表同源**＝`prefab-partition-wall-solution/reference.md`
  - 吻合临界频率参考表（:254—:267）由原印值（系统性偏低约 1.4 倍，如 12mm 石膏板原 1950-2450）改按**声明参数域包络**复算：fc_min 取最小 ρ／最大 E、fc_max 取最大 ρ／最小 E。10 行范围（石膏 9.5mm=3498-4435／12mm=2769-3511／15mm=2216-2809；硅酸钙 6mm=3728-6239／8mm=2796-4679／10mm=2237-3743／12mm=1864-3119；ALC B06 100mm=273-357／150mm=182-238／200mm=137-178）与参数表（:243—:250，**字节未改**）同源。
  - 异质复合搭配表（:274—:281）改按**统一标称基准**复算（石膏板 ρ800/E2.5/σ0.25、硅酸钙板 ρ1400/E6/σ0.22、9.5mm 石膏板取 ρ900），四行 fc1/fc2/Δfc 与有效性判定逐项自洽：12+10＝~2961/~3056/~96/❌、12+8＝~2961/~3821/~860/✅、15+10＝~2368/~3056/~688/✅、12+9.5＝~2961/~3967/~1006/✅；列名「频率差」补「Δfc」。三表（参数域→fc 范围→搭配）自此同源、标称点落包络内（12mm 石膏 2961∈2769-3511、10mm 硅钙 3056∈2237-3743、15mm 石膏 2368∈2216-2809、9.5mm 石膏 ρ900 3967∈3498-4435）。
- **绝对结论收窄＋披露**＝
  - :283 判据补「工程判据、非标准规定值」与非单调告警。
  - :285 参数敏感性说明按修正公式复算 Δfc 对硅酸钙板 E **非单调**：E=8GPa→Δfc≈314、E≈6.3GPa→两板 fc 相交 Δfc→0、E=5→4GPa→Δfc 回升≈388→783（V 形，真过零点 E≈6.395GPa）。据此显式判**「任何合理参数下均有效错开（Δfc ≥ 690 Hz）」这一绝对结论不成立**（690 无来源、E≈6.4 时 Δfc→0 直接反证），披露包络重叠（15mm 石膏板 2216-2809 与 10mm 硅酸钙板 2237-3743，最小角点差约 22 Hz），要求按所选产品实测 ρ/E 逐案核算、不由板材类型先验保证。
  - :287 设计警示同口径对齐（「仍须复算而非先验保证」）；:269 表后散文改包络口径（撤原「1550-2450」「均落入…显著影响」）。
- **同步纠错 ACE Δfc 示例**＝`acoustic-calculation-engine/reference.md:91—:95` 原 E=8→314／E=6→131 两点不可复算（131 实对应 ρ1400 下 E≈7、E=6 应为 96），改为与 PW 同源的非单调叙述（E=8→314、E≈6.3→0、E=5→4→388→783）；:88「Δfc = |fc₁ - fc₂| ≥ 300 Hz」判据字面**未改**（守卫 regex 钉住）。
- **母项状态不改**＝G-1 ACE 侧（acoustic-calculation-engine/reference.md:232/:592 M5 公式面）、G-2（缝隙 1% 损失 ACE↔PW 口径）本批未覆盖，PL-038 仅追加 G-3 局部进度注记、仍待处置（部分闭合不得改状态纪律）。

## 三、守卫与反向自证
- `TestM2CoincidenceDocConsistency`（ace_regression_test.py，10 例，v1.4.0→v1.5.0，116→126）：`setUpClass` 载 ACE＋PW 两件、支持 `ACE_SKILL_DIR`／`PW_SKILL_DIR` 覆写；`_param_domains_from` 从 PW 参数表现读三组声明域；`assert_fc_range_table_is_envelope`（10 行 ±1Hz 复算包络）、`assert_combination_table_same_source`（4 行标称∈域∈包络且判定-Δfc 自洽）、`assert_absolute_claim_narrowed`（非单调三点）、`assert_ace_correction_factor_text`（「3-8%」须消失且 70%／29.8%／2961→883／2.6% 须在场）、`assert_ace_delta_fc_nonmonotonic`；5 常态＋4 负向注入＋1 控制例。
- clean 态：`Ran 126 tests，OK`，exit 0（取证目录 `门禁日志/ace_clean_v1.5.0.txt`）。
- 改前原像重注入（`ACE_SKILL_DIR`＋`PW_SKILL_DIR` 指向原像目录）：`failures=6`，exit 1（`门禁日志/ace_preimage_reinject.txt`）＝守卫非恒真自证。
- 全域包络直算证据：取证目录 `m2_recalc.py`＋`m2_recalc_output.txt`（fc 范围表 10 行、搭配表 4 行、Δfc 非单调链、包络最小角点差逐项复现）。

## 四、SRE 独立复核（prefab-standards-reviewer，B 级审批要件）
只读子代理复核，**总体结论 PASS with warnings、必改项无**（不阻断发布）。复核员用完整式独立验算，抽查点全部逐字吻合：
1. **物理/数学正确性**：√12=3.4641、σ=0.25 时 √(12(1-σ²))=3.3541、简化/完整=0.2981（偏低 70.2%）；端点反例 2960.6→882.7（比值 3.3541）；σ 0.2→0.3 变化 −2.64%；Δfc 非单调真过零点 E≈6.395GPa（V 形先减后增、不能单点外推）；包络四角点差 {22,1528,572,935} 最小 22 Hz；ρ 取体密度口径无混淆、无单位错误。原「误差约 3-8%」确属量级性错误（把主导项 √12 的省略误当泊松比次级项，实际差 3.35 倍）。
2. **收窄恰当**：绝对结论站不住（690 无来源＋过零反证），改「按实测 ρ/E 逐案核算」符合 C 类工程纪律；无收窄不足（敏感性过零＋包络重叠两条失效模式都点到）、无过度收窄（✅ 有效三行仍保留有效判定）。
3. **三表同源**：包络由参数域端点算出、标称点落包络内、四行 Δfc 全吻合，同源成立。
4. **两处治理判断**：(a) 转 records 而非重冻——**认可**（这些值已被实际复算复核，重冻等于对新字节不经复核重新祝福，违反 baseline「迁移存量待复核」用途；沿 PL-070 先例、符合 STALE_BASELINE 规则文本）；(b) 14 行按 N 类登记——**有告警、属如实披露的过渡态、非骗绿**（每行附「结构上不可绑定来源」的受限具文理由、底层 fc/Δfc 值真 C 类且已逐一复算可复现、PL-072 显式承载取源义务；告警点＝N 判据②文本张力，PL-072 若长期挂起即沦为事实错分类）。
5. **遗漏写面／数值本体误改／安全隐患**：git diff 实证参数表 :243-250 字节未改（`GB/T 9775`/`JC/T 564`/`蒸压加气`/`参数来源` 检索零命中）、:88 判据为上下文行字面保留、ACE 仅 :72/:91/:96 改动与声明一致；整改方向系加严（撤绝对保证＋要求逐案实测＋披露重叠），不引入放宽或误导性安全结论。

**建议项（非阻断）处置**：
- 建议1（PL-072 须实质闭合）：已新登台账 PL-072 承载「为两表补来源列后按 C 类逐行取源」义务（到期日 2026-12-23），本记录显式声明 14 行 N 类系「过渡态 N＋PL-072 承载」，非默认 N 骗绿。
- 建议2（过零 E 精度）：正文两处取「E≈6.3 GPa」，复核真值为 E≈6.395GPa（6.3 处 Δfc=22 Hz）。因 fc 表已声明 ±10% 估算精度、6.3 在 ±10% 内可接受，本批**不改写面**（改写面将触发单元 id 变化→重登记→重跑门禁的成本，且值在声明精度内）；真值 E≈6.395 登记于此供追溯，PL-072 补来源列时可一并对齐精度表述。
- 建议3（fc 旧印值新旧对照留痕）：本记录§二已保留新旧对照（12mm 石膏板原 1950-2450→包络 2769-3511 等），历史快照不改写。

## 五、治理登记面（L1→运行时→镜像三层）
- `change-governance.md` 编辑面：头部「最后更新」新行＋旧行降级历史快照；§九 插 CG-20260924-005 行；台账 PL-065「待处置」→「已闭合」＋处置依据具文；PL-038 追加 G-3 局部进度注记（母项仍待处置）；**新登 PL-072**（承载 PW fc／Δfc 无来源列 14 表行取源义务）。
- **批内自造缺陷一处，据实登记并已修净**：§九 CG-20260924-005 行「影响范围」列原写「`文档/专业整改任务总单_v1.md` §十验收证据指针」，而组5 指针实际落 §十一（§十 系组4 CG-20260924-004 历史行、按快照纪律不可动）——属本批自增行的交叉引用笔误。按「就地订正本批自增行、不触历史行」纪律修净：以「写面 2 件」唯一锚定 442 行改 §十→§十一（组4 441 行 §十 引用零命中改动，修后全文「§十一验收证据指针」命中 1＝本批行、「§十验收证据指针」命中 1＝组4 历史行）。该订正使 442 行 table_row 单元 id 变化，按检查 10 `STALE_REVIEW` 同批补登记（见§六）。
- 纯 LF，**976,434 B**；订正后 sha256＝`3202252f061e2e0c…`，**L1／运行时 shared／技能仓备份三层全等**（本记录定稿时复算确认，CR 0）。〔订正前为 976,431 B／`26920407bc13f9bc…`，差额 3 B＝「一」字 UTF-8 字节数〕
- 机算复核：§九 CG 行 **134**（末行 CG-20260924-005、重复 0）；台账 **72** 行＝待处置 25／已闭合 46／已裁定不做 1（25＋46＋1＝72）。

## 六、检查 10 残量登记（第十三批）
- 脚本 `登记与残量/第十三批_登记残量组5M2.py`（临时面执行，关键产物已归档）；预期摘失效 records 11／baseline 18、新登 34，count-assert 与实际逐值全等后回写。
- **PW 侧 18 个原冻结存量单元按 `STALE_BASELINE`「从 baseline 移除并转为复核记录」转为 records（沿 PL-070 先例，非重冻）**：
  - 14 个无来源列 fc／Δfc 表行（fc 表 10 行 :258—:267＋搭配表 4 行 :278—:281，`source_cells` 现读为空、C 类 table_row 经 `SOURCE_BINDING` 结构上不可登记）按 **N 类**登记并逐处具文披露＋新登 PL-072 承载取源义务；
  - :285 敏感性散文按 **C 类**登记（`source_spans` 绑标称基准「石膏板 ρ800/E2.5、硅钙板 ρ1400」，Δfc 复算链可复现）；
  - :269／:283／:287 三散文按 N 类登记（fc 表转述指针／判据规则声明／设计建议）；
  - 3 个原 records 单元（:254／:274 表题散文＋:276 表头）按同位置改判 N 类 records。
- **ACE 侧 6 个段落单元**按 N／C 分类 records：:72 修正因子算术链 C（source_spans 绑 ρ800/E2.5/σ0.25）＋纪律尾 N、:91 算例输入基准 C、:92／:93／:94 Δfc 复算 C-spans（各绑 E 值片段）、:96 结论建议 N。
- 本文件 §九 本行、§11.2 PL-072 新行与编辑面单元按 CG_RULES 逐格判 N。
- 清单终态：records 8,580→**8,603**／baseline 3,836→**3,818**（version 2）；序列化往返等值校验通过；段区间从 0 连续无重叠覆盖单元全文（`SEGMENT_COVERAGE` PASS）；检查 10 单面 PASS（失败 0／警告 2＝既有 acoustic A_VERIFY_DATE，不动、不填推定日期）。改前清单备份 `登记与残量/数据分类复核清单_pre登记.json.bak`（sha256 75af30c83830bedf）。
- **批内订正补登记（第十三批补）**＝§五 所述 §十→§十一 交叉引用订正使 change-governance.md 442 行 table_row 单元 id 变化，按检查 10 `STALE_REVIEW` 同批对账：脚本 `登记与残量/第十三批补_登记残量组5M2.py`（沿第十三批 CG_RULES 与 cell_segments 口径、仅动 shared/change-governance.md 一键）摘旧行单元失效 record 1、按列规则重登新行单元 1（8 段全 N），断言 dead_r=1／dead_b=0／added=1 通过后回写；**净 records 不变（8,603）、baseline 不变（3,818）**。补登记前清单备份 `登记与残量/数据分类复核清单_pre补登记.json.bak`。
- **棘轮披露**：baseline 减量（18 个 PW 冻结单元移出）系本批写面的机械后果（字节变化即失配），**不是迁移进展**；PL-060 承重判据不因本行变化。

## 七、门禁终态（十二门禁全绿）
| 门禁 | 读数 |
|---|---|
| validate_governance 全量 | **PASS 通过 243／失败 0／警告 3，exit 0**（警告构成＝检查 3 聚合 1＋A_VERIFY_DATE 2＝ACE reference.md:709/:710，基线不变；通过计数随本体浮动、不钉死，硬判据＝失败 0＋exit 0） |
| verification_policy | Ran 6，OK |
| ace_regression | **Ran 126，OK**（116→126 系本批新增 `TestM2CoincidenceDocConsistency` 10 例） |
| sre_regression | Ran 66，OK（缺口信号 0／指纹 4f53cda18c2baa0c 不变） |
| sr_ic07_compliance | Ran 20，OK |
| pending_ledger | Ran 43，OK（含新登 PL-072 行结构合法性、7 列、日期可解析、状态枚举、CG 键闭合） |
| cross_layer_eol | Ran 25，OK |
| index_ref_consistency | Ran 26，OK |
| skill_cross_layer | Ran 21，OK |
| data_class_consistency | Ran 70，OK（含 34 条新 records 段覆盖与来源字面） |
| storage_anchor_guard | Ran 19，OK |
| wetzone_construction_guard | Ran 15，OK |
| sync_skill_backup | 写面带平后 `--check`「范围 73｜一致 72｜新增 0｜更新 1｜移除 0」（更新 1＝`sre_regression_report.json` 机器本地产物，复跑 SRE 门禁的机械后果，非写面漂移；写面两件 ACE／PW 与 shared/change-governance.md 均已一致） |

## 八、顺带观测（按快照纪律不改写他批登记，只在此具文）
1. ACE A_VERIFY_DATE 警告行号由 :708/:709 漂至 :709/:710——系 ACE reference.md 本批增长使后续行号机械下移，警告**条数与内容不变**（仍 2 条，沿检查 3 语义不视作标准失效、不填推定日期，取真须走 T-5 官方核验另线）。非缺陷。
2. AGENTS.md validate 门禁行「通过 241」与「243」并存系该文件自述的提交前/后双口径（commit 后单源件两副本进入检查 6 (C) 比对浮至 243），本批实跑 243 与之一致，无需订正。
3. AGENTS.md ace 门禁行读数「Ran 116 tests」滞后本批守卫升版（CG-20260924-005 新增 `TestM2CoincidenceDocConsistency` 10 例后为 126），本批已将该行更新为 126 并追加 116→126 归因（沿组4 同款处理，属门禁基线登记面随守卫升版的机械后果、非规则改动；90→105→116 历史归因按快照纪律保留不改写）。AGENTS.md 系仓根锚点件、不入镜像与检查 6/9/10 扫描面，编辑不触发残量登记。

## 九、状态唯一来源声明
PL-065 转已闭合、PL-038 G-3 局部进度注记（母项仍待处置）、PL-072 新登（待处置），均已具文于 `change-governance.md` §11.2 台账（状态唯一来源）；总单§十一仅登记验收证据指针，不改 PL 状态。
