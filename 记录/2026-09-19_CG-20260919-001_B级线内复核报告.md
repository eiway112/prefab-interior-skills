# CG-20260919-001 B 级线内复核报告

> **被复核批次**：CG-20260919-001（检查 6 增列第三条独立断言 (C)「git 可见副本 ↔ `HEAD` 前像行尾形态一致」＋ `validate_governance.py` v1.9.0→v1.10.0 ＋ 新增专项测试 `程序文件/cross_layer_eol_test.py` ＋ PL-020 闭合）
> **定级**：B 级（改动校验器检查面，`change-governance.md` §二 第 3 步；先例 CG-20260916-010〔v1.6.0 新增检查 6〕／CG-20260918-005〔v1.9.0 新增检查 7〕同型）
> **复核时刻**：2026-09-19 09:20—09:52（在登记文字与三层带平之后；第四节门禁与 sync 读数为复核尾部**终态复跑**，与登记文字同批取得）
> **复核方式**：发起人**线内自复核**——非 -005／-009 的「独立只读子代理」先例，偏离成因与影响见 OBS-1
> **结论**：**PASS（附 OBS 五项）**，其中 OBS-1 为程序性·独立性受限，OBS-2／OBS-3 两项缺陷**已在复核中当场订正并复跑取证**，OBS-4／OBS-5 为覆盖面如实披露（不新增机算，登记为口径注记而非遗留）
> **独立性声明**：本报告全部读数由复核步骤**重新实跑**取得，不引用发起人在 §九／`CHANGELOG.md` 登记文字中的自述值；两者不符处按本报告读数订正登记文字（已发生三处，见 OBS-3）。台账与门禁判据的复算改用**独立解析器与独立注入路径**（不复用被复核件自身的代码），以免自证。

---

## 一、复核前置：授权范围与写面边界

| 项 | 复核实读 | 结论 |
|---|---|---|
| 开批授权 | 用户 2026-09-19 明文开线，P0 项即「PL-020——检查 6 只比跨层互等，抓不到跨层同漂移」，并规定「期望成果＝给『与 `git show HEAD:<path>` 前像的行尾／字节形态一致性』取得机算承担者，并按 §11.3 第 3 条口径显式披露覆盖不到什么；负向注入须证明该判据仍能失败（不得恒真）」 | 本批落点与授权范围**逐字对应**（判据＋披露＋负向注入三要素齐备） |
| 定级是否成立 | `git status --porcelain` 实读 7 件：`AGENTS.md`、`CHANGELOG.md`、`_专题_技能合集策划/change-governance.md`、`技能仓备份/shared/change-governance.md`、`技能仓备份/同步说明.md`、`程序文件/validate_governance.py`、`?? 程序文件/cross_layer_eol_test.py` | **B 级成立**（改校验器检查面）；**不升 A 成立**——本批零触标准数值／状态字段／契约／红线／索引本体／运行时技能件（见 R6） |
| 写面是否越界 | 上述 7 件外，`记录/` 本报告为新增；**未出现**任何 `_专题_*` 策划稿、`成果/`、`文档/`、运行时技能目录内件 | 无越界写面；§九 与 §11.2 全部历史行未改（R2） |
| 授权边界是否守住 | 本批只做本地编辑与实跑；未执行 `git commit`、未执行 `git push`（开场实测 origin/master 落后 2 个提交，发起人明示推送待整改事项完成后统一进行） | **守住**（提交态／推送态按 §11.3 第 5 条以 `git log`／`git rev-list` 现查为准，不由登记面自报） |

## 二、判据有效性核验（本批核心：该断言是否真能失败）

| # | 核验项 | 复算判据 | 复算读数 | 结论 |
|---|---|---|---|---|
| 1 | (C) 的真值源是否**在三层互等闭环之外** | 现读 `git_head_blob()` 取物路径 | 走 `git cat-file -e HEAD:<rel>` ＋ `git cat-file blob HEAD:<rel>`，真值源＝HEAD 对象，非任一工作区副本 | **PASS**（不自我循环） |
| 2 | 覆盖面是否恰为声明表的 git 可见副本 | 独立从 `CROSS_LAYER_SET` 现读 7 件、按 `L1／REPO_ROOT／REPO_SHARED` 三层重算期望集 | 期望 **15** 件；实跑 (C) 比对项 **15** 项，逐件字面为 `名@层`（`change-governance.md@L1`…`standards-reasoning-rules.md@REPO_SHARED`） | **PASS**（期望数＝实跑数，无 glob 扩容、无静默漏件） |
| 3 | 反向注入取证一（**复核换件复做**，不采信初稿的 `glossary.md` 自述） | 向 `interface-contracts.md@L1` 注入**单字节** `\r`（行尾插 CR，非整文件改写）→ 实跑校验器 | `(C) FAIL「工作区行尾形态 混合行尾 ≠ HEAD 前像 纯 LF」`；同跑总评 **FAIL｜通过 222｜失败 3｜exit 1**（失败 3＝(C) 形态 ＋ (A) 该件两处下游副本落后） | **PASS**：判据**不恒真**，单字节级漂移即可报红，且不与 (A) 互相掩盖 |
| 4 | 注入是否残留 | `finally` 二进制还原 → 工作区字节与注入前**全等**比对 ＋ `git status --porcelain` 该件 | 哈希同值 `30d286cdb975fbe8`；`git status` **不含**该件＝与 HEAD 全等 | **PASS**：取证未污染登记面 |
| 5 | 「跨层同漂移」这一 (A)(B) 盲区是否真被 (C) 抓住 | 一次性临时 git 仓：两处副本写成**字节全等**而整体偏离 HEAD（即 CG-20260918-010 ⑩ 事故本体），(A)(B) 在此结构下**必绿** | `test_same_drift_across_layers_still_fails` 内 (C) 对两副本**逐项报红**；另一例证「部分漂移只报漂移件」不牵连好件 | **PASS**：该失效模式自此有机算承担者 |
| 6 | 五类形态口径是否自洽（防空跑与假红两头） | `EolFormPure` 逐类断言＋**真实内容编辑不得改形态**一条 | Ran 25 OK；含「单行 CRLF 注入」「整文件 LF→CRLF」「追加一行内容不改形态」「无行尾分隔符与空文件同类」 | **PASS**（首版按 LF 计数比对口径会造成每次合法编辑假红，已被该守卫纠正为「比形态不比字节」） |
| 7 | 降级与不判红分支是否静默放过 | `git_unavailable`／`no_head` 两分支实测 | 运行时目录 `git rev-parse --show-toplevel` 实读 **rc=128「not a git repository」**→ (C) 对运行时两层无断言（经 (A)(B) 传递），此即 §11.3 第 6 条第 ① 款的实证前提；零提交仓 → `git_unavailable`（**不等价于** `no_head`），报聚合 WARN 且逐项列出 | **PASS**（三态分类与披露口径一致） |

## 三、内容与登记面一致性核验（逐项复算，独立解析器）

| # | 登记面自述 | 复算读数 | 结论 |
|---|---|---|---|
| R1 | `change-governance.md` 三层带平 | L1＝运行时 `shared/`＝`技能仓备份/shared/` sha256 前 16 **互等 `cc544fc1dfd54a7e`**，三份 **CR 计 0** | **PASS**（判据取「互等」这一状态关系；本文件终值哈希写入登记面即变，故不入登记面，依 CG-20260916-004 ⑧(b) 先例） |
| R2 | 历史行零改写 | 与 `git show HEAD` 版逐行比对：HEAD 421 行 → 工作区 426 行；**净消失 2 行**＝旧「最后更新」行（改标签为历史快照后重新出现）＋ PL-020 旧行；**§九 历史行删除 0 条** | **PASS**（PL-016／PL-019 所在的历史行字节未动，其处置属 P1 待裁定，本批未越界） |
| R3 | 台账形制 | 独立解析（按转义 `\|` 切格）：台账 **20 行 × 列数集合 {7}**，表头 7 列；状态分布 **待处置 11／已闭合 8／已裁定不做 1**；编号重复 0；来源登记为空 []；PL-020 到期日 `—`／状态 `已闭合`／依据列非空 | **PASS**（与检查 7 实跑读数同值） |
| R4 | 键闭合 | §九 逐行首列实读 **94** 个 CG 编号；台账引用 **16** 个 CG 编号，全部 ⊆ §九；含 `CG-20260919-001` | **PASS** |
| R5 | 校验器版本与形制 | `validate_governance.py v1.10.0`，sha256 前 16 `47588b0d635b22ca`／2019 行／**CR 0**；`cross_layer_eol_test.py` 终态 `9e594049836ef680`／300 行／CR 0（订正后值，见 OBS-2） | **PASS**（登记文字已按订正后哈希改写） |
| R6 | 未触碰面反查 | 其余六件治理文件各层副本哈希：`standards-index.md` 5 处、`glossary`／`interface-contracts`／`redlines-registry`／`standards-reasoning-rules` 各 3 处，git 可见层内部**同层互等**；`platform-adapter-reference.md` 4 处分两组（根面 `cea4bb23296ea5ee`／`shared` 面 `364fe021c4d78027`），系 SOT 与其只读指针存根的**设计差异**（正文差 4 行，内容即「谁是 SOT」两条声明），非漂移 | **PASS**（本批对该六件只读取不写入） |
| R7 | (C) 面上「HEAD 形态＝规范值」否 | `platform-adapter-reference.md@REPO_ROOT／@REPO_SHARED` 的 HEAD 前像形态即**纯 CRLF**，(C) 报绿 | **不判缺陷，但须如实定性**：这正是 §11.3 第 6 条第 ②③ 款的活实例——该断言守「未提交态的漂移」，不担保「行尾应为 LF」的绝对规范 |
| R8 | 门禁与 sync | 见第四节 | **PASS** |

## 四、门禁与同步实跑（复核阶段全部重跑，未引用初稿自述）

| 命令 | 复核实跑读数 | exit |
|---|---|---|
| `python -B 程序文件/validate_governance.py` | **PASS｜通过 225｜失败 0｜警告 1**；检查 6 **(A)14／(B)9／(C)15**；检查 7 台账 20 行 × 7 列 clean、键闭合 94、待处置零超期；唯一 WARN＝检查 3 的 38 条无有效核验日期聚合 WARN（本批未触索引面） | 0 |
| `python -B 程序文件/verification_policy_test.py` | Ran 6 tests，OK | 0 |
| `python -B 程序文件/ace_regression_test.py` | Ran 90 tests，OK | 0 |
| `python -B 程序文件/pending_ledger_test.py` | Ran 36 tests，OK | 0 |
| `python -B 程序文件/cross_layer_eol_test.py` | Ran 25 tests，OK（订正清理缺口后复跑） | 0 |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py` | Ran 66 tests，OK；缺口信号 0 条、跨轮指纹 `4f53cda18c2baa0c`、连续一致 114 轮（收尾复跑后终态） | 0 |
| `python -B ~/.qoder/skills/prefab-standards-reviewer/sr_ic07_compliance_test.py` | Ran 20 tests，OK | 0 |
| `python -B 程序文件/sync_skill_backup.py` | 范围 122｜新增 0｜移除 0（判据成立）；更新件＝机器本地 `sre_regression_report.json` | 0 |

**通过计数 225 的归因复核**：210→225 的 +15 全额落在 (C) 的 15 条 [OK]，与第二节第 2 项独立重算的期望副本数 15 同值——**非检测口径放松**（(A)(B) 计数 14／9 与 CG-20260918-010 终态同值，档位未变）。

## 五、OBS 登记

- **OBS-1（程序性·独立性受限，不判不通过的理由）**：本批复核为**发起人线内自复核**，非 -005／-009 的「独立只读子代理」先例。发起人未放行为本批编排独立子代理，故按开线纪律的替代路径执行。补偿措施三条：① 全部读数**重新实跑**，不引用初稿自述；② 复算改用**独立解析器**（表列切分、§九 首列取号、`CROSS_LAYER_SET` 现读期望集），与被复核件不共享代码路径；③ 反向注入**换件复做**（初稿用 `glossary.md`，复核用 `interface-contracts.md` 且只注入单字节），使「判据仍能失败」这一结论不由同一次取证支撑两次。独立性仍弱于跨会话子代理，属程序性缺陷，如实留痕而非隐去。
- **OBS-2（缺陷，已当场订正并复验）——新增测试件自清不彻底**：`cross_layer_eol_test.py` 的 `test_repo_without_any_commit_is_git_unavailable_not_no_head` 自建临时仓（前缀 `nocommit-`）**未登记清理**，复核实测 `_tmp-scripts\cross_layer_eol_scratch\` 残留 **3** 个仓目录（另含早期迭代遗留的 `qoder-eol-outside-*`／`dbg-*`）。这使登记文字「跑后自清」在订正前**不成立**。改法＝`self.addCleanup(rmtree_force, root)`；清空历史残留后复跑 **Ran 25 OK、残留 0**，该件哈希由 `89a1584bafc17774`／299 行变为 `9e594049836ef680`／300 行，`CHANGELOG.md` ④ 已按终态改写。定级不变（属测试件自身卫生问题，不触判据）。
- **OBS-3（登记文字三处与实跑不符，已按实跑订正）**：① ⑦ 原写「sync（跑前清两层 `__pycache__`）」，实测两层 `__pycache__` 与 `*.pyc` **计 0、无需清理**，已改为「实测 0」并补记 sync 完整读数；② ⑦ 原写治理件 `git diff --numstat` 为「4 插入／1 删除量级」，系**头部改写之前**的中间态读数，终态为 **7 插入／2 删除**（净消失行仅两条、§九 历史行 0 条），已按终态改写；③ ④ 的测试件哈希与行数随 OBS-2 订正改写。教训＝收尾期任何在写入前取得的读数都不得留在终态文字里，登记文字须绑终态复跑。
- **OBS-4（覆盖面如实披露，不新增机算）**：(C) 的「提交后即转绿」窗口在本批复核中可见——`platform-adapter-reference.md` 两层副本的 HEAD 前像形态为**纯 CRLF**（其余 13 件为纯 LF），(C) 仍报绿。这不是缺陷（口径明写真值源是 HEAD 而非规范值），但意味着若坏形态在一次提交中被带入，防线只剩「提交前跑门禁」这一时序。本批未把它升格为机算（升格须引入规范值常量，属另一轮定级），而以 §11.3 第 6 条第 ②③ 款披露。
- **OBS-5（覆盖面边界确认）**：(C) 与 (A)(B) 同样只覆盖声明表 7 件治理文件；技能目录内约 114 件与机器本地 `sre_regression_report.json` 不经检查 6（PL-010 面），且 `程序文件/` 内脚本（含本批两件）**不在**声明表内、无三层镜像，故其行尾形态不受 (C) 保护——本批该三件实读 CR 均为 0，属人工纪律而非机算保障。

## 六、结论

**PASS（附 OBS 五项；其中 OBS-2／OBS-3 已在复核中当场订正并复跑复验，OBS-1 为程序性独立性受限，OBS-4／OBS-5 为覆盖面如实披露）**。

判据成立的三条要点：① (C) 的真值源在三层互等闭环之外（HEAD 对象），故为「跨层同漂移」这一 CG-20260918-010 ⑩ 暴露的盲区取得唯一机算承担者；② 反向注入两次独立取证（单件单字节漂／多件同漂）证明该判据**不恒真**，且与 (A) 各自报红不互相掩盖；③ 口径与不覆盖面（运行时层无 git 面、HEAD 非规范值、只比形态）已在 §11.3 第 6 条显式披露，与 §11.3 第 3 条「披露而非机算」同一取舍。

PL-020 闭合成立；PL-016／PL-019（P1）、PL-001／PL-006／PL-009（P2）、PL-011（P3）本批**未触碰**，P1 的处置路径待发起人裁定后再开批。
