# 丙项｜热层超限腾退 — 去重合并执行记录（CG-20260918-006，C 级）

- **执行日期**：2026-09-18
- **授权范围**（用户 2026-09-18 AskUserQuestion 第一轮裁定「按建议范围授权（推荐）」）：**仅去重合并＋冻结新增，不做批量删除**
- **落点**：仓外 Qoder 记忆目录（**不在本仓工作树内**，不入 `sync_skill_backup.py` 范围、不受检查 6 跨层比对覆盖）
  - project 级：`C:\Users\聂卫华\.qoder\projects\D--Qoder-Files----------\memory\`
  - user 级：`C:\Users\聂卫华\.qoder\memory\`
- **对应台账**：PL-014（本批闭合其授权范围内部分）／PL-017（本批新登记 user 级残余）

---

## 一、执行前置：备份（可回退判据）

记忆目录在仓外、**无 git 安全网**，故动手前先做全量副本并机算核验：

| 备份目标 | 备份位置 | 核验 |
|---|---|---|
| project 级 57 件 `.md` | `D:\Qoder-Files\_memory-backup-20260918\project\` | 源↔备份 sha256 **哈希集合全等 True** |
| user 级 95 件 `.md` | `D:\Qoder-Files\_memory-backup-20260918\user\` | 源↔备份 sha256 **哈希集合全等 True** |

备份位置选在 `D:\Qoder-Files\` 而非仓内：**在本仓工作树之外**，故不会污染 git 状态、不会被 `sync_skill_backup.py` 当作「范围外差异」移除，也不违反「临时文件禁止堆积在 C 盘」。回退方式＝从该目录逐件复制回原位。

---

## 二、实测现状（不采信记录中的旧计数）

`记录/2026-09-18_agent-wiki_v1.0.0对齐梳理与融合前提重估.md` 裁定 5 记的是「project 55>30、user 43>40、≥10 组同名重复」。本次动手前重新实测，**两处与记录不符，均以实测为准**：

| 目录 | `.md` 文件数（不含 MEMORY.md） | MEMORY.md 索引行 | 唯一 `name` | 同标题重复组 | 内容哈希唯一值 | 索引↔文件双向 |
|---|---|---|---|---|---|---|
| project 级 | **56** | **56** | **30** | **26** | 31 | 引用集 == 文件集（0 悬空、0 孤儿） |
| user 级 | **94** | **44** | 94 | **0** | 94 | **50 件存在于磁盘但未被索引** |

三点定性：

1. **project 级重复面比记录更严重**：不是「≥10 组」而是 **26 组**，且 56 件里只有 **31** 个唯一内容哈希——即接近全盘两两重复。索引侧 56 行与文件 56 件严格一一对应，说明重复是**写入侧同一条经验被落两次**（文件名带 `-2` 后缀或另一串哈希后缀），不是索引漏删。
2. **user 级不存在可合并对象**：94 件的 `name` 与内容哈希**双双全唯一，重复组 0**。故「去重合并」在 user 级**产出为零**——这是本次执行的关键事实：授权范围内的手段对 user 级超限**无解**。
3. **user 级真问题是索引覆盖率，不是重复**：94 件中 **50 件从未进入 MEMORY.md 索引**。索引是常驻注入面，未被索引＝该条经验**从未被注入过任何一轮对话**，等于写了不用。孤儿按 `(type, category)` 分布：`user/user_communication` **33**、`feedback` **9**、`reference` **6**、`user/user_behavior` 1、`user/user_info` 1；文件名形态上 `user-communication-<hash>.md` 占多数，另有 `qoderwork-memory-<hash>.md` 9 件与 `qoderwork-daily-*.md` 2 件属早期迁移期产物。

---

## 三、执行动作（project 级，授权范围内）

**正典选择规则**（确定性、可复算，不靠主观取舍）：按 **MEMORY.md 索引出现顺序**建组，组内**第一件**为正典、其余移除。理由＝索引顺序即注入顺序，保留在前者可维持既有注入语义不变。

**内容无损判据**（先干跑取证、后落盘）：对每一组，比对正典与待移除件的「frontmatter（剔除 `source:` 字段）＋正文」是否**逐字全等**；不等即中止，不按纯去重执行。

- 干跑结果：26 组，**正文/frontmatter（除 `source`）不一致的组＝0**，待移除 26 件。
- 唯一字节差异组：`装配式隔墙技术手册页面结构与编辑/存储机制`（`project-introduction-16d88d97.md` vs `project-introduction-0f387ad8.md`，同为 1761 字节），差异**仅** frontmatter 一行 `source: user` vs `source: auto`。索引在前的正典恰为 `source: user`（16d88d97），即**保留了来源更强的那份**，两条规则未冲突。
- 落盘顺序：**先重写索引、再删文件**（中断也不会产生悬空引用）。

**执行读数**：索引减行 **26**｜删除文件 **26**｜终态 文件 **30**、索引行 **30**、唯一标题 **30**、残余重复标题组 **0**、文件集 == 索引引用集 **True**、悬空引用 **0**、意外新增 **0**。

**事后对着备份做内容无损审计**（不是自证，是与独立副本比对）：

| 审计项 | 读数 |
|---|---|
| 备份件 / 现存件 / 已移除 | 56 / 30 / 26 |
| 移除件的标题在现存集中有同名条目 | **26 / 26** |
| 移除件的正文（除 `source` 字段）在现存同标题条目中**逐字命中** | **26 / 26** |
| 内容丢失条目 | **0** |
| 判定：内容无损 | **True** |

**结论**：project 级由 56 件降至 **30 件**，与 `记录/2026-09-18_agent-wiki_v1.0.0对齐梳理与融合前提重估.md` 裁定 5 所载 project 级阈值 **30** 恰好相等，**未删除任何一条内容**，全部 26 条被移除文件的正文均在现存同标题条目中逐字可寻。

> **阈值来源与其核验状态（不冒充已核验）**：cap 30／cap 40 两个数字**转引自上述梳理报告裁定 5**，本次**未能对其配置源核验**——尝试在 `C:\Users\聂卫华\.qoder` 下检索含 `memory` 的配置文件时被自动模式权限门拦下（判定为超出工作区范围，拦截合理，未重试）。故本记录中凡涉 cap 的比较均为「与该报告所载阈值比较」，**不是**与平台真实配置比较；若平台实际阈值不同，第三节「与阈值 30 恰好相等」这一定性需相应调整，但**执行动作本身与该阈值无关**（去重合并的正当性来自「同标题两份、正文逐字相同」，不来自是否超限）。

---

## 四、未执行的动作及其理由（授权边界，非遗漏）

| 动作 | 是否执行 | 理由 |
|---|---|---|
| user 级去重合并 | **无对象可执行** | 实测 94 件 `name` 与内容哈希双双全唯一，重复组 0 |
| user 级把 50 件孤儿补进索引 | **不执行** | 属**新增索引行**，与授权闸门「冻结新增」直接冲突；且会使索引由 44 行增至 94 行，远超 cap 40，反向恶化热层信噪比 |
| user 级删条目以回到 cap 40 | **不执行** | 超出授权（明确「不做批量删除」） |
| 把冻结条款写进 MEMORY.md 正文 | **不执行** | MEMORY.md 是纯索引面（实测两文件均自第 1 行起即为 `- [标题](文件) — 提示` 形态，无标题、无 frontmatter）。注入其解析形态不可见，插入散文行有破坏注入的风险；且「协议式约定」弱于结构性隔离，故冻结以**本记录＋台账 PL-017** 为载体，不改索引文件形态 |
| 新增记忆条目登记本次冻结 | **不执行** | user 级已 44 > cap 40，再新增即违闸门。本记录（仓内）为唯一载体 |

**冻结新增的实际含义**（本批据以执行）：在 project 级回到 cap、user 级超限未裁定前，**不向两个记忆目录新增条目**；本轮新增的 `feedback-registration-file-splice-safety.md`（CG-20260918-004 期间写入）系冻结生效**之前**的既成条目，已实测确认其在 user 索引内、非孤儿。

---

## 五、需用户另行裁定的事项（已登记 PL-017，本批不动手）

user 级同时存在**两个方向相反**的缺口，二者不能同时满足，故必须裁定而非自行取舍：

- **缺口 A｜索引覆盖率**：50 件记忆从未进注入面（其中 33 件为 `user_communication` 类，正是协作偏好类经验，未注入的代价最直接）。补齐＝索引 44→94 行。
- **缺口 B｜热层容量**：索引 44 行已 > cap 40（超出 4 行）。补齐 A 后索引将达 **94 行**＝cap 40 的 **2.35 倍**（94÷40）、当前 44 行的 **2.14 倍**（94÷44），即修 A 会把 B 从「超 4 行」推到「超 54 行」。

**可选路径**（供裁定，本批不预设结论）：① 先做**语义合并**（33 件 `user_communication` 中同主题者合成一条，索引行数不增反降），再择优补索引——最贴合「不删内容」但需逐件判读，成本高；② 按类型分批补索引并接受 cap 上调；③ 维持现状，仅把 9 件 `feedback`＋6 件 `reference`（合计 15 件，规则类、复用价值高于叙事类）补进索引，其余留档不注入；④ 判定 50 件为历史迁移残留，授权归档到仓外冷层目录（移出注入面但不删）。

---

## 六、可复算性

本记录全部读数由 `.tmp-verify/mem_dedup.py`（干跑／`--apply` 两态）与事后审计脚本产出；该临时目录按纪律在提交前删除，故**复算路径以本记录所载判据为准**：文件数、索引行数、唯一标题数、`文件集 == 索引引用集`、以及「移除件正文在现存同标题条目中逐字命中 26/26」。备份副本 `D:\Qoder-Files\_memory-backup-20260918\` 保留，作为回退与再审计的独立基准。
