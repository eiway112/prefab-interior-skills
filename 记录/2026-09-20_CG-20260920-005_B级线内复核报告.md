# CG-20260920-005 B 级线内只读复核报告

> 复核性质＝§二 第 3 步：发起人**线内只读子代理**独立复算（B 级不占外部复核通道）。
> 独立性要求＝不得用被测检查件自证。本报告读数由子代理以**独立算法**复算（AST 现读 `sync_skill_backup.py` 的 `SKILL_DIRS` → 自建两层文件枚举 → 自建 `git check-ignore --stdin` 排除面 → 逐件 `sha256(原始字节)` 互等，**未调用** `check_skill_cross_layer`），再与发起人声明值比对；并以 `git show HEAD:程序文件/validate_governance.py` 前像跑双版本对照核差额。
> 复核时间＝2026-09-20。工作目录＝`D:\Qoder-Files\装配式装修技能开发`。

## ① 总体判定：PASS-with-OBS

六项 claimed 读数全部独立取证相符，无停线项（检查 9 零真 FAIL、排除面无白名单、未做「不做项」、通过数差额经双版本实测成立）。唯一中间态＝两份 `change-governance.md` 镜像待带平（收尾必做，非缺陷）。**OBS-2（控制例标号）已在实施批内当场收口**（补 C3/C4 标签，见 §③）。

## ② 逐条读数核对（claimed vs 独立算出）

| claimed | 独立算出 | 判定 |
|---|---|---|
| 检查 9 小节 [OK] 恰 2（D 自洽＋字节全等） | 实跑 `+` 行 2 | 符合 |
| 纳入判据 62／运行时∪镜像 120／gitignored 58；62+58=120 | 独立复算：15 目录、并集 120、排除 58、纳入 62、漂移 0、单层缺失 0 | 符合 |
| 当前总评「234 通过／2 失败／1 警告」，2 失败全为检查 6 (A) 跨层落后 | 实跑 `FAIL｜通过 234 失败 2 警告 1｜exit 1`；两条 FAIL 均为 `change-governance.md` RT_SHARED／REPO_SHARED `ab41329d…` ≠ L1 `2ce0cdbf…`，检查 7／8／9 零 FAIL | 符合（正常「登记→带平」时序中间态） |
| 第九门禁 `Ran 21 tests, OK`, exit 0 | 复跑（补标号后）仍 `Ran 21 tests, OK` | 符合 |
| 排除面＝`git check-ignore` 结构判据、零白名单 | `git_ignored()` 走 `git check-ignore --stdin`，`_walk_layer_files` 零排除；`sre_regression_report.json`／`*_pre*`／`.bak`／`.pyc` 仅出现在 INFO 披露文案，无逐个点名跳过逻辑、无白名单常量 | 符合 |
| 不做项：`standards-index.md` 五副本＋技能件正文零改动、`sync_skill_backup.py` 行为面零改动、历史快照行零改写 | `git status` 改动仅 `AGENTS.md`／`CHANGELOG.md`／L1 `change-governance.md`／`validate_governance.py`＋新增 `skill_cross_layer_test.py`；standards-index 各层经检查 6 绿（对 HEAD 无漂）；旧「最后更新」行降级为历史快照、正文逐字节 identical；§11.3 第 7 条仅末句括号订正 | 符合 |
| §11.2 PL-010：状态→已闭合、事项／来源／登记日期(2026-09-18)／到期日(2026-12-17) 四列零改写；PL-029 维持已闭合 | PL-010 列 0—4 与 HEAD 逐列 SAME，仅状态＋依据列改判；PL-029 整行 SAME | 符合 |
| 通过数 234→236，差额 2＝检查 9 净增 2 条 [OK]、非口径放松 | HEAD 版校验器（临时副本、猴补丁路径、对当前盘态）实跑 **232／2 失败**；与现版 234 之差恰＝检查 9 净增 2 条 [OK]；`validate_governance.py` diff 为 +172/−2，两处删除均为版本头行 → 纯增量。带平后 234+2=236 逻辑自洽 | 符合 |

## ③ OBS 清单

- **OBS-1（中间态，收尾消解）**：两份 `change-governance.md` 镜像（运行时 `shared/` 与 `技能仓备份/shared/`）尚未带平，即 §② 第 3 行的 2 条 (A) FAIL 之来源；收尾三层带平后须复跑至 validate **236／0／exit 0**，此为本批收尾必做步骤，非设计缺陷。
- **OBS-2（控制例标号，已当场收口）**：首轮复核发现 `skill_cross_layer_test.py` docstring 声明「控制例 C1—C5」而代码仅显式标 C1／C2／C5。发起人随即把两处既有「有牙」例补齐标签——`test_oneside_*` 补 **C3**（单层缺失双向复红，证判据非恒真）、`test_missing/extra_mirror_dir` 补 **C4**（双向互查漏配／越界两侧各有牙），使登记面「C1—C5 全通过」与代码逐条可核；补标号后复跑仍 `Ran 21 OK`。
- **OBS-3（口径留痕）**：真实仓 clean 态基线钉死「纳入判据 62 件／[OK] 恰 2」，属时点现测；技能内容合法增减时须随批更新钉值（与第七／八门禁同风格），非回归判据。
- **OBS-4（独立性受限）**：线内子代理与发起人共享同一工作区真值；发起人「停线前置 `sync --check` 一致 122」读数未由子代理复跑（避免写盘），其实质（62／120／58、漂移 0、单层缺失 0）已由子代理独立复算覆盖，故不受影响。

## ④ 停线判据（均未触发）

检查 9 有真 FAIL → 否；排除面含文件／行号白名单 → 否；本批做了「不做项」（改索引本体／技能件正文／历史快照行／sync 行为面）→ 否；通过数差额归因不成立 → 否（双版本实测＝净增 2）。
