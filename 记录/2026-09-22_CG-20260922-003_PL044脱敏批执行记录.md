# CG-20260922-003 执行记录：本仓发布面用户名路径脱敏（PL-044 处置面落地）

> 本件自身不得含本机用户名绝对路径与仓外布局路径字面（隐私优先于逐字快照，且本目录仅 `local_repo_path` 类被豁免）。
> 凡引用他件原路径，一律写相对路径或 `Users\<user>` 占位形态。

## 一、授权与写面

发起人 2026-09-22 明示（引用时用户名段已按本批口径写作 `<user>`，余字面逐字）：

> 「解除『不要改装配式装修技能开发任何文件』的边界，授权一版本仓脱敏批：把 记录/、文档/、CHANGELOG.md、
> _专题_*/ 里 83 处 `C:\Users\<user>\…` 改为 `C:\Users\<user>\…`（含历史快照件，按隐私优先于逐字快照裁定），
> 并按该仓 change-governance.md 定级登记」

即：被改面与改后面在原句里只差用户名那一段——本件引用后两句字面相同，读时按上句＝真实段、下句＝占位段理解。

授权含本地写入，**不含 commit、不含 push**。原句里的 83 处系发起人给的量级；本批按现读扫描取准，
落地面为 18 件／100 处替换（其中 17 件含真实用户名段共 99 处，另 1 件是省略号假阳归一，见 §二 第 ④ 项）。

定级＝**B 级**，依据与不升 A／不采 C 的理由载于 `change-governance.md` §九 本批行的级别列，此处不复制。

## 二、改法与自证配方

写脚本：`_tmp-scripts/cg_003_sanitize.py`（内容面）＋`_tmp-scripts/cg_003_register.py`（登记面）。

① **只替换一个 token**＝正则 `(Users[\\/])`＋用户名 一个捕获组后的用户名段 → `\1<user>`，不做任何相邻改写。
② **零越界自证**＝替换后把 `<user>` 反替换回真实用户名须与原串逐字符全等（`assert rev == text`），
   任一文件不等即整批不写。
③ **形态不漂自证**＝逐件断言 CR 计数与 BOM 标志在替换前后相等（本仓治理面为纯 LF，替换不引入 CR），
   否则检查 6 (C)「git 可见副本 ↔ `HEAD` 前像行尾形态一致」会因本批报红。
④ **同件假阳归一**＝`记录/2026-09-20_CG-20260920-007_公开发布面暴露审计与整改执行记录.md:42`
   原写 `file:///C:/Users` 段后直接接省略号，门禁 `local_user_path` 词条把省略号读成用户名而命中（属词条假阳，非真实泄露）；
   不改则本批终态该项仍报 1 处红。改后为 `file:///C:/Users/<user>/…`，与全仓占位口径一致。
⑤ **镜像层不手改**＝`技能仓备份/` 是 `sync_skill_backup.py` 产物，其内 3 行用户名段经
   L1 → 运行时 `~/.qoder/skills/shared/` → `sync_skill_backup.py` 带平落地，不属手工写面。
⑥ 取物面＝`git show HEAD:<path>` 前像；下表"改前字节／sha256 前 16"即前像值，可事后逐件复算。

### 逐件写面账（18 件／替换 100 处／净 −290 B）

| 件（相对仓根） | 替换处 | 字节 改前 → 改后 | sha256 前 16 位 改前 → 改后 | 行尾 | BOM |
|---|---|---|---|---|---|
| `CHANGELOG.md` | 2 | 401,238 → 401,232 | 034769523387c940 → 68810ef855208b2f | LF | 否 |
| `_专题_技能合集策划/change-governance.md` | 6 | 864,926 → 864,908 | 045d5c210ec6872e → 74dbf85f0a301b0c | LF | 否 |
| `成果/ST_MI_运行时回归测试报告_20260813.md` | 1 | 7,589 → 7,586 | 3dd6b8fcb8a144fa → 810702af5dd17dee | LF | 否 |
| `文档/SRE确定性体检设计方案_v1.0.md` | 2 | 244,066 → 244,060 | 70652b85739c0acd → 9a5b10b30bafee8d | LF | 否 |
| `记录/2026-09-18_agent-wiki_v1.0.0对齐梳理与融合前提重估.md` | 2 | 26,548 → 26,542 | eef8706c51bce6d5 → d986e2c052c0a851 | LF | 否 |
| `记录/2026-09-18_丙项热层去重合并执行记录.md` | 3 | 9,358 → 9,349 | c8d1e79651c8ae93 → b94f069a5ff63eba | LF | 否 |
| `记录/2026-09-19_CG-20260919-009_B级线内复核报告.md` | 2 | 22,608 → 22,602 | df30cf029dd43863 → 9a051fd8e3c12ac1 | LF | 否 |
| `记录/2026-09-19_CG-20260919-010_B级线内复核报告.md` | 1 | 39,181 → 39,178 | a1d60b2d782a27d6 → e61dc76d7cf53eb6 | LF | 否 |
| `记录/2026-09-19_CG-20260919-011_B级线内复核报告.md` | 1 | 10,291 → 10,288 | e1b9bf5f7ca2f4c0 → cdf23fdaa74a6198 | LF | 否 |
| `记录/2026-09-20_CG-20260920-002_B级线内复核报告.md` | 1 | 9,720 → 9,717 | f6ab3d2241a887ec → b6a915c219bead8f | LF | 否 |
| `记录/2026-09-20_CG-20260920-007_公开发布面暴露审计与整改执行记录.md` | 1（假阳归一，非真实用户名段） | 10,613 → 10,620 | cb0184d8f70c4102 → f86ddb7e3c12231a | LF | 否 |
| `记录/2026-09-21_CG-20260921-005_B级线内复核报告.md` | 2 | 11,345 → 11,339 | 6ebf336498e64a54 → 95a53b55e9e84f43 | LF | 否 |
| `记录/2026-09-21_PL-031环境层真降级注入记录_v1_T3批.md` | 1 | 22,469 → 22,466 | 1a2ec5ec3aa64b95 → f278945ac709081b | LF | 否 |
| `记录/2026-09-21_PL-031阶段1试跑记录_v1_探针批.md` | 70 | 173,745 → 173,535 | a72307fab25e0ac2 → 22a14bbe365ca494 | LF | 否 |
| `记录/2026-09-22_CG-20260922-001_B级线内复核报告.md` | 2 | 31,409 → 31,403 | f19efa46ab45640e → 12ec5e8eeeb72b65 | LF | 否 |
| `记录/BK_厨卫技能功能测试报告_20260813.md` | 1 | 8,426 → 8,423 | 940a17be9cc77104 → 51329e0ca09b1dfa | LF | 否 |
| `记录/OR_编排器功能测试报告_20260813.md` | 1 | 12,374 → 12,371 | 3e7f266f33ff91ac → 0faac98e5f927370 | LF | 否 |
| `记录/OR_编排器端到端LLM回归测试报告_20260813.md` | 1 | 3,276 → 3,273 | c3a80fc5e1f2aecf → 71642e93a07acab0 | LF | 否 |

（登记面另含本文件与 `CHANGELOG.md` 的追加写，其字节数不列入上表——上表只记内容面脱敏。）

### 门禁词条读数（可事后复算）

复算配方＝对被测文本逐行跑 `publish_redact_terms.json` 编译后的词条正则，
并按 `exempt_kinds_for(相对路径)` 判该行是否被豁免，未豁免即计一行。

| 口径 | `local_user_path` | `local_repo_path` |
|---|---|---|
| `git ls-files` 面 ＋ `HEAD` 前像（批前） | 仍红 95 行 | 仍红 9 行 |
| 工作区现读（内容面已改、带平未跑） | 仍红 3 行（全在 `技能仓备份/shared/change-governance.md`） | 仍红 9 行 |

## 三、终态读数（带平＋九门禁＋复扫）

次序沿 CG-20260921-010 固化下来的纪律：内容面 → 登记面一次写足 → 三层带平 → 九门禁实跑 → 收尾 sync → 只读 `--check` → 门禁只读复扫。

**① 三层带平**（本件引用的三层 `change-governance.md`，取 PL-059 补登后的终态实跑值）

| 层 | 字节 | 行尾 | BOM | sha256 前 16 |
|---|---|---|---|---|
| L1 `_专题_技能合集策划/` | 873,634 | CR 0／LF 528 | 无 | c27ee2267719f6be |
| L2 运行时 `~/.qoder/skills/shared/` | 873,634 | 同 | 同 | c27ee2267719f6be |
| L3 镜像 `技能仓备份/shared/` | 873,634 | 同 | 同 | c27ee2267719f6be |

三对全等。台账 59 行／§九 124 行。`CHANGELOG.md` 终态＝403,610 B／CR 0／LF 1,010／无 BOM／sha16 631f0d7e00d18062
（该值含本节之后为补登 PL-059 而做的一次追加，读时以「对 `CHANGELOG.md` 现取字节＋sha256 前 16」的配方复算为准）。

**② 九门禁终态全绿**（末次实跑，全部 exit 0）

| 门禁 | 读数 |
|---|---|
| `validate_governance.py` | 总评 PASS｜通过 237／失败 0／警告 1（沿旧的那条检查 3 核验日期 WARN），exit 0 |
| `verification_policy_test.py` | Ran 6 tests，OK |
| `ace_regression_test.py` | Ran 90 tests，OK |
| `sre_regression_test.py` | Ran 66 tests，OK｜缺口信号 0 条｜指纹 4f53cda18c2baa0c（连续一致轮次属机器本地浮数，不作判据） |
| `sr_ic07_compliance_test.py` | Ran 20 tests，OK |
| `pending_ledger_test.py` | Ran 43 tests，OK |
| `cross_layer_eol_test.py` | Ran 25 tests，OK |
| `index_ref_consistency_test.py` | Ran 26 tests，OK |
| `skill_cross_layer_test.py` | Ran 21 tests，OK |

通过数 237 未变＝本批零改校验器检查面，属定级时声明的 no-op（硬判据取「失败 0＋exit 0」）。

**③ sync 与只读 `--check`**

- 收尾 sync 写态：范围 72｜一致 71｜新增 0｜更新 1｜移除 0，唯一更新件＝机器本地 `prefab-standards-reviewer/sre_regression_report.json`（复跑 SRE 门禁即重写，故置于九门禁之后）。
- 末次只读 `--check`：范围 72｜一致 72｜新增 0｜更新 0｜移除 0，exit 0 ＝ 运行时净变化 0 的机算凭据。

**④ 发布门禁复扫终态**（`publish_export_gate.py --repo` 只读）

verdict FAIL｜scanned 235｜hits 9｜exempted 192｜untested 0。9 处命中构成＝`local_repo_path` 9、`local_user_path` **0**。
逐类仍红行数按 §二 末配方复算：`local_user_path` 由 `HEAD` 前像的 95 行降为 **0**；`local_repo_path` 9 行不变（本批不动，另登 PL-058）。
推送面根因未消＝`pre-push` 活钩子只跑该门禁、门禁 FAIL 即 exit 1，故 PL-044 保持待处置。

**⑤ 工作区健康度**（`_工具/workspace_health.py` 只读实跑，exit 1）

汇总 24 OK／5 WARN／1 FAIL。三项与本批相关者均为 WARN 而非 FAIL：
出口裁定台账「裁定非终态 2/11」含 `git:装配式装修技能开发(FAIL)`；发布回执链「3/3 缺失或失效」含本仓 `MISSING`（发起人明示暂不签）；
推送就绪预读本仓行＝`ahead 10／behind 0`，其「问题」项有两条＝`主通道不可达（层 1 窗口，等触发或换备通道）`
与`门禁裁定 FAIL（按设计拦截）`。那条唯一的 FAIL 不属发布面＝根目录白名单检测报三个未登记根目录
（`_archive-20260921-traces`／`_preimage-20260922-batchA`／`_tmp-scripts`），属根仓整理面、不在本批授权写面内。
② 收窄词表系以「身份词条保持armed」换取命中面收窄，本就不以把健康度读绿为目标。

**⑥ git 面**（只读，`git -c core.quotepath=false`）

`git status --short` 24 行＝20 件已跟踪修改（内容面 18 件中的 16 件＋三层治理件与 `同步说明.md`）＋4 件未跟踪新增；
`git diff --stat`＝20 files／+121／−102；`git rev-list --count origin/master..HEAD`＝**10**（原 brief 说的 9 个提交之后又落了 CG-20260922-002，故推送授权句须按 10 重发）。
本批未做 commit，工作区改动全部留在待提交态。

## 四、批内自纠与口径订正（如实登记，不抹）

1. **登记文本自身触发门禁词条**：首版 §九 本批行与 `记录/2026-09-20_CG-20260920-007…` 里那处省略号写法，
   都因「`Users` ＋斜杠 ＋省略号」被 `local_user_path` 词条读成用户名，带平后三层各 1 行＋本记录件 1 行仍报红。
   处置＝把该字面改写为「`file:///C:/Users` 段后直接接省略号」（斜杠不再紧跟 `Users`），
   改写前后逐字符仅差这一处、CR 计数不变，改后重跑带平＋九门禁＋复扫，终态即 §三 所列。
   教训＝脱敏批的登记面若照抄被脱敏的原字面，会把假阳重新写回真值面；引用占位形态时须让词条正则无从命中。
2. **首版登记脚本的插入次序缺陷（未落盘即拦下）**：三处插入若按锚点原索引升序执行，
   头部行的插入会把后两个锚点各下移一行，台账改写将落在 PL-043 上，而 `md_cells` 的「列数＝7」断言对此不敏感。
   处置＝改为降序落子＋每处插入后回读断言该行前缀（`| PL-058 |`／`| CG-20260922-003 |`）与
   `md_cells(...)[0]` 等于预期编号，并以「LF 增量＝新插行数」封顶。
3. **量级口径**：授权原句给「83 处」，本批现读为 `HEAD` 前像仍红 95 行／替换 99 段（一行可含多段），
   差额即 §二 第 ④ 项那条假阳与逐行/逐段口径差；登记面一律写现读值并注明配方，不沿用授权句的量级。
4. **未做项**：`local_repo_path` 9 处不在授权字面内，未改；PL-044 未闭合；新登 PL-058 承载该剩余前置；
   commit 与 push 均未做（本批授权只含本地写入）。
5. **收尾另发现两类残余，均采「登记不改面」**（发起人当场裁定「登记 PL-058／PL-059 为剩余前置」）：
   - 9 处 `local_repo_path`（含 `AGENTS.md:56,58,59`、`技能仓备份/prefab-governance-sync/SKILL.md:17,70`、
     `程序文件/cross_layer_eol_test.py:19,42`、`程序文件/skill_cross_layer_test.py:12,31`）→ **PL-058**。
     其中两处 `:42`／`:31` 是 `SCRATCH_ROOT` 功能常量，改即两门禁跑不动；故改字面属读数装饰而非消因。
   - 真实姓名以**非路径形态**存于 2 件 tracked 件（门禁词条零命中，靠词条扫不出）→ **PL-059**：
     `成果/第三方架构与内容体系评估_20260915.html:11`（页面署名位）与
     `文档/SRE确定性体检设计方案_v1.0.md:316`（表格「裁定」列值）。
     这条是本批真正的口径教训：`local_user_path` 词条只覆盖「绝对路径里的用户名」，
     脱敏批若只按词条命中面取工作范围，会系统性漏掉署名与表格里的姓名本体，
     故终态扫「0 处」不等于「姓名不再出现在发布面」。

