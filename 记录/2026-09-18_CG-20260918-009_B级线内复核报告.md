# CG-20260918-009（B 级）线内独立只读复核报告

- **复核日期**：2026-09-18（Git Bash）
- **复核对象**：变更批次 CG-20260918-009「`standards-index.md` §二『官方核验记录』表补登序号 68（GB 30981.1-2025）行 ＋ PL-003／PL-004 闭合 ＋ PL-007 解锁」
- **复核范围**：补行落位与表格形态、无标准数值/状态/替代关系被擅改、PL-003「T-B4／T-B7 首列序号守卫缺陷」历史陈述的现读代码真伪、收录总数不变、索引跨层带平、`change-governance.md` 本批登记内容自洽性
- **复核方式**：**只读 ＋ 内存内实跑**。全程未修改任何被测文件、未跑 `validate_governance.py`／各 `*_test.py`／`sync_skill_backup.py`；取证用 `sha256sum`／`git diff`／`git show HEAD:`／`python -B` 在内存内分别驱动改前（HEAD 前像）与改后（工作树）加载器比对两字典。唯一落盘动作＝发起人依本报告 次要观察 对登记面措辞所作的两处订正（见 §三）。
- **复核者独立性局限**：子代理由发起人编排、取证范围由任务书给定；代码取证以运行时 `sre_reasoner.py` 实读＋`git show` 前像内存内复算为据，非对运行时文件的写核验。

---

## 一、判据 1—6 逐项结论

### 判据 1 — §二 序号 68 补行落位且形态正确 — **PASS**

| 证据 | 实测 |
|---|---|
| 表归属 | `_专题_技能合集策划/standards-index.md:100` = `**官方核验记录**（已核验标准）：`；表头 `:102` = `序号\|标准编号\|核验日期\|来源\|核验结论\|影响条文说明`（**6 列**） |
| 新行 | `:115`，首列 `68`、第二列 `GB 30981.1-2025`、机算单元数 6（与表头/前一行一致），`:114` 为序号 67（GB 50303-2015）行、`:116` 表尾空行 |
| 各层同位置 | 运行时根、运行时 `shared`、`技能仓备份/standards-index.md`、`技能仓备份/shared/standards-index.md` 均命中同一行（行号一致） |

### 判据 2 — 无标准数值/状态/日期/替代关系被擅改 — **PASS**

- `git diff --numstat`：L1 `standards-index.md` **2 插入 / 0 删除**；`技能仓备份/standards-index.md` 2/0；`技能仓备份/shared/standards-index.md` 2/0；三份 `grep -c '^-[^-]'`＝**0**、`grep -c '^+[^+]'`＝**2** → 改动纯为新增、无既有行改写。
- 两条 `+` 行 = ①§二 核验记录 `:115`；②§八 变更日志新行（4 列表、无「状态」列亦无编号列 → 不参与任何载入）。
- **机算级复算（强于 diff）**：`git show HEAD:` 前像与工作树现像分别驱动运行时加载器 → `STANDARD_STATUS` 前后为**同一字典**（`pre == post` True、键对称差 ∅、值差 ∅），`INDEX_L1_IDS`（15 条）零变化。
- 新行内三个数据点（`2025-05-30`／`2026-06-01`／`全部代替 GB 18582-2020`）均可在主表序号 68 行与 §7.2 GB 18582-2020 过渡期行逐字命中 → 未引入任何新数值。

### 判据 3 — PL-003 守卫结论复核 — **PASS（历史陈述判定为「已过期」成立）**

代码证据（运行时 `sre_reasoner.py`，版本头 `:4` = **v1.7**；该文件 git 未修改，与 `技能仓备份/prefab-standards-reviewer/sre_reasoner.py` sha256 前 16 同为 `9d38750c04c07677` → 「代码零改动」成立）：

- `_classify_status_table` **:347–356**：表头命中 `_VERIFY_HEADER_KEYS`（`:313` = 核验日期/核验状态/核验结论）即返回 `None`。
- `_classify_verify_table` **:359–369**：含编号列（`_ID_COLS`，`:309` = 标准编号/图集编号）且含「核验日期」或「核验状态」→ `True`。
- `_load_standards_index` **:372–443**：verify 分支 **:398–419** 用 `header.index("标准编号")`（**:399**）取编号并在 **:419** `continue`，**永不进入状态分支**；状态分支编号列同为按列名定位（**:423**），**从不读首列「序号」**。`STANDARD_STATUS`（**:291**）键 = 编号（**:430/:434**）。
- 现算：`_classify_status_table([核验记录表头]) = None`；`_classify_verify_table([核验记录表头]) = True`；门禁侧 `validate_governance.py` 的 `classify_index_table(同一表头) = 'verify'`，且其 `STD_ID_SHAPE`（**:851–852** 前缀白名单）对 `"68"` **不匹配**。
- 字典实测（改前 `git show HEAD:` 内存内重载 vs 改后工作树）：

| 项 | 改前（HEAD） | 改后（工作树） |
|---|---|---|
| `STANDARD_STATUS` 条数 | 79 | 79（逐键逐值与改前全等） |
| `STANDARD_STATUS` 键集合含裸 `68` | 否（无任何纯数字键） | 否（同） |
| `STANDARD_VERIFY` 键数 | 26 | **27**；新增键集合 = `{GB 30981.1-2025}`，移除 = ∅ |
| `_verification_confirmed('GB 30981.1-2025')` | `(False, '…既有核验依据未确认…')` | `(True, '')`（today 2026-09-18，age 3 ≤ `REVIEW_DAYS_L1=92` → 在期成立） |

- 登记面所写「改前 `STANDARD_STATUS` 79 条 sha16 `a346a26c18777b9c`」**独立复算命中**：`sha256(json.dumps(STANDARD_STATUS, ensure_ascii=False, sort_keys=True))[:16] = a346a26c18777b9c`（改后同值，因字典未变）。
- 「历史陈述曾为真、现过期」有实物佐证：重构前副本 `skills/prefab-standards-reviewer/sre_reasoner.py_pre20260916CG003:254–265` 唯一守卫是 `re.match(r"^\d+$", parts[1])`（只看首列序号）后 `status = parts[6]` 并无条件覆盖主表状态 → 对该 6 列核验行 `parts[6]` 正是「影响条文说明」自由整句。故 CG-20260915-002 当时的判断成立，现属**过期**而非「当时就错」。

**结论**：「T-B4／T-B7 首列序号守卫缺陷未修、补行会注入错误状态载入」这一历史陈述已过期，守卫成立，补行安全。

### 判据 4 — 收录总数仍 69 条 — **PASS**

- `standards-index.md:11` 「收录标准总数：69 条…」在 diff 中无删除行 → 未改动。
- 独立计数（加载器 `main` 表分类口径遍历 §二—§六）：HEAD = 69 行 / 工作树 = 69 行，分段同值 `§二 15 + §三 24 + §四 5 + §5.1 6 + §5.2 6 + §5.3 9 + §六 4 = 69`；`INDEX_L1_IDS` 前后均 15。新增行落在 6 列核验记录表，不属主表计数区。

### 判据 5 — 索引跨层带平 — **PASS**（拓扑措辞可精确化）

`sha256(原始字节)` 前 16 位（复核时点五处**全等**，`77791f9b0a488ac0`，87,922 B）：

| 层 | 路径 |
|---|---|
| L1 开发源 | `_专题_技能合集策划/standards-index.md` |
| L2 运行时根 SOT | `~/.qoder/skills/standards-index.md` |
| L3 运行时 shared 镜像 | `~/.qoder/skills/shared/standards-index.md` |
| 仓内镜像 root 面 | `技能仓备份/standards-index.md` |
| 仓内镜像 shared 面 | `技能仓备份/shared/standards-index.md` |

拓扑说明：登记面「四层」措辞未含 `技能仓备份/standards-index.md`（root 镜像），该件确由 `sync_skill_backup.py:72–75` 的 `ROOT_FILES` 纳入同步范围，实测亦已带平 → 无缺口，仅措辞可精确化为「五处副本」。运行时根另存 `_pre*` 回退件，不属同步范围（脚本按声明集而非 glob），不计入带平面。

### 判据 6 — `change-governance.md` 本批登记内容自洽 — **PASS（L1 内容面）**

- §九 本批行在位（CG-20260918-009，日期 2026-09-18，级别 **B 级**）。
- §11.2 台账（表头 **7 列**，18 条数据行，列数全 = 7）：状态分布 **待处置 11／已闭合 6／已裁定不做 1**（较 -008 批 13／4 恰为本批 PL-003＋PL-004 两行迁移，自洽）。PL-003／PL-004 状态 `已闭合`、到期日 `—`、处置依据列非空；PL-007 仍 `待处置`、依据列明文「前置 PL-003 已随 CG-20260918-009 闭合…本项已解锁可另批执行」。
- 「键闭合真值源实读」写作 **92**，**独立复算成立**：按「§九 逐行取第一列 CG 编号」→ 工作树 92 个（无重复）、HEAD 91 个，差集恰 `{CG-20260918-009}`；台账→§九 方向悬空 CG 引用 = 无。

---

## 二、次要观察（复核提出，发起人已据此就地订正登记面）

复核在内容判据全 PASS 之外提出三处措辞面观察，其中 1、2 属本批自写登记文字的确切性问题，发起人已在带平前订正：

1. **守卫归因不确切 → 已订正**：`standards-index.md` §八 本批行与 `change-governance.md` §九 本批行初稿把「拦下裸序号」记在 `_VERIFY_ID_SHAPE`／`STD_ID_SHAPE`「二次守卫」名下。实测运行时 `_VERIFY_ID_SHAPE = ^[0-9A-Za-z].*\d`（`sre_reasoner.py:325`）对纯数字 `"68"` **匹配为 True**（它拦的是散文兜底单元、不拦纯数字），真正不产生裸 `68` 键的原因是「编号按列名取、加载器从不读首列序号」＋门禁侧 `STD_ID_SHAPE` 对 `"68"` 不匹配。两处措辞已改为该确切归因，并在 `change-governance.md` 头部保留的「裸序号被 `STD_ID_SHAPE` 拦下」（此即门禁侧，正确）不动。
2. **物理行号被本批插行推移 → 已订正**：§八 本批行初稿以插入前行号（`:345／:349／:350`、`§7.2 :312`、主表 `:92`）作指称，本批 §二 插行致其下移。已改按批次号（CG-20260915-002／-001／-004）与内容（主表序号 68 行、§7.2 GB 18582-2020 过渡期行）指称，与 CG-20260918-007 ②(e)「登记面不得以易漂移物理行号作唯一指称」纪律一致。
3. **时点措辞观察（非本批写面，登记不处置）**：`standards-index.md` 一处历史活文本仍含「本批不刷新任何核验日期」（前批遗留，本批确实未刷新既有日期，句仍真但属易陈旧时点措辞）。不属本批授权写面，未改。

---

## 三、总结论

**PASS**——内容判据 1—6 全部实测通过：补行落位正确（6 列、序号 67 后）、改动纯新增、运行时零代码改动、`STANDARD_STATUS` 逐条不变、`STANDARD_VERIFY` 仅增一键且无裸 `68`、`_verification_confirmed` 由「依据未确认」翻「已确认（在期 3 天）」、收录总数独立复算仍 69、索引各副本 sha256 前 16 互等、台账 PL-003／PL-004 已闭合且依据非空、PL-007 解锁在位、§九 有本批行且「实读 92」经独立复算为真。

复核提出两处放行条件（均属登记→带平正常时序、非实现缺陷）：① `change-governance.md` L1 末次订正（守卫归因＋行号指称）后须重新带平至运行时 shared／`技能仓备份/shared`，复算「三份 sha256 前 16 互等」；② 落地 `AGENTS.md` 41→40、`CHANGELOG.md` v1.12.24 与本复核报告，使登记面对其指称转为真。两条件由收尾时序（编辑→带平→六门禁→最后 sync）闭合。

**同意按 B 级继续**：带平后复跑六门禁（validate 硬判据失败 0＋exit 0、指纹 `4f53cda18c2baa0c` 不漂移、余门禁全 OK exit 0、sync 范围 122＋新增 0＋移除 0）须成立。提交与推送均须用户另行明文确认，本批复核不构成提交动作。
