# CG-20260924-001｜PL-062 湿区构造层序与工序整改执行记录

> 第二批实施批（B 级技术修订）。状态唯一来源＝L1 `change-governance.md` §11.2；本记录只承载证据指针与终态读数，不在别处复述 PL 状态。取证目录＝`D:\Qoder-Files\_整理与清理\2026-09-24\专业整改二批-PL062-CG-20260924-001\`（下称 `<EV>`）。

## 一、授权与定级

- 发起日期：2026-09-24；发起人：本仓发起人；执行方：本线 AI（主线程）。
- 授权原文：「请回读上序已完成事项，通读'专业整改任务总单_v1.md'及其他必要文件，基于项目当前真实状态及依赖关系，审慎评估、并推进后序工作。」＋（首次 Critic 子代理调用因连接掉线中断后）「请继续完成未完成的工作。」＋（订正失实记载、裁定补做独立复核）「按你建议执行A。同步更新'专业整改任务总单_v1.md'文档及其他你认为必要性文档…」
- 定级 **B 级技术修订**：修正 PL-062 缺陷本体（构造层序物理倒置、工序与闭水覆盖错序、预埋与末端混同），属文案／构造逻辑纠错与适用边界补充，按 `change-governance.md` §一、§二执行。**不升 S／A**（未改全局公式、规范限值、契约或红线注册表）；**不采 C**（非排版／路径／BOM 类）；级别不继承首批 CG-20260923-005 的 S 级。先例：CG-20260923-003（FL-F01 B 级局部整改）。
- **不提交、不推送**：授权面不含 commit／push；`.git/hooks/pre-push` 受 PL-044 阻断（exit 1），推送就绪预读亦裁 FAIL（按设计拦截）。提交态以 `git log`、推送态以 `git rev-list --left-right --count HEAD...origin/master` 为准。
- 中止条件：发现并发改动、同步方向不明、原像缺失，或整改必须改变数值本体（坡度／蓄水／水封／坑距等限值）、弱化红线 BK-R-P0-5、或扩面至他技能正文／全局治理件——任一触发即停该扩面另批。本批未触发。

## 二、变更请求与影响评估

- **写面（运行时 L2 `prefab-bathroom-kitchen-system/` 三件）**：
  - `reference.md` 16,097→18,661 B（改前 sha256 `2245df48dffe0fb6…`）
  - `SKILL.md` 26,333→26,566 B（改前 sha256 `1b36659e19abd086…`）
  - `examples.md` 7,919→8,136 B（改前 sha256 `547edeb6fe070ba5…`）
- **新建门禁**：`程序文件/wetzone_construction_guard_test.py`（18,103 B，**第十二门禁**），守护运行时 BK 三件防 PL-062 缺陷回归。
- **登记面**：L1 `change-governance.md`（header 最后更新行＋§九 CG-20260924-001 行＋§11.2 PL-062 转已闭合＋新登 PL-070）、`CHANGELOG.md`（v1.13.21）、`AGENTS.md`（第十二门禁行）、`文档/专业整改任务总单_v1.md`（§九 验收证据指针）、`_专题_技能合集策划/数据分类复核清单.json`（检查 10 残量登记）。
- **受影响技能**：仅 BK（prefab-bathroom-kitchen-system）。**未触**＝`redlines-registry`／`interface-contracts`／`standards-index`／`data-classification` 判据本体／`glossary`／他技能正文／既有十一道门禁判据。
- **同步方向**：运行时 BK 三件 → 仓内镜像；L1 治理登记 → 运行时 shared → 仓内镜像。
- **回滚依据**：覆盖前原像备份 `<EV>/原像/`（BK 三件＋`数据分类复核清单.json`）＋逐件 sha256 清单 `<EV>/before_hashes.txt`＋逐件 diff `<EV>/diff_{SKILL,reference,examples}.md.patch`。恢复前须比对本批终态，发现后续修改即停，不覆盖并发工作。
- **初始取证**：`<EV>/before_git_status.txt`；开工前 BK 运行时↔镜像字节全等、治理三方全等。

## 三、缺陷本体与修正判据（专业判断与取舍）

**缺陷本体**（登记证据见总单 §三 PL-062 行）：
1. `reference.md` 构造图把「结构楼板／架空地面」置于找坡层**之上**＝物理倒置；未区分墙面／地面及干湿工法。
2. 工序块先防水及闭水（第 5 步），再找坡（第 7 步），最后「地漏安装」（第 9 步）＝闭水早于找坡与地漏，**宣称验收尚未形成的接口**。
3. `:160` 另要求「地漏法兰与防水层密封」，与工序中地漏晚于闭水**自相矛盾**。
4. 末端安装篦子与预埋地漏本体／法兰**被混为同一工序**。

**五项闭合条件**（总单 §三 PL-062 行，逐条对应具文于 §11.2 处置依据列）：①分清被描述的实际系统；②每套层序对应施工顺序、排水路径和防水连续面；③预埋本体／法兰与末端附件分开；④闭水及必要复验覆盖实际防水节点，不把尚未形成的接口宣称已验；⑤无设计依据时改条件化示意而非虚构通用剖面。

**修正判据（守卫 R1—R4，结构判据非子串）**：
- R1 层序物理序：`FLOOR_ANCHORS=('饰面板/瓷砖','保护层','防水层','找坡层','结构楼板')` 以 `_layer_idx` 结构匹配取序（防子串假绿）；`_fenced_all` 取全部围栏块，故追加式注入不漏判。
- R2 工序覆盖序：embed＜wp＜bt＜prot＜fin＜grate（预埋→防水→闭水→保护→饰面→末端篦子）。
- R3 闭水晚于预埋。
- R4 预埋本体／法兰与末端附件分列＋节点行判据。
- 正向锚 P1—P6：含体系边界三字面、「不得宣称已验」、「仅适用 SMC 底盘一体体系」、「保护层与饰面施工前」、`### 5.2 …（独立防水层体系）`、**数值本体字面守卫**（撤掉 `坡度≥1%，淋浴区≥1.5%` 或 `蓄水≥20mm，≥24h` 即 P-MISS 复红）。

**数值本体零改（不做项自证，机器现读）**：
- 性能字面 12 项 ×3 件逐项计数相等、差异 **0**（坡度≥1%／≥1.5%／蓄水≥20mm／≥24h／水封≥50mm／≥I级／DN50／DN75／≥2%／坑距 300-400mm／2.5mm²）。
- 数值型 token 增量仅章节指针与工序步号（`2.1`＋1／`5.1`＋2／`5.2`＋3／`13`＋1／`14`＋1／`7`＋2）。
- 标准号 token 16／30／11 零差异（无新增臆造条款号面）。
- `SKILL.md` 红线体系区 6,742 B **字节全等**（BK-R-P0-5 未弱化）。
- **本节只证「零改」，不证「数值本体正确」——限值效力与经验坡度口径属 PL-063，不在本批。**

## 四、复核（独立 Critic＋Evaluator，据实订正后补做）

- **复核独立性订正（据实）**：本批按验收等级 L2 采独立只读 Critic／Evaluator 子代理复核（如 CG-20260923-003 先例）。**首次 Critic 子代理调用因连接掉线中断**（工具调用未完成，非发起人拒绝）。原登记面误将此中断记载为「发起人拒绝子代理调用」并据此声明「复核降级为主线程线内执行、独立背书弱于 CG-20260923-005」——**该记载失实，本批据实订正**：发起人 2026-09-24 当场纠正「我没有拒绝 Critic 子代理，我只是掉线了，打断了你的执行动作」；原记载违反「分析用户是否执行某操作须依客观证据、不得主观猜测」纪律，系执行方把工具调用中断主观推断为用户拒绝所致。
- **补做独立复核（方案 A，发起人裁定）**：经发起人裁定按 L2 标准补做独立只读复核，Critic 与 Evaluator 两子代理彼此独立（Evaluator 不读 Critic 结论）、均未改文件／未提交／未推送，结论逐字存档于 `<EV>/2026-09-24_独立复核Critic报告.md` 与 `<EV>/2026-09-24_独立复核Evaluator报告.md`。
  - **Critic 判定＝PASS·必改项无**：5 条闭合条件逐条满足（附行号原文摘录）；数值本体零改经正则多重集差分独立复现（三件 REMOVED=NONE、ADDED 仅章节指针／工序步号）；红线区 6,742 B 字节全等（sha256 `c8b03999f9e77268…`，BK-R-P0-5 未弱化）；守卫非恒真（内存内直调：改前原像触发 R1—R4＋P-MISS、改后 `[]`）；BK↔FL 跨技能无矛盾。唯一非阻断观察＝reference.md §C4 小节号 3.1／3.2／3.3 与 §C3 重号，系改前既存、非本批引入、属另线（本批不扩面处置）。
  - **Evaluator 判定＝A—E 全达标·必返工缺陷无·整体验收不放行**：独立复现守卫「改前 12 findings／改后 `[]`」；十一道测试门禁＋validate 全绿；workspace_health 唯一 FAIL＝根目录白名单三目录（命名日期 09-21／09-22，前批既有、非本批 09-24 引入）；据门禁纪律「FAIL 在位即不放行整体验收」，判**局部成功标准满足 ≠ 整体验收放行**，本批属前者。
- **机器自证五项**（均 exit 0／可复跑，与独立复核互为客观佐证）：①性能字面对齐 12×3 差异 0；②数值 token 增量仅章节指针／步号；③标准号 token 16／30／11 零差异；④红线区 6,742 B 字节全等；⑤第十二门禁 clean 态 exit 0、`--runtime-dir <EV>/原像` 改前态 **exit 1／failures=2**（`test_real_runtime_files_pass`＋`test_copy_is_clean`）。
- **复核结论**：独立 Critic＋Evaluator 均判本批技术面成立、无必改／必返工缺陷；同型面跨技能扫描 0 缺陷（不扩面、不代改他技能）。**整体验收不放行**（workspace_health FAIL＝前批既有根目录白名单，见 §六）；本批不声明整体验收完成或真实工程性能合格，PL-062 闭合系技术面闭合。

## 五、范围与状态

- **PL-062 → 已闭合**：§三五项闭合条件逐条满足＋第十二门禁 clean exit 0／改前原像 exit 1 反向自证＋数值本体零改机器自证，均已落地；状态唯一来源＝§11.2，该处已由待处置转已闭合、处置依据已具文。
- **PL-070 新登待处置**（2026-09-24→2026-12-23，来源 CG-20260924-001）：检查 10 残量登记中 5 个单元脱离 baseline，其中 4 个有来源列面（`reference.md :49—:55` 与 `:310—:317`，仍属 PL-060 存量），1 个（`reference.md:182`「周围50mm范围加强」，D 类）**无来源列面**→结构性 `MISSING_SOURCE` 约束，无法在本批登记为 A／D。处置路径：①补来源列另批，或②复核判定为非量化工艺描述。
- **不扩面**：PL-060 不开展整体迁移（只处理本批直接编辑导致失效的分类单元）；PL-063 数值验证不在本批；历史策划稿、历史 CG 行、旧报告均不改写；全局治理激活台账不在授权写面。
- **不提交、不推送**。

## 六、运行证据与终态

**十二门禁全 exit 0**（日志 `<EV>/门禁日志/*.log`）：

| # | 门禁 | 读数 |
|---|---|---|
| 1 | `validate_governance.py` | 总评 **PASS｜通过 243｜失败 0｜警告 3**（3 警告＝检查 3「无有效核验日期」聚合 1＋检查 10 `A_VERIFY_DATE` 2 条于 `acoustic-calculation-engine/reference.md:708/:709`，**均既有、非本批**） |
| 2 | `verification_policy_test.py` | Ran 6，OK |
| 3 | `ace_regression_test.py` | Ran 105，OK |
| 4 | `sre_regression_test.py` | Ran 66，OK；缺口 0｜指纹 `4f53cda18c2baa0c`｜连续一致 239 轮 |
| 5 | `sr_ic07_compliance_test.py` | Ran 20，OK |
| 6 | `pending_ledger_test.py` | Ran 43，OK |
| 7 | `cross_layer_eol_test.py` | Ran 25，OK |
| 8 | `index_ref_consistency_test.py` | Ran 26，OK |
| 9 | `skill_cross_layer_test.py` | Ran 21，OK |
| 10 | `data_class_consistency_test.py` | Ran 70，OK |
| 11 | `storage_anchor_guard_test.py` | Ran 19，OK |
| 12 | **`wetzone_construction_guard_test.py`（新）** | **Ran 15，OK** |

- **第十二门禁反向自证**：`--runtime-dir <EV>/原像` → **exit 1／FAILED (failures=2)**＝`test_real_runtime_files_pass`＋`test_copy_is_clean`（改前态复红，证取数接线非空跑）。
- **检查 10 残量登记**：`records` 8,465→8,483（−7 stale，＋25 new 均 N 类附逐单元理由），`baseline` 3,898→**3,893**（validator 自证「冻结存量 3,893 条／46 件」），`records＋baseline＝12,376＝units_total`、未判定 0，棘轮判据成立。**披露**：baseline −5 系本批直接编辑使 5 单元脱离冻结存量的**机械后果，非迁移进展**；不以批量默认 N 骗绿（25 新单元逐条具文 N 类理由，并明示 N≠「无需证据」）。
- **同步终态**：收尾 `sync_skill_backup.py`（写）范围 73｜一致 72｜**更新 1**（仅机器本地 `prefab-standards-reviewer/sre_regression_report.json`）｜移除 0；只读 `--check` 范围 73｜一致 73｜新增 0｜更新 0｜移除 0。
- **三层哈希**：`change-governance.md` L1／L2（运行时 shared）／L3（仓内镜像）三方 sha256 全等 `47c8e4a8e7680628…`，bytes 941,241，**CR＝0（纯 LF）**，LF 556；HEAD 前像 `f7129e23fa4e2dce…` bytes 896,392 LF 534——**行尾形态与 HEAD 一致（均纯 LF）**，内容 +22 行系本批登记编辑（未提交）。
- **总单 §九 路径修复（本批自查发现并修净）**：追加 §九 时经 Python 非 raw 串写入，`\2026` 被解释为八进制转义 `\202`（chr 0x82）＋`6`，致取证目录路径损坏 1 处。收尾自查全文控制字符扫描发现，二进制安全替换（唯一锚命中数＝1 断言＋后验 CR＝0／U+0082＝0／字节 +2），41,172→41,174 B；修复后 `validate`＋`data_class` 复跑仍绿（总单不在数据分类扫描面）。其余三件登记面（CHANGELOG／AGENTS／change-governance）同法扫描控制字符 0、CR 0。
- **工作区健康检查（`定期体检.py`，后台运行 exit 0，verdict FAIL）**：`overall_status=FAIL`、`can_proceed=false`、**31 项／24 OK／6 WARN／1 FAIL**（报告 `<EV>/门禁日志/workspace_health.log`＋`_整理与清理/2026-09-24/体检报告/`）。
  - **1 FAIL**＝根目录白名单：既有未登记目录 `_archive-20260921-traces`、`_preimage-20260922-batchA`、`_tmp-scripts`——**前批既有、本批未创建或调整、未删目录／未改白名单绕过**。
  - **6 WARN**：①文件命名格式（`视频制作工作流` 过程件，他项目）②台账登记纪律（2026-09-21 行长 390＞200，既有）③出口裁定台账（git:装配式 FAIL、api:tts UNTESTED）④发布回执链（git:装配式 MISSING）⑤推送就绪预读（装配式 ahead 14／behind 0、主通道层 1 窗口、门禁 FAIL 按设计）⑥**模块膨胀监控（装配式 脚本 +3／交付物 +0）←本批新增第 6 项 WARN**。
  - **第 6 项 WARN 定性与判据**：系本批新增门禁脚本 `wetzone_construction_guard_test.py` 触发的工作区级 heuristic 代理信号（空转信号 3）。**判断：非真实空转**——该脚本服务于可运行交付物（第十二门禁，守护 BK 运行时三件防 PL-062 缺陷回归，已接入 `AGENTS.md` 门禁表并随十二门禁实跑 exit 0）；工具「交付物 +0」是因其 taxonomy 未把守卫测试计为交付物，属代理指标假信号。**不冻结、不删除该脚本以求消警**（消警须改工具口径，不在本批授权面）。
- **整体状态：已运行验证失败（工作区健康门禁阻断）**。本批 BK 湿区构造与工序的**局部技术检查通过**（十二门禁＋第十二门禁反向自证＋数值本体零改自证＋独立 Critic／Evaluator 复核 PASS），但**不声明整体验收完成或真实工程性能合格**；PL-062 闭合系技术面闭合，不含数值本体正确性背书（属 PL-063）。
- **验收覆盖（四计数）**：总计 **13 项机器验收**（十二门禁＋工作区健康），已执行 **13**／跳过 **0**／覆盖率 **100%**；通过 **12**／失败 **1**（工作区健康）。独立复核 2 项（Critic＋Evaluator）已执行并判 PASS／达标（证据存档于 `<EV>`，见 §四），**不混入机器验收计数**。
- **写面核对清单**：运行时 BK 三件（reference／SKILL／examples）；本仓 `程序文件/wetzone_construction_guard_test.py`（新）、L1 `change-governance.md`、`CHANGELOG.md`、`AGENTS.md`、`文档/专业整改任务总单_v1.md`、`_专题_技能合集策划/数据分类复核清单.json`、本执行记录；仓内 BK 镜像三件、治理镜像、`同步说明.md`。均属授权内直接整改或必要登记／生成件；另有机器本地 `sre_regression_report.json` 两层产物，不计业务写面。
- **本地改动保留，未暂存、未提交、未推送。**
## 七、订正附录（据实订正＋补做复核后的终态自证）

- **订正缘起**：本批首次 Critic 子代理调用因**连接掉线中断**（工具调用未完成），执行方主观推断为「发起人拒绝子代理调用」，并据以在四处登记面写下「复核降级为主线程线内执行、独立背书弱于 CG-20260923-005」——该记载**失实**。发起人 2026-09-24 当场纠正：「我没有拒绝 Critic 子代理，我只是掉线了，打断了你的执行动作。」此违反「分析用户是否执行某操作须依客观证据、不得主观猜测」纪律。**工具调用中断／掉线 ≠ 用户拒绝。**
- **补做独立复核（方案 A，发起人裁定）**：按 L2 标准补做独立只读 Critic＋Evaluator 复核，两子代理彼此独立（Evaluator 不读 Critic 结论），均未改文件／未提交／未推送。逐字结论存档 `<EV>/2026-09-24_独立复核Critic报告.md`（Critic＝PASS·必改项无，红线区 6,742 B sha256 `c8b03999f9e77268…` 全等，数值本体正则多重集差分独立复现零改）与 `<EV>/2026-09-24_独立复核Evaluator报告.md`（Evaluator＝A—E 全达标·必返工缺陷无·整体验收不放行，独立复现守卫改前 12 findings／改后 `[]`）。详见 §四。
- **四处失实表述订正清单**（均二进制安全替换、唯一锚命中数＝1 断言、后验 CR＝0）：①L1 `change-governance.md`（头部「最后更新」行＋§九 CG 行列4／列6＋§11.2 PL-062 处置依据 (a)）；②`CHANGELOG.md`（v1.13.21 ⑥）；③`文档/专业整改任务总单_v1.md`（PL-062 三处行）；④本执行记录（§一授权链、§四整节据实重写、§六复核结论行、本附录）。订正后 `change-governance.md` 三层 sha256 全等 `47c8e4a8e7680628…`、bytes 941,241、CR 0、LF 556（HEAD 前像 `f7129e23fa4e2dce…`／896,392 B／LF 534 不变，+22 行系本批登记编辑）。
- **检查 10 残量重登记（适配订正后单元）**：订正编辑使 `change-governance.md` 在册单元 id 变化，同批重登记 3 removed／3 added（1:1 替换，附逐单元理由，无溢出——scope guard 证未波及其他件）。终态：`数据分类复核清单.json` sha256 `6860ddaec8c6a379…`、bytes 4,194,222、CR 0，`records` 8,483、`baseline` 3,893，records＋baseline＝12,376＝units_total、未判定 0，棘轮判据成立。
- **订正后门禁终态（实跑复现，非引用既有报告）**：validate_governance **PASS 243／失败 0／警告 3** exit 0；data_class_consistency **Ran 70，OK** exit 0；十二门禁其余十道（verification 6／ace 105／sre 66 指纹 `4f53cda18c2baa0c`／ic07 20／pending 43／eol 25／indexref 26／skillcross 21／anchor 19／wetzone 15）均 OK；`sync_skill_backup.py --check` 范围 73／一致 73／新增 0／移除 0。三层哈希全等。git 未提交（ahead 14／behind 0）。
- **整体验收仍不放行**：唯一阻断＝workspace_health 根目录白名单 FAIL（`_archive-20260921-traces`／`_preimage-20260922-batchA`／`_tmp-scripts` 三目录，命名日期 09-21／09-22，**前批既有、非本批 09-24 引入**）；本批不删目录、不改白名单绕过。PL-062 闭合系**技术面闭合**，不含数值本体正确性背书（属 PL-063）。
- **_tmp-scripts 清理边界**：仅清理本批（09-24）新建的空 scratch 目录；前批留存的 1,042 项与 `_tmp-scripts` 本体**保留不动**（本体属既有白名单 FAIL 面，删除即绕过门禁，不做）。
- **本地改动保留，未暂存、未提交、未推送。**
