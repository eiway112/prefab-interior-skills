# CG-20260925-002 专业整改九批-组8 M7 校准外推整改执行记录

- 日期：2026-09-25
- 变更编号／级别：CG-20260925-002／**B 级技术修订**（定级依据＝`change-governance.md` §一 B 级「修正错误、不影响安全性」直接命中；不继承登记批 CG-20260923-004 的 C 级任务定义，按总单§五「级别不继承登记批」）
- 授权：发起人 2026-09-25 明文「开『组8 M7 批（PL-067）』并按 B 级走 §二 流程……全程内部复算闭合，不等外部通道」
- 承接：总单 §二 组8 与 §三 PL-067；台账 PL-067 本批转已闭合
- 取证目录：`D:\Qoder-Files\_整理与清理\2026-09-25\专业整改九批-组8M7-CG-20260925-002\`

## 一、写面清单（运行时 2 件＋守卫 1 件，均纯 LF）

| 件 | 改前 | 改后 | 净变化 |
|---|---|---|---|
| `acoustic-calculation-engine/reference.md` | 45,653 B `829a3b75…` LF723 | 47,364 B `a8458c0b…` LF728 | +1,711 B／+5 LF |
| `acoustic-calculation-engine/SKILL.md` | 11,448 B `90960e45…` LF199 | 11,991 B `92566ffc…` LF199 | +543 B／LF 不变 |
| `程序文件/ace_regression_test.py` | v1.7.0／Ran 147 | v1.8.0／Ran 159 | 新增 `TestM7CalibrationExtrapolation` 12 例 |

- 改前原像＝取证目录 `原像-runtime/` 两件＋`before_hashes.txt`（与本表逐值全等自证）；**未新建 `_pre` 备份**（避免增 PL-042 类残留面），回退凭据＝原像 sha256 留存＋`git show HEAD:` 前像。
- `prefab-partition-wall-solution` 及其他技能正文零改；`shared/` 契约／红线／索引／分类判据本体零改。

## 二、逐 PL 写面明细（PL-067 闭合条件五条逐条处置）

① **所有区间输出入口按数值排序**（覆盖减重／增重／等质量与适用边界）：
- reference.md Step 3（:357 块）：对称变化行撤位名式「同时输出区间 [ΔR_low, ΔR_high]」，改「端点一律按数值升序，即 Step 2 的 [min(ΔR_low, ΔR_high), max(ΔR_low, ΔR_high)]，不得按位名写作 [ΔR_low, ΔR_high]（r<1 时该序为降序，见端点判据条）」，并披露「中值＝两界算术平均，与端点书写顺序无关（r<1 复算：中值 (−12.04−6.02)/2＝−9.03 dB）」；
- Step 5（:378 块）：输出区间改 [Rw_base＋ΔR_下界＋Σ修正, Rw_base＋ΔR_上界＋Σ修正]，ΔR_下界／上界＝Step 2「输出区间（恒升序）」的 min／max，加同一常数与同一 Σ修正后区间保序；
- SKILL.md :176 方法概要块补恒升序声明（[min(ΔRw_low, ΔRw_high), max(ΔRw_low, ΔRw_high)]，r<1 时不得按位名写作 [low, high]）；
- 三态覆盖复算：r=0.5 位名序 [−6.02, −12.04] 系降序（减重侧即本条修正对象）、r=2 两序一致（增重侧）、r=1 退化 [0, 0]；适用边界 r∈[0.5, 2] 行与「r 超出 [0.5, 2] 属大幅外推降级仅趋势判断」行字节未改。
- **控制锚零改**＝Step 2「输出区间（恒升序）」行与 :353 端点判据条（组6 前先批已修）、算例 :431 已按 min/max 升序印 [−3.0, −1.5]（复算 [−2.96, −1.48] 吻合，判非本批缺陷、不改）。

② **差 > 5 dB 不得自动优先 M7**：reference.md :387「交叉核对要求」撤「超阈即优先报 M7」，改「两者差 > 5 dB 不构成自动择优规则」＋四类根因顺序核查（①构造同族／②机理级变化（连接方式、叶片数）／③参数与口径（面密度计量口径、r 域、M4 表适用域）／④模型条件（评价量同法、基准测量标准版本、输入完整性））；SKILL.md :189 交叉核对段同步转述。

③ **无充分证据时降级而不是选一个数**：:387／SKILL :189 均规定「任一项存疑或差异无法归因时，不单独报任一数值，输出区间/趋势并标注『交叉核对未通过』及原因」；并显式新增**禁止的推断**条款「以基准数据可信推定变体外推可信——锚点可信度不随差值大小传递到外推结果」。

④ **路线措辞联动（非新增语义）**：SKILL.md :150／reference.md :521 路由块 :529 行／:549 组合表行统一「路线优先≠结果自动择优」（:549 系独立复核 R-01 指出初稿具文与写面不符后订正入写面清单）；「M7 应优先」旧肯定式全件零残留。

⑤ **精度与来源不代结**：精度声明（:400／:402）与基准来源留痕（:440）未触及＝PL-055／PL-068 范围，本批零越面；Σ修正为区间时的端点配对法残面候选登记留组9 裁定（复核建议 S1）。

## 三、守卫与反向自证

- `程序文件/ace_regression_test.py` v1.7.0→v1.8.0，**147→159**：新增 `TestM7CalibrationExtrapolation` 12 例＝R1 Step2 恒升序控制锚在场、R2 Step3 位名式复红＋「端点一律按数值升序」／−9.03 复算锚、R3 Step5 组装式锚、R4 交叉核对四类核查锚＋「优先报 M7」计数守卫（reference.md 恒 0／SKILL.md 恒 1 且唯一命中须在「不得直接」否定语境）、R5 路线措辞三处＋「M7 应优先」零残留、R6 SKILL 恒升序声明撤除复红；5 常态＋1 数值性质（三态排序＋中值恒等＋:431 印值控制锚，与文档字面解耦的独立复算）＋4 负向注入＋1 良性控制例（＝登记行所述「预复绿半条」并入良性不误伤组）。
- clean 态：`Ran 159 OK` exit 0（`门禁日志/ace_regression_clean_v1.8.0.log`，收尾复跑 `ace_regression_final.log` 同读数）。
- 反向注入：`ACE_SKILL_DIR`＋`PW_SKILL_DIR` 指向 `原像-runtime/` 复注入 **FAILED (failures=10)／exit 1**，十项全部落在新守卫组、既有 147 例零波及（`门禁日志/ace_preimage_inject.log`，注入日志内嵌原像字节与 `原像-runtime/` 全等，复核 f 项逐字符验证）＝守卫非恒真自证。
- ACE 原有用例与参考函数逐字不变。

## 四、SRE 独立复核（prefab-standards-reviewer 视角只读子代理，B 级审批要件）

- **总体判定＝PASS with warnings**（报告＝取证目录 `独立复核报告_SRE.md`）：技术写面 a—g 全项 PASS——a 数学独立验算逐值吻合（−6.0206／−12.0412／−9.03／[−3.0,−1.5] 与复算 [−2.96,−1.48]）、b Step3/Step5 保序自洽（另 2000 例随机验证违例 0）、c 闭合条件五条逐条命中且无 PL-055/068 代结、d 旧肯定式四类字面 0 命中（2 处否定语境受计数钉死；全 29 运行时技能目录扫描无第三写面）、f 守卫无恒真、g「保守性方向随 r 而反」与位名条同向无矛盾。
- **必改 1 条 R-01＝具文面非写面**：`变更请求与影响评估.md` 把 reference.md 改前 :544（改后 :549）组合表行误列不做项「判合规不改」而实际该行已改措辞。处置＝按建议甲同批订正具文（§一 涉及文件补行、§三 W7 行补记、§四 移入写面并披露订正事实，见该件 §十），技术写面与守卫零改。
- 建议项 7 条非阻断处置：
  - S1（Σ修正区间端点配对法＋:431 叠加上界式在 r>1 侧会降序）＝登记 M7 口径面残面候选，留组9（PL-068 参数与证据批）一并裁定，本批不扩面；
  - S2（SKILL :179—180「下限／上限」位名标签可读性）＝与 :181 恒升序声明并列的次级措辞面，登记留后续批；
  - S3（−9.03 示例取 r=0.5 域边界）＝此处在本记录披露：该值系适用域 [0.5, 2] 含端点内取值（与失效条件「超出」口径及守卫 `m7_is_valid_extrapolation` 一致），非越域给确定区间；
  - S4（控制例 `replace(...,0)` 字面 no-op）＝语义仍为良性编辑断言、且该组另有 R1—R6 真实断言与注入复红支撑，无恒真风险，不改；
  - S5（文档式子系数与测试 helper 耦合仅两处字面）＝后续批可增强，本批不阻断；
  - S6（登记面与镜像未跑）＝本记录§五—§七即其执行，同批闭合；
  - S7（门禁日志形制无命令/退出码头）＝已按建议落 `门禁日志/gate_results.json`（命令＋exit code＋日志 sha256＋关键读数）。

## 五、治理登记面（L1→运行时→镜像三层）

- `change-governance.md`：头部快照轮换（CG-20260925-001 行降级「此前更新（历史快照）」、新最后更新行）＋§九 CG-20260925-002 行（137→138）＋§11.2 PL-067 行待处置→已闭合（原处置依据以「原各行处置依据保留如下：」前缀保留）。
- 编辑面备份＝取证目录 `change-governance_登记前备份.md`（sha `548ab3c3…`）；改后＝1,010,413 B、纯 LF 578 行、sha256 前 16 `82a72a3d2945b99b`，写入脚本 `登记治理面.py`（唯一锚＋命中数断言）。
- 机算复核：§九 CG 行 138；台账 72 行＝待处置 22／已闭合 49／已裁定不做 1（22＋49＋1＝72；PL-067 转已闭合）。
- 三层带平：L1→运行时 shared→`技能仓备份/shared/` 三副本哈希全等；`sync_skill_backup.py` 首跑「范围 73｜更新 3｜新增 0｜移除 0」，更新件恰为本批三写面（ACE reference.md／SKILL.md／shared/change-governance.md）。

## 六、检查 10 残量登记（第十六批）

- 载体＝`_专题_技能合集策划/数据分类复核清单.json`；登记前备份＝取证目录 `登记与残量/数据分类复核清单_登记前备份.json`；登记脚本＝`第十六批_登记残量组8M7.py`（探针 `探针_单元差异.py`＋`转储_新单元.txt` 先行为凭）。
- 差异与校验器 `STALE_*` 读数逐键集合全等：摘失效 records **11**（SKILL.md 2／ACE reference.md 6／change-governance.md 3）＋baseline **1**（SKILL.md `6be5f769…:1`，被编辑的冻结存量按 `STALE_BASELINE`「从 baseline 移除并转为复核记录」处置、其改后单元以新 id 具文登记，非重冻）。
- 新登 **15** 条逐单元具文理由（不批量默认 N）：SKILL :136 N（流程块转述指针）／:176 **C＋N＋N**（20·lg／40·lg 界限式 C 绑 r 定义与两界限行、source_spans 块内字面，复算取证 §一；升序纪律 N；组装式定义 N）／:189 **N＋D＋N**（>5 dB 阈值沿旧键 D 档口径，绑「> 5 dB」字面；四类核查方法指令 N）；ACE ref :357 **N＋C＋N**（升序书写纪律 N；中值复算 −9.03 C 绑 '(−12.04−6.02)/2＝−9.03'；单侧 20·lg 与 r 超域降级 N）／:378 **N＋C**（定义式 N；区间保序性质 C 绑「加同一常数与同一 Σ修正后区间保序」）／:387 **N＋D＋N**（阈值 D；流程声明与方法指令 N）／:438 **D＋C＋N＋N**（沿旧失效键三段 D／C／N 连续登记＋本批新写路线纪律句 N）／:521 N／:549 N（路由行，沿旧 #d8390a19 口径）；governance :7／:9／:11 头部快照 N＋:452 §九 行 8 段全 N（CG_RULES 列规则）＋:472 引用续行 N＋:558 PL-067 行 8 段全 N（该行为台账行形制无结尾竖线，末列按旧登记形制识别，列数判定分支同组6 台账先例）。
- 终态：records 8,618→**8,622**／baseline 3,815→**3,814**（version 2）。**棘轮披露**＝baseline 减量系写面机械后果、非迁移进展。
- 复跑：`validate_governance.py --data-class-only` 总评 **PASS／失败 0／警告 2**（＝既有 A_VERIFY_DATE，不填推定日期）。

## 七、门禁终态（十二门禁全绿＋workspace_health 如实记录）

- `validate_governance.py` 全量：总评 **PASS｜通过 243／失败 0／警告 3／exit 0**（警告构成与基线同＝检查 3 聚合 1＋A_VERIFY_DATE 2，行号由 :715/:716 漂至 :720/:721 系 ACE 增长机械下移、条数内容不变）。
- 十二门禁全 exit 0：verification_policy **6**／ace_regression **159** OK（v1.8.0）／sre_regression **66** OK（缺口 0／指纹 `4f53cda18c2baa0c` 连续一致 254 轮）／sr_ic07 **20**／pending_ledger **43**／cross_layer_eol **25**／index_ref **26**／skill_cross_layer **21**／data_class **70**／storage_anchor **19**／wetzone **15**（日志＝取证目录 `门禁日志/`，命令＋退出码＋日志哈希汇总＝`gate_results.json`）。
- `workspace_health.py`（只读实跑）：**exit 0／26 OK／4 WARN／0 FAIL**。组6 时点的唯一 FAIL「装配式装修Web开发: 缺 C1/C2」已消（该仓 C1 补齐后按适应性豁免转 OK，非本批动作）；4 WARN＝①视频制作工作流过程文档缺日期前缀 1 份（本工作区外既有）②台账行长超口径 3 处（存量 51/59 行待消化，本批未新增超标行——登记行行长守 §11.1 口径系登记治理面脚本内核对后落笔）③出口裁定 `api:tts-fishaudio` UNTESTED（他线既有）④发布回执链 `git:装配式装修技能开发 STALE_CONTENT`＝本批写面未提交未重签的机械后果，发布须发起人明文另签。
- 收尾复跑 `sync_skill_backup.py --check`：读数见 `gate_results.json`（预期更新件仅机器本地 `sre_regression_report.json` 或全一致）。

## 八、顺带观测（按快照纪律不改写他批登记，只在此具文）

1. A_VERIFY_DATE 警告行号随 ACE 增长机械下移（:715/:716→:720/:721），判定级与条数不变，不填推定日期，取真须走 T-5 官方核验另线——判正常，无需动作。
2. AGENTS.md ace 门禁行 147→159 随批更新；AGENTS.md 不入检查 6／8／9／10 扫描面，不触发残量登记——判正常。
3. 台账行「无结尾竖线」形制（PL-067 行沿旧行形制）被检查 7 `md_cells` 正常判定（行列数 7 全等），残量登记脚本按旧登记先例适配——判正常，非数据错误。
4. 复核 S7 所提两件旧日志（clean／inject）无命令与退出码头，已按建议以 `gate_results.json` 补命令面凭据，旧日志本体不改（快照纪律）。

## 九、状态唯一来源声明

- PL-067 状态唯一来源＝L1 `change-governance.md` §11.2（本批已登记转已闭合），总单与本记录不改状态、只登证据指针（总单§十四）。
- git 提交与推送授权以发起人指令为准：本批提交含于「开组8 M7 批并走 §二 流程」既有授权线（组6 先例同口径）；**推送须另发明文**，提交态以 `git log`、推送态以 `git rev-list --left-right --count HEAD...origin/master` 现读为准，不据本记录推定。
