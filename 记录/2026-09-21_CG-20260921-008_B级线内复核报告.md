# CG-20260921-008 B 级线内独立复算报告（冻结基准升 v3 与留痕残留归档批）

> **一句话定位**：本批（新建 `行为与专业证据层冻结基准_v3.md`／§二 硬软分档承 PL-040／§4.3 盲评规程重写承 PL-041／PL-043 真唯一残留归档不删／新登 PL-044 发布出口裁定链锁死与推送阻断）的自述结论，交一个**线内只读子代理**独立复算。首轮 13 项声明逐条实跑取证，结论 **PASS-with-OBS**（11 真／2 项判"假"与"部分真"——两项均落在**主代理复核指令的措辞**上、载体登记面实测无误）。本报告**不抹首轮结论**，处置与口径差额单列 §二，端到端终态在 §三。
>
> **代理侧约束（指令原文摘）**：严格只读（禁写／禁改／禁 commit／禁 sync 写态，临时件只准落 `D:\Qoder-Files\_tmp-scripts\review\`）；不得相信主代理任何数字，一切以其现算为准；每条给「真／部分真／假」＋可核证据。
>
> **本报告的判据地位**：与冻结基准 §一 第 5 条同口径——复核为真只证明「登记面自述可复算」，**不构成 PL-031 的行为证据面**。本批尤其如此：v3 是**判据载体的修订**，不是行为测量；PL-031 的闭合仍须外部输入（盲评责任人、ACE 实测、异机资源），故本批零项推进 PL-031。

---

## 一、首轮复算结果（13 项，原样入库）

| # | 主代理声明（缩写） | 代理实测 | 判定 |
|---|---|---|---|
| A1 | v3 件度量 44,376 B／CR 0／LF 223／sha256 `5204f37d…` | 四项全中 | **真** |
| A2 | （指令写作）"v2 度量 27,028 B／`d2fba820…`，本批零改写" | v2 工作区与 HEAD(`08069b0`) 均实测 **30,622 B／`0d52490ccaa9943a`**；`27,028`／`d2fba820…` 系 **v1** 值。"本批零改写"半句成立（`git status` 中 v2 无修改、`git cat-file blob 08069b0:…_v2.md` 与工作区字节全等） | **假（仅指令侧）**——载体登记面见 §二 处置① |
| A3 | v3 含使用者名的路径字面 0 处（PL-030 (c) 合规面） | v3＝0、v2＝0 | **真** |
| A4 | 四段切片两版全等：§一 1—5 条 `19599c49…`/1,284 B、§三 `9f9a4c7f…`/10,058 B、§4.1+4.2 `27a8aa63…`/2,614 B、§4.4 `44cd4b71…`/345 B（指令把配方转述为"一律 rstrip／取到分隔行之前"） | 四值**两版互等都成立**，但代理按**指令转述**的配方只能复现 §一 一项：§三 须**含** `\n---\n` 行（否则 10,052／`1957f1b2`）、§4.1+4.2 与 §4.4 须**不 rstrip**（否则 2,613／344）、v3 侧 §一 须截到第 6 条首行 `6. ` 之前（否则多吞第 6 条得 2,004／`7639b86f`）→ 判"配方未钉死" | **部分真（仅指令侧）**——见 §二 处置② |
| A5 | H-1 内容包聚合哈希：`08069b0`→`413ab919…`／71 件，同配方换 `7506107`→v2 登记值 `7f80ab70…`／71 件（证配方未变） | `collect_scope()` 现读 72 件、剔 `sre_regression_report.json` 后 71；两枚逐字全等（全值 `413ab919b5f406e5f4d90d10a36a08222c22746dc50b1eec49fa8bb4ab5f386d`） | **真** |
| A6 | L1 `change-governance.md` 780,064 B／CR 0／LF 503／`2c35f6aec76a0be4`，且与运行时 `shared/`、`技能仓备份/shared/` 字节全等 | 全中，三层比 True/True | **真** |
| A7 | 台账 44 行、状态分布 已闭合 31／待处置 12／已裁定不做 1；PL-040／041／043 已闭合且依据列含本批 CG 键；PL-044 存在且待处置 | 全中（首行 444、七列）；已闭合 31＝24 普通字面＋7 粗体字面 | **真** |
| A8 | §九 双口径 119＝119 | 逐行 119；`iter_md_tables` 表级 119（header 6 列）；两口径相等，v1.14.0 检查 7 新增断言现报同值 | **真** |
| A9 | CHANGELOG 363,989 B／LF 934／`46165adfb1fe3030`，`## v1.13.10` 置顶且纯新增 | 全中；`--numstat` 15 增 0 删 | **真** |
| A10 | PL-043 三件真唯一残留归档不删：3 件／318,752 B ＋ `MANIFEST-unique-residual.txt` 739 B，源↔归档逐件全等 | 全中（`shared/change-governance.md_pre20260916CG006` 200,372 B；两份 `standards-index.md_pre20260916CG006` 各 59,190 B 且同哈希）；原件仍在运行时层 | **真** |
| A11 | 推送阻断：`.git/hooks/pre-push` 与 `_工具/hooks/pre-push-publish` sha256 全等 `a05953cc1ed666e2…`，门禁 exit≠0 即 exit 1 | `core.hooksPath` 未设置→生效路径即 `.git/hooks/pre-push`；2,053 B／哈希全等；正文含 `[ "$rc" -ne 0 ] → exit 1` 与 GATE 缺失 `exit 1` | **真** |
| A12 | 台账 `records` 追加式、本仓最新＝FAIL、`workspace_health.py` 取末条而非改写、本仓回执 0 枚 | `records` 11 条、本仓 exit 键 3 条（16:32／17:01／17:02）皆 FAIL 且末条最新；`workspace_health.py:1155—1158` 以 `latest[r["exit"]]=r` 顺序覆写；`check_publish_receipts`(:1216) 不改台账；回执目录本仓 JSON 计数 0 | **真**（"FAIL 记录被机器改写为 PASS" 不属实，由此机算坐实） |
| A13 | 扫描详情 155 命中＝`local_repo_path` 129＋`local_user_path` 26；顶层分布 记录 65／技能仓备份 25／_专题 24／CHANGELOG 16／文档 11／成果 7／程序文件 4／AGENTS.md 3；两处余量系 `SCRATCH_ROOT` 功能常量 | 全中；`cross_layer_eol_test.py:42`／`skill_cross_layer_test.py:31` 均 `SCRATCH_ROOT = Path("D:/Qoder-Files/_tmp-scripts/…")`，两脚本内各引用 5／4 次 | **真**（删改即致第七、九门禁不可运行，故"修净"不作前置） |

**总体结论（首轮）＝PASS-with-OBS**：13 项中 11 项判真，A2／A4 两项的"不符"落在**主代理复核指令的措辞**上（转述值与转述配方皆非载体所载），无一项指向登记面错误，不需回退任何登记结论。

---

## 二、批内处置（两项指令侧差额逐条落地）

**处置①（A2）——登记面复核：错标不在载体，在指令**。主代理给代理的指令把 v1 的度量当作 v2 的写入（"v2＝27,028 B／`d2fba820…`"）。落地面逐处现读核验：`change-governance.md` 与 `CHANGELOG.md` 内 `27,028`／`d2fba820` 共 3 处命中（第 11／73／400 行区）**全部与 v1 同句**，v2 的度量在 CG-20260921-004／-005 登记行内已另记（`30,622` 命中 3／2 处、`0d52490c` 命中 2／1 处），本批 §九 行与 CHANGELOG ⑤ 均未引用 v2 自身字节数。主代理收尾另加一枚三版度量表（自算，不采信代理读数）：v1＝27,028 B／CR 0／LF 193／`d2fba82085cf1a59…`；v2＝30,622 B／CR 0／LF 195／`0d52490ccaa9943a…`；v3＝44,376 B／CR 0／LF 223／`5204f37d48a1b129…`。**处置结论＝登记面零订正**；指令侧教训入库为 §四 OBS-1（复核指令中的度量与配方须逐字抄自载体，不得凭记忆转述）。

**处置②（A4）——切片配方现读复算，四值全回主代理侧**。代理转述的配方系主代理指令所写，而 v3 §七 v3 条实际钉的是另一套：§一 1—5 条＝`\n## 一、` 起、第一个 `\n---\n` 止再 rstrip（v3 侧另显式给出等价判据"取到第 6 条首行 `6. ` 之前"）；§三＝`\n## 三、` 起、`\n## 四、` 止（**含** `---` 行、不 rstrip）；§4.1+4.2＝`\n### 4.1` 起、`\n### 4.3` 止；§4.4＝`\n### 4.4` 起、`\n## 五、` 止（后三段均不 rstrip）。主代理按该**载体现读配方**独立写脚本复算（`D:\Qoder-Files\_tmp-scripts\review\verify_recipe2.py`），四段在 v2／v3 两侧**同时**回落到声明字节与哈希：1,284／`19599c49…`、10,058／`9f9a4c7f…`、2,614／`27a8aa63…`、345／`44cd4b71…`，`ALL-MATCH=True` 四项。**处置结论＝配方已随值入库、载体零订正**；代理判"部分真"的原因是指令转述丢字，与 CG-20260921-004 处置①（"值对而配方未随值入库"）系**反面**情形——本批配方在件内、指令在件外，故收口方向不同：不改 §七，改本报告的指令存档口径。

---

## 三、端到端终态与授权边界

- **写面**（全部在本工作区）：新建 `_专题_技能合集策划/行为与专业证据层冻结基准_v3.md`（A1）＋`change-governance.md` 三层带平（头部行／§九 本批行／§11.2 四行改写，A6）＋`CHANGELOG.md` v1.13.10（15 增 0 删，A9）＋本报告件。仓外仅两处、皆主代理既有授权范围内：归档目录 `D:\Qoder-Files\_archive-20260921-traces\unique-residual\`（A10）与临时复核脚本 `D:\Qoder-Files\_tmp-scripts\review\`。
- **零改面**：15 技能件正文／契约文件／`validate_governance.py` v1.14.0 与九门禁件／`AGENTS.md` 结构／`standards-index.md` 本体／冻结基准 v1 与 v2。故通过数 237 为 no-op，硬判据取「失败 0＋exit 0」。
- **九门禁终态（全绿，逐条实跑）**：validate **237／0／1、exit 0**（内含 §九 双口径 119＝119 与检查 9 纳入判据 62 件字节全等）｜verification 6｜ace 90｜sre 66（指纹 `4f53cda18c2baa0c`、缺口 0）｜ic07 20｜pending_ledger 43｜cross_layer_eol 25｜index_ref 26｜skill_cross_layer 21，各 OK、exit 0。
- **sync 收尾**：写态「范围 72｜一致 71｜新增 0｜更新 1｜移除 0」（唯一更新件＝机器本地 `sre_regression_report.json`，因 SRE 门禁复跑而重写）；末次只读 `--check`「范围 72｜一致 72｜新增 0｜更新 0｜移除 0」。
- **授权边界**：本批**只本地提交、不含推送**。且 A11/A12 实测出一条**承重外部事实**：本仓活钩子 `pre-push` 在出口裁定 FAIL 下 exit 1 ⇒ **本仓当前不可推送**，历史上"推送随 PL-031 阶段 1 那批一起做"的安排不可执行。PL-044 已登记该项并给出三择一（例外登记／放宽词表覆盖面／接受本仓不公开推送），其处置面属根仓 `D:\Qoder-Files\_工具\`，**超出本工作区写权限，本批不代作，须发起人另线指示**。

## 四、OBS 与后续判据（不作本批阻塞项）

- **OBS-1（流程）**：复核指令里的度量值与切片配方若凭记忆转述，会把"载体正确、指令失真"报成"登记面为假"。本批即两例（A2／A4）。后续同类批：指令中的每个数字与配方须标"抄自载体第 X 节"或直接由代理现读文件自取。
- **OBS-2（载体字面）**：§11.2 状态列同时存在「已闭合」与 `**已闭合**` 两种字面（机算需归一，A7 实测 24＋7）。归一属排版面，须与所属行为面修订同批，本批不动。
- **OBS-3（措辞）**：A11 声明宜明写「`core.hooksPath` 未设置，生效者为 `.git/hooks/pre-push`」，否则读来像两处并存。登记行文字按历史行纪律不改写，此口径记于本报告。
