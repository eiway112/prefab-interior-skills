# CG-20260925-003 专业整改十批-组9 参数与证据整改执行记录

> 承接：`文档/专业整改任务总单_v1.md` §二 组9 ＋ §三 PL-068 闭合条件；另处置组8 独立复核移交项 S1（Σ修正端点配对法裁定）。
> 定级 **B 级技术修订**；授权＝发起人 2026-09-25 明文指令「推动后序工作：组9（PL-068 参数与证据批，含 S1 Σ修正端点配对法裁定）」；全程内部复算闭合，不开外部通道。
> 取证目录：`D:/Qoder-Files/_整理与清理/2026-09-25/专业整改十批-组9参数与证据-CG-20260925-003/`

## 一、写面清单（运行时 4 件＋守卫 1 件，均纯 LF／CR 0）

| 件 | 改前 | 改后 | 净增 | LF |
|---|---|---|---|---|
| `acoustic-calculation-engine/reference.md` | 47,364 B `a8458c0b…` | **51,963 B `5e38af75…`** | +4,599 | 728→745 |
| `acoustic-calculation-engine/SKILL.md` | 11,991 B `92566ffc…` | **12,391 B `d6c658f2…`** | +400 | 199→201 |
| `prefab-floor-system/reference.md` | 99,246 B `a2a48c4b…` | **99,643 B `ffc4b2f5…`** | +397 | 1812 不变 |
| `prefab-floor-system/SKILL.md` | 27,688 B `0ec96355…` | **27,804 B `0f74f060…`** | +116 | 475 不变 |
| `程序文件/ace_regression_test.py`（守卫） | v1.8.0／159 tests | **v1.9.0／176 tests** | — | 唯一已跟踪改动 |

- 开工原像自证：ACE 两件改前字节数与 sha256 与 CG-20260925-002 执行记录§一「改后」列逐值全等 ⇒ 本批原像＝组8 终态、期间无并发写入；FL `reference.md` 改前 sha 与开工 `before_hashes.txt` 全等。原像留存 `原像-runtime/`（含 `FL/` 子目录两件）。
- **未新建 `_pre` 备份**（避免增 PL-042 类残留面）；回退凭据＝原像 sha256 留存＋未 commit 前 `git show HEAD:<path>` 前像。

## 二、逐 PL 写面明细（PL-068 闭合条件三条逐条处置）

### 子面① m' 口径（区分有效振动质量与永久荷载）

- `ACE reference.md:234` 参数表行：「面层+垫层总面密度」→「浮筑层有效振动面密度 m'｜弹性垫上方共同运动各构造层的单位面积质量之和；不含结构楼板、不含弹性垫自身，组成与排除见下方「m' 口径」条」。
- `:236—:240` 新立「m' 口径」条三项：**组成与排除**（三项排除式）／**与永久荷载的关系**（结构恒荷载口径与本量不得互代，误代后果复算量化并标复算凭据）／**模型外假设**（集中质量假设限定 f>f₀ 质量控制区，不提供高频有效质量折减定量式）。
- `:272` `m_slab` 输入行补「仅用于查裸板 Ln,w 基准表，不进入 f₀ 与 ΔLw 的质量项」；`:274` m' 输入行补「与 m_slab 分列，不得合并为恒荷载合计代入」。
- 跨件联动：`FL reference.md:207` m' 行同口径成文（供 FL 独立降级路径使用）、`:258` 恒荷载行补「结构承载力口径、两者不得互代」；`FL SKILL.md:381` 输入参数行把 m' 由「饰面层」扩为「弹性垫上方共同运动各构造层之和，非仅饰面层」。两侧排除式由守卫跨件字面一致断言锁定（先例＝组5 跨件一致性）。
- 误代后果（复算入文）：ΔLw 虚高约 **10.2**／低估约 **12.4**／偏大约 **0.5** dB；f₀ 低估约 **48%**／高估约 **120%**。判据＝「计入即口径错误」，不因偏差方向保守而豁免。

### 子面② C 的证据状态

- `:250` 撤两句无据说法：「默认值经多个常见构造校准所得」与「C=12 对典型浮筑构造系统性高估约 5 dB」（无可定位数据即撤回，**不给替代解释值**）；C=10 与典型区间 8-12 明标待独立验证、不得对外作可核验精度证据。`ACE 默认 C=10` 字面保留（`test_M5_default_C` 正则锚，零改常数）。
- `:252` 新增灵敏度反证：ΔLw 式中 C 为加法项 ⇒ ∂ΔLw/∂C ≡ 1 dB/dB；C 10→12 恒为 **2.00 dB**，C 在全典型区间 [8,12] 内变动的全部影响幅度＝ **4.00 dB** ⇒ 5 dB 量级偏差不应归因于 C，正确排查方向改列 m' 口径／s 取值与单位标度／基层刚度·垫层老化·边缘隔离。

### 子面③ 演示身份 ＋ S1 端点配对裁定

- `ACE SKILL.md:160`「已知案例」→「演示对照提示…不得拆出引用为已校准证据」；`reference.md:455` 同构造对照、`:457` 留痕缺口条收口为「演示数据，不计为验证样本」，并声明本算例一切派生读数承继同一演示身份。
- **S1 裁定**＝Step 5（`:386`）Σ 修正与 ΔR 的端点配对取 **甲口径＝区间 Minkowski 和**（下界端加 Σ修正_下界、上界端加 Σ修正_上界）。判据三条：甲对任意 ΔR／Σ 区间**保序**、区间**中点恒等**于 Rw_base+ΔR中值+Σ中值（与 Step 3 中值口径自洽）、**不丢 ΔR 区间宽度**；乙口径（ΔR 先取中值再叠 Σ 区间）本例丢宽 1.48 dB 故弃。
- 算例 `:445`／`:453` 按甲复算，并订正文档原「50~51」的**中值取整误差**（原写 50.00~51.00、中点 50.50，恒等中点实为 50.277）为 Rw_B∈**[49.0, 51.5]**、单值≈**50.3**；Rw_A∈**[48.0, 49.5]**、单值≈**48.8**。
- 组8 三条判据（Step 2 恒升序控制锚 `:356`／端点判据条 `:361`／算例印值 `:445` [−3.0, −1.5]）**字面全保留**；判据系**一般化而非削弱**，由 `assert_step5_compatible_with_group8` 显式复验。

### 未代结面（显式）

PL-055 精度声明、PL-053 经验域、PL-057 现场关系三面字面未被本批改写；图集页码／CMA-CNAS 报告编号须另走 T-5 官方核验或检测报告通道。

## 三、守卫与反向自证

- `程序文件/ace_regression_test.py` v1.8.0→v1.9.0，**159→176** tests；新增 `TestM5M7CaliberAndEvidence` **17 例**＝R1 m' 排除式跨件一致（ACE 口径条↔FL 参数表 m' 行↔FL SKILL 输入参数行）、R2 三个 ΔLw／两个 f₀ 误代后果按文档参数独立复算比对（容差 0.05 dB／1%）、R3 撤回锚＋C 灵敏度复算 2.00／4.00、R4 Σ 端点配对定义与「甲宽于乙」、R5 算例印值复算（[−3.0,−1.5]／[48.0,49.5]／[49.0,51.5]）、R6 演示身份与派生读数承继句、R7 精度面（PL-055／PL-053）字面未被本批改写、R8 组8 判据兼容复验；**9 常态（含 3 条复算锚）＋2 纯数值性质＋5 负向注入＋1 良性控制例**。
- clean 态：`Ran 176 tests`／`OK`／exit 0（`门禁日志/ace_regression_clean_v1.9.0.log`，UTF-8）。
- 反向自证：`ACE_SKILL_DIR`＋`FL_SKILL_DIR` 指向改前原像复注入 ⇒ `FAILED (failures=14)`／exit 1（`反向注入-原像复跑.log`）。构成＝**13 条落本批新守卫组**＋第 14 条系组8 注入源锚点随 W5 同批更新后在原像态「注入未命中」（**判据未削弱**，clean 态组8 全绿）。新组 4 例在原像态天然为绿并**逐条给因**：两例纯数值性质不取文档字面、一例判「未越界改写」故两态皆绿、一例因原像态判据本已复红而无法区分（其非恒真性由 clean 态注入自证承担）。
- ACE 原有 159 例与参考函数逐字不变。

## 四、SRE 独立复核（只读子代理，B 级审批要件）

按§二 B 级流程经只读子代理独立复核（不接收主线程判断、不预读结论），报告存 `独立复核报告_SRE.md`。**总判＝PASS-with-必改**。

- **a 复算独立性逐值吻合**：f₀=59.59 Hz／ΔLw=34.77；三个误代后果 +10.21／−12.37／+0.51 dB 与 −48.0%／+120.5%；量纲 (N/m³)/(kg/m²)=s⁻²→Hz；C 灵敏度 2.00／4.00 dB；甲口径保序与中点恒等 20000 随机组违反 0；乙丢宽 1.48 dB；算例印值 r=43/51=0.8431→[−3.0,−1.5]／[48.0,49.5]／[49.0,51.5] 与单值 48.8／50.3 全符。
- **b 闭合条件三条全命中**：旧字面在活技能面 0 残留（仅 `_pre*` 留痕件与本文件史载引用保留）；C=10 与 [8,12] 常数保留。
- **c 越界检查未越**：`shared/`、红线注册表、标准索引、仓内镜像零动，PL-053／055／057 面零触及，定级 B 正确。
- **d 守卫非恒真亲跑复现**：clean 176 OK／原像注入 failures=14，13＋1 构成与本记录§三相符，4 绿例归因披露诚实；组8 判据经 `assert_step5_compatible_with_group8` 复验未削弱。
- **e 漏项扫描**：未见新同型缺陷（Σ 组装仅 ACE 两件＋治理引用）。
- **必改 1 条（具文面）**＝`变更请求与影响评估.md` §一「涉及文件」与§三写面表漏列 FL `SKILL.md:381` 与 ACE `reference.md:264`（m_slab 行）两处实改 ⇒ 同批补列 **W14／W15** 并订正§一，技术写面零改（先例＝CG-20260925-002 必改 R-01：具文与实改不符即订正具文，不静默扩写也不撤改动）。
- **建议 2 条（不阻断）**：a `FL examples.md:56/:749` 标签仍作「浮筑层总面密度」（数值口径正确，判**非缺陷**）与同处 107→95 未披露修正 ⇒ 登记为后续批候选，本批不动；b `test_old_mass_row_injection_rejected` 的 `assertRaises` 内第二条调用因第一条先抛而永不可达 ⇒ 已拆为两个 `assertRaises` 使其真实可达，修后复跑读数不变（clean 176 OK／原像 failures=14）。

## 五、治理登记面（L1→运行时 shared→仓内镜像三层）

- 载体＝`_专题_技能合集策划/change-governance.md`，经 `登记治理面.py`（唯一锚 patch＋命中数=1 断言＋纯 LF 写回）：头部快照轮换＋§九 CG-20260925-003 行（插在 -002 行后）＋§11.2 PL-068 待处置→**已闭合**并前置本批处置依据（原各行史载处置依据保留，尾部「常规 TTL…」句未删）。
- 读数＝1,010,413→**1,023,865 B**，sha256 `f07341ea2458…`，LF 581／CR 0。
- 三层带平后三份 `change-governance.md` sha256 全等（L1／`~/.qoder/skills/shared/`／`技能仓备份/shared/` 均为 `f07341ea2458`）。
- 机算复核：§九 CG 行 138→**139**；台账 **72 行不变**，状态分布 待处置 22→**21**／已闭合 49→**50**／已裁定不做 **1**（21＋50＋1＝72）。

## 六、检查 10 残量登记（第十七批）

- 载体＝`_专题_技能合集策划/数据分类复核清单.json`；脚本＝取证目录 `登记与残量/第十七批_登记残量组9参数与证据.py`（五键＝ACE reference／SKILL＋FL reference／SKILL＋`shared/change-governance.md`）。
- 预期与实际＝**摘失效 records 15／baseline 1**、**新登 26 条**（逐单元具文理由；C 段绑块内公式字面 `source_spans`＝f₀／ΔLw 各式与 20·lg／40·lg 界限式，D 段承 C=10 与 [8,12] 经验控制值口径，N 段逐处具文）；records 8,622→**8,633**／baseline 3,814→**3,813**。
- **棘轮披露**：baseline 减量系写面机械后果（冻结存量转复核记录），非迁移进展。
- 残量核对前置＝`探针_单元差异.py`＋`转储_新单元.py`（26 个新单元正文逐条转储）。

## 七、门禁终态（十二门禁全绿＋validate＋workspace_health 如实记录）

末次实跑（全部产物性步骤之后）：`门禁日志/跑门禁.py` ⇒ 12 项＋全局健康各 exit 0，逐条命令／退出码／末行／脚本 sha256 落 `门禁日志/gate_results.json`。

- `validate_governance.py`＝**总评 PASS｜通过 243／失败 0／警告 3**（通过数与组8 终态一致，本批零增检查项）。警告 3 构成不变＝检查 3「38 条无有效核验日期」聚合 1 条＋检查 10 `A_VERIFY_DATE` 2 条；后两条行号因本批正文增行由 `:708/:709` 机械下移为 **`acoustic-calculation-engine/reference.md:737/:738`**，条数与语义不变。
- 各计数门禁：verification **6**／ace **176**／sre **66**（指纹 `4f53cda18c2baa0c`）／ic07 **20**／pending **43**／cross-layer-eol **25**／index-ref **26**／skill-cross-layer **21**／data-class **70**／storage-anchor **19**／wetzone **15**，全部 `OK`。
- `sync_skill_backup.py` 带平三段读数：写面后首跑「范围 73｜一致 69｜新增 0｜更新 4｜移除 0」（更新件＝本批四写面）；**全部产物性步骤之后**末次 sync「范围 73｜一致 72｜新增 0｜更新 1｜移除 0」（唯一更新＝机器本地 `prefab-standards-reviewer/sre_regression_report.json`，系门禁复跑的机械后果）；终态 `--check`「范围 73｜一致 73｜新增 0｜更新 0｜移除 0」＝带平。
- `D:/Qoder-Files/_工具/workspace_health.py`＝**exit 0／0 FAIL**；本批各轮实跑的汇总读数在 **26 OK／4 WARN** 与 **25 OK／5 WARN** 之间摆动，逐项比对后唯一差额项＝「推送就绪预读：备通道 SSH:22 已认证」（网络通道探测的时态性读数，与本批改面无因果），故本行**不钉固定计数**、只钉恒定判据 **exit 0＋0 FAIL**（沿「开放全集计数不得钉固定值」纪律）；本仓行「装配式装修技能开发：ahead 2/behind 0；主通道通；门禁 PASS」。WARN 全部不阻断，构成见下方注 ②。**注**：① 该件本批实跑为 exit 0，与前些批次「全局健康仍阻断」的叙述不同，以本行为准；② 其余 WARN 项（兄弟仓 `视频制作工作流` 文件命名缺日期前缀、跨仓台账登记纪律超口径 3 处、出口裁定台账非终态 1 条、发布回执 STALE_CONTENT）均属跨仓既有存量或未提交态，本批不代改、不代结，只对本仓行与「exit 0＋0 FAIL」负责。

## 八、顺带观测（按快照纪律不改写他批登记，只在此具文）

1. `AGENTS.md` ace 门禁行旧基线读数 159 与本批终态 176 之差属登记滞后，同批订正（含 `A_VERIFY_DATE` 行号）。
2. 早前用 shell `>` 直落的 `ace_regression_clean_v1.9.0.log` 曾为 GBK 字节（未带 `PYTHONIOENCODING=utf-8`），已带编码重跑覆盖为 UTF-8，内容仍 `Ran 176／OK`——属取证载体编码缺陷，不涉判据。
3. 台账 PL-038（母项）、PL-031／046／049、PL-070／072 等余下待处置项依赖外部真值或宿主实跑，不在本批授权面内。
4. `workspace_health.py` 的「台账登记纪律」WARN 系跨仓台账行长超口径（390／275／217 > 200），不属本仓 §11.2 台账面，故不据此增删本批登记内容。

## 九、状态唯一来源声明

- 本批写面状态以运行时 `~/.qoder/skills/` 现读 sha256 为准；三层一致性以检查 6／检查 9 实跑 [OK] 为准。
- 提交态以 `git log` 现读、推送态以 `git rev-list --left-right --count origin/master...HEAD` 现读为准（本记录写作时点＝**ahead 2、未提交本批改动**）。
- **git 提交与推送各自须发起人另发明文指令**（含「允许执行上述 git commit」／「允许推送」式肯定句）；本批不据治理登记行推定授权。

## 十、覆盖率四计数与逐项终态记账

**四计数＝开工前清单 22 项／完成 22／跳过 0／覆盖率 100%**（失败与未执行与不适用分列：本批无失败项、无未执行项，不适用项见「不做项」栏且不入分母）。

| # | 开工前清单项 | 终态 | 凭据位置 |
|---|---|---|---|
| 1 | 取证目录建目＋原像快照 | 完成 | `原像-runtime/`（含 `FL/`）＋`before_hashes.txt` |
| 2 | m' 误代后果复算 | 完成 | `m5e_recalc_output.txt` §一 |
| 3 | C 常数灵敏度复算 | 完成 | 同上 §二 |
| 4 | Σ 端点配对甲／乙复算＋保序证明 | 完成 | 同上 §三 |
| 5 | 位名式 r>1 侧排序性质复算 | 完成 | 同上 §四 |
| 6 | 变体 A 完整区间复算 | 完成 | 同上 §五 |
| 7 | 同型面扫描（全 29 运行时技能目录） | 完成 | `变更请求与影响评估.md` §二末段 |
| 8 | 变更请求与影响评估具文 | 完成 | `变更请求与影响评估.md` §一—§十 |
| 9 | ACE reference.md 写面（子面①②③＋S1） | 完成 | 本记录§二 |
| 10 | ACE SKILL.md 写面 | 完成 | 同上 |
| 11 | FL reference.md 写面 | 完成 | 同上 |
| 12 | FL SKILL.md 写面 | 完成 | 同上 |
| 13 | 守卫扩写（17 例）＋clean 实跑 | 完成 | 本记录§三 |
| 14 | 改前原像反向注入自证 | 完成 | `反向注入-原像复跑.log` |
| 15 | SRE 独立只读复核 | 完成 | `独立复核报告_SRE.md` |
| 16 | 复核必改项处置（具文面 W14／W15） | 完成 | 报告§6＋评估件§一／§三 |
| 17 | 复核建议 b 处置（assertRaises 拆分） | 完成 | 本记录§四末段 |
| 18 | 治理登记面（CG 行＋PL-068 转闭合） | 完成 | `登记治理面.py`＋§九 139 行现读 |
| 19 | 三层带平＋哈希全等 | 完成 | 三份 `change-governance.md` sha `f07341ea2458…` |
| 20 | 检查 10 残量登记（第十七批） | 完成 | `登记与残量/`＋records 8,633／baseline 3,813 现读 |
| 21 | 十二门禁＋validate＋全局健康实跑 | 完成 | `门禁日志/gate_results.json`（13 项全 exit 0） |
| 22 | 收尾登记（执行记录／CHANGELOG／总单／AGENTS） | 完成 | 本件＋`CHANGELOG.md` v1.13.30＋总单§十五＋`AGENTS.md` 门禁行 |

**不做项（本批授权面外，非失败）**：PL-055 精度统计、PL-053 经验域、PL-057 现场关系、图集页码与 CMA-CNAS 报告编号（须外部通道）、FL `examples.md:56/:749` 术语统一与 107→95 披露（复核建议 a，留后续批）、`_pre*` 留痕件清理与根目录白名单登记（属 PL-042／环境面）、git 提交与推送（须发起人文面授权）。

**末次收尾读数（本记录定稿时点现读）**：`跑门禁.py` 重跑⇒13 项全 exit 0；`sync_skill_backup.py`「范围 73｜一致 72｜新增 0｜更新 1｜移除 0」（唯一更新＝机器本地 `sre_regression_report.json`），随后 `--check`「范围 73｜一致 73｜新增 0｜更新 0｜移除 0」；`git status --short`＝12 件已跟踪改动＋1 件未跟踪执行记录，`git rev-list --left-right --count origin/master...HEAD`＝**0 2（ahead 2，本批未提交）**。
