# CG-20260920-007 公开发布面暴露审计与整改执行记录

**日期**：2026-09-20　**批次**：CG-20260920-007　**定级**：B 级（依据＝`change-governance.md` §一 B 级「修正错误、不影响安全性」）
**复核**：发起人线内只读子代理独立复算，报告＝`记录/2026-09-20_CG-20260920-007_B级线内复核报告.md`
**边界**：本批含本地 commit，**不含 push**；不含 `D:\WorkBuddy`／`D:\CodexProjects` 外部工作区原件的任何写面。

---

## 一、任务来源与授权

用户 2026-09-20 就公开仓 `https://github.com/eiway112/prefab-interior-skills` 提两问：① GitHub 上的版本是否为最新；② 是否存在不应该发布的内容。审计后用户令「以上问题，请逐项整改」。整改面涉及发布物内容取舍，故以四项裁定取得授权（AskUserQuestion，逐字口径）：

| 裁定项 | 用户选择 | 落到本批的执行 |
|---|---|---|
| 审阅台账 CSV（801 数据行含外部包逐文件清单＋绝对路径列） | 移出工作树，保留在 git 历史 | 删「绝对路径」列与 787 行外部明细，只留本仓 14 行；前像留历史 |
| 成果 HTML 的本机路径链接 | 链接改相对路径，署名保留 | 16 处绝对锚点中 14 处转仓内相对、2 处降级为无链接说明；署名行保留 |
| `记录/备份归档/` 14 件旧技能副本 | 移出工作树 | `git rm -r` 移出，前像留历史 |
| 登记面历史行与 `CHANGELOG.md` 中的旧路径／个人记忆库细节 | 不改写，只登记公开面结论 | 零改写；残余公开面新登 **PL-030** 携常规 TTL 追办 |

## 二、问题一「版本是否为最新」的取证与口径限制

远端实时校验**未取得**：`git ls-remote origin` 报 `Recv failure: Connection was reset`（网络通道不可达），故本节读数只能证明**本地引用一致**，不能证明与 GitHub 服务端一致。四项本地量测（本批执行前）：

| 量测 | 读数 |
|---|---|
| `git rev-parse --short HEAD` ／ `git rev-parse --short origin/master` | `a345c61` ／ `a345c61`（同） |
| `git rev-list --left-right --count HEAD...origin/master` | `0  0`（无未推送、无未拉取） |
| `git tag --list \| wc -l` | 7 |
| 工作树状态（`git status --porcelain`） | 空（clean） |

**结论口径**：本地 `master` 与跟踪引用 `origin/master` 零分叉，且与用户截图所示 HEAD（`a345c61`、132 提交、7 Tag）吻合，故「已推送内容即最新批次」这一判断成立；但**远端服务端现态未经联网复核**，如需断言须在有网络通道时重跑 `git ls-remote`。

## 三、问题二「不应发布的内容」：四类暴露与逐项处置

### ① `README.md`（对外事实错误 2 类，改 3 处）

- **技能计数错**：「16 个技能本体」与「技能清单（16 个）」→ **15**。真值源＝`程序文件/sync_skill_backup.py` 的 `SKILL_DIRS` 经 AST 现读 **15** 条；README 自身清单亦实列 15 条；`技能仓备份/` 内第 16 个目录系 `shared/`（治理文件镜像，非技能本体）。
- **安装路径错**：Windows 由 `C:\Users\<你的用户名>\.qoderwork\skills\` 改为 `...\ .qoder\skills\`，macOS/Linux 由 `~/.qoderwork/skills/` 改为 `~/.qoder/skills/`。实测本机 `~/.qoderwork/skills` **不存在**、`~/.qoder/skills` **存在**；技能实际加载处＝`AGENTS.md` 三层表的 L2。该过时路径系 `CHANGELOG.md` 既登遗留 (e)，本批首次落到对外文档面。

### ② 成果 HTML `成果/第三方架构与内容体系评估_20260915.html`

- 订正前绝对路径锚点 **16** 处＝10 处含本机用户名的 `file:///C:/Users/…` ＋ 6 处指向本仓开发目录的 `file:///D:/Qoder-Files/…`。
- 处置：**14 处**改为仓内相对路径（`../技能仓备份/…`、`../程序文件/…`，逐条断言目标文件存在，实测 EXISTS 14／悬空 0）；**2 处**（`agent-wiki` 技能件、外部工作区 `semantic-surfaces.md`）仓内无副本，降级为不带链接的 `class="ext-src"` 说明 span。
- 订正后：含用户名路径 **0** 处；全文用户名命中余 **1** 处＝署名行（发起人裁定保留）；余 **25** 处 `file:///` 全为不含用户名的外部证据指针（`D:/WorkBuddy` 14＋`D:/CodexProjects` 11）。行尾形态仍为纯 LF（CRLF 0）。
- **自述纠偏（登记面留痕，不改写初稿叙述）**：审计初稿把 HTML 的 `file:///` 记作「29 处含用户名」，`29` 系 `grep -c` 的**行数**口径而非锚点数——现测全文件锚点 **41** 处、其中含用户名 **10** 处。

### ③ 审阅台账 `成果/第三方架构与内容体系评估_20260915_审阅台账.csv`

- 前像：449,797 B，含表头 802 物理行（＝801 数据行）／11 列，来源列分组＝WorkBuddy 787 行／本仓 Codex 14 行，含「绝对路径」列。
- 订正后：**5,197 B**，含表头 15 物理行（＝14 数据行）／10 列；已删「绝对路径」列与全部外部工作区逐文件明细；实测 `D:\`／`C:\`／`file:///`／`WorkBuddy`／`CodexProjects`／用户名 命中均为 **0**；BOM 与 CRLF 形态保持。
- 汇总口径（总数、分组分布、终验结论）保留在报告正文与本文件 §九，明细以 `git log --follow -- 成果/第三方架构与内容体系评估_20260915_审阅台账.csv` 前像为据。
- `文档/SRE确定性体检设计方案_v1.0.md` 第 897 行 LEDGER 行的 801／787／分组分布原述属**当时事实快照**，按纪律**不改写**，只在同行**追加**「公开面订正（CG-20260920-007）」句；实测原述 156 字节为现值的逐字前缀（唯一位移＝表格行闭合符 ` |` 移到追加文字之后，属表格语法必需）。

### ④ `记录/备份归档/` 已跟踪历史副本

- **14 件**跟踪件经 `git rm -r` 移出工作树（含 `2026-08-06/standards-index.md_pre_20260806` 与同名 `backup_skills_shared_20260806/standards-index.md`、`prefab-wall-surface-system` 三个版本态目录（`v1.2.0`／`v2.0.0-preTCSUSfix`／`v2_1_3_pre`）的 `SKILL.md`／`reference.md`／`examples.md`／`agents/openai.yaml`、`ACE_v1.0.0_pre-M7` 两件），消除「克隆结果内存在看起来像技能本体的过期副本」的误复制风险；14 件全部可经 `git show HEAD:<path>` 取回（实测 14/14 非空）。
- 该目录现存 **18** 件 `.bak` 系 gitignored（`.gitignore:10`），从未进入发布面，作为机器本地保险件保留不删。

## 四、门禁与收尾读数

**改动前后各跑一次九门禁；下表为收尾终态（全部 exit 0）**：

| # | 门禁 | 读数 |
|---|---|---|
| 1 | `python -B 程序文件/validate_governance.py` | 总评 **PASS**，通过 **236**／失败 **0**／警告 **1**，exit 0（本体 v1.13.0 零改动；本批零新增检查项，通过数对检查 7／8／9 为 **no-op**；硬判据＝失败 0＋exit 0） |
| 2 | `verification_policy_test.py` | Ran **6** tests，OK |
| 3 | `ace_regression_test.py` | Ran **90** tests，OK |
| 4 | `~/.qoder/skills/prefab-standards-reviewer/sre_regression_test.py` | Ran **66** tests，OK；缺口信号 0；跨轮指纹 `4f53cda18c2baa0c`（连续一致 153 轮） |
| 5 | `sr_ic07_compliance_test.py` | Ran **20** tests，OK |
| 6 | `pending_ledger_test.py` | Ran **39** tests，OK（含真实治理件 clean 态；§11.2 键闭合含新行 PL-030 通过） |
| 7 | `cross_layer_eol_test.py` | Ran **25** tests，OK |
| 8 | `index_ref_consistency_test.py` | Ran **26** tests，OK |
| 9 | `skill_cross_layer_test.py` | Ran **21** tests，OK |

**唯一 WARN**（沿活基线）＝检查 3 的 38 条无有效核验日期聚合项，非本批引入。

**带平与 sync**：
- `change-governance.md` 三层（L1 `_专题_技能合集策划/` ↔ L2 `~/.qoder/skills/shared/` ↔ L3 `技能仓备份/shared/`）sha256 前 16 位互等＝**`18a4497ad20dcc25`**（688,762 B，纯 LF）。本批 §九 行不写终值哈希（依 CG-20260916-004 ⑧(b) 自指先例），复算判据取「三份互等」。
- `sync_skill_backup.py`：首跑「范围 122｜更新 1（`shared/change-governance.md`）｜新增 0｜移除 0」→ 门禁复跑后「更新 1（机器本地 `sre_regression_report.json`）」→ **末跑 `--check`＝范围 122｜一致 122｜新增 0｜更新 0｜移除 0**（判据＝范围 122＋新增 0＋移除 0；一致/更新随批浮动）。`技能仓备份/同步说明.md` 哈希清单随之刷新。

**登记面结构读数**：§九 新增 CG 行 1 条（6 列）；§11.2 台账 29→**30** 行，状态分布＝待处置 **5**／已闭合 **24**／已裁定不做 **1**；§九 `\| CG-` 行 110→111、历史行改写／删除 **0**；`CHANGELOG.md` diff＝新增 13 行／删除 0 行（只追加 v1.13.2 节）。

## 五、残余公开面（新登 PL-030，本批不代作决定）

工作树面已订正，但下列三项**仍在 git 历史内对克隆者可见**，且不在本批授权范围内：

1. §九 历史 CG 行与 `CHANGELOG.md` 历史节中的本机绝对路径字面与个人记忆库细节（第四项裁定＝不改写，属快照纪律）；
2. ②③④ 已订正／已移出件的**前像**（`git log --follow` 可完整复原 801 行台账与 14 件旧技能副本）；
3. 成果 HTML 余 **25** 处指向外部工作区的 `file:///` 指针（不含用户名，但外部包名与文件清单对外可见）。

**观测必须带判据**：这三项是本批整改后**如实残留**的暴露面，不是「已处理完毕」——其风险等级取决于本仓是否继续 Public 发布，故**判为须追办**，登记 **PL-030**（常规 TTL，到期 **2026-12-19**），处置三择一由发起人裁定：**(a)** 仓转 Private 或以不含该历史的路径重新发布；**(b)** 违例改写历史行（须明文授权，与快照纪律正面冲突，须同批改判并逐字符对 `git cat-file HEAD:<path>` 前像自证）；**(c)** 只约束此后新增件（＝现状）。

**另一项本批只披露不处置**：`文档/SRE确定性体检设计方案_v1.0.md` 同节另有指向 `D:\资料\...` 的本机外部路径，属仓内文档面、非本次四项裁定范围，未作改动。

## 六、未触及清单（写面纪律自证）

`程序文件/` 全部（`validate_governance.py` v1.13.0 与九门禁测试件零改动）、`AGENTS.md`、`standards-index.md` 本体、15 个技能目录全部技能件（运行时与仓内镜像）、其余六件治理文件、§11.3、§九 全部历史行、`CHANGELOG.md` 全部历史节、`成果/` 其余七件、`_专题_*` 策划稿、外部工作区原件。
