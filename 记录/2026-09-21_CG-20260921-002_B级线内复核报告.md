# CG-20260921-002 B 级线内复核报告（2026-09-21）

- **被复核批**：CG-20260921-002（B 级）——R-01 订正批：`prefab-acceptance-checklist-generator/SKILL.md` §11.1 IC-10 请求参数表由 IC-07-Request 字段集错接改为 IC-10 正式五字段
- **复核方式**：发起人线内委派**独立只读子代理**（Explore，禁止写盘、禁止跑门禁）对最终态逐项独立复算（§二 第 3 步 B 级口径，先例＝CG-20260920-004/005/007 复核报告）
- **判定**：**PASS-with-OBS**（8 项声明逐条实测为真；OBS 2 条不改判）

## 逐项核对表（声明 → 实测 → 真伪）

| # | 声明 | 独立实测 | 判定 |
|---|------|---------|------|
| 1 | 订正后运行时件 21,835 B、LF、sha16 `934085fd3502e337` | 21,835 B，CRLF=0，sha256 前 16 位一致 | 真 |
| 2 | `技能仓备份/` 同件与运行时字节全等（sync 带平） | 逐字节 identical，同哈希 | 真 |
| 3 | §11.1 与 IC-10 契约一致 | 契约（`shared/interface-contracts.md` IC-10 请求参数约束表＋IC-10-Request Schema）required＝[项目类型, 空间类型]，项目所在地／构造体系／性能需求均可选；订正后五字段名与必填口径逐字段一致（订正后行 331—335）。另：契约含可选 `return_trace`，不属最小请求参数集，技能表未列不违规 | 真 |
| 4 | IC-07 归属注与 §11.4 分工无残留误用 | IC-07 required 四字段（引用的标准编号／引用条文号／项目所在地／应用场景描述）与表后注（行 337）及 §11.4（行 358—360）「条文级核验走 IC-07、场景筛选走 IC-10」语义一致 | 真 |
| 5 | 无表行形制残留 | `\| 引用的标准编号 \|`／`\| 应用场景描述 \|`／`\| 引用条文号 \|` 表行命中 0；旧字段名仅现于行 11 版本说明（散文提及，允许） | 真 |
| 6 | 波及面唯一（仅验收清单一件误用） | 复核代理按其指令口径扫 prefab-*＋skill-qa-tester（13 目录）：唯一其余命中为 `prefab-standards-reviewer`＝IC-07 接收方合法引用；主代理另以 AST 现读 `SKILL_DIRS` **全 15 目录**（运行时＋仓内镜像两层）补验，结论一致（见 OBS-2） | 真 |
| 7 | Step 2 行／§11.1／§11.4／版本说明四处自洽 | 行 102「五字段」名单＝§11.1 表＝行 11 版本说明；frontmatter version: 2.2.2（行 4） | 真 |
| 8 | 行为-neutral 判据 | 技能件系提示文档、无代码消费端；契约文件零改故无 IC-10 版本升级；校验器／门禁件零改动 | 真 |

## OBS（如实披露，不计失败）

- **OBS-1**：`程序文件/ace_regression_test.py:27` 的 v1.2.0 变更记录 docstring 内含旧路径字样 `~/.qoderwork/skills/acoustic-calculation-engine/reference.md` 1 处（全文件仅此 1 处）。属机器本地测试件对当时历史的自述（沿「历史行不改写」快照纪律），非技能发布面、非活路径引用，本批不改写；不属 PL-030 公开面（该文件在私有测试目录但已随仓发布——其内容为版本历史叙述，与 CHANGELOG 历史节同类，PL-030 (c) 裁定「只约束新增」覆盖）。
- **OBS-2**：委托指令中「15 个运行时技能目录」的列举措辞（prefab-* ＋ skill-qa-tester）实测覆盖 13 目录（`acoustic-calculation-engine`、`skill-qa-tester` 命名不同所致），系**指令口径表观缺口非仓事实**；主代理已按 `SKILL_DIRS` AST 现读 15 目录在两层面补验，波及面结论（唯一件）不变。

## 结论

CG-20260921-002 各项声明核实为真；订正与 `interface-contracts.md`（IC-10 v1.9.0 约束表与 Schema、IC-07 v1.5.5 字段集）逐字段一致，无残留误用、无额外波及、三层带平成立。判定 **PASS-with-OBS**，随批入库。九门禁终态实跑与 sync 读数由主代理执行，见 `CHANGELOG.md` v1.13.4 与 §九 CG-20260921-002 行。
