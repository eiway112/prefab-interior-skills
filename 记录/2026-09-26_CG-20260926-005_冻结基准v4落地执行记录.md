# 2026-09-26 CG-20260926-005 执行记录 — 冻结基准 v4 落地（B 级，冻结机制修订线第二步＝落判据，闭合 PL-049）

## 一、事项、线与定级

- 线＝`成果/2026-09-25_项目全面审视与下一阶段执行方案.md` §五「**冻结机制必要修订**」行；取证半面已由 CG-20260926-004 做完，本批落判据半面。
- 台账承接项＝**PL-049**，本批**转已闭合**（状态分布 待处置 20→19／已闭合 61→62）。
- 本批动作＝执行 `记录/2026-09-26_CG-20260926-004_冻结基准H1检测力诊断执行记录.md` §四 的最小修订案 **R1—R5**，新建 `_专题_技能合集策划/行为与专业证据层冻结基准_v4.md`，v3 冻结态留档不改写。
- 定级 **B 级**（§一「新增内容、不影响安全性」）：改评估判据即改行为比较口径，**不采 C**（C 级＝不影响功能与准确性的排版类变更）；**不升 A**＝零改标准数值与限值、零改 `shared/` 契约与索引本体、零改技能件、零改校验器与十二门禁判据。先例＝同件 v1／v2／v3 均定 B（CG-20260921-003／-004／-008），且上一批记录 §一 第 4 条已预判「基准件的实质修订属 B 级、须走 §二 四步并留前版」。
- 授权字面：发起人 2026-09-26「通读图片内容，基于项目现状及事实依据，评估，并推进！」——该图片即上一批收尾句「只差你一句话：**允许按记录 §四 的 R1—R5 落地冻结基准 v4，B 级，走 §二 四步并保留 v3 前版**（含独立复核与负向注入自证）」与「若下轮接着跑，起点就一个：R1—R5 批不批」。据此按 §二 四步走完：第 1 步发起＝本记录；第 2 步影响评估＝§五 写面清单与零改面；第 3 步复核＝§四；第 4 步发布＝§六 终态。

## 二、落地内容（R1—R5 ↔ v4 位置）

| 提案 | v4 落点 | 落地读数 |
|---|---|---|
| R1 H-1 拆 H-1a／H-1b | §二 2A 表前两行 | H-1a＝`751164809b54e4ab98de38cf6e0b316fcea4220b78eb918f2b76513c5410d5ac`（72 件）；H-1b 不钉固定值，只钉配方与比对判据 |
| R2 H-1b 键集与字节同源同时点 | §二 2A H-1b 行取法列 | 键集＝当次工作树脚本 `collect_scope()` ∖ `git check-ignore` 命中件；字节＝`~/.qoder/skills/<键>` 原始字节 |
| R3 差异按行为面／登记面归类 | §二 2A-补 五步规程＋冻结时点归类基线表 | 72 件全落档：行为面 71（62 技能目录＋7 `shared/`＋2 根）／登记面 1（`shared/change-governance.md`） |
| R4 脚本版本入取物基准 | §二 2A H-1a 行＋2B 表 | 复算须 `git cat-file blob 36dee02:程序文件/sync_skill_backup.py` 后 exec，禁工作树现版 |
| R5 排除面改结构判据 | §二 2A H-1a 行 | `git check-ignore` 恰命中 1 件＝`prefab-standards-reviewer/sre_regression_report.json`（`.gitignore:23`），与 v3 文件名硬列**集等价**⇒ 对读数为 no-op |

- **两处落地偏差（据实登记，非照抄提案）**：
  - **(a) R1 的「不动既有复算基」未照搬**——H-1a 基线由 `08069b0` 推进到 `36dee02`。必要性＝新键集下 `shared/data-classification.md` 在 `08069b0` 无 blob，而 v4 同时禁「跳过」，按旧基复算**必中断**（自证 N2a）；照搬 R1 字面则 R1 与 R2／R4 不能同时成立。旧基读数 `413ab919…` 随 v3 文件留档不改写，仅自 v4 起不再作当前冻结读数。
  - **(b) R3 的判据无机械取法，换实现而非换结论**——提案原文「15 技能目录内容件与 `shared/` 判据件＝行为面；`shared/change-governance.md` 等纯登记载体＝登记面」**在档位归属上与本表一致**（它已豁免该件为登记面），偏差不在归属，而在「是否被宿主技能加载器读入并影响输出」无可机算取法；唯一可算的代理量「被技能件引用」实测亦不成立（见 §三 反例读数）。故落地为两级判据＋逐件引用命中行原文落判＋基线表完备性双向互查。
- **一处本批自查新增（不在 R1—R5 内，属必要修正）**＝§4.3 第 8 条的基准版本枚举原写「v1／v2／v3」，不扩为含 v4 则该条按字面无法指挥实跑标注所依版本（＝本仓已登记的「条文存在≠机制生效」同型缺口）。只改枚举串与「v4 起须同时引用 H-1a／H-1b 两枚」，不改任何判据；该行 282→351 B。

## 三、复算与自证读数（全部脚本现算、无手抄）

- **H-1a 复算**（凭据＝`程序文件/脚本/cg20260926_005_h1_recompute.py`）：键集 73 ∖ 结构判据命中 1＝72 件，逐件 `git cat-file blob 36dee02:技能仓备份/<键>` 聚合＝`75116480…`，与 v4 §二 登记值**逐字 MATCH**；blob 缺失 0 件。
- **H-1b 复算（登记面写入前）**＝同值，两枚**逐字相等**、差异 0 件、键集差 0 件 ⇒ **v4 落地即绿，无须先升 v5**。（带平后读数见 §七。）
- **配方正确性的反向自证**＝同法把基线换回 `08069b0` 并按 v3 文件名口径排除，复算得 `413ab919b5f406e5f4d90d10a36a08222c22746dc50b1eec49fa8bb4ab5f386d`，与 v3 §二 登记值逐字 MATCH。
- **未改节全等性**（`程序文件/脚本/cg20260926_005_build_baseline_v4.py` 打印）：§一 `ee65f0a204237d5f…`／§三 `9f9a4c7f347d575a…`／§4.1+§4.2 `27a8aa63235928bc…`／§4.3 第 1—7 条 `154ecaf8b0dff116…`／§4.4 `44cd4b710d3316f2…`／§五 `487bf131253708c0…` 六节 v3↔v4 逐字全等；其中 §三／§4.1+§4.2／§4.4 三节值**与 v3 §七 登记的旧值逐字一致**（该三值同时证明「我的切片口径与 v3 声明口径相同」与「内容未变」）。§4.3 第 8 条不参与全等断言，改按「把 v4 侧枚举串换回 v3 串后与 v3 该行逐字全等」单列自证＝True。
- **R3 反例的实测证据**＝`shared/change-governance.md` 在 15 技能目录 `.md`／`.py` 内命中 **13 处**，其中 4 处为表格内流程指引句（`skill-qa-tester/SKILL.md:262`、`prefab-governance-sync/SKILL.md:83／108／109`）、1 处为 `#` 注释行（`prefab-standards-reviewer/sre_reasoner.py:275`），均非取数位；同法现算其余 7 件治理层命中数为 8—43 处／件。⇒「命中数>0 即行为面」与「按行形态启发式分流」两式皆会误判，故 v4 禁用之。
- **九项负向注入全 PASS（`--selftest`，exit 0）**＝N1 错取脚本版本产 `62fb5e99…`（71 件）；N2a 跨版本混取时如实中断于 `shared/data-classification.md`、N2b 同参数改「跳过」即以 71 件聚合得 `413ab919…`＝与 v3 登记值逐字相等（证跳过是漏检而非无害）；N3a H-1b 字节侧改回 blob 即与 H-1a 恒等（证「不钉固定值」不是放水）、N3b 取运行时现字节报漂移 36 件＋新增 1 件（撤件 0）；N4a 结构判据命中集＝文件名单件集、N4b 排除面多吞一件产 `3d4f3b29…`；N5 基线表删一行即报未列件；N6 命中 13 处仍落登记面。
- **表格结构机算**＝v4 全文 12 个表块（v3 为 11，新增＝归类基线表），列数不一致块 **0**。
- **手抄副本消除**＝凭据脚本的归类表由 `gov_table_from_v4()` **从 v4 现读**（解析到 10 件治理层＋技能目录档＝行为面），不在脚本里硬列；解析不到即抛错停批。原因：初版脚本内置了一份 `GOV_CLASS` 字典，那正是本仓反复登记的「手抄副本漂移」失效族的来源。

## 四、§二 第 3 步复核（B 级须复核；线内只读独立复算）

复核方式＝线内只读子代理，**自实现配方、不复用本批凭据脚本**（沿 CG-20260921-003「线内只读子代理首轮复算」先例）。其六项读数：

| 项 | 结论 | 关键读数 |
|---|---|---|
| 1 H-1a 配方独立复算 | PASS | 自实现得 `751164809b54…`、72 件（62＋8＋2），键集 73 ∖ 恰 1 件，逐字命中登记值 |
| 2 H-1b 独立复算 | PASS | 同值、72 件、运行时缺件 0、与 H-1a 差异 0 件 |
| 3 配方能否失败（复核人自设注入） | PASS | 六注入全被捕获：I1 错脚本版本→`62fb5e99…`（71 件）；I2 H-1b 取 blob→与 H-1a 恒等；I3 漏排机器本地件→blob 缺失直接中断；I4 v3 配方回算→`413ab919…` 逐字 MATCH；I5 混取＋跳过→吞 1 件仍得 v3 登记值；I6 多吞 1 件→`3d4f3b29…` |
| 4 未改节是否真未改 | PASS | 按 §七 v4 口径独立切六节，sha256 前 8 与本批全对（`ee65f0a2`／`9f9a4c7f`／`27a8aa63`／`c8375840`／`44cd4b71`／`487bf131`），H-2／H-3／S-1—S-8／哈希口径段逐字在场 |
| 5 五步规程判断力审查 | PASS（指出三处文字偏差） | (a) 第 3 步取证机算、落判留凭据，无恒真「机算」声称；但第 1 步「②③必属行为面」与第 4 步「不得默认任一档」文字冲突。(b) `change-governance.md` 命中确 13 处，两锚行属实；唯「5 处表格内」逐行实为 **4 处**。(c) 62＋8＋2＝72 全覆盖，无两档不落件。(d) 未见致命洞：运行时改而未 sync→git log 无 commit→归因失败被停批兜住；篡改工作树脚本缩范围→撤件→行为面触发 |
| 6 R1—R5 落地忠实度 | 基本 PASS | R1／R2／R4／R5 忠实；R3 订正判为**必要非扩面**（提案判据不可机算，所增基线表／完备互查／归因停批是使其可判定的最小机制，改动未泄出 §二）。另指出两点本批未自报：① 提案 R1 原话「不动既有复算基」与基线推进不符，本批未自报；② §七 初稿把提案 R3 引述为「按 `shared/` 即行为面」属**稻草人**（提案原文已豁免该件） |

**总判＝可发布（不阻断）**，建议随批勘误三处。本批处置：三处勘误**全部修入 v4 终稿**（第 1 步改为按第 2／3 步落档并披露「声明面变化／磁盘在场性变化」；第 3 步「5 处」订正为「4 处」并列明行号；§七 补登基线推进为 R1 落地偏差、删除稻草人引述改为准确表述），复核后重跑 §三 全部自证仍全绿。

- **复核未列出而由主代理自查发现的一项**＝§4.3 第 8 条版本枚举缺口（见 §二 末与 §三）。据实标注出处，不记入复核所获。
- **复核读数与终稿的一处差异（属预期）**＝复核切得的 §4.3 值 `c8375840…` 是**其读稿时**（第 8 条尚未扩写）的整节值；本批随后按自查结论改了第 8 条，故终稿改按「§4.3 第 1—7 条 `154ecaf8…` 全等＋第 8 条单列枚举差异」自证。两值不冲突，差在该第 8 条。

## 五、写面清单

| 件 | 改前→改后 | 内容 |
|---|---|---|
| `_专题_技能合集策划/行为与专业证据层冻结基准_v4.md` | 新建（终态 **59,014 B／LF 246／CR 0／sha256 前 12 `a0b625d621b5`**；对照 v3＝44,376 B／LF 223） | §二 整节重写（H-1a／H-1b ＋ 2A-补 五步规程 ＋ 归类基线表）＋§4.3 第 8 条枚举扩写＋§六 第 6 条 v4 注＋§七 v4 日志条＋头部定位段 |
| `程序文件/脚本/cg20260926_005_build_baseline_v4.py` | 新建 | v4 生成器：对 v3 定点替换（非切片重组，避免丢节间 gap）＋唯一锚命中数断言＋分节 sha256 自证 |
| `程序文件/脚本/cg20260926_005_h1_recompute.py` | 新建 | H-1a／H-1b 可复跑凭据＋三态差异归类＋逐件 commit 归因＋九项负向注入＋归类表从 v4 现读 |
| `程序文件/脚本/cg20260926_005_governance_patch.py` | 新建 | 登记面写入（头部快照轮换＋§九 新行＋台账 PL-049 转档）＋写后自检 |
| `_专题_技能合集策划/change-governance.md` | LF 614→**617**，CR 0 | 头部快照轮换＋§九 CG-20260926-005 行（§九 148→**149**）＋§11.2 台账 PL-049 状态转已闭合（**82 行不变**） |
| `_专题_技能合集策划/数据分类复核清单.json` | 由 `cg20260926_001_mirror_register.py` 复跑 | 治理镜像面按列判 N 重登记（改面即改指纹）：摘失效 2／新登 5 |
| `CHANGELOG.md` | 新增 v1.13.39 | 本批自述 |
| `成果/2026-09-25_项目全面审视与下一阶段执行方案.md` | §五 更新表一行 | 「冻结机制必要修订」行状态转已闭合（防下轮读成「未启动」，原 §五 首表两行按快照纪律不改写） |
| `文档/专业整改任务总单_v1.md` | §三 PL-049 条加状态注 | 原文不改写，注内说明该条三项要求各由 v4 何处满足 |
| `技能仓备份/` | sync 带平 | 三层同哈希 |

- **零改面**＝`行为与专业证据层冻结基准_v1/v2/v3.md`（前版冻结态）、15 技能目录全部内容件、契约／红线／索引／分类判据本体、`validate_governance.py` 与十二门禁判据、§九 历史 CG 行、§11.2 台账除 PL-049 外全部 81 行、`素材/`。

## 六、门禁终态（实跑，非预期值）

| 门禁 | 读数 |
|---|---|
| `validate_governance.py` | 总评 **PASS**｜通过 **243**／失败 **0**／警告 **3**，exit 0（警告构成同基线＝检查 3 聚合 1 条＋`A_VERIFY_DATE` 2 条） |
| 十二门禁（11 个 unittest 套件） | 全 **exit 0**、**合计 488 例**：verification 6／ace 176／pending 43／cross_layer_eol 25／index_ref 26／skill_cross_layer 21／data_class 70／storage_anchor 19／wetzone 16／sre 66／ic07 20（非零退出件清单＝空）；SRE 门禁缺口信号 0 条、跨轮指纹 `4f53cda18c2baa0c` 连击 14 轮 |
| 检查 10 残量 | 复跑 `cg20260926_001_mirror_register.py`：摘失效 2／新登 5，逐条＝line 9 paragraph（段数 1）头部新快照行／line 11 paragraph（段数 1）被降级的旧头部行／line 481 table_row（段数 8）§九 新行／line 501 raw（段数 1）裸「>」结构行／line 568 table_row（段数 9）台账 PL-049 改写行。**「段数」＝检查 10 的单元分段数，不是 Markdown 列数**（§九 表列数另由登记脚本自检＝6＝表头 6，见下行）；终态 records 8,722→**8,725**／baseline 3,780 不变 |
| 三层带平 | L1 ↔ 运行时 ↔ `技能仓备份/` 三处均 **1,107,191 B／LF 617／CR 0／sha256 前 12 `abd7f124bbb6`** |
| 登记面拼接自证 | 脚本写后自检：头部新行存在 True／旧行降级且唯一 True／§九 新行唯一 True 且列数 6＝表头 6／台账 PL-049 唯一且列数 7＝原 7、状态列读回「已闭合」、闭合凭据在位 True；台账 82 行、状态分布 已闭合 62／待处置 19／已裁定不做 1；§九 CG 行 149；行数 614→617（＋3＝头部两物理行＋§九 一行，台账行为原地改写）；CR 0→0 |
| `sync_skill_backup.py` | 带平首跑「范围 73｜一致 72｜新增 0｜更新 1｜移除 0」（更新件＝本批治理件）→ 门禁复跑后「更新 1」＝机器本地 `sre_regression_report.json` → 末次 `--check`「范围 73｜一致 73｜新增 0｜更新 0｜移除 0」 |

## 七、带平后 H-1b 首跑＝v4 差异归类规程的首个生效实例

登记面写入并三层带平后复算（凭据脚本同 §三）：

- H-1a 仍＝`751164809b54…`（**取物基准是 `36dee02` 的 blob，不受本批登记面影响**——这正是把「登记面写入」与「被测对象变化」分离的机制本身）；
- H-1b＝`b40a2b9f2f8bf7a628edd2ebaedc85cc898fb19b843abf4221f11b636eb0e8a2` ≠ H-1a；
- 三态×两档归类＝**字节漂移 1 件／登记面**，全等 71 件／行为面；
- 漂移件身份现读确认＝`shared/change-governance.md`；
- **触发口径读数＝行为面为空 ⇒ 不触发「另批升版重新冻结」，只把该件差异归因到 CG-20260926-005**。

⇒ 成果 §五 该行验收标准「能追溯完整环境，同时不把纯登记变化误当技术行为变化」由这一次实跑同时成立；而 v3 的单枚 H-1 在此场景下只能给出「恒真（看不见变化）」或「恒红（把登记写入当行为变化）」两种错误读数之一。

- **一处时序披露**：本件写于 commit 之前，故 §三 的 commit 归因表对本批自身产生的差异件读数为「未归因」（`git log 36dee02..HEAD` 尚无本批提交）。该读数属时序，非规程缺陷——commit 后归因即命中本批；对已提交区间（`08069b0..HEAD`）的 37 件差异，未归因件数 **0**。

## 八、复算凭据与提交态

- **复跑本批全部判据**（不依赖本记录的叙述）：`python -B 程序文件/脚本/cg20260926_005_h1_recompute.py`（主复算＋三态归类＋归因，exit 0）与 `… --selftest`（九项负向注入，exit 0）；分节全等自证＝`python -B 程序文件/脚本/cg20260926_005_build_baseline_v4.py`（幂等，重跑产同一 v4 字节并打印六节 sha256）。上一批记录 §七 所披露的「复算未固化为仓内脚本」缺口由本批闭合。
- **PL-031 阶段 1 实跑的连带影响（不属本批改面，据实提示）**＝该线取物须自本批起按 v4 §二 引用 **H-1a 与 H-1b 两枚**并标注基准版本 v4；§4.3 第 8 条枚举已随之扩写。既有按 v3 取物的实跑记录不改写，其版本标注由该条自身约束。
- commit 随本批（T1）；push 不随批（T2，须发起人当轮命名整句明文）。
