# CG-20260923-001 B 级线内复核报告（独立只读子代理复算）

**日期**＝2026-09-23　**批次**＝CG-20260923-001（B 级，数据分类与取证判据单源件发布）
**拓扑**＝主代理派发 1 个严格只读子代理复算本批自述终态读数；代理被明令不得写仓内／运行时文件、
不得 `git add/commit/stash/checkout`、不得跑写盘模式的 `sync_skill_backup.py` 与
`sre_regression_test.py`（后者会重写机器本地报告使只读 `--check` 假红）。
**独立性局限**（沿 CG-20260921-009 ⑧ 先例如实披露）＝复核代理由主代理编排并给定取证范围，
其「独立」限于取数与判定不复用主代理脚本，不等于第三方审计。
**判词入库纪律**＝§一 为代理输出原样搬运（脚本内联，非手抄、未改写），首轮结论不因主代理订正而追溯修改；
主代理的采纳／不采纳逐条列于 §二。

---

## §一　代理复算原文（原样入库）

# CG-20260923-001 线内只读复算报告

## 逐项复算（A1—A13）

| 项 | 自述 | 实算读数（本代理独立取得） | 判定 | 判据出处 |
|---|---|---|---|---|
| A1 | validate 总评 PASS／241／0／3／exit 0；警告 3＝检查 3 聚合 1＋检查 10 `A_VERIFY_DATE` 2（`:708`/`:709`） | 总评 PASS｜通过 **241**｜失败 **0**｜警告 **3**｜exit **0**；警告逐条＝「无有效核验日期 38 条」聚合 1 条（输出第 207 行）＋`acoustic-calculation-engine/reference.md:708 [A_VERIFY_DATE]`（第 347 行）＋`:709`（第 348 行），无第 4 条 | 真 | `python -B 程序文件/validate_governance.py`（本会话实跑，输出存 `_tmp-scripts` 后已自清） |
| A2 | 其余九门禁 Ran 6／90／66／20／43／25／26／21／70 全 OK、exit 0 | 本代理实跑：verification **6**／ace **90**／ic07 **20**／pending **43**／cross_layer_eol **25**／index_ref **26**／skill_cross_layer **21**／data_class **70**，全部 `OK`＋exit 0；**sre 66** 系主代理留档读数（`发布终态_十门禁.txt:75` `Ran 66 tests`），**非本代理实跑**（按纪律跳过） | 真（66 一项为档案件复核） | 各脚本实跑输出＋`_tmp-scripts/数据分类门禁_20260922_c17c636d/发布终态_十门禁.txt` |
| A3 | 三层 `change-governance.md` 887,848 B／CR 0／LF 531／无 BOM／`81301b8fdbfa35ae`；§九 本批行＝第 417 行单一物理行、行内未转义竖线 7；全文总行数 532 | 三层（L1／运行时 shared／镜像 shared）字节全等：**887,848 B｜CR 0｜LF 531｜无 BOM｜sha256 前 16 `81301b8fdbfa35ae`** 三对全等 ✓；第 417 行以 `\| CG-20260923-001 \|` 起、第 418 行为 `---`（非续行）＝单一物理行 ✓；逐字符扫描未转义竖线 **7**、转义竖线 **0** ✓；总行数：内容行 **531**（`wc -l`＝LF 数，文件以换行结尾），`split('\n')` 段数才为 532（末段为空串） | 部分真（唯「532」须钉明为 split 口径；其余全部实算吻合） | 二进制读＋`sha256(原始字节)`；逐字符竖线计数脚本（临时件已自清） |
| A4 | JSON 4,134,043 B／records 8,409／baseline 3,927（46 件）；抽 3 条核段区间从 0 连续无重叠 | **4,134,043 B** ✓；records（dict）**8,409** ✓；baseline（dict）**3,927** ✓；baseline 键 `#` 前文件段去重＝**46 件** ✓；且不止抽样——**全部 8,409 条**段区间按 start 排序后首段 start=0、相邻段 end==next start，非连续者 **0 条**。「末段 end==单元全文长」JSON 内无单元文本不可直核，由检查 10「缺口即 FAIL」断言＋data_class 门禁 70/70 通过间接背书 | 真 | `json.load` 全量遍历判据 |
| A5 | 发布集 33（改 28／新增 5）、缺件 0；5 处「写入后≠候选」＝validate／L1 data-class／运行时 data-class／运行时 change-gov／L1 清单，其余 28 件 sha==candidate | 计划 33 项目标逐件现读：存在 **33/33**、缺件 **0** ✓；`target_existed=false` 恰 **5 件**（与自述新增 5 件同集）、改 **28** ✓；但 sha≠candidate 实为 **6 件**、相等仅 **27 件**——多出的一件＝`治理L1/change-governance.md`（cand `064e9af4…`→现读 `81301b8f…`）。执行记录 §三表已自证该件「＝候选：否」并以「**除登记面**」括注把它排除在 5 之外，属口径约定而非隐瞒 | 部分真（「5 处／其余 28 件相等」仅在剔除登记面本体口径下成立） | `发布计划_实测.json` × 真实面 `sha256(原始字节)` 逐件比对 |
| A6 | 单源件三层同值 9,220 B／LF 70／`601689b28c1fd13d`；validator 173,941 B／LF 3,247／`77422d1f3811398c` | `data-classification.md` 三层各 **9,220 B｜CR 0｜LF 70｜`601689b28c1fd13d`** 全等 ✓；`validate_governance.py` **173,941 B｜LF 3,247｜`77422d1f3811398c`** ✓ | 真 | 二进制读＋哈希 |
| A7 | 版本行 v1.15.0；docstring「校验十项」含检查 10 条目；`CROSS_LAYER_SET` 8 件且单源件 note 无「草案」；检查 9「覆盖面边界」运行时文案无「7 件治理文件」 | `validate_governance.py:5`＝`v1.15.0` ✓；`:7`＝「校验十项」✓；`:71`＝检查 10「数据分类与来源一致性」条目 ✓；`CROSS_LAYER_SET` `:1563—1594` 逐数 **8 件**；单源件 note `:1574`＝「CG-20260923-001 发布：…单源件（检查 10 的判据来源）」，全文件「草案」出现 **0 处** ✓；检查 9 运行时文案 `:2569—2570`＝「声明表治理文件（现 8 件）」，不含「7 件治理文件」✓（「7 件」仅存于 docstring `:61—62` 与变更日志 `:205` 的「设立时 7 件」历史注记，属登记性文字非运行时文案） | 真 | grep/Read 现读行号 |
| A8 | AGENTS.md 仅 5 hunk（20 目录结构 8 份／25 构成行／50 活基线／56 第七门禁④／59 sync 判据＋第十门禁行）；CR 0、行尾与 HEAD 同形；历史归因未改写 | `git diff -U0` hunk 数＝**5**，位置 `@@ -20／-25／-50／-56／-59 +59,2 @@` 与自述逐一对应（第 5 hunk＝新插第十门禁行＋sync 判据 72→73）✓；现读 CR 0／LF 64，`HEAD` 前像 CR 0／LF 63＝两侧同为纯 LF、形制一致（差一行＝新插入）✓；第 50 行内「声明表 7 件治理文件」「比对 15 项」两串**均在**（历史归因保留）✓。注：第 56 行旧文「比对项恰 15」被本批改写为状态判据＋「`test_shape_matches_declaration` 设立时 15」注记——该改写本身即自述 5 hunk 之一，非静默抹除 | 真（OBS：15 的保留形态是「设立时 15」新注而非原句未动） | `git diff -U0 -- AGENTS.md`；`git cat-file blob HEAD:AGENTS.md`；二进制 CR/LF |
| A9 | CHANGELOG 新增 v1.13.16 一节 Δ+6,343 B，现读 409,953 B／CR 0／LF 1,026；节内数字全部可核 | 现读 **409,953 B｜CR 0｜LF 1,026** ✓（`:5` 起 v1.13.16 节存在）。**Δ 不符**：v1.13.16 节插入 27 行＝净 **+8,721 B**，文件对 HEAD 全差分 **+8,715 B**（含另 2 行历史行「真实姓名→`<user>`」净 −6 B，属未提交的上一批 CG-20260922-003 脱敏侧写，非本节新增）——**+6,343 与任何可复算口径都对不上**。节内其余数字逐项核对全中：33（改 28／新增 5）✓、缺件 0 ✓、237→241 归因 ✓（validate 输出第 248／249／262 行＝单源件 (A)(A)(B) 三条新 [OK]＋末行检查 10「数据分类覆盖与来源字面检查通过」1 条，恰 4）、范围 72→73 ✓（`SHARED_FILES` AST 现读 8 件、`--check` 73）、records 8,409／baseline 3,927 ✓、Ran 6／90／66／20／43／25／26／21／70 ✓、三层哈希 ✓、9,220／173,941 及前 16 ✓、「19 件内容件」✓（镜像层非 shared 差异件恰 19） | 部分真（唯 Δ+6,343 B 为假；另注意 diff 非纯单节新增） | `git diff -- CHANGELOG.md` 分 hunk 字节统计 |
| A10 | `sync_skill_backup.py --check`＝范围 73｜一致 73｜新增 0｜更新 0｜移除 0｜exit 0 | 只读模式实跑：**范围 73｜一致 73｜新增 0｜更新 0｜移除 0**，exit **0**，输出首行自注「检查模式（未写盘）」 | 真 | `python -B 程序文件/sync_skill_backup.py --check`（未用写盘模式） |
| A11 | (a) `素材/` 零条目；(b) 契约/红线两文件各 1 hunk 仅散文；(c) 技能件零带单位数值增删；(d) §九 历史行除 417／头部／PL-060 外逐字不变 | (a) `git status --porcelain` 58 条中「素材」**0** ✓；(b) 两文件各恰 **1 hunk**（`@@ -1230 +1230`／`@@ -246 +246`），单行改动、数字 token 仅新增日期（2026/09/22）零删除 ✓；(c) 21 件镜像差异件全文数字多重集：20 件净删除 0，`acoustic-calculation-engine/reference.md` 净删除 token 全部为**标准编号本体**（9775/520/564.1/412.1/11835/13350/50081/15762，移入新「来源」列或说明行）与 S 等级标号小数字，**带单位数值（dB/kg/MPa/mm/℃/kPa/MN/m·m³…）增删＝0** ✓（注：自述写「标准编号自身**年份**」，实测删的是编号本体非年份，措辞级偏差）；(d) 对 HEAD 前像按题设口径比对：**另有 7 处不同**——行 48／354／379（用户名脱敏）、行 416（CG-20260922-003 自登 §九行）、行 513／514（PL-058／059）、行 495（PL-044 行追加〔进度 CG-20260922-003〕注并去掉两处 `**` 粗体）——经逐字符归因**全部属未提交的上一批 -003 侧写**，本批（-20260923-001）自身对 §九 历史行零改写 | (a)(b)(c) 真；(d) 部分真（题设判据面被上一批未提交态污染；本批无违例，行 495 的粗体删除属 -003 批对历史行的文字侧编辑，归责不在本批） | `git status --porcelain`；`git diff -U0`；逐 hunk 数字 token 多重集；`git cat-file blob HEAD:<件>` + `difflib` 行级 | 
| A12 | 新断言非恒真；形制数钉 17 仍是覆盖面检测器 | 两处改动＝`test_shape_matches_declaration` 15→17 与 `test_real_repo_head_eol_form_is_clean` 的 `checked == len(files) − len(disclosed)`＋`checked > 0`。内存直调实读：`declared_git_visible`＝**17**、`run_report` checked＝**15**、fails 0、warns 0、disclosed＝{`data-classification.md@L1`, `@REPO_SHARED`}（2 处「HEAD 无该路径对象」INFO）→ 等式现为真；**抹掉披露面**（disclosed 置空）等式即假（15≠17）；**任一副本被判定器静默跳过**（checked−1）等式亦假——非恒真成立。形制数 17：由 8 件声明表逐层展开现算（7@L1＋2@REPO_ROOT＋8@REPO_SHARED），声明表增删任一件或某副本 git 可见性变化即改数复红——**仍是覆盖面变更检测器**；且与自述「commit 前 15／后 17」吻合（提交后 disclosed 清空、checked 转 17，等式仍守） | 真 | `程序文件/cross_layer_eol_test.py:273—300` 现读＋内存直调探针 |
| A13 | 两份记录件／§九 本批行／CHANGELOG v1.13.16 四者读数互致 | 四者对 33（28/5）、缺件 0、241/0/3 及警告构成、十门禁计数、三层哈希 81301b8f、9,220／601689b2、173,941／77422d1f、4,134,043、records 8,409／baseline 3,927（46 件）、范围 72→73、写态「73｜72｜0｜1｜0」全部**互相一致且与实算一致**。**唯一读数级不一致**：§九 PL-060 行（第 515 行）钉「候选扫描面 **12,334** 条中 **8,406** 条已由 records 判定、**3,928 条**／46 件冻结」，而清单终态与其余三面均写 **8,409／3,927**（总数现 12,336）——即 CHANGELOG ⑨、执行记录 §八、归因报告 §十三 的「冻结存量 **3,927** 条」与登记面该行自己的「**3,928 条**」相差 1（PL-060 行保留登记时点快照，行内无「截至登记时」字样）。**另发现一处措辞级残留（OBS）**：`程序文件/sync_skill_backup.py:5—6` 头部仍写「v1.6（2026-09-22，数据分类迁移批**草案**…正式登记随该批 CG 行）」，本批既已登记发布，sync 侧自述未随 F-a 四处订正一并刷新（七项门禁不钉此文案，不构成红） | 部分真（一处四面值差 3,928↔3,927＋sync 头「草案」残留；无「未判定写成已判定」类反例——检查 10 输出面用 `[??]`「未判定亦 FAIL」标签与「仅证明覆盖…不证明分类判断正确」边界披露，措辞与语义相符） | 四件现读＋行 515 全文；`grep` 数字串逐面比对 |

## 反例（自设注入，全部内存内／临时件，未触仓内文件）

- **R1（A12 状态判据非恒真）**：真实声明面直调得 `checked=15, disclosed=2, files=17`，等式真；将披露面人为清空→等式**假**；将 checked 减 1（模拟副本静默脱离判定）→等式**假**。证「改状态判据」仍守得住它声称要守的失效形态（执行记录 F-b 的「15 ≠ 17 − 0」反向自证独立复现成立）。
- **R2（A5 哈希对账有牙）**：本批第 6 件差异（L1 `change-governance.md`，candidate `064e9af4…` ≠ 现读 `81301b8f…`）即 sha 比对判据的真实失败样本——该判据不是恒真比对器，能对「计划快照后再写入」如实报差。
- **R3（A4 连续覆盖谓词有牙）**：对「段区间从 0 连续无重叠」谓词做合成注入——含缺口（10→11）／重叠（12/10）／首段非 0 三型，谓词分别报假，仅连续型报真；再对真实 8,409 条全量跑该谓词得非连续 0 条。证 A4 的「真」不是谓词恒真所致。
- **R4（A11(c) 带单位数值多重集有牙）**：同一比较器对真实「HEAD 前像 ↔ 现读」输出 `(空, 空)`；在内存中把 `| 岩棉 | 半填 | 1-3 dB` 篡改为 `2-4 dB` 后重跑→立即报 `{'4 dB':1}/{'3 dB':1}`。证「零带单位数值增删」是能被打破而实测未被打破的断言。
- **R5（A9 节尺寸的口径自查）**：按插入 hunk 逐字节累计＝8,694 B 内容＋27 换行＝8,721 B，与文件级 delta 8,715（−6 来自 −003 脱敏两行）交叉吻合，两条独立取数路径互证 Δ+6,343 B 为**不可归因读数**（仓内四件文档亦检索不到 6,343／409,953 字样，该数仅存于任务自述）。

## 总判

**PASS-with-OBS** —— 十门禁、三层哈希、发布集对账、承诺未做面反查等全部承重项经独立实跑／实算吻合且各判据经注入自证非恒真；但三处需登记侧订正或加注：①A9 的「Δ+6,343 B」实算为 +8,715 B（节本体 +8,721），②A5 的「写入后≠候选＝5 处／其余 28 件相等」严格量测为 6 处／27 件（仅「除登记面」口径下自洽，且执行记录表内已如实标否），③A13 的 §九 PL-060 行「3,928 条／8,406」与终态及另三面「3,927／8,409」相差 1 条未注口径；另有 sync 头「草案」残留（A13-OBS）与 A3「532 行」的 split 口径两处措辞级注记，均不动门禁结论。

---

## §二　主代理逐条处置（订正轮）

| # | 代理报出 | 主代理实算复核 | 处置 |
|---|---|---|---|
| D1 | A11(c)：§九 本行与 CHANGELOG 写「除序号位／标准编号**自身年份**／本批日期注外」，实算删的是编号本体非年份（措辞级偏差） | 复算净删除 token＝9775／520／564.1／412.1／11835／13350／50081 等，逐一对应 `EN 520`（旧 3→新 1）、`GB/T 9775`（旧 3→新 1）、`GB/T 13350`（旧 2→新 1）、`GB/T 50081`（旧 2→新 1）等编号，其中 564.1／412.1 是 `JG/T 564.1`／`JG/T 412.1` 的编号本体而非规定值；且旧计数高于新计数处即手抄面收为指针所致 | **采纳**：两处措辞改为「标准编号本体」并列净删除清单（`_第七轮_复算订正.py`，唯一锚＋命中数 1 断言） |
| D2 | A5：严格量测「写入后 ≠ 候选」＝6 件／相等 27 件，自述「（除登记面）5／问题计数 5」仅在排除 L1 登记面一层的口径下自洽 | 复算 6 件＝`程序文件/validate_governance.py`、L1 `change-governance.md`、L1 `data-classification.md`、L1 `数据分类复核清单.json`、运行时 `shared/data-classification.md`、运行时 `shared/change-governance.md`；执行记录 §三表内 row 1（L1 登记面）确已标「否」，但计数句把它排除掉了 | **采纳**：计数句改为「严格量测 6 件／相等 27 件，其中登记面 L1 与运行时 `shared/` 两层同因（同一登记动作的两次带平），故归因表并列 1 行、表内 5 行」 |
| D3 | A13：§九 PL-060 行钉「12,334／8,406／3,928」与终态及另三面「12,336／8,409／3,927」差 1 且行内无「登记时点」字样 —— **真实内部不一致** | 复算终态 records 8,409＋baseline 3,927＝12,336（46 件），与登记面该行不符；且该行自身每次编辑都会再动这两数，钉终态仍会漂 | **采纳并改判据**（不止改数字）：行内改为「登记时点读数＋本行终态不钉数（自指），承重凭据取状态判据『records＋baseline＝扫描面判定单元总数 且 检查 10 未判定 0 条』」，终态读数指向各批执行记录 §四现读 |
| D4 | A13-OBS：`程序文件/sync_skill_backup.py:5—6` 头部仍写「数据分类迁移批**草案**…正式登记随该批 CG 行」 | 现读确认残留（F-a 四处自述漂移订正未覆盖此件） | **采纳**：改标「2026-09-22 起稿、2026-09-23 随 CG-20260923-001 发布」并补范围判据 72→73 |
| D5 | A3：「总行 532」与 LF 531 差 1 未注口径 | 532＝`text.split('\n')` 的元素数，末行无行尾分隔符故比 LF 多 1，两者同值 | **采纳为口径注**：报告 §十二／§十三 两处各加 split 口径注（不改数） |
| D6 | A9＋R5：Δ+6,343 B 判「不可归因读数」（仓内四件文档检索不到该数） | 6,343＝409,953 − 403,610，即**本会话编辑前工作区态 → 写入后**的插入差额；代理取的是 vs HEAD 401,238 的口径，两者差 2,372 B 恰为上一批 CG-20260922-003 脱敏侧写的未提交量。代理的 8,715／8,721 与主代理的 6,343 各自可复算，**不是虚报，是两口径未并列** | **不采纳其定性，采纳其暴露的问题**：把两口径并列写进记录件（见 §三），后续自述一律注明口径而非只给一值 |
| D7 | 承重项 A1／A2／A4／A6／A7／A8／A10／A12 与 A11(a)(b)(d) 全部判「真」，R1—R4 注入自证三处新断言非恒真 | 主代理独立实算同值；A11(d) 代理另报「按题设口径另有 7 处不同，逐字符归因全属 -003 未提交侧写」，与本批零改写 §九 历史行的承诺不冲突 | **无需处置**：登记为已核；A11(d) 的 7 处污染面在 §三注明属上一批未提交态，不并入本批写面 |

**订正轮影响面**＝`_专题_技能合集策划/change-governance.md`（§九 本行＋PL-060 行两处，登记后三层带平并同批复登残量）、
`CHANGELOG.md`、`程序文件/sync_skill_backup.py`、`记录/` 两件本批记录件；
零改＝标准数值／公式常数／检测区间本体、契约与红线本体、十门禁判据、`技能仓备份/` 除带平外的内容。
