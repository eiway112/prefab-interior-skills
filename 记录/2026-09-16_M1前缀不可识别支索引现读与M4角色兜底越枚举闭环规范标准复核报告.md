# M1 前缀不可识别支索引现读 + M4 `角色` 兜底越枚举闭环（CG-20260916-009）规范标准复核报告

## 复核日期

2026-09-16（改码、取证、登记同日完成；四门禁末次实跑见末段）

## 复核对象与触发依据

| 项 | 内容 |
|---|---|
| 被测件 | 运行时 `~/.qoder/skills/prefab-standards-reviewer/sre_reasoner.py`（改前 `acfbf7069bcb3ad9`／37,158 B／833 行 → 改后 `a4096102b769945f`／39,645 B／864 行／CR 字节 0；版本头 v1.2→v1.3；7 处 hunk，+49／−18 行） |
| 载体 B | 同目录 `sre_regression_test.py`（改前 `163e2dbbd0e25c68`／43,374 B／887 行 → 改后 `9dca938096d18b67`／51,402 B／1,015 行／CR 0） |
| 规则 SOT | `_专题_技能合集策划/standards-reasoning-rules.md` §1.4（L1 `f596038084d09f88`／38,898 B／565 行 → `0bfb4f529c88133e`／43,491 B／568 行；版本 v1.2.2→v1.2.3；两层镜像随批带平至同哈希） |
| 触发依据 | CG-20260916-005 遗留 (d)「M4 `else` 兜底同类越枚举与 `角色` 断言覆盖面移交另案」＋ CG-20260916-006 遗留 (d) 同源；发起人 2026-09-16 以建单图片授权开本线并指明三项同批（① 两编号角色逐条裁定落合法值 ② `角色` 断言由单场景扩为全矩阵且配负向注入自证 ③ 裁定若动契约措辞则同批改契约） |
| 定级 | **A 级**。`change-governance.md` §4.5「推理算法步骤修改（M1 模块）＝ A 级」——本批不止改了 M4 兜底值（该行原判 B 级），改了 M1 未知处理支的判定顺序（降级前新增索引 §二 现读），故升档。发起人当场裁定「同批做，定 A 级」，满足 §二 第 3 步「S/A 级须复核 + 变更发起人确认」 |
| 授权面 | 运行时两文件 ＋ 其 `技能仓备份/` 镜像 ＋ 本套台账 ＋（当场追加裁定）`standards-reasoning-rules.md` 三层同步。**未授予** git commit；`standards-index.md` 本体、`interface-contracts.md`、`程序文件/validate_governance.py`、设计方案文档本批零改动 |
| 建单 ③ 前件判定 | **不成立，契约零改动**。IC-10 `角色` 五值枚举（`interface-contracts.md:2140` 模板行／`:2187` 字段约束表／`:2287-2289` Schema enum，件哈希 `581e83dd15a63cba`／148,832 B）在本批前后逐字未变——裁定只是把越界值收敛进既有五值，未新造角色语义 |

## 独立性声明与本次复核的局限（如实披露）

1. 本批复核由同一执行线在线完成（沿 CG-20260915-001、-002、-003、-004、-005 先例），**非独立第三方审阅**。为压低自证风险：`角色`／`权限`／`时间状态` 合法集与索引 §二 L1 成员集一律现读（契约与索引各自解析），测试侧 `_index_l1_ids()` 刻意**不取** `sre_reasoner.INDEX_L1_IDS`，使两侧同错时仍会暴露；另设反向注入 I1—I3 与合并注入作为断言绑定力的外部检验。
2. 本批未调用 prefab-standards-reviewer 的「技术指标表格核验」主功能：索引本体与技能 reference 未触及，无数值型指标改动。实际核验面为「分类结论是否可溯源、输出是否落契约枚举、守卫是否仍有绑定力、改动是否溢出到无关编号」四项。
3. 建单里的规模数字未直接采信：本轮在改码前以载体 B 的矩阵口径重测（见下表），与建单口径的差由 F-01 交代。

## 改前逐项实测（绑改前件哈希 `acfbf7069bcb3ad9`，配方可复算）

| 口径 | 组合 | 条目 | `角色` 越 IC-10 五值 | 分解 |
|---|---|---|---|---|
| 载体 B 矩阵（守卫实际遍历）：`ACTIVATION_TABLE` 34 对 × `LOCATIONS` 7 值 | **238** 次调用 | 2,289 条 | **84** 处 | `GB 18580-2025→reference` 70 ＋ `08J931→reference` 14 |
| 建单探测口径：34 对 × 19 个地点串 | 646 次调用 | 5,961 条 | 228 处 | 190 ＋ 38 |

两口径同因子：84 × (19/7) = 228、70 → 190、14 → 38，越枚举项逐地点等频，故建单的 19 值中多出的 12 个地点串对这两编号不产生新赋值路径（F-01）。

写面实测：`else: role = "reference"`（改前 `:630`）是唯一产出越界值的语句；`PREFIX_PATTERNS:48-52` 的五处 `"reference"` 系元组第 4 元，`:386` 解包为 `authority`、落 `:402 "权重": "reference"`，属 IC-10 `权限` 三值枚举合法成员，**不属本缺陷面，本批未改**（逐处复核确认）。连带面：`_role_priority()` 的 order 五值表不含 `reference`，故 84 处条目此前按未知优先级 99 参与 Step 5 排序。

## 改后逐项实测（同口径复算，绑改后件哈希 `a4096102b769945f`）

| 项 | 读数 |
|---|---|
| 载体 B 矩阵越枚举 | 84 → **0**（238 调用／2,289 条不变，无条目增减） |
| 建单口径越枚举 | 228 → **0** |
| `INDEX_L1_IDS` | 导入态 **15** 条；与测试侧 `_index_l1_ids()` 独立现读结果对称差 **0** |
| `GB 18580-2025` | `[L4, reference, reference]` → `[L1, red_line, mandatory_check]`，confidence 由 inferred 转 deterministic |
| `08J931` | `[L4, reference, reference]` → `[L3, reference, construction_guide]`（命中 §1.2「数字+J→图集」） |
| 无关编号抽样探针 | `GB 8624-2012`（索引 §五 产品标准 L4）、`SJG 159-2024`、`T/CSUS 40-2022`、`ZZ 9999-1999` 分类前后逐字段一致 |
| 爆炸半径（与改前件逐场景比对） | 5,655 个 (场景, 编号) 键在改前／改后两侧同时存在，变化 **216 键**，全部且仅为上述两个编号：`→[L1,red_line,mandatory_check]` 180 键、`→[L3,reference,construction_guide]` 36 键；其余 5,439 键零变化 |
| 载体 B | `Ran 32 → 37 / OK / exit 0`；`^\s*@unittest\.expectedFailure` 全文 **0 处**；缺口信号 **0** 条；跨轮指纹 `4f53cda18c2baa0c`（与 -005 基线同值，未漂移） |
| 另三门禁 | `validate_governance.py` 总评 **PASS｜通过 171｜失败 0｜警告 2｜exit 0**；`ace_regression_test.py` `Ran 90 OK`；`sr_ic07_compliance_test.py` `Ran 20 OK`；T-A1 名称差分维持 **14 处** |

## 反向注入取证（断言绑定力，逐项回退被测修复）

注入方式：内存中改 `sre_reasoner.py` 字节 → 子进程跑载体 B → `finally` 还原并以 sha256 断言逐字节回到 `a4096102b769945f`。四组全部还原通过。

| 案 | 注入内容 | 结果 | 复红用例 |
|---|---|---|---|
| I1 | M4 `else` 兜底值 `construction_guide` → `reference` | `Ran 37 / FAILED (failures=1) / exit 1` | `test_M4_fallback_role_stays_in_contract_enum` |
| I2 | 图集正则 `^\d+J(?=\d)` → `^\d+J\b` | `failures=2` | `test_atlas_j_series_matches_prefix`、`test_atlas_standard_gets_construction_guide` |
| I3 | 禁用索引 §二 现读支（`if matched is None and std_no_norm in INDEX_L1_IDS:` → 前置 `False and`） | `failures=2` | `test_index_section2_id_is_l1_not_demoted`、`test_index_section2_standard_gets_mandatory_check` |
| I1+I2+I3 | 三处同注＝完整回到改前语义 | `failures=6` | 上述五例 ＋ **`test_standard_item_schema`**（扩面后的全矩阵守卫） |

**关键读数**：单注入 I1 时全矩阵守卫不复红（见 F-03），单注入 I2 或 I3 时亦不复红（层级由 L1/L4 变 L4/L1 仍落合法枚举），只有三处同时回退到改前态才触发矩阵守卫——即建单所指「唯一一条正在发生的契约违反」的守卫绑定力，由**全矩阵遍历 + 负向注入用例**两层分工承担，缺一即漏。

## 逐项核验结果

| # | 核验项 | 判据 | 结论 |
|---|---|---|---|
| 1 | 两编号角色值是否新造语义 | `GB 18580-2025`→`mandatory_check` 出自 rules.md §3.2 Step 4 表首行「L1 标准中有适用的强制性条文」＋ §1.2 L1↔red_line 耦合；`08J931`→`construction_guide` 出自同表「L3 图集」；`else` 兜底 `construction_guide` 出自同表「L4 产品标准」——**三值均系既有条文的直接对应，零新造** | 通过（建单 ①「回 rules.md／契约找出处」满足） |
| 2 | 层级真值是否手抄 | 代码侧 `INDEX_L1_IDS` 由 `_load_standards_index()` 按 `##` 小节切块现读；测试侧 `_index_l1_ids()` 各自现读索引；两侧交叉核对 15 条零差异；`grep` 代码与测试内**无编号清单字面量副本** | 通过 |
| 3 | 为何不取「无 `/T` 即 L1」的宽正则 | 索引 §五 把 `GB 8624-2012` 钉为 L4 产品标准，宽正则将其误升为红线；改后该编号分类逐字段未变（抽样表） | 通过（窄判据优于宽判据的实证） |
| 4 | §1.4 措辞与代码是否同源 | 代码注释指向 `standards-index.md §二 / standards-reasoning-rules.md §1.4`；rules.md §1.4 第二行现含「先查索引 §二 → 命中即确定性分类 → 未命中才降级 → 降级不改 `角色` 取值域」四要素，与 `classify_standard()`／M4 `else` 支一一对应 | 通过 |
| 5 | 是否引入新的静默路径 | 现读命中与未命中两支均落 `classification` 证据对象（命中=deterministic、未命中=inferred），`return_trace` 可见；扩面矩阵内空族／崩溃 0 | 通过 |
| 6 | 兜底支在真实数据面是否仍被执行 | M1 修净后 238 组合内 `else` 支 0 次命中（I1 复红仅由合成编号用例达成）——属预期的「不可达防御支」，非死代码（未知前缀编号在 `DOMAINS` 扩展时仍会走到） | 通过（登记为 F-03 的分工依据） |
| 7 | 建单「不误伤」面是否守住 | `PREFIX_PATTERNS:48-52`／`:386`／`:402` 三处 `reference` 逐处未改（diff 仅 7 hunk，均不落在这些行）；`_role_priority()` order 表未改 | 通过 |
| 8 | 契约与索引本体 | `interface-contracts.md`（`581e83dd15a63cba`）、`standards-index.md` 本批写入 0 次（哈希与改前一致） | 通过（禁改面实核零写入） |

## 复核发现与处置

| 编号 | 级别 | 发现 | 处置 |
|---|---|---|---|
| F-01 | 低 | 建单规模 646／5,961／228 与守卫矩阵 238／2,289／84 不一致，若不交代即成两个真值 | 当场核清为地点维度差（19 串含 12 个对这两编号无差异的地点），两口径并列登记，比例因子 19/7 可复算 |
| F-02 | 高 | 测试侧共享解析器 `_table()` 两处缺陷：① 空行被当合法分隔行；② 行集不在首个非表格行截断，致同小节第二张表（索引 §二 末尾「官方核验记录」）的表头单元被当编号读入。新增的索引现读用例首跑即因此误红 7 条伪编号（`标准编号`／`---------`／`已官方核验` 等） | 当场修两处判据；修后两侧现读 15 条零差异、37 例全绿。此缺陷为既存（-004 起即在），本批扩面才暴露，不追溯改写历史批结论。**定性为复踩**：个人记忆《摘档/修净后的守卫须仍能失败》已登记「注入件必须与被测守卫同法取数、同字面匹配」，本例即测试侧自写 `_table()` 与运行时 `_iter_md_tables()` **不同法取数**所致，失效点不在判据未知而在落笔前未查自己踩过没有。反面证据是本批坚持的「两侧各自现读、不做副本」交叉核对设计——正是它让差异在首跑即显形，而非静默放行 |
| F-03 | 中 | M1 修净使 `else` 支在真实数据面不可达，扩面后的全矩阵断言对**该支**无绑定力（I1 注入下矩阵仍绿） | 由合成编号负向注入用例 `test_M4_fallback_role_stays_in_contract_enum` 承担该支绑定力，并按 I1 实测把分工写入台账；不得以「矩阵已全绿」主张兜底支被守住 |
| F-04 | 低 | `standards-reasoning-rules.md:3` 部署状态行仍写运行时镜像路径 `~/.qoderwork/skills/shared/`，实际为 `~/.qoder/skills/shared/` | 既存陈旧声明，与本批判据无关，不改写、登记移交（沿 -005「范围外登记不处置」） |
| F-05 | 程序性 | 建单授权写面为运行时两文件，M1 索引现读支的规格出处需改 rules.md §1.4 方不成「新造语义」，属扩范围 | 动登前暂停并向发起人申请，获裁定「同批做，定 A 级」；据此把定级由预判 B 升为 A，并按 §4.5 A 级要求核回归覆盖面（34×7 全矩阵 ⊇「至少 3 个典型场景，覆盖住宅/非住宅/地方标准激活」） |
| F-06 | 低 | `sync_skill_backup.py` 的同步范围固定为「15 个技能目录 ＋ 根治理文件 ＋ shared 7 文件」，凡落在 `技能仓备份/` 内而不属该范围的件会在首跑被当差异**移除**——本批置于 `技能仓备份/shared/` 的 `_pre20260916CG009` 即被删（读数「移除 1」）。与已登记的「不排除 `.pyc`」是同一范围判据的两个方向：前者虚增、后者误删 | 不改脚本（建单禁改面）。改为在 `AGENTS.md` 门禁表该行补记「仓内镜像侧不是 `_pre*` 备份件的留存位置，回退判据取 `git show HEAD:<path>`」，本报告的复算判据随之由「两份快照」改写为「运行时侧一份 ＋ git 前像三件」 |

## 遗留（登记不处置，须另行授权）

1. `DOMAINS` 死映射键：`environmental["医院/学校"]`（`elif "默认"` 支恒先命中，`GB 30981.1-2025`／`T/CSUS 03-2019` 永不出）、吊顶 `吊顶板材`、墙面 `板材`、`深圳` 四键同型；属 M3 Step 2 匹配顺序缺陷。
2. M4 规则 5 的 `"SJG" in std_no` 判据为死测试——规则 3「名称含`评价`」恒先命中 `SJG 159-2024`。
3. `程序文件/validate_governance.py:77` 的 `RUNTIME_SHARED_DIR` 声明后零引用，校验面不含跨层一致性比对（-006 已登记，本批 F-02 属其同源症状）；补该比对可防三层漂移复发，属结构性加固。
4. T-A1 名称差分余 14 处（含 `08J931`：SRE「隔声、吸声构造」／索引「建筑隔声与吸声构造」），依赖外部真值通道，慢线。
5. `sre_reasoner.py` 版本头本批已递增（v1.2→v1.3），-003—-008 六批共拖项就此闭合；`standards-reasoning-rules.md` 版本头同步至 v1.2.3。

## 回退判据

三层 `standards-reasoning-rules.md` 改前件：`git show HEAD:"_专题_技能合集策划/standards-reasoning-rules.md"`（`f596038084d09f88`／38,898 B）。运行时两文件改前件：`git show HEAD:技能仓备份/prefab-standards-reviewer/{sre_reasoner.py,sre_regression_test.py}`（`acfbf7069bcb3ad9`／37,158 B、`163e2dbbd0e25c68`／43,374 B）。本机 `_pre20260916CG009` 仅为镜像层落盘前快照，不版本化（沿 CG-20260916-007 裁定）。**实测现存一份**：原在 `~/.qoder/skills/shared/`（`f596038084d09f88`／38,898 B，留存）与 `技能仓备份/shared/`（已被 `sync_skill_backup.py` 首跑按范围外件**移除**，见 F-06）各落一份，故仓内无可回退快照，回退以提交号 ＋ `git revert` 为准。

## 三层镜像与收口读数（2026-09-16 收尾实跑）

| 件 | 三层读数 | 判据 |
|---|---|---|
| `change-governance.md` | L1 ＝ 运行时 `shared/` ＝ `技能仓备份/shared/`（收尾复算时一次读数为 `45bd73ee0bbd186e`／245,399 B） | 判据取**状态关系**「三份 sha256 前 16 位互等」，不写终值哈希：该文件在本报告写作后仍被继续编辑，随值写出的哈希必不自洽（依 -004 ⑧(b)） |
| `standards-reasoning-rules.md` | 同三层 ＝ `0bfb4f529c88133e`／43,491 B | 三份互等 |
| `sre_reasoner.py` | 运行时 ＝ `技能仓备份/` ＝ `a4096102b769945f`／39,645 B | 两层互等 |
| `sre_regression_test.py` | 运行时 ＝ `技能仓备份/` ＝ `9dca938096d18b67`／51,402 B | 两层互等 |
| `interface-contracts.md` | `581e83dd15a63cba`／148,832 B（未写入） | 与建单改前值一致 |
| CR 字节 | 上述六件全部 0 | 归一配方：裸字节计数，不做换行转换 |
| `sync_skill_backup.py` | 首跑「范围 122｜一致 118｜新增 0｜更新 4｜移除 1」→ 二跑「一致 122｜新增 0｜更新 0｜移除 0」 | 二跑归零即收敛；`.pyc` 已跑前删除 |
| 四门禁终读 | `validate_governance.py` **PASS｜171｜0｜2｜exit 0**（T-A1 14 处）／`ace_regression_test.py` `Ran 90 OK`／`sr_ic07_compliance_test.py` `Ran 20 OK`／`sre_regression_test.py` `Ran 37 OK` exit 0、缺口信号 0、指纹 `4f53cda18c2baa0c` | 与改前基线相比仅 SRE 用例数按设计升档 |

**未执行项（如实登记）**：git commit 未获授权，故本批成果现处工作树未入库态；`AGENTS.md` 门禁表 SRE 行已随批改写为 `Ran 37` 基线。
