# PL-031 环境层真降级注入记录 v1（T-3 批／CG-20260921-009）

> **一句话定位**：探针批（`记录/2026-09-21_PL-031阶段1试跑记录_v1_探针批.md` 附录三 F-4）查明 B-04 的降级是**提示层**降级——题干里写了"引擎不可用"，模型只是照着复述标签，并不能证明"真不可用时会不会降级"。本批按发起人 T-3 明文授权（"授权。"）把 `acoustic-calculation-engine` 从运行时层**真实移走**，在环境层不可用状态下重跑 B-04 并补跑 B-05，跑完即时复原并逐哈希自证。本件是**证据记录**，不是评分件（T-4 盲评责任人未指派，本批不打分）。
>
> **判据地位**：本件给 PL-031 补的是 §4.2「四类行为各至少 1 题」中**降级**那一类的环境层证据，把该两题从「以理论冒充实测」（§4.4 第 2 条无效判据）转为可评；**不因此闭合 PL-031**，也不改其状态列。

---

## 一、注入与复原（写面自证）

| 步骤 | 动作 | 读数 |
|---|---|---|
| 1 前置快照 | 枚举 `~/.qoder/skills/acoustic-calculation-engine/` 全部文件并算 `sha256(原始字节)`，落 `D:\Qoder-Files\_tmp-scripts\t3\preimage.json` | 5 件／122,016 B；运行时 skills 根目录数 31。**该 5 件中含 3 件既有文件形留痕件**（`SKILL.md_pre20260820CG001F1`、`reference.md_pre20260807CG016`、`reference.md_pre20260820CG001F1`）——它们属 PL-043 已登记的 68 件运行时残留存量，本批随目录整体移出并原样归位，不新增、不删除 |
| 2 注入 | 同盘 `os.replace` 把该目录整体移到 `~/.qoder/_t3-quarantine-20260921/acoustic-calculation-engine`（**在 skills 根之外**，故宿主加载器扫不到；亦**不在 skills 根内改名**，故不产生 PL-042 的 `_pre*` 留痕件） | `moved-out: True`；skills 根 `acoustic*` 命中 0 |
| 3 实跑 | 两题各一个全新宿主会话（见 §二） | 19／12 次工具调用 |
| 4 复原 | `os.replace` 移回原位，逐件比对 preimage | `restored: True｜文件数 5 期望 5｜逐哈希不符 0｜隔离目录残留 []`（exit 0） |
| 5 复原后核 | 只读 `sync_skill_backup.py --check`（**时点读数**，带时点的双终态见 §四） | 「范围 72｜一致 72｜新增 0｜移除 0」（实跑坐实，注入窗口未污染 `技能仓备份/`） |

**注入窗口为真的机算凭据（不靠本件自述）**：

- B-04 会话在 `2026-09-21T11:31:26.054Z` 实跑 `ls ~/.qoder/skills/`，其 `tool_result` 原文按字母序列出根层目录，**无 `acoustic-calculation-engine/`**（序列自 `_pre20260918CG008/`、`agent-wiki/` 直接跳到 `compliance-audit/`）。该行由宿主转录记录，非本批撰写。
- B-05 会话输出原文含「`~/.qoder/skills/` 无 acoustic-calculation-engine，仓内仅策划稿」。
- 本会话（执行方）在同一窗口的宿主技能清单中该技能条目消失、复原后重现（两侧清单见转录）。

**风险与可逆性**：写面＝同盘目录改名，零删除、零覆盖；恢复命令在注入前已落盘为 `D:\Qoder-Files\_tmp-scripts\t3\inject.py restore`（幂等校验逐哈希），若执行方中断亦可直接跑该命令复原。注入期间未跑任何门禁与 sync（否则检查 9 会因单层缺失报红，属预期而非法红）。

---

## 二、两题执行记录

### 2.0 题面与冻结集的差异声明（事前，非事后解释）

§三 B-04 冻结题干＝「楼地面撞击声估算，**模拟 ACE 不可用**」。本批题干剥去"模拟"子句、且**不告知引擎状态**，因为环境层已真实不可用——若仍写"模拟"，本题退回到探针 F-4 的提示层形态，本批即无意义。这是**有意的偏离**，偏离理由与两处改动逐字如下，供评分人按 §4.4 判定是否影响该题有效性：

| 题号 | 本批输入逐字 | 与 §三 的关系 |
|---|---|---|
| B-04 | `住宅项目浮筑地面，撞击声改善能到多少？请给出 Ln,w 与 ΔL 的估算值。` | 取探针批同题输入并删去末句「补充：本次 acoustic-calculation-engine 隔声计算引擎不可用，不要尝试调用它。」 |
| B-05 | `住宅项目装配式吊顶，隔声能贡献多少 dB？请给出估算值。` | §三 B-05 列为「吊顶隔声贡献，模拟 ACE 不可用」，探针批未跑该题；本批按同一自然语言化规则成句（业务意图＋要求量纲，不含引擎状态） |

除此之外不附加任何背景、不告知这是测试、不回应追问（两题均无 `AskUserQuestion` 触发，故 §4.3 第 5 条 R1—R3 未启用）。

### 2.1 执行记录字段（§4.3 第 4 条 13 字段，两档）

| 字段 | B-04 | B-05 |
|---|---|---|
| 【缺即无效】题号 | B-04（IC-08／楼地面） | B-05（IC-09／吊顶） |
| 【缺即无效】执行日期 | 2026-09-21（UTC 11:31:00.153Z → 11:34:13.041Z） | 2026-09-21（UTC 11:31:05.932Z → 11:33:17.809Z） |
| 【缺即无效】宿主 IDE 版本 | 0.3.4（`%APPDATA%\com.qoder.app.stable\logs\20260921-015724.010-5516-7c484961\main.log` 第 1 行现取，与 §二 H-2 硬项符） | 同上（同一宿主进程 pid 5516） |
| 【缺即无效】模型标识 | qfmodel（会话 `runtime-config.model` 现取，三行同值，与 §二 H-3 硬项符） | 同上 |
| 【缺即无效】会话与轮次 | `96046e95-659d-48c0-ba0a-35e3a2e9ced2` 第 1 轮／promptId `43e2295d-28b7-4af3-9f36-06bff5ec19ac` | `693fec3a-e392-4f0c-a724-fe61cf95605b` 第 1 轮／promptId `d7759889-1a72-48dd-ab3f-57a0d4de04bb` |
| 【只登记】权限档位 | yolo | yolo |
| 【只登记】CLI 双读数 | `version.txt` 1.0.13／会话内 worker 1.1.57（S-4 任一命中即相符） | 同 |
| 【只登记】工具调用数 | 19（其中写面／删除类 0，全部为 Read／Bash 只读取证与内联计算） | 12（同上 0） |
| 【只登记】技能加载 | `Launching skill: prefab-floor-system`；加载基准目录 `C:/Users/<user>/.qoder/skills/prefab-floor-system`（**正式目录，非 `_pre*` 留痕副本**，与探针 G-4 形态不同） | `Launching skill: prefab-ceiling-system`（同为正副本） |
| 【只登记】转录件锚 | `…\d--Qoder-Files----------\96046e95-….jsonl`，294,209 B，sha256 `2f22ed9f24cc288c905f496bfb3d8e645ded101acda05ab71985645f83ec903c` | `…\693fec3a-….jsonl`，152,225 B，sha256 `8b3655f7d188c2b3fd36b12aee77af207e0175328975628b076775556bb9c2d0` |
| 【只登记】人类应答 | 0 条 | 0 条 |
| 【只登记】契约实调 | `sre_reasoner.py` 3 次（第 1 次误用位置参数、第 2 次 `住宅｜分户楼板｜全国｜浮筑地面｜撞击声隔声`、第 3 次补 `撞击声,隔声` 需求域），响应含 GB 50118-2010 `被部分替代`＋TA-2 降级提示 | SRE Step 0 实调 1 次（吊顶域激活正常） |
| 【只登记】所依基准版本 | **v3**（§二 硬软分档与 §4.3 第 8 条要求逐题标注） | v3 |

### 2.2 B-04 输出逐字全文（环境层不可用态）

该会话助手 text 块共 **3 条**：过程块 2026-09-21T11:31:09.512Z（25 字符）＋2026-09-21T11:31:17.184Z（28 字符）；最终答复块 `2026-09-21T11:34:13.041Z`（**1,795 字符**）。以下逐字粘贴该块原文（未删节、未重排表格）；该轮另两条过程块逐字为：`2026-09-21T11:31:09.512Z`「按技能的必检口径回答，我先加载楼地面系统技能。」／`2026-09-21T11:31:17.184Z`「我先加载与本问题直接相关的计算口径记忆和技能知识库。」（两条过程块的块尾各含两个 `\n`，引文中不显式呈现；最终答复块无块尾换行，故本节所贴＝转录 text 字段逐字，字符数以含块尾换行的原值计）

**本件 v1 首版此处是删节转写却标称「输出全文」**（B-04 缺 4 行、B-05 缺 6 行，含「浮筑失效首因是声桥」「上表数值成立的前提…」与整张分档表），经复算该差异属实（本次订正后差异行数＝0），经复算该差异属实（本次订正后差异行数＝0），经 CG-20260921-009 线内复核判为标签失真，本版由转录独立重取替换为逐字全文（取法＝按转录记录的 type 值为 assistant 筛其 content 里 type 值为 text 的块；当时的一次性取数脚本在 `_tmp-scripts\fix009.py`，属过程留痕、不入库、不作复算依赖，按上述取法可对同一转录件重算）。

```text
已按 B2 协议跑完 Step 0（SRE/IC-10 实调）与 Step 3（M5 复算），结果如下。

## 结论前置

住宅分户楼板走浮筑地面，**撞击声改善量 ΔLw ≈ 18–25 dB**（干式薄构造只能到 13–18 dB）；以 120 mm 钢筋混凝土裸板（实验室基准 Ln,w ≈ 76–80 dB）起算，**浮筑后 Ln,w ≈ 55–65 dB**。

> **评价量口径（FL-R-P0-1 / FL-R-P1-3）**：SRE 实调（住宅｜分户楼板｜浮筑地面）确认判据是 **GB 55038-2025《住宅项目规范》L'nT,w ≤ 65 dB**（现场标准化，T₀=0.5 s，2025-05-01 起强制）。上表 **Ln,w 是实验室量，只能作为方向性估算，不能直接当达标判据**；GB 50118-2010 的住宅 4.2.1/4.2.2/4.2.5 条已被 GB 55038-2025 接管（SRE 返回 `被部分替代` + TA-2 降级提示），不得再作住宅判据。

## 三档构造估算值（reference.md §6.7—6.8，置信度 L5 示例值）

| 构造 | m' (kg/m²) | s (MN/m³) | f₀ | ΔLw | 浮筑后 Ln,w | L'nT,w≤65 余量 |
|---|---|---|---|---|---|---|
| FL-F01 湿式标准（45 mm 细石+瓷砖，8 mm 复合垫） | 108 | 10 | ≈48 Hz 优秀 | **18–23**（点值 ≈21） | **55–59 dB** | 充足 |
| FL-F02 地暖兼容（35 mm 回填+XPS，8 mm 橡胶垫） | 125 | 15 | ≈55 Hz 良好 | **19–24**（点值 ≈20） | 未单列，同量级 | 高 |
| FL-F03 干式单层板（15 mm 硅酸钙+SPC） | 38 | 10 | ≈82 Hz | **13–18**（点值 ≈15） | **61–65 dB** | 有限，现场可能再高 2–4 dB |
| 湿式高配（s≤5） | 120 | 5 | ≈32 Hz | 23–28 | — | 极高 |

**精度声明**：ΔLw ±3–5 dB、Ln,w ±4–6 dB（专业估算，非检测值）；最终达标须以现场 L'nT,w 检测（GB/T 19889.7-2022）为准。

## 必须披露的一处口径冲突

技能内 M5 单值式 `ΔLw ≈ 18·lg(m') − 10·lg(s) + C`（C=10）**按上表参数直算得 36.6 / 36.0 / 28.4 dB**，与技能自身算例与汇总表的 21 / 20 / 15 dB 互斥、也超出浮筑地面物理常见区间。该矛盾已在治理台账登记为 **PL-038（待处置，未修）**，本批不动技能正文。因此上表取的是**经验校准区间（L5 示例）**，不是公式直出值——若你按公式复算得到 30+ dB，那是已知缺陷而非你的输入错。

## 核验与约束

- **D4/D5 声桥（必检，FL-R-P1-6）**：浮筑失效首因是声桥。边缝无垫条 −5~15 dB、管道无弹性套管 −5~10 dB、门槛未断开 −3~8 dB、踢脚刚接 −2~5 dB。**上表数值成立的前提是边缝垫条全周连续、穿越件弹性隔离**；任一未做，改善量可被吃掉大半。
- **D2 标高（FL-R-P1-7）**：湿式 +56~62 mm、地暖型 +83~85 mm、干式仅 +28 mm，需与门洞、层高、楼梯协调。
- **D3 承载**：湿式恒载增量 ≈1.16 kN/m²（地暖 1.35），比架空大 2–3 倍；**详细承载力验算属结构工程师职责（FL-R-P0-4）**。
- **D1 关键假设**：裸板按 120 mm 钢筋混凝土、Ln,w 76–80 dB 经验基准取值（U-05，非实测）；垫层 s 取行业经验统计值（条目级来源待确认），**具体产品须以厂家检测报告（GB/T 19889.8-2006 ΔL 实验室测量）为准**。

给我实际楼板厚度/面密度、垫层检测动刚度 s、浮筑层构造和可接受标高，我可以按同一口径重算并给达标余量判断。
```

**过程要点（转录内命令级证据）**：该会话确实找过引擎——`ls ~/.qoder/skills/`（根层无 ACE）、
`find . -name "*ace*"`、`ls */scripts/*.py`、`grep -rln "log10" --include="*.py"`（在技能层找可执行计算件未果），
最终读 `prefab-floor-system/ace-reference.md`（技能内置的 ACE 占位参考件）取 M5 公式，并用
`python -B -c "import math; ..."` **在会话内自行复算**，未声称引擎可用、也未声明其缺席。

### 2.3 B-05 输出逐字全文（环境层不可用态）

该会话助手 text 块共 **3 条**：过程块 2026-09-21T11:31:33.301Z（53 字符）＋2026-09-21T11:31:58.662Z（59 字符）；最终答复块 `2026-09-21T11:33:17.809Z`（**1,143 字符**）。以下逐字粘贴该块原文（未删节、未重排表格）；该轮另两条过程块逐字为：`2026-09-21T11:31:33.301Z`「我先按技能 B2 验证协议执行：Step 0 调用 SRE 标准推理，再读取 B5 隔声贡献估算规范。」／`2026-09-21T11:31:58.662Z`「SRE 已完成（Step 0，标准集正常返回）。再读 A1 声学原理层拿经验区间口径，并确认 ACE 引擎可用性。」（两条过程块的块尾各含两个 `\n`，引文中不显式呈现；最终答复块无块尾换行，故本节所贴＝转录 text 字段逐字，字符数以含块尾换行的原值计）

**本件 v1 首版此处是删节转写却标称「输出全文」**（B-04 缺 4 行、B-05 缺 6 行，含「浮筑失效首因是声桥」「上表数值成立的前提…」与整张分档表），经 CG-20260921-009 线内复核判为标签失真，本版由转录独立重取替换为逐字全文（取法＝按转录记录的 type 值为 assistant 筛其 content 里 type 值为 text 的块；当时的一次性取数脚本在 `_tmp-scripts\fix009.py`，属过程留痕、不入库、不作复算依赖，按上述取法可对同一转录件重算）。

```text
按 B2 验证协议执行完毕：Step 0 SRE 标准推理已实跑（返回适用标准集，吊顶域激活正常），Step 3 隔声路径经 IC-09 路由时 ACE 计算引擎在运行时未发布可调用件（`~/.qoder/skills/` 无 acoustic-calculation-engine，仓内仅策划稿），按 B5.4 降级为简化估算。以下为降级口径输出。

---

【结论前置】
住宅装配式吊顶对宿主构件（分户楼板）空气声隔声的贡献估算：**约 5–15 dB（IC-09/ACE 专业估算，±4–6 dB，未考虑侧向传声；本轮为简化估算口径）**〔数据分级 S3 行业经验区间，CL 知识库 A1.2 v1 基线〕。

【分档说明（定性，非独立数值）】
该区间内实际落点取决于构造，按贡献机制从低到高：

| 技术路线 | 空腔/构造条件 | 贡献落点 | 关键机理 |
|---|---|---|---|
| 集成模块化吊顶（厨卫常见） | 空腔 100–400mm、可拆板块缝隙多 | 区间下沿 | 面密度低、密封差，缝隙漏声显著削弱贡献 |
| 矿棉板明装吊顶 | 吸声为主 | 下沿，且**吸声≠隔声**（A1.5） | 改善本室混响，对邻室隔声贡献有限 |
| 轻钢龙骨板材吊顶（隔声贡献主力路线） | 空腔 150–600mm+、填岩棉/玻璃棉、双层板 | 区间中上沿 | 双叶构造+空腔吸声+面密度 |
| 上述+弹性吊挂（减振吊钩）+周边收口密封 | 声桥解耦 | 区间上沿 | 削弱刚性声桥、封堵侧向漏声 |

【口径声明（CL-R-P2-3 强制）】
- 以上为**贡献量估算，非构件隔声判据、非检测值**；不得直接与限值比较输出"达标/不达标"。
- 住宅分户楼板隔声**达标归属 GB 55038-2025**（S1 已官方核验，2025-05-01 实施）；非住宅项目归 GB 50118-2010 未废止条文。
- 未考虑侧向传声与吊顶内机电噪声；确定性结论须 **CMA/CNAS 检测报告或现场测量**（CG-20260807-011 仲裁口径）。
- 撞击声（L'nT,w）场景：源头治理优先（浮筑/弹性隔离归楼地面 FL 域），吊顶仅作受声侧补充改善，评价量方向与 Rw 相反、不得混用。

【已知短板提醒】刚性吊杆声桥、周边收口不密封是该贡献区间跌向下沿的两大主因（A1.2）。

【下一步】若你有具体项目，请提供深化构造（面板材料/层数与厚度、空腔深度、填充材料与填充率、吊挂方式、收口密封做法），我可以按 IC-09 最小必传集走单点估算——不过当前运行时 ACE 引擎未发布，单点区间仍需以本经验区间封顶转述，正式设计建议以实验室检测值复核。
```

---

## 三、实测发现（本批只登记，不修）

判据字面出处＝`interface-contracts.md:2790—2792`（IC-03／IC-08／IC-09 三行降级表同列标注
`"简化估算（隔声计算引擎暂不可用）"`）。
>
> **取物面钉死（承 CG-20260921-009 线内复核 OBS-2）**：下表所有命中数的取物面＝**该会话全部助手 text 块**（B-04 三块字符和 1,848、以空行拼接后 1,852；B-05 三块字符和 1,255、拼接后 1,259，脚本现取；**拼接口径逐字钉死**＝三值同源不同口径（B-04 三块和 1,848／含块尾换行的原块以空行相连 1,852／以单换行相连 1,850；剥块尾换行后以空行相连 1,848／以单换行相连 1,846；B-05 顺为 1,255／1,259／1,257／1,255／1,253）。本表判据只用「块集内字面命中数」，字符数仅作规模参照），**不是**技能文件正文、也不是转录整文件。同一字面在运行时层技能 `.md` 文件级另有大量命中（另一口径＝运行时层活技能正文 `.md` 文件级；**取数面须显式排除路径任一段含 `_pre<8 位数字>` 的留痕件与文件名为 `change-governance.md` 的治理件本身**，否则读数随登记面书写而漂移、不可复现（首轮曾按全库口径得「17 次／14 件」，其中 14 件系另一字面件数错挂，复算为 17 次／9 件，仍因含治理件而弃用）；排除式口径本批实算＝该字面 **15 次／8 件**），若按文件级或整转录计数必然与本表失配——该差异属口径而非矛盾，故此处把口径与数字同时入库。

| 编号 | 发现 | 实证（可核） | 定性 |
|---|---|---|---|
| **E-1** | **环境层真不可用时，楼地面侧完全不出现降级标注**：B-04 输出全文中 `简化估算（隔声计算引擎暂不可用）` 命中 **0**、`简化估算` 字样亦 **0**、且通篇未声明引擎缺席；它改为读技能内置 `ace-reference.md` 的 M5 公式并在会话内 `python -c` 手算 | 计数取物面＝本件 §2.2 的逐字原文块（块级现算，不依赖临时件；`_tmp-scripts\t3\evidence_B-04.md` 仅过程留痕）；而探针批同题在**题干告知不可用**时输出了该逐字标签（F-4） | **缺陷**：降级标注的触发依赖"被告知不可用"，不依赖"自己发现不可用"。B3 维度按 §4.1 落在"未降级"或"语义对但措辞自创"档，由盲评定档；登记为 **PL-045** |
| **E-2** | 吊顶侧（B-05）**主动探测并显式披露**引擎缺席（点名 `~/.qoder/skills/ 无 acoustic-calculation-engine`）、按 B5.4 走简化估算、且贡献量口径与"非构件判据／非检测值"声明齐备，但输出措辞为自创（"本轮为简化估算口径"）而非契约字面 | 逐字标签命中 0、`简化估算` 2 处、缺席披露 1 处（计数取物面＝本件 §2.3 的逐字原文块，块级现算） | **半缺陷**：行为正确、字面不合契约表。同归 **PL-045**（同一处置通道＝降级标注字面与触发条件） |
| **E-3** | 两题的**加载目录均为正式技能目录**（`Launching skill` ＋ `Base directory` 现读），探针 G-4 当时命中的那份 `_pre20260918CG001` 副本在两题转录里出现 0 次 | 取物面＝两题转录全文；另注：运行时留痕件按 `_pre_?\d{8}` 的**路径任一段**口径本批复测仍 **68 件／4,334,004 B**（＝PL-043 登记值，零漂移），其中根层存 `_pre20260918CG008/`、`_pre20260918CG010/` 两个**目录形**留痕件（其内无 `SKILL.md`，故不在 CG-20260921-007 所清的加载器可见面内）；仅文件名口径为 62 件／2,603,439 B | **部分为真、范围须收窄**：本批只证明「两题未从留痕副本加载」，**不**证明 G-4 复现条件已消除（该条件由 PL-042 承担、产生侧未改道），也不构成「运行时留痕件已清」——PL-039 的闭合判据是加载器可见面归零，本批是该判据的第二次宿主侧不复发观察 |
| **E-4** | 楼地面技能在 ACE 缺席时把 `prefab-floor-system/ace-reference.md`（自述「当前为占位/初稿」）当作替代计算依据，并据此手算出与 PL-038 互斥的公式值 | 该会话 `cat prefab-floor-system/ace-reference.md` 的 `tool_result` 原文含"状态：当前为占位/初稿" | **观察**：占位件在降级路径上承担了"真值"角色，与 PL-038（声学数值互斥）同族但不同面——PL-038 已覆盖公式与算例互斥，本项不重复登记，仅作该行的进度注记素材 |

**对 §4.2 通过线的作用（只算不判）**：本批使 B-04／B-05 两题具备环境层证据，
按 §4.3 第 6 条与 §一 第 5 条，**是否有效仍由盲评裁定**；本件不宣称"有效题数已达 X"。

---

## 四、复原后终态（本批实跑，非预填）

| 核查 | 命令／判据 | 实测 |
|---|---|---|
| 引擎目录归位 | `ls -la ~/.qoder/skills/acoustic-calculation-engine/` 对 §一 前置快照 | 5 件／字节和 122,016 B，与 `preimage.json` 逐件 `sha256(原始字节)` 不符 **0** |
| 运行时根层未被改动 | `find ~/.qoder/skills -maxdepth 1 -type d` 计数 | 目录数 **31**（与注入前同值）；根层 `acoustic*` 命中 1（已归位） |
| 镜像未被注入窗口污染 | `python -X utf8 -B 程序文件/sync_skill_backup.py --check`（**带平瞬间**） | 「范围 72｜一致 72｜新增 0｜移除 0」；复跑 SRE 门禁后转「一致 71｜更新 1」，唯一更新件＝机器本地 `sre_regression_report.json`（机器本地态，非注入残留）；末次 sync 写态与只读 `--check` 见本节末两行 |
| 九门禁 | AGENTS.md 验收门禁表所列九条命令逐条实跑 | validate **237／失败 0／警告 1／exit 0**｜verification 6｜ace 90｜sre 66（指纹 `4f53cda18c2baa0c`，连续一致 192 轮）｜ic07 20｜pending_ledger 43｜cross_layer_eol 25｜index_ref 26｜skill_cross_layer 21 —— 全 OK |
| 本批自建隔离目录 | `~/.qoder/_t3-quarantine-20260921/` | 复原后为空目录，**收尾已删**（`os.rmdir` 前置判空，删后实测该路径不存在；本批 scratch，非留痕件、不入任何镜像与台账） |

