# 装配式装修技能合集 — 标准推理引擎规则

> **文件定位**：合集级共享资源，定义标准推理引擎（SRE）的分类协议、锚定集、场景推理链和适用性裁判规则。路径：`shared/standards-reasoning-rules.md`。部署状态（2026-08-08 发布，CG-20260808-025）：L1 开发源 = 项目仓 `_专题_技能合集策划/standards-reasoning-rules.md`，运行时只读镜像 = `~/.qoder/skills/shared/standards-reasoning-rules.md`；镜像内容修订须经 change-governance.md 流程定级审批，由 L1 修改后字节级同步。
>
> **版本**：v1.2.6（2026-09-17，CG-20260917-009：复查到期与标准效力解耦；§五 M6 分离依据不足降级与到期提醒，运行时 v1.7；IC-10 v1.9.0 不变）
>
> **历史版本说明**：v1.2.5（2026-09-17：核验时效降级判据接入生效路径（CG-20260917-006，P1 item 9/10，遗漏A＋遗漏E）——① §五 M6 表「标准状态为"待核验"→降低输出确定性」行（此前**无代码承担者**：门禁时效支锚点 `^#.*官方核验记录` 命中 0 处而恒真空跑、运行时不读核验字段）改写为机器可判据口径：`核验状态` 不以「已官方核验」开头／无 `核验日期` 记录／核验日期超分档阈值（强制性国标 92 天、其余标准 183 天，严格大于）三条任一命中 → applicability 证据 `置信度` deterministic 降 `inferred` ＋ 追加 `degradation` 证据，**复用既有降级语义、不新造枚举**（IC-10 items 为 `additionalProperties: false`，不得给 item 加字段）；并明文记账「补官方核验」与「降低确定性」为**等价出口**，无 `核验日期` 者按**已超期**处理而非"免检"（整改方案 §6.2 账本纪律）；② 表下新增注：阈值与字段真值源为 standards-index.md §1.1A、本文件不存数值副本，到期由机器从「核验日期」列现算、索引散文不再承载，核验记录表与状态主表**按列名区分并互斥读取**（T-B4／T-B7 根因），并列编号单元只按全角 `／` 切分（半角 `/` 属 `GB/T`、`DBJ/T` 本体）。代码侧同批 `sre_reasoner.py` v1.5→v1.6、`validate_governance.py` 检查 3 分档台账；全矩阵取证 238 调用／2,387 条目，**适用标准集条目面 0 变化**，deterministic 6,130→4,312、degradation 证据 210→2,028。M1/M2 锚定集 13 条/§3.2 激活表与五步算法/§七 结构化规则包不动，interface-contracts.md 零改动。v1.2.4 = 2026-09-17：第三方审阅整改 P0 断点6/断点5 判据同步（CG-20260917-002，遗漏A）——① §3.1 输入「性能需求」补 `吸声`/`撞击声`（原仅 §3.2 Step 1a 逻辑含此二值、输入声明与 IC-10 枚举缺）；② §3.2 Step 1 激活表「厨卫湿区」由误置项目槽 `（厨卫湿区｜墙板）` 校正为空间槽 `（住宅｜厨卫湿区）`，并加「空间维度归一」注（卫生间/厨房经 SPACE_ALIASES 归一化命中）；③ §五 M6 降级表新增「激活域命中外协占位」行——外协不得静默过滤，须以结构化 `外部协同` 条目对外可见 + degradation 证据，话术取「已迁出至独立项目」而非「待建」。§1.2 DB/DBJ=L2 本已正确（断点7 真值源，代码侧 L3→L2 校正见 CG-20260917-002），§1.3 五步算法、M2 锚定集 13 条、§七 结构化规则包不动。v1.2.3 = 2026-09-16：§1.4「前缀不可识别」支新增降级前置查表——先查 standards-index.md §二 L1 主表，命中即按 §1.2 的 L1↔red_line 耦合确定性分类，未命中才降级；同时澄清降级不改变 `角色` 字段取值域（L4 降级件即 Step 4 表的 construction_guide）。v1.2.2 = 2026-09-15：T-A2 孤儿引用闭合——§2.4 目录计数 67→69、§3.2 environmental 医院/学校 标准族由 GB 18582-2020（官方已废止）改指 GB 30981.1-2025（2026-06-01 实施），零推理算法、零锚定集变更；v1.2.1 = 2026-08-13：维护规则文义矛盾修复——"变更级别不低于 A 级"笼统表述修订为按 change-governance.md §4.5 分类定级，闭合 CG-20260807-019 留档观察项，零推理逻辑变更；v1.2 = 2026-08-13 引擎化第一阶段；v1.1 = 2026-08-08；v1.0 = 2026-06-21 初稿）
>
> **定位与成熟度声明（2026-08-13）**：SRE 进入**引擎化第一阶段**。本文件仍保留 LLM 可读的声明式规则文本，但同步定义结构化规则包（structured rule package）、程序化推理器接口、证据对象（evidence object）与决策轨迹（decision trace）格式，使 IC-10 响应可被机器校验和复现。`prefab-standards-reviewer/sre_reasoner.py` 为参考实现，回归测试集覆盖 M1/M3/M4/M6 核心场景。在引擎化完全成熟前：按编号前缀的分类结论仍标注为 syntactic_classification；安全类与合规类结论必须保留条文级证据并经人工复核；applicability_decision 须由调用方在 SRE 输出基础上结合项目具体约束做出。各文档中"推理引擎"与"推理规则"两种表述均指向本文件，以本声明为准。
>
> **维护规则**：本文件的修改须经 prefab-standards-reviewer 复核，并按 change-governance.md §4.5 分类定级审批：锚定标准废止/替代 = S 级；锚定新增入选、推理算法步骤修改（M1/M3）= A 级；M4 裁判规则修改、领域激活表扩展、M6 降级协议修改、锚定降级 = B 级。历史表述"变更级别不低于 A 级"已按 CG-20260807-019 审查结论修订为本分类路由。
>
> **最后更新**：2026-09-17（v1.2.6，CG-20260917-009：依据不足仍降级，单纯复查到期仅提醒；不修改适用标准成员、标准效力或条文替代数据）。
>
> **此前更新（历史快照）**：2026-09-17（v1.2.5 核验时效降级判据接入生效路径，A 级，CG-20260917-006：§五 M6 表「标准状态为"待核验"→降低输出确定性」行改写为机器可判据口径并新增表下真值源注——此前该行**无代码承担者**（门禁 `validate_governance.py` 检查 3 时效支以 `^#.*官方核验记录` 为锚点，而索引里该记录是粗体行非 `#` 标题，锚点命中 0 处 → 无条件报 ok，属恒真空跑守卫；运行时侧完全不读核验字段），到期实际由索引正文散文承载。本批：三条触发（核验状态不以「已官方核验」开头／无核验日期／超 92·183 天档）→ applicability 证据降 `inferred` ＋ `degradation` 证据，复用既有降级语义不新造枚举，无核验日期者按已超期处理而非免检，「补官方核验」与「降低确定性」为等价出口；代码侧 `sre_reasoner.py` v1.5→v1.6（新增 `STANDARD_VERIFY` 台账＋`_classify_verify_table()`＋`_verification_confirmed()`，`_apply_applicability()` 接入且不改 item 任何字段）、`validate_governance.py` 检查 3 改为分档台账现算（警告 2→3、通过 195 不变、失败 0／exit 0）；全矩阵取证 238 调用／2,387 条目，适用标准集条目面 0 变化。此前 2026-09-17：v1.2.4 第三方审阅整改 P0 断点5/6/7 判据同步，A 级，CG-20260917-002：§3.1 输入「性能需求」补 `吸声`/`撞击声`、§3.2 Step 1 激活表「厨卫湿区」校正为空间槽并加空间维度归一注、§五 M6 新增「激活域命中外协占位」行（不得静默过滤、须结构化 `外部协同` 条目＋degradation 证据、话术取「已迁出至独立项目」），§1.2 DB/DBJ=L2 为断点7 真值源而代码侧由 L3 校正为 L2；IC-10 升 v1.9.0；守卫 44→49 例（**该批次未同步本「最后更新」行，本批补齐**）。此前 2026-09-16（v1.2.3 M1 未知处理降级前置查表，A 级，CG-20260916-009：§1.4「前缀不可识别」支改为先查 standards-index.md §二 L1 主表命中即确定性分类、未命中才降级，并澄清降级不改变 `角色` 取值域；同批代码侧 `sre_reasoner.py` 三处修复——图集正则 `^\d+J\b` 改 `^\d+J(?=\d)`（§1.2「数字+J→图集」的既有语义此前实现未命中）、`_load_standards_index()` 按 ## 小节切块现读 §二 L1 成员集（代码内不维护编号清单副本）、M4 Step 4 `else` 兜底值由越 IC-10 枚举的 `reference` 改为 §3.2 Step 4 表 L4 对应的 `construction_guide`；§1.3 五步算法结构、M2 锚定集 13 条、§七 结构化规则包不动；IC-10 `角色` 五值枚举未变，interface-contracts.md 零改动；此前 2026-09-15：v1.2.2 T-A2 孤儿引用闭合，S 级批次 CG-20260915-002：standards-index.md 主表补录序号 68 GB 30981.1-2025、§7.2 登记 GB 18582-2020 已废止；本文件 §2.4 目录计数同步 67→69、§3.2 environmental 医院/学校 标准族改指 GB 30981.1-2025，M1/M3 算法与 13 条锚定集不动；此前 2026-08-13：v1.2.1 维护规则文义矛盾修复，B 级，CG-20260813-038：头部维护规则"变更级别不低于 A 级"修订为按 change-governance.md §4.5 分类定级路由，闭合 CG-20260807-019 留档观察项；零推理逻辑变更，M1-M6 规则与锚定集不动。此前：v1.2 引擎化第一阶段，A 级，CG-20260813-035：① 新增§七结构化规则包与引擎化规范，定义规则包组成、推理器接口、证据对象、决策轨迹、引擎化成熟度分级与 IC-10 同步要求；② 头部成熟度声明升级为"引擎化第一阶段"；③ 配套实现 `prefab-standards-reviewer/sre_reasoner.py` 参考推理器与 `sre_regression_test.py` 回归测试集；④ interface-contracts.md IC-10 同步升级至 v1.5.8；⑤ 注册 SRE 专属红线。更早：2026-08-08 v1.1 发布批次 CG-20260808-025；记录见 §八变更日志）
>
> **依赖关系**：本文件定义推理逻辑，`standards-index.md` 提供版本数据，`interface-contracts.md` 定义 IC-07/IC-10 接口。三者共同构成标准管理基础设施。

---

## 一、标准分类协议（M1）

### 1.1 设计原则

中国建设领域标准编号遵循确定性编码规则。SRE 通过解析编号前缀即可判定标准的类型、层级、权限和地域范围，无需预知标准内容。这使得技能面对从未见过的标准编号时仍能正确分类。

### 1.2 编号模式语法

```
标准编号 := 前缀 [空格] 序号 [-分部] [-年份]
```

| 前缀模式 | 标准类型 | 默认层级 | 默认权限 | 地域范围 |
|---------|---------|---------|---------|---------|
| `GB 55xxx` | 全文强制性国标 | L1 | red_line | 全国 |
| `GB 50xxx` / `GB 5xxxx` | 含强制性条文的国标 | L1 | red_line | 全国 |
| `GB/T` | 推荐性国标 | L2 | binding_support | 全国 |
| `JG` / `JGJ` | 行业标准（建设） | L2 | binding_support | 全国 |
| `JG/T` / `JGJ/T` | 推荐性行业标准（建设） | L2 | binding_support | 全国 |
| `JC` / `JC/T` | 行业标准（建材） | L2 | binding_support | 全国 |
| `HG` / `HG/T` | 行业标准（化工） | L2 | binding_support | 全国 |
| `DB` + 省份代码 | 地方标准 | L2 | binding_support | 对应省份 |
| `DBJ` + 省份代码 | 地方建设标准 | L2 或 L4 | binding_support（设计/验收）或 reference（产品应用） | 对应省份 |
| `T/` + 机构代码 | 团体标准 | L2 | reference | 全国（适用性取决于管辖地是否采纳） |
| 数字+`CJ` / 数字+`J` / 数字+`ZJ` | 图集 | L3 | reference | 全国或区域 |
| `RISN-TG` | 技术导则 | L2 | reference | 全国 |

**省份代码对照表**（常用）：

| 代码 | 省/市 | 代码 | 省/市 | 代码 | 省/市 |
|------|------|------|------|------|------|
| 11 | 北京 | 31 | 上海 | 44 | 广东 |
| 33 | 浙江 | 35 | 福建 | 46 | 海南 |
| 32 | 江苏 | 61 | 陕西 | 41 | 河南 |

### 1.3 五步分类算法

对任何标准编号字符串 S，执行以下步骤：

**Step 1 — 解析前缀**：提取首个空格或数字序列之前的前缀，匹配 §1.2 语法表。输出：`standard_type`。

**Step 2 — 确定层级**：按 §1.2 表映射 L1-L4。

**Step 3 — 确定地域范围**：
- `DB`/`DBJ` 前缀：**优先从 `standards-index.md` §5.3 的"适用地区"列现读**（DBJ 系编号段如 `DBJ/T 15`、`DBJ/T 13` 是地方建设标准自有序号，与 GB 行政区划码无关，按编号取模会误判，如 `DBJ/T 15`→广东、`DBJ/T 13`→福建）；仅当索引未覆盖该编号时，才回退到提取前缀省份代码查省份对照表，并标注"待确认省份"。
- 国标/行标：全国
- 团体标准：全国（但适用性由 §五 LA 规则判定）

**Step 4 — 确定性能领域**：从编号段和名称关键词推断：

| 编号段/关键词 | 性能领域 |
|-------------|---------|
| GB 55037, GB 50016, GB 50222, 07J905, "防火""耐火""燃烧" | fire |
| GB 50118, GB 55038, GB/T 50121, GB/T 19889, GB/T 45305, "隔声""声学" | acoustic |
| GB 55031, GB 50210, GB 55032, "验收""质量" | acceptance |
| JGJ/T 491, DB11/T 1553, DB33/T, "装配式""装配化" | prefab |
| GB 8624, GB/T 23451, JG/T 544, "产品" | product |
| "室内""空气""甲醛" GB 18580 | environmental |

**Step 5 — 组合分类结果**：

```
{
  标准编号: "GB 55038-2025",
  标准类型: "全文强制性国标",
  层级: "L1",
  权限: "red_line",
  地域范围: "全国",
  性能领域: ["acoustic", "acceptance"],
  分类置信度: "deterministic"
}
```

### 1.4 未知标准处理

**前缀不可识别 ≠ 层级未知**：§1.2 表按编号段枚举强制性国标（`GB 55xxx` / `GB 5xxxx`），无 `/T` 的四、五位编号（如 GB 18580-2025、GB 6566-2010、GB 30981.1-2025）不在表内。此类编号降级会把红线标准降为参考级，故降级前须先查 `standards-index.md` §二「第一层级：强制性国标（红线标准）」主表：命中即按 §1.2 的 L1↔red_line 耦合确定性分类，未命中才落入下表第二行的降级处理。层级真值只从该表读取，规则与实现内均不维护编号清单副本。

| 情况 | 处理方式 |
|------|---------|
| 前缀可识别但编号不在 standards-index.md 中 | 确定性分类（confidence: deterministic），标注"未在索引中收录，建议通过官方渠道核实状态" |
| 前缀不可识别（新行业代码等），且索引 §二 未收录 | 降级分类：authority=reference，confidence=inferred，标注"分类基于推断，请核实"；降级不改变 `角色` 字段的合法取值域，Step 4 仍须按 §3.2 表在 IC-10 `角色` 五值内分配（L4 降级件即 construction_guide） |
| 编号格式异常（无法解析） | 拒绝分类，返回"编号格式异常，无法自动分类，请人工核实" |

### 1.5 特殊规则

| 规则 | 说明 |
|------|------|
| GB 55xxx 系列 | 无论主题，一律 L1 red_line。这是 2022 年后全文强制性标准的编号段 |
| GB 50xxx 系列 | L1 red_line，但须检查是否已被 GB 55xxx 部分替代 |
| "xxxx年版"标注 | 标准编号中的年份不变（如 GB 50016-2014），修订年份作为元数据（2018年版） |
| 同编号不同年份 | 分类相同，状态不同。须查 standards-index.md 确认时间状态 |

---

## 二、最小锚定集（M2）

### 2.1 锚定标准的角色

锚定标准不是"最重要的标准"，而是定义推理坐标系的基准点。它们校准每一层级的权限基准，为场景推理链提供确定性起点。

### 2.2 入选准则（满足至少一条）

| 准则 | 说明 |
|------|------|
| C1 层级定义性 | 该层级的典型范例，定义该层的权限基准和编号模式 |
| C2 跨技能引用 | 3 个以上子技能的核心引用标准 |
| C3 冲突裁决性 | 在其性能领域内裁决权限冲突（替代/优先关系） |
| C4 校准承载性 | 为 ACE 或其他计算引擎提供"计算值 vs 标准限值"的边界参考 |

### 2.3 锚定标准清单（13 条）

**L1 锚定（强制性国标，5 条）**

| 锚定ID | 标准编号 | 入选准则 | 校准角色 |
|--------|---------|---------|---------|
| A-L1-01 | GB 55031-2022 | C1+C2 | 民用建筑通用要求总纲，定义 L1 层级的权限基准 |
| A-L1-02 | GB 55037-2022 | C1+C3 | 防火通用规范，防火领域的最终裁决标准 |
| A-L1-03 | GB 55038-2025 | C3+C2 | 住宅项目规范，替代 GB 50118 住宅隔声条文，定义 DnT,w+C 强制指标 |
| A-L1-04 | GB 50210-2018 | C2 | 装修验收标准，全部品技能的验收基准 |
| A-L1-05 | GB 50118-2010 | C1+C4 | 隔声设计规范，非住宅条文仍有效，定义各建筑类型隔声阈值 |

**L2 锚定（推荐性标准/行业标准，4 条）**

| 锚定ID | 标准编号 | 入选准则 | 校准角色 |
|--------|---------|---------|---------|
| A-L2-01 | GB/T 50121-2005 | C4 | 隔声评价标准，定义 Rw/C/Ctr/DnT,w 等评价量，ACE 输出必须标注的量 |
| A-L2-02 | JGJ/T 491-2021 | C1+C2 | 装配式内装技术标准，行标层级的权限基准，术语定义来源 |
| A-L2-03 | DB11/T 1553-2025 | C1 | 北京地标，校准"地方标准地域推理"的认知基准 |
| A-L2-04 | T/CSUS 40-2022 | C1 | 声环境团标，校准"团体标准权限推理"的认知基准 |

**L3 锚定（图集，2 条）**

| 锚定ID | 图集编号 | 入选准则 | 校准角色 |
|--------|---------|---------|---------|
| A-L3-01 | 07CJ03-1 | C1 | 轻钢龙骨石膏板隔墙/吊顶，图集类型的权限基准 |
| A-L3-02 | 08J931 | C2 | 隔声与吸声构造，隔墙/楼地面/吊顶三技能共同引用 |

**L4 锚定（产品标准，2 条）**

| 锚定ID | 标准编号 | 入选准则 | 校准角色 |
|--------|---------|---------|---------|
| A-L4-01 | GB/T 23451-2023 | C1 | 轻质隔墙条板，产品标准类型的权限基准 |
| A-L4-02 | GB 8624-2012 | C2 | 燃烧性能分级，全部品防火等级基准 |

### 2.4 锚定集与 standards-index.md 的关系

- 锚定是 standards-index.md 条目的**属性标记**（`is_anchor: yes/no`），不创建独立数据文件
- standards-index.md 保留完整 74 条目录作为**版本注册中心**
- 锚定标记区分两种角色：**校准基准**（is_anchor=yes）和**版本追踪**（is_anchor=no）
- 技能文件只需引用 13 条锚定标准 + 推理引擎规则，无需复制完整目录

### 2.5 锚定集变更规则

| 变更场景 | 变更级别 | 说明 |
|---------|---------|------|
| 锚定标准被废止/替代 | **S 级（安全紧急）** | 锚定是推理链的承重结构，缺失会导致全链路失稳 |
| 新增 GB 55xxx 系列标准需评估为锚定 | **A 级** | 可能改变 L1 层级的权限基准 |
| 锚定标准的入选准则不再满足 | **B 级** | 降级为普通目录条目，选出新锚定 |
| 锚定集总数调整（13→N） | **A 级** | 影响全部技能的标准引用模式 |

---

## 三、场景→标准推理链（M3）

### 3.1 输入参数

```
项目类型：[住宅/酒店/医院/学校/办公/商业/其他]
空间类型：[分户墙/客房隔墙/走廊隔墙/卫生间/教室/病房/...]
项目所在地：[省/市，或"未提供"]
构造体系：[轻钢龙骨/条板/模块化/...]（可选）
性能需求：[隔声/吸声/撞击声/耐火/防火/环保/验收/...]（可选，默认"按规范"；参与 Step 1a acoustic 子域激活裁定。吸声/撞击声 为声学细分需求，CG-20260917-002 补入，与 IC-10 `性能需求` 枚举及运行时 `acoustic_demands` 对齐）
```

### 3.2 六步推理算法

**Step 1 — 激活性能领域**

根据（项目类型, 空间类型）确定激活的性能领域集：

| 项目类型 | 空间类型 | 激活领域 |
|---------|---------|---------|
| 住宅 | 分户墙 | acoustic, fire, acceptance, prefab |
| 住宅 | 户内隔墙 | acoustic, fire, acceptance |
| 酒店 | 客房隔墙 | acoustic, fire, acceptance, prefab |
| 医院 | 病房/诊疗室 | acoustic, fire, acceptance, environmental |
| 学校 | 教室/实验室 | acoustic, fire, acceptance |
| 办公 | 办公室/会议室 | acoustic, fire, acceptance |
| 商业 | 商铺/展厅 | fire, acceptance |
| 医院 | 病房/手术部墙面 | fire, environmental, acceptance, prefab |
| 酒店 | 客房墙面 | acceptance, prefab, acoustic(边界提示) |
| 住宅 | 户内墙面 | prefab(认定), acceptance, environmental |
| 学校 | 教室墙面 | fire, environmental, acceptance |
| 办公 | 办公墙面 | acceptance, environmental, prefab |
| 住宅 | 厨卫湿区 | prefab, waterproof(外部协同), acceptance |
| 住宅 | 分户楼板 | acoustic(impact), fire, acceptance, prefab |
| 住宅 | 楼地面（一般） | acoustic(impact), acceptance, prefab |
| 酒店 | 客房地面 | acoustic(impact), acceptance, prefab |
| 办公 | 办公地面 | acceptance, prefab |
| 医院 | 病房地面 | acoustic(impact), acceptance, environmental |
| 学校 | 教室地面 | acoustic(impact), acceptance |
| 医院 | 手术部/洁净吊顶 | fire, environmental, acceptance, prefab |
| 医院 | 病房吊顶 | fire, acceptance, prefab |
| 酒店 | 客房吊顶 | acoustic(贡献), fire, acceptance, prefab |
| 住宅 | 厨卫集成吊顶 | acceptance, prefab, environmental, waterproof(边界) |
| 住宅 | 户内吊顶 | acceptance, prefab |
| 学校 | 教室吊顶 | acoustic(吸声), fire, acceptance |
| 办公 | 办公吊顶 | acoustic(吸声), fire, acceptance |
| 任意 | 任意 | fire, acceptance（始终激活的最小集） |

> **空间维度归一（CG-20260917-002 断点6）**：`厨卫湿区` 是**空间类型**（本表第二列），不是项目类型——IC-10 `项目类型` 枚举（住宅/酒店/医院/学校/办公/商业/其他）不含它。调用方传入 `卫生间`/`厨房` 等空间时，SRE 经 `SPACE_ALIASES` 归一化到 `厨卫湿区` 后按 `（住宅, 厨卫湿区）` 命中本表；不得把厨卫湿区填入项目类型槽（旧版误置项目槽致按契约调用永不命中、别名路径形同虚设）。

**Step 1a — 性能需求激活裁定（acoustic 子域参与；2026-08-08 机制定稿，IT-4）**

Step 1 表的结果为**候选域集**；最终激活域集 = 表行候选域 ∪ 最小集（"任意 | 任意"行 fire/acceptance），并由输入参数"性能需求"对 acoustic 子域做增补/过滤裁定——**仅 acoustic 子域族**（acoustic / acoustic(impact) / acoustic(贡献) / acoustic(吸声)）参与本裁定；acoustic(边界提示) 为提示性标注，不参与增补/过滤裁定，恒按表行保留：

1. **增补**：性能需求显式含声学类需求（隔声/撞击声/吸声）时，即使表行未载 acoustic，也激活对应子域——撞击声场景（楼板）→ acoustic(impact)；贡献量构件场景（吊顶/隔墙等，隔声性能归属宿主构件）→ acoustic(贡献)；吸声为主角场景（教室/办公吊顶等）→ acoustic(吸声)。例：住宅 | 户内吊顶 + 性能需求 [隔声] → acoustic(贡献) 增补激活。
2. **过滤**：性能需求显式枚举且不含声学类需求时，候选集中的 acoustic 子域不激活——表行 acoustic 条目为场景提示而非强制激活。例：酒店 | 客房吊顶 + 性能需求 [防火, 验收] → acoustic(贡献) 不激活。
3. **兼容**：性能需求未提供（默认"按规范"）时按表行候选域原样保留，不做过滤（v1.0 既有行为不变）。
4. 非声学领域（fire/environmental/waterproof(外部协同)/prefab/acceptance）不受本裁定影响；最小集 fire/acceptance 始终激活。

> 裁定依据：IC-10 联调 T1/T2/T3 三场景推导（记录/IC-10接口联调验证报告_20260808.md §六 IT-4）。"住宅 | 户内吊顶"表行经评估不补 acoustic(贡献)：机制层裁定已覆盖该高频场景，行内预置会导致无隔声需求场景（如普通厨卫铝扣板吊顶）误激活。

**Step 2 — 领域→标准族映射**

每个激活领域按建筑类型和构造体系查找对应标准族。

> **地点入参归一化**：下方各表的地点键（如"+地点=北京""+地点=广东"）为简称，输入的所在地可能带行政后缀（"北京市""广东省"）。做地点键匹配前须先归一化——去掉末尾的"省/市"再比对，与 §四 M4/LA 的地点判定口径一致；否则 M3 映射会按全称匹配不到、漏激活对应地方标准族。

**acoustic 领域**：

| 建筑类型 | 标准族（按权限优先级排列） |
|---------|----------------------|
| 住宅 | GB 55038-2025（mandatory, DnT,w+C）→ GB 50118-2010（实验室参考）→ GB/T 50121-2005（评价量定义） |
| 非住宅 | GB 50118-2010（mandatory）→ GB/T 50121-2005（评价量定义） |
| +构造=轻钢龙骨 | → [+ GB/T 19889.x 系列（测量方法）, JG/T 544-2018（产品标准）, 07CJ03-1（L3 图集）, 08J931（L3 图集）] |
| +构造=条板 | → [+ GB/T 23451-2023（产品标准）] |
| +构造=吊顶 | → [+ GB/T 11981-2024（龙骨产品标准）, GB/T 9775-2025（石膏板面板）, JC/T 564.1-2018（硅酸钙板面板）, GB/T 25998-2020（矿棉吸音板面板）, 07CJ03-1（L3 图集）, 08J931（L3 图集）]；注：吊顶对空气声隔声为贡献量而非独立判据，估算经 ACE（IC-09），达标归属宿主构件标准（GB 55038-2025 / GB 50118-2010） |

**acoustic(impact) 领域**（楼地面撞击声）：

| 建筑类型 | 标准族（按权限优先级排列） |
|---------|----------------------|
| 住宅分户楼板 | GB 55038-2025（mandatory, L'nT,w≤65dB）→ GB 50118-2010（非住宅参考）→ GB/T 50121-2005（评价量定义） |
| 非住宅 | GB 50118-2010（mandatory）→ GB/T 50121-2005（评价量定义） |
| +构造=浮筑地面 | → [+ GB/T 19889.7-2022（现场测量，代替 2005 版）, GB/T 19889.8-2006（ΔL实验室测量）, GB/T 45305.3-2026（撞击声实验室测量，前版 19889.6-2005 已废止）, 08J931（L3 图集）] |
| +构造=架空地面 | → [+ 08J931（L3 图集）] |

**fire 领域**：

| 建筑类型 | 标准族 |
|---------|-------|
| 全部 | GB 55037-2022（mandatory）→ GB 50016-2014 2018版（补充，检查替代状态）→ GB 50222-2017（装修防火） |
| +涉及板材 | → [+ GB 8624-2012（燃烧性能分级）, 07CJ03-1（L3 图集，轻钢龙骨构造防火参考）] |
| +构造=吊顶 | → [+ GB/T 11981-2024, GB/T 9775-2025, JC/T 564.1-2018, GB/T 25998-2020（产品标准，面板燃烧性能验证）, 07CJ03-1（L3 图集）] |

**acceptance 领域**：

| 建筑类型 | 标准族 |
|---------|-------|
| 全部 | GB 50210-2018（national）→ GB 55032-2022（质量控制）→ 07CJ03-1（L3 图集，轻钢龙骨验收参考） |
| +地点=浙江 | → [+ DB33/T 1168-2019] |
| +地点=北京 | → [+ DB11/T 1553-2025] |
| +地点=广东 | → [+ DBJ/T 15-208-2020] |
| +地点=上海 | → [待补充：上海地方验收标准（DB31 或 DG/TJ 08 系列），收录后须同步更新 standards-index.md] |
| +构造=吊顶 | → [+ GB/T 11981-2024, GB/T 9775-2025, JC/T 564.1-2018, GB/T 25998-2020（产品标准，验收验证参考）, 07CJ03-1（L3 图集）] |

**prefab 领域**：

| 范围 | 标准族 |
|------|-------|
| 全国 | GB/T 51129-2017（评价）→ JGJ/T 491-2021（技术标准）→ RISN-TG 055-2025（导则） |
| +地点=浙江 | → [+ DB33/T 1259-2021] |
| +地点=北京 | → [+ DB11/T 1553-2025] |
| +地点=福建 | → [+ DBJ/T 13-428-2023] |
| +地点=深圳 | → [+ SJG 159-2024] |
| +墙面饰面系统（WS 域） | → [+ T/CECS 1018-2022（官方文本已核验 2026-08-06；墙面系统首要技术依据）, JG/T 579-2021（集成墙面产品）, JG/T 578-2021（墙板通用，与隔墙协同）] |

**environmental 领域**：

| 场景 | 标准族 |
|------|-------|
| 医院/学校 | GB 18580-2025（甲醛）→ GB 30981.1-2025（建筑涂料有害物质限量，2026-06-01 实施，代替已废止的 GB 18582-2020）→ T/CSUS 03-2019（医院专项） |
| 住宅/酒店/办公（一般） | GB 18580-2025（甲醛；2026-06-01 实施，替代 GB 18580-2017） |
| 吊顶板材（环保） | GB 18580-2025（甲醛，涉及人造板及其制品时适用）→ GB 6566-2010（放射性核素限量，无机板材适用） |

**Step 3 — 权限冲突裁决**

结果集中同一指标有多条标准覆盖时：

```
FOR 每对 (S_a, S_b) 覆盖同一指标:
  IF S_a.层级 < S_b.层级 (L1 < L2 < L3 < L4):
    S_a 优先作为合规判据，S_b 降级为"补充参考"
  IF S_a.层级 == S_b.层级 AND S_a 是地方 AND S_b 是全国:
    IF S_a 的指标值更严格:
      S_a 优先，标注"严于国标"
    ELSE:
      S_b 优先
  IF S_a 是团体标准 AND S_b 是推荐性国标:
    S_b 优先，除非合同文件指定 S_a
```

**Step 4 — 分配角色**

| 角色 | 定义 | 分配条件 |
|------|------|---------|
| mandatory_check | 必须校验合规，输出通过/不通过 | L1 标准中有适用的强制性条文 |
| design_basis | 设计值的主要依据 | L2 标准中有具体的设计阈值 |
| verification_reference | 用于校准/验证计算结果 | L2 方法/测量类标准 |
| construction_guide | 施工方法参考 | L3 图集、L4 产品标准 |
| prefab_evaluation | 装配率评价依据 | 装配式评价类标准 |

**Step 5 — 排序输出**

按角色优先级排列：mandatory_check → design_basis → verification_reference → construction_guide → prefab_evaluation

**Step 6 — 标注**

每条标准附：
- 分类记录（类型/层级/权限/地域/领域）
- 角色说明（在當前场景中的具体作用）
- 地域适用性标注（地方标准须注明是否适用）
- 替代/废止警告（如有）

### 3.3 输出格式

```
┌─ 强制性校验 (mandatory_check)
│  1. [标准编号]《标准名称》
│     层级: L1 | 权限: red_line | 领域: [domain] | 范围: [scope]
│     → [具体作用说明]
│
├─ 设计依据 (design_basis)
│  2. [标准编号]《标准名称》
│     层级: L2 | 权限: binding_support | 领域: [domain] | 范围: [scope]
│     → [具体作用说明]
│     [替代/废止警告（如有）]
│
├─ 验证参考 (verification_reference)
│  ...
│
├─ 施工指导 (construction_guide)
│  ...
│
└─ 装配式评价 (prefab_evaluation)
   ...
```

---

## 四、适用性裁判规则（M4）

### 4.1 地方标准适用性（LA 规则组）

| 规则编号 | 规则内容 |
|---------|---------|
| LA-1 | DB/DBJ 前缀地方标准**仅当**项目所在地匹配该标准的地域范围时适用 |
| LA-2 | 地方标准适用且其指标值**严于**对应国标时，地方标准优先。输出："项目所在地 [地名] 执行的 [地标编号] 要求 [指标值]，严于国标 [指标值]，按地方标准执行" |
| LA-3 | 不适用的地方标准（项目在异地）仅作"对比参考"。输出："[编号] 为 [地区] 地方标准，本项目所在地不适用，仅作参考对比" |
| LA-4 | 项目地点**未知**时，**必须追问**后才可引用任何地方标准。输出："请确认项目所在地，以便确定适用的地方标准"。此追问是强制行为，不可跳过 |

### 4.2 团体标准权限（GS 规则组）

| 规则编号 | 规则内容 |
|---------|---------|
| GS-1 | T/xxx 团体标准默认**不优先于** GB/T 推荐性国标（同一领域同一指标时） |
| GS-2 | 例外（团体标准优先）：(a) 项目合同文件明确指定执行 T/xxx；(b) 地方政府已将 T/xxx 纳入引用标准；(c) 该领域无 GB/T 存在（填补空白） |
| GS-3 | 团体标准指标更严但无 GS-2 例外时：以 GB/T 为设计依据，标注"团体标准 [编号] 要求更高（[指标值]），可自愿采用" |

### 4.3 时间适用性（TA 规则组）

| 规则编号 | 规则内容 |
|---------|---------|
| TA-1 | 状态为"即将实施"的标准：可作为前瞻性参考引用，**不得**作为合规判据。标注："该标准将于 YYYY-MM-DD 实施，当前仍以 [现行标准] 为准" |
| TA-2 | 状态为"被部分替代"的标准：引用的每条条文须逐一检查是否已被替代。被替代条文切换到替代标准；未替代条文正常引用并附注 |
| TA-3 | 状态为"已废止"的标准：**触发红线 PW-R-P1-7**。有替代标准时自动切换并告知用户；无替代标准时标注"已废止且暂无直接替代，建议咨询主管部门" |

> **参考实现边界**：已废止项由 `sre_reasoner.py` 独立降低 applicability 确定性并附 TA-3 禁止作为现行判据的提示，item 仅保留作风险披露；GB 50118 的已登记替代警告继续输出。自动替代切换、锚定废止自动暂停及通用条文级替代判定尚未实现，相关义务由调用方与维护者执行，不以提示冒充完整执行。

### 4.4 跨领域标准（MD 规则组）

| 规则编号 | 规则内容 |
|---------|---------|
| MD-1 | 跨多个性能领域的标准（如 GB 55038-2025 同时涵盖隔声、结构、防火），在当前场景中对**所有相关领域**分别激活 |
| MD-2 | 每次激活独立计算角色。同一标准可能在隔声领域为 mandatory_check，在防火领域为 design_basis |
| MD-3 | 引用跨领域标准时，**必须指定条文/领域**：写"GB 55038-2025 §6.1.2（隔声性能）"，不得只写"GB 55038-2025" |

---

## 五、降级与兜底协议（M6）

| 降级场景 | 降级行为 | 输出标注 |
|---------|---------|---------|
| 标准编号无法分类（前缀不可识别） | 返回 unknown_type + reference 权限 + confidence=inferred | "标准 [编号] 的编号格式无法自动分类，请通过官方渠道核实" |
| 项目地点未提供 | 跳过所有地方标准，触发 LA-4 追问 | "请确认项目所在地，以便确定适用的地方标准" |
| 既有核验依据未确认（从索引核验记录表按列名现读）：① `核验状态` 存在，既不以「已官方核验」开头、也不为「到期需复核」（条文级核验仍不能扩大为整标准确认）；② 无 `核验日期`；③ 日期不可解析或晚于当前日期 | applicability 证据降为 `inferred`，追加 `degradation` 证据指向 standards-index.md §1.1A，不新增枚举或 item 字段。不得作为唯一合规判据，安全／消防／隔声强制指标须走 IC-07。出口二选一：补足与结论范围相符的真实核验记录，或接受降级态；缺记录不等同标准已失效 | "标准 [编号] 既有核验依据未确认（[事由]），不等于标准已失效；按正式文本或权威公告核实，不以本地、网站或知识库获取渠道定级" |
| 仅复查维护到期（非降级）：既有核验记录可复用，日期超过分档阈值（强制性国标 92 天／其余 183 天，严格大于），或已标「到期需复核」 | 不因到期改变标准效力、适用成员、角色、排序或证据置信度；复查提醒写入既有 applicability 证据并引用 §1.1A，门禁为 INFO。不删除台账、不设永久免检；未知状态保持降级，已废止状态独立降为 `inferred` 并附 TA-3 不可引用提示，GB 50118 的既有部分替代警告保留 | "复查提醒：保留既有核验结论，不代表本次已核实最新状态；项目引用前仍须核对适用条文及废止、替代、修订或勘误依据" |
| 场景未命中激活表（项目类型+空间类型组合不在 §3.2 Step 1 激活表内） | 降级为最小集 fire+acceptance，并将该降级写入"未覆盖领域"对外可见（不得静默返回最小集） | "场景未命中激活表（项目类型=[X], 空间类型=[Y]），已降级为最小集 fire+acceptance；建议确认场景表述或转专项技能咨询" |
| 推理链无匹配标准（场景过于特殊） | 返回空结果 + 建议 | "当前场景无明确适用标准匹配，建议：(a) 咨询项目所在地的建设行政主管部门；(b) 参考 [最近似标准] 作为技术参考" |
| 激活域命中外协占位（如 `waterproof(外部协同)`，标准族仅含「（…」前缀占位串、无真实编号） | **不得静默过滤**（CG-20260917-002 断点5）：占位串不进 `适用标准集`，改以结构化 `外部协同` 条目对外可见（`协同技能`/`技能状态`/`触发域`/`协同事项`），并同步产出一条 `证据类型=degradation`、`规则来源` 含「外部协同」的证据。`waterproofing-expert` 已于 2026-08-14 迁出至独立项目「建筑专业辅材技能合集」，`技能状态` 取「已迁出至独立项目…本运行时不可直接调用，需跨项目协同」，**不得为「待建」**（与 interface-contracts.md 头部迁出记录一致） | "外部协同：waterproofing-expert（已迁出至独立项目「建筑专业辅材技能合集」，本运行时不可直接调用，需跨项目协同）" |
| 锚定标准本身被废止 | 触发 S 级变更，暂停推理 | "锚定标准 [编号] 已废止，推理引擎需更新后方可使用，请联系技能维护者" |

> **核验判据真值源与承载**：字段与维护周期以 `standards-index.md` §1.1A 为准；92／183 天沿用既有派生值，仅用于提醒，不作为标准效力或置信度的时限。若改档位，本表及 `validate_governance.py` 的 `REVIEW_DAYS_MANDATORY`／`REVIEW_DAYS_OTHER`、`sre_reasoner.py` 的 `REVIEW_DAYS_L1`／`REVIEW_DAYS_OTHER` 须同批刷新。核验表与状态主表按列名互斥读取；核验台账取编号、核验日期、核验状态，状态列缺失不否定官方核验表中的有效日期记录。到期由机器现算，散文不作日期锚点；并列编号只按全角 `／` 切分，半角 `/` 属编号本体。证据真实性和范围仍需内容核验，代码不以渠道推定真伪，也不承担自动联网变更监控。

---

## 六、与现有架构的集成

### 6.1 与 IC-07（逐条核验）的两层协作

```
Layer 1 — IC-10（本引擎，场景查询）：
  分析开始时，确定本场景的适用标准集（面）
  → 技能据此构建分析框架

Layer 2 — IC-07（standards-reviewer，逐条核验）：
  输出生成时，验证每条引用的版本状态（点）
  → 技能据此调整具体引用

执行顺序：IC-10 先于 IC-07
```

### 6.2 与 ACE 计算引擎的三方分工

| 组件 | 职责 | 不做的事 |
|------|------|---------|
| ACE | 计算 Rw/DnT,w/f0 等物理值 | 不知道标准限值是什么 |
| SRE（本引擎） | 确定本场景的标准限值 | 不做任何物理计算 |
| 技能 B2 协议 | 将 ACE 输出与 SRE 阈值对比，做出合规判断 | 既不算物理值也不找标准 |

**SRE 驱动 ACE 评价量选择**：
- 住宅分户墙场景 → SRE 识别 DnT,w+C 为强制指标 → 技能请求 ACE 返回 DnT,w+C
- 办公隔墙场景 → SRE 识别 Rw+C 为设计指标 → 技能请求 ACE 返回 Rw+C

### 6.3 嵌入位置

SRE 嵌入 `prefab-standards-reviewer`（SR 技能），不作为独立技能。理由：
- SR 已有 IC-07 的标准核验基础设施
- IC-10 场景查询是 IC-07 逐条核验的自然扩展
- 避免增加独立技能带来的路由复杂性

### 6.4 与八步协同方法的集成

在 `interface-contracts.md` 定义的八步协同方法中，新增 **Step 0（标准推理）**：

```
Step 0: 标准推理（新增）
  总入口调用 IC-10，传递统一上下文
  获得本场景的适用标准集
  将标准集注入统一上下文，传递给所有子技能

Step 1-4: SRE 上下文增强
  Step 1 结合适用标准集明确各技能的标准约束范围
  Step 2 统一上下文增加【适用标准集】和【标准基准】
  Step 3 各技能须在适用标准集约束下分析，引用标准版本须与标准集一致
  Step 4 新增检查项：引用的标准是否均在适用标准集内

Step 5-7: 原有流程不变
```

---

## 七、结构化规则包与引擎化规范（v1.2 新增）

### 7.1 规则包组成

SRE 结构化规则包由以下 5 类文件组成，部署时须保持同目录或按相对路径可解析：

| 文件 | 作用 | 必须 | 格式 |
|------|------|------|------|
| `standards-reasoning-rules.md` | 声明式规则主文档（本文件） | 是 | Markdown |
| `standards-index.md` | 标准版本注册中心 | 是 | Markdown |
| `sre_reasoner.py` | 程序化推理器参考实现 | 是（v1.2 起） | Python 3 |
| `sre_regression_test.py` | 回归测试集 | 是（v1.2 起） | Python 3 |
| `sre_evidence_schema.json` | 证据对象 JSON Schema | 否（由 IC-10 Response Schema 覆盖） | JSON Schema |

### 7.2 推理器接口约定

推理器须实现以下最小接口：

```python
def reason(project_type: str, space_type: str, location: str | None,
           system: str | None, demands: list[str] | None,
           return_trace: bool = False) -> dict:
    """
    返回符合 IC-10-Response Schema v1.8.0 的字典。
    必须包含：适用标准集、推理路径、证据对象。
    可选包含：未覆盖领域、决策轨迹（return_trace=True）。
    """
```

### 7.3 证据对象（Evidence Object）

每条证据对象对应推理链中的一个确定性步骤，字段与 IC-10 Response Schema 保持一致：

| 字段 | 类型 | 说明 |
|------|------|------|
| 证据ID | string | 全局唯一，如 EVID-001 |
| 证据类型 | enum | classification / activation / mapping / arbitration / applicability / degradation |
| 规则来源 | string | 触发本证据的规则定位，如 `standards-reasoning-rules.md §1.2` |
| 输入事实 | string | 触发本证据的输入项或中间结论 |
| 输出结论 | string | 本证据产生的结论 |
| 置信度 | enum | deterministic / inferred / unknown |

**证据产生规则**：
- 每执行一次 M1 分类产生一条 `classification` 证据；
- 每激活一个场景行产生一条 `activation` 证据；
- 每完成一个领域→标准族映射产生一条 `mapping` 证据；
- 每触发 LA/GS/TA/MD 裁决产生一条 `arbitration` 证据；
- 每判定地方/团体/时间适用性产生一条 `applicability` 证据；
- 每进入 M6 降级分支产生一条 `degradation` 证据。

### 7.4 决策轨迹（Decision Trace）

`return_trace=true` 时，推理器返回 `决策轨迹` 字符串，格式如下：

```
Step 0 输入: 项目类型=住宅, 空间类型=分户墙, 所在地=未提供, 构造体系=轻钢龙骨, 性能需求=隔声
Step 1 激活域: fire, acceptance, prefab, acoustic(由性能需求增补)
Step 1a 裁定: 性能需求含[隔声]，增补 acoustic 子域
Step 2 族映射: acoustic → GB 55038-2025 / GB 50118-2010 / GB/T 50121-2005 + 轻钢龙骨构造族
Step 3 权限裁决: GB 55038-2025(L1) 优先于 GB 50118-2010(L2)
Step 4 角色分配: GB 55038-2025 → mandatory_check; GB 50118-2010 → design_basis; ...
Step 5 排序输出: mandatory_check → design_basis → verification_reference → ...
Step 6 标注: 地域适用性=全国; 时间状态=现行有效
```

### 7.5 引擎化成熟度分级

| 等级 | 标志 | 当前状态 |
|------|------|---------|
| L0 声明式 | 仅文本规则，无程序实现 | v1.1 及以前 |
| L1 可执行 | 有参考 reasoner + 回归测试 | **v1.2 目标** |
| L2 可校验 | 证据对象与决策轨迹被 QA 自动校验 | v1.3 目标 |
| L3 可裁决 | 安全类结论经条文级证据自动校验 | 未来目标 |

### 7.6 与 IC-10 的同步要求

- `sre_reasoner.py` 的输出字段必须与 `interface-contracts.md` IC-10-Response Schema v1.8.0 保持一致；
- 新增规则或变更算法时，必须同步更新 `sre_regression_test.py` 并通过全部用例；
- 证据对象中引用的规则来源必须能在本文件中找到对应章节。

---

## 八、变更日志

| 日期 | 变更类型 | 说明 |
|------|---------|------|
| 2026-06-21 | 初稿创建 | 建立六大模块：M1 分类协议、M2 锚定集（13条）、M3 场景推理链、M4 适用性裁判、M5/M6 输出与降级、与现有架构集成方案 |
| 2026-06-21 | Phase 2.4 修复 | 交叉验证修复：§6.4 Steps 1-4 SRE 增强描述准确化（原"不变"表述与 IC 文件不一致）；§3.2 Step 2 映射表补全 L3 图集激活路径（07CJ03-1/08J931）；acceptance 领域新增上海地标待补充占位 |
| 2026-08-06 | M3 扩展 | 墙面技能 v2（阶段 8）：Step 1 激活表新增墙面域 6 行（医院/酒店/住宅/学校/办公/厨卫湿区，与 WS SKILL M3 表一致）；Step 2 prefab 域新增 WS 墙面标准族（T/CECS 1018-2022、JG/T 579-2021、JG/T 578-2021）；environmental 域新增住宅/酒店/办公一般行（GB 18580-2025）。关联 CG-20260806-001 |
| 2026-08-06 | 备案记录 | WS v2 combined_reference_v2.md 将 S1 由 IC-08 的"国标/行标"扩展为"官方发布的标准文本"（含地方标准/团体标准/导则/汇编；未经官方平台核验的团体标准按 standards-index 规则标注"待官方核验"）。本条为备案记录，不改变 SRE 推理行为；S1 口径跨技能统一化提请 prefab-standards-reviewer 后续评审 |
| 2026-08-06 | 评审统一 | prefab-standards-reviewer 评审批准 WS S1 扩展口径并升格为合集级统一定义：S1=官方标准文本（国标/行标，及官方发布的地方标准、团体标准、导则、汇编），判定须满足"官方发布+官方文本核验"两要素，未官方核验者标注"待官方核验"且不得单独支撑安全类 pass。定义源 interface-contracts.md 材料参数数据源说明（IC-08 附属说明，位于 IC-08/IC-09 之间）已同步更新；prefab-standards-reviewer 技能置信度表同步。关联 CG-20260806-001 |
| 2026-08-06 | 官方核验 | T/CECS 1018-2022（prefab 域 WS 标准族）完成官方出版物全文核验（中国建筑工业出版社正式出版本，载公告第 1098 号：发布 2022-02-25、实施 2022-07-01；关键条文 2.0.1~2.0.7/3.0.2/3.0.3/5.4.2/5.4.3 原文核对一致）；Step 2 激活表 WS 域行"待官方核验"标注清除。SRE 推理行为不变。关联 CG-20260806-004 |
| 2026-08-06 | 标准入库 | SJG 159-2024《装配式装修评价标准》（深圳，发布 2024-06-05、实施 2024-09-01，深圳市住建局官方发布 PDF 全文核验）入库 standards-index 序号 58；Step 2 评价标准族激活表新增"+地点=深圳"行。SRE 推理行为不变。关联 CG-20260806-005 |
| 2026-08-07 | 标准入库同步 | GB/T 45305.3-2026《声学 建筑构件隔声的实验室测量 第3部分：撞击声隔声测量》（发布 2026-01-28、实施 2026-08-01，全国标准信息公共服务平台核验现行）收录入 standards-index 序号 17（全索引重编号，总数 58→59 条）；§2.4 计数同步；M3 acoustic(impact) 域浮筑地面激活链补充该标准（实验室撞击声测量方法链补全）。关联 CG-20260807-015 |
| 2026-08-07 | M3 吊顶域扩展 | 吊顶技能（CL）v2 重构阶段 1 标准锚定：Step 1 激活表新增吊顶域 7 行（医院手术部/洁净吊顶、病房吊顶、酒店客房吊顶、住宅厨卫集成吊顶/户内吊顶、学校教室吊顶、办公吊顶，项目类型单值枚举与 PW/WS/FL 域一致）；Step 2 族映射四处——acoustic 域构造=吊顶行（GB/T 11981-2024/GB/T 9775-2025/JC/T 564.1-2018/GB/T 25998-2020 + 07CJ03-1/08J931，附贡献量路由注记：估算经 ACE IC-09、达标归属宿主构件标准）、fire 域构造=吊顶行、acceptance 域构造=吊顶行、environmental 域吊顶板材行（GB 18580-2025/GB 6566-2010）；§2.4 计数 59→63（standards-index 补录序号 60-63）。变更定级 B 级（§4.5"规则调整"，沿 CG-20260624-004/CG-20260806-001 M3 扩展先例）。关联 CG-20260807-019 |
| 2026-08-08 | **v1.1 发布批次（A 级）** | ① M3 Step 1a 新增——性能需求激活裁定机制显式化：激活域 = 表行候选域 ∪ 最小集，仅 acoustic 子域族由性能需求做增补/过滤裁定（增补：显式声学需求→按场景激活 impact/贡献/吸声子域；过滤：显式枚举无声学需求→表行 acoustic 不激活；兼容：默认"按规范"按表行原样保留）；IT-4 裁定闭环，依据 IC-10 联调 T1/T2/T3 三场景推导（记录/IC-10接口联调验证报告_20260808.md §六）；"住宅 \| 户内吊顶"行经评估不补 acoustic(贡献)（机制层覆盖，避免无隔声需求场景误激活）；② §2.4 计数 63→67（CG-023 standards-index 增量后漏改，发布前漂移修复）；③ 头部部署状态更新为已发布；④ 首次发布至运行时 shared 镜像（L1→镜像字节同步，CRLF=0）；⑤ 回归 4 场景通过（住宅增补/酒店过滤/地方标准激活/默认兼容）+ prefab-standards-reviewer 复核有条件 PASS（6 PASS/1 WARN/0 FAIL），WARN（Step 1a 子域族枚举补 acoustic(边界提示) 排除条款 + OBS-4 术语统一）已于镜像同步前闭环。变更定级 A 级（§4.5"推理算法步骤修改"，用户确认发布）。关联 CG-20260808-025 |
| 2026-08-13 | **v1.2 引擎化第一阶段（A 级）** | ① 新增§七结构化规则包与引擎化规范，定义规则包组成、推理器接口、证据对象、决策轨迹、引擎化成熟度分级与 IC-10 同步要求；② 头部成熟度声明由"声明式规则文档"升级为"引擎化第一阶段"；③ 配套实现 `prefab-standards-reviewer/sre_reasoner.py` 参考推理器与 `sre_regression_test.py` 回归测试集；④ interface-contracts.md IC-10 同步升级至 v1.5.8，新增证据对象与决策轨迹字段；⑤ 注册 SRE 专属红线（SR-R-P1-5 ~ SR-R-P2-3）。变更定级 A 级（§4.5"推理算法步骤修改/引擎化目标形态推进"）。关联 CG-20260813-035 |
| 2026-08-13 | **v1.2.1 维护规则文义修复（B 级）** | 头部维护规则"变更级别不低于 A 级（详见 change-governance.md §十）"修订为按 §4.5 七类变更分类定级路由（S/A/B 分级显式列出），闭合 CG-20260807-019 留档观察项（该 CG 审查结论：B 级领域激活表扩展与"不低于 A 级"笼统表述矛盾，判 B 级妥当、表述修订建议留档）；零推理逻辑变更，M1-M6 规则、锚定集、结构化规则包均不动。变更定级 B 级（§4.5 分类外的治理表述澄清，不涉及推理算法/锚定集变更）。关联 CG-20260813-038 |
| 2026-09-15 | **v1.2.2 孤儿引用闭合（并入 S 级批次）** | SRE 确定性体检 T-A2 修复（设计方案 v1.1 §9.2）：① §3.2 Step 2 environmental 医院/学校 标准族的 `GB 18582-2020` 改指 `GB 30981.1-2025`（前者经 §7.5 通道 ②＋§7.6.3 发现 5 通道 ① 双通道确认已废止、后者 2026-06-01 强制实施；本行为该编号在本文件的唯一命中）；② §2.4 目录计数 67→69，随 standards-index 主表补录序号 68 与 §5.3 拆行新增序号 69 同步（门禁 4b 反查项）。M1 分类协议、M3 六步算法结构、13 条锚定集、§七 结构化规则包均不动。变更定级：本文件侧动作属"引用编号更替＋计数同步"，随 GB 18582-2020（强制性国标废止）批次按 S 级/24 小时一并处置，不单独定级。关联 CG-20260915-002 |
| 2026-09-16 | **v1.2.3 M1 未知处理降级前置查表（A 级）** | 根因：M4 Step 4 `else` 兜底把 IC-10 `权限` 三值枚举的成员 `reference` 写进 `角色` 五值槽位（两枚举共用该词），全矩阵实测越枚举 228 处（34 组激活对 × 7 地点取值 = 646 次调用／5,961 条条目），全部来自 2 个编号——GB 18580-2025 190 处、08J931 38 处，且因 `reference` 不在 §3.2 Step 5 优先级表内而按未知优先级（99）参与排序。两支前缀不可识别才是其上游落点：① §1.2 前缀表按编号段枚举强制性国标（`GB 55xxx`/`GB 5xxxx`），无 `/T` 的四、五位编号不在表内；本文件 §1.4 第二行改为**降级前置查表**——先查 standards-index.md §二「第一层级：强制性国标（红线标准）」主表，命中即按 §1.2 的 L1↔red_line 耦合确定性分类（deterministic），未命中才落入降级，并明确层级真值只从该表现读、规则与实现内均不维护编号清单副本；② 同支补一句取值域澄清：降级不改变 `角色` 合法取值域，Step 4 仍须在五值内分配（L4 降级件即本表 construction_guide）。代码侧同批三处（`sre_reasoner.py` v1.2→v1.3）：图集正则 `^\d+J\b` 改 `^\d+J(?=\d)`（`\b` 在 J 与数字间不成立，§1.2「数字+J→图集」既有语义此前实现未命中）、`_load_standards_index()` 按 `##` 小节切块现读 §二 L1 成员集（导入态 15 条）、M4 `else` 兜底值 `reference`→`construction_guide`。守卫同步扩面（`sre_regression_test.py` 载体 B）：`test_standard_item_schema` 由单场景（1 组）改为 `ACTIVATION_TABLE` × `LOCATIONS` 全矩阵遍历并加漏组/空跑判据，`角色` 合法集由契约现读取代硬编码五值字面量，新增 M1/M4 五例（含合成编号负向注入用例，因 M1 修净后 `else` 支在真实数据面已不可达）。修复后全矩阵越枚举 228→0、`INDEX_L1_IDS` 两侧（运行时/测试各自现读索引）交叉核对同集 15 条零差异；改前态反向注入 I1（M4 兜底）／I2（图集正则）／I3（索引现读支）／I1+I2+I3 逐项复红 1/2/2/6 例。§1.3 五步算法结构、M2 锚定集 13 条、§五 M6 降级协议、§七 结构化规则包不动；IC-10 `角色` 五值枚举未变，interface-contracts.md 零改动。变更定级 A 级（§4.5"推理算法步骤修改（M1 模块）"），其"至少 3 个典型场景回归测试（覆盖住宅/非住宅/地方标准激活）"要求由 34×7 全矩阵遍历覆盖。关联 CG-20260916-009 |
| 2026-09-17 | **v1.2.4 第三方审阅整改 P0 断点5/6/7 判据同步（A 级）** | 来源：《成果/第三方机制与行为审阅评估及整改方案_20260916.md》P0 断点5/6/7。本文件侧（遗漏A：判据须与代码同在生效路径）三处——① §3.1 输入声明「性能需求」补 `吸声`/`撞击声`（原 §3.2 Step 1a 裁定逻辑早已含此二值、但输入声明与 IC-10 枚举均缺，按契约调用无法表达声学细分需求）；② §3.2 Step 1 激活表「厨卫湿区」行由误置项目槽 `（厨卫湿区｜墙板）` 校正为空间槽 `（住宅｜厨卫湿区）`，并新增「空间维度归一」注——厨卫湿区是空间类型而非项目类型，卫生间/厨房经 `SPACE_ALIASES` 归一化命中，旧版误置致按契约调用永不命中且别名路径形同虚设（断点6）；③ §五 M6 降级表新增「激活域命中外协占位」行——外协占位（`waterproof(外部协同)` 等「（…」前缀串）不得静默过滤，须以结构化 `外部协同` 条目对外可见 + 一条 `degradation` 证据，`技能状态` 取「已迁出至独立项目「建筑专业辅材技能合集」」而非「待建」（与 interface-contracts.md 2026-08-14 迁出记录一致，断点5）。§1.2 DB/DBJ=L2 本已正确，系断点7 层级真值源——代码侧 `sre_reasoner.py` `PREFIX_PATTERNS` 旧给 L3 与之实质分歧（报告§二「机制层唯一实质分歧」），本批校正为 L2（图集仍 L3）。代码侧同批（`sre_reasoner.py` v1.3→v1.4）：断点7 DB/DBJ `L3→L2`；断点6 `ACTIVATION_TABLE` 厨卫湿区改空间槽；断点5 新增 `EXTERNAL_COLLAB_SKILL`/`EXTERNAL_COLLAB_STATUS` 常量 + `_external_collab_entry()` helper + Step 2 占位分流（「（」前缀→`外部协同`、其余→`raw_standards`）+ 响应新增 `外部协同` 字段。IC-10 同步升 v1.8.0→v1.9.0（性能需求枚举宽化 + 空间类型释义 + 新增响应字段 `外部协同` + 层级释义校正）。守卫（`sre_regression_test.py` 44→49 例）：`test_db_is_local` 断言 L3→L2、新增 `test_dbj_is_local_l2`/`test_BP6_wet_area_matches_via_space_dimension`/`test_BP6_acoustic_demands_accepted`/`test_BP5_external_collab_structured_visible`/`test_BP5_external_collab_carries_degradation_evidence`，`test_BP4` 失效场景 `（住宅,卫生间）`→`（商业,地下车库）`（前者现已命中）。反向注入自证 4 项全 RED（断点7 层级回退 2 例、断点6 厨卫湿区退回项目槽 1 例、断点5 话术改回「待建」1 例、断点5 占位去「（」不分流 2 例），基线/还原均 GREEN；其中 `test_BP5_external_collab_carries_degradation_evidence` 初版松判据被「地方标准不适用」无关 degradation 证据顶替而恒真，已收紧为锁定 `规则来源` 含「外部协同」的专属证据（注入4 实测复红）。**另案遗留**：团体标准 `T/` 层级 rules.md §1.2 定 L2 而运行时 `PREFIX_PATTERNS` 仍按 L4，属本批新发现分歧，未处理（需自身裁定）。变更定级 A 级（§4.5"推理算法步骤修改（M3 激活表）＋接口契约字段取值集变更"），3 典型场景回归由 49 例 + 34×7 全矩阵覆盖。关联 CG-20260917-002 |
| 2026-09-17 | **v1.2.5 核验时效降级判据接入生效路径（A 级）** | 来源：《成果/第三方机制与行为审阅评估及整改方案_20260916.md》P1 item 9（时效分档＋账本纪律，断点7）与 item 10（`_load_standards_index()` 增读核验字段＋复用 `STATUS_UNKNOWN` 降级语义，遗漏E）。**根因（条文存在≠机制生效）**：本文件 §五 M6 早写有「standards-index.md 中标准状态为"待核验" → 降低输出确定性」，但代码无承担者——门禁侧 `validate_governance.py` 检查 3 的时效支以 `^#.*官方核验记录` 为锚点扫描，而索引里该记录是**粗体行非 `#` 标题**，锚点命中 0 处 → `expired_review` 恒空 → 无条件报 ok「无超期未核验标准（6 个月内）」，即"到期由索引正文散文承载"的恒真空跑守卫；运行时侧则完全不读核验字段。本文件侧（遗漏A）两处——① M6 表该行改写为机器可判据口径：三条触发（`核验状态` 不以「已官方核验」开头／无 `核验日期` 记录／核验日期超分档阈值 92·183 天，严格大于）任一命中 → applicability 证据 `置信度` deterministic 降 `inferred` ＋ 追加 `degradation` 证据，**复用既有降级语义、不新造枚举**（IC-10 items 为 `additionalProperties: false`，不得给 item 加字段，故降级只走既有 `置信度`＋证据＋可选 `替代警告`）；并明文记账「补官方核验」与「降低确定性」为**等价出口**，无 `核验日期` 者按**已超期**处理而非按"免检"处理（整改方案 §6.2 账本纪律：积压不阻塞业务又不假装合规）；② 表下新增注：阈值与字段真值源为 `standards-index.md` §1.1A、本文件不存数值副本，到期由机器从「核验日期」列现算、索引散文不再承载，核验记录表与状态主表**按列名区分并互斥读取**（否则会把自由文本列误当状态读入，即 CG-20260916-004 T-B4／T-B7 根因），并列编号单元只按全角 `／` 切分（半角 `/` 属 `GB/T`、`DBJ/T` 编号本体）。代码侧同批：`sre_reasoner.py` v1.5→**v1.6**——新增 `REVIEW_DAYS_L1=92`／`REVIEW_DAYS_OTHER=183`／`CONFIRMED_VERIFY_PREFIX`／`STANDARD_VERIFY` 台账（导入期从索引核验记录表按列名现读，代码内不存编号或日期副本）、`_classify_verify_table()`（与 `_classify_status_table()` 刻意互斥）、`_verification_confirmed()`（三判据；`核验状态` 列缺失不否定日期记录，因产品标准核验记录表本就无该列），`_apply_applicability()` 接入后**不改 item 任何字段**；`validate_governance.py` 检查 3 时效支由恒真空跑改为分档台账现算（表头感知 `classify_index_table`＋按列名取日期＋`norm_std_id`/`STD_ID_SHAPE` 剔除散文兜底单元＋同编号取最新日期＋档位边界 INFO）。改前实测台账（TODAY=2026-09-17，主表 69 行）：强制性国标档 15 条 → 在期 11／已核验但超期 0／无「核验日期」4；其余档 52 条（另 2 处指南类无编号条目单列）→ 在期 15／超期 0／无日期 37；无日期合计 41；档位边界 3 条（GB 55037-2022／GB 50118-2010／GB 55038-2025，均 2026-06-17，恰第 92 天，2026-09-18 起转超期）→「已核验但超期」为 0，故分档落地不打破门禁硬判据（失败 0＋exit 0），警告 2→3（新增无日期聚合 WARN），通过 195 不变（删 1 条恒真 ok、加 1 条分档 ok）。运行时取证（34 组激活对 × 7 地点 = 238 次调用／2,387 条目）：`STANDARD_VERIFY` 26 键、散文假键 0；**适用标准集条目面 0 差异场景**（降级只动确定性与证据，不动成员）；`置信度` deterministic 6,130→4,312、inferred 15→3,651；`degradation` 证据 210→2,028（新增 §1.1A 核验时效降级 1,818 条）。M1 分类协议、M2 锚定集 13 条、§3.2 激活表与五步算法、§七 结构化规则包均不动；interface-contracts.md 零改动（IC-10 维持 v1.9.0）。**另案登记不处置**：索引核验状态实测存在 3 种字面值越 §1.1 四值枚举（`条文级核验（S1）`／`条文级核验（S1-）`／`已官方核验（条文级）`），属索引数据面缺陷、须官方核验后另批修，本批按「不以「已官方核验」开头即未确认」口径统一降级、不为其新造枚举。变更定级 A 级（§4.5"M6 降级协议修改"本属 B 级，但同批改动推理输出的确定性判定并触及门禁判据承载方式，按就高原则定 A 级），线内复核＋发起人确认（用户选定 item 9/10 单独成批）。关联 CG-20260917-006 |
| 2026-09-17 | **v1.2.6 复查到期与效力解耦（A 级）** | CG-20260917-009：§五 M6 分为依据未确认降级与维护到期提醒，单纯到期不降低置信度，提醒写入 applicability 证据；缺日期、明确未确认及异常日期仍降级，未知／废止／部分替代保护不动。真值源为索引 §1.1A，来源渠道不决定内容证据等级；无永久免检或自动联网监控声明。reasoner v1.7、test v1.9，IC-10 v1.9.0 不变，标准数据不动。 |
