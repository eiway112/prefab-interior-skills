## 第三方审核建议评估报告

评估日期：2026-06-12

总体结论：第三方审核质量较高，五条建议中四条经实际文档验证后确认事实依据成立，建议方向合理。其中两条建议（路径统一、变更治理）应当优先采纳，一条（测试量化）需部分采纳并调整，一条（ACE 前置）需谨慎对待，一条（可校验契约）当前阶段投入产出比不高，可延后实施。

---

### 建议一：统一共享资源路径和状态口径

**事实核查：问题确实存在**

审查发现飞书映射方案中使用 `../standards-index.md`（无 shared/ 前缀），而隔墙方案、standards-index.md 自身、以及总入口方案的建仓规划中均使用 `shared/standards-index.md` 或 `../shared/standards-index.md`。此外，总入口策划方案内部也存在矛盾——第四节将 standards-index.md 列在技能根目录下，第八节又将其归入 shared/ 子目录。

**采纳意见：完全采纳，立即修复**

这属于文档笔误层面的问题，修复成本极低但不修会在建仓时引发实际引用错误。建议统一为 `shared/standards-index.md`（相对于总入口技能目录）或 `../shared/standards-index.md`（相对于子技能目录），并在飞书映射方案、隔墙方案、总入口方案中做全文统一。同时对 glossary.md 和 interface-contracts.md 也执行一次路径引用巡检。

---

### 建议二：将接口契约从"文本约定"升级为"可校验契约"

**事实核查：现状描述准确，但建议时机值得商榷**

interface-contracts.md 中 7 条跨技能契约（IC-01 至 IC-07）确实全部采用自然语言 + 伪 JSON 描述，没有任何机器可校验的 schema 定义。但当前处于策划阶段，仅 7 条契约，且已有版本号管理（v1.0）和向后兼容原则。

**采纳意见：原则认同，延后实施**

在当前策划阶段，为 7 条契约各补充一套 JSON Schema 的投入产出比不高。建议分两步走：近期在 interface-contracts.md 中为最复杂的 IC-03（隔声计算引擎接口）补充一份结构化的字段约束表（字段名、类型、必填/可选、取值范围），作为向形式化过渡的中间产物；待技能合集进入实际建仓和联调阶段时，再为全部契约补充机器可校验 schema。这样既不增加当前阶段的过度负担，也为未来的自动化验证预留了演进路径。

---

### 建议三：测试门禁量化到"测试用例级"

**事实核查：问题确实存在，且比第三方描述的更严重**

路线图中阶段 1-6 的测试量化严重不足：阶段 3 仅要求 2 个端到端用例覆盖 11 个验证维度，阶段 1、2、5、6、9 完全没有具体测试用例数量。阶段 7 的量化相对好一些（功能测试 5 + 红线测试 17 + 回归测试 4 = 26 个），但考虑到 11 个验证维度和 5 种输入类型的组合空间，26 个用例的覆盖率仍然偏低。

**采纳意见：部分采纳，需调整预期**

第三方建议的方向完全正确，但需要注意避免"为了量化而量化"。建议按以下方式落实：阶段 4（红线体系）至少 17 个对抗性触发用例 + 5 个边界模糊用例，这是投入产出比最高的补充点，因为红线违反是硬性质量门槛；阶段 7 集成验证在现有基础上增加"IC-03 接口格式模拟测试"（用预定义 mock 数据），以及 3-5 个跨技能端到端场景测试；阶段 1、2、5、6 暂不强加测试用例数量要求，这些阶段以知识内容建设为主，用 checklist 验收比用测试用例更合适。总体目标：整个 v2 版本的最小测试集不低于 50 个用例。

---

### 建议四：调整 ACE 实施顺序，不宜完全放到阶段 8

**事实核查：依赖关系存在，但当前安排有其合理性**

审查发现当前路线图的降级策略设计是完善的——隔墙主技能在阶段 2 已保留简化版计算方法论（A2.3，约 800 字）作为降级备用，确保阶段 3-7 可在没有 ACE 的情况下完成验证。并行窗口声明（"8.1-8.5 可与阶段 5-6 并行"）与依赖关系图之间存在轻微表述歧义，但不影响执行。

**采纳意见：谨慎采纳，微调而非重构**

第三方将 ACE 拆成"早期 stub + 后期完整"两阶段的思路有参考价值，但需要注意一个风险：过早引入 ACE stub 可能导致阶段 3-7 的测试基于一个不真实的降级接口，反而掩盖了真实联调时的问题。建议采取折中方案：维持 ACE 主体在阶段 8 的安排不变，但在阶段 7 集成验证中增加一项"IC-03 接口契约一致性检查"——用预定义的 mock 请求/响应对验证接口格式是否符合 interface-contracts.md 的 IC-03 定义。这样既不增加阶段 7 之前的开发负担，又能在阶段 8 联调前暴露接口层面的不一致。同时修正并行窗口的表述歧义，明确"8.1-8.5 内容编写可并行，8.6 接口联调依赖阶段 7 完成"。

---

### 建议五：补齐"知识变更治理"规则

**事实核查：这是最被忽视但风险最高的领域**

审查确认当前各共享资源文件（standards-index.md、interface-contracts.md、redlines-registry.md、glossary.md）各自有独立的维护规则，均要求"经 prefab-standards-reviewer 复核"，但整个体系缺少统一的变更审批流程、跨文件影响评估机制、变更优先级分类、版本发布协调和定期审查制度。

当前技能合集仅注册了 14 个技能标识中的 2 个（OR 和 PW），变更影响范围有限。但当子技能数量增长到 5 个以上时，一条标准废止可能同时影响多个技能的 reference.md、输出模板和红线判断逻辑，仅靠各文件各自声明"须通知"是远远不够的。

**采纳意见：完全采纳，优先级最高**

这是五条建议中战略价值最高的一条。建议新增 `shared/change-governance.md`，至少覆盖以下内容框架：变更分类（安全紧急/标准废止/常规更新/优化调整四级）、审批流程（发起-影响评估-复核-发布四步）、影响评估清单模板（受影响的技能列表、需回归的测试用例、兼容性结论、回滚方式）、通知机制（变更发布后须在 24 小时内同步所有受影响技能）、定期审查周期（每季度由 standards-reviewer 对全部共享资源执行一次全量复核）。此文件可在建仓阶段同步建立，不影响当前策划工作。

---

### 总结与优先级排序

| 优先级 | 建议 | 采纳程度 | 理由 |
|-------|------|---------|------|
| P0 | 统一共享资源路径 | 完全采纳 | 修复成本极低，不修会在建仓时产生实际错误 |
| P0 | 补齐知识变更治理 | 完全采纳 | 战略价值最高，是技能合集长期可维护性的关键基础设施 |
| P1 | 测试门禁量化 | 部分采纳 | 重点补充阶段 4 和阶段 7 的测试用例，其他阶段以 checklist 为主 |
| P2 | ACE 实施顺序微调 | 谨慎采纳 | 当前安排基本合理，仅需在阶段 7 增加 IC-03 接口格式模拟测试 |
| P3 | 接口契约可校验化 | 延后实施 | 先为 IC-03 补充字段约束表，全面 schema 化等建仓阶段再做 |

---

### 落实记录（2026-06-12）

以下为本次评估后已实际执行的修改，按优先级排序。

**P0 — 统一共享资源路径（已完成）**

- 飞书映射方案：将第 72 行 `../standards-index.md` 修正为 `../shared/standards-index.md`
- 总入口策划方案第四节：将 standards-index.md 和 glossary.md 从总入口技能目录列表中移除，明确标注为 `shared/` 目录下的共享资源，总入口通过 `../shared/` 引用。新增"共享资源引用说明"段落
- 总入口策划方案第八节：在 shared/ 目录中新增 change-governance.md 条目，在共享资源归属说明中同步补充

**P0 — 创建知识变更治理规则（已完成）**

- 新建 `技能合集策划/change-governance.md`（初稿），包含：变更四级分类（S/A/B/C）、四步审批流程（发起→影响评估→复核→发布）、影响评估清单模板、四类共享资源各自的专项变更规则、跨文件变更协调规则（原子发布/引用方向/冲突优先）、五项定期审查机制（季度标准巡检/半年术语扫描等）、紧急变更快速通道、变更请求记录模板、变更日志
- 更新项目索引文件，纳入 change-governance.md

**P1 — 测试门禁量化（已完成）**

- 路线图阶段 3：端到端测试用例从 2 个增加到 4 个（2 个标准场景 + 2 个边界场景）
- 路线图阶段 4：红线测试用例从"对抗性测试用例覆盖每条红线"（模糊）量化为"≥22 个"（17 条红线对抗性触发 + 5 个边界模糊场景）
- 路线图阶段 7：功能测试用例从 5 个增加到 10 个（5 种输入类型各 2 个）；新增 IC-03 接口格式模拟测试（≥3 个 mock 用例）；新增跨技能端到端测试（≥3 个场景）；新增边界测试项（参数不足追问）；阶段 7 最小测试集量化为 ≥46 个用例
- 路线图质量门禁汇总：新增"v2 版本最小测试集总量目标"表，整体目标 ≥75 个测试用例
- 路线图阶段 7 验收门槛：新增 IC-03 模拟测试和跨技能端到端测试两项通过条件

**P2 — ACE 并行窗口表述修正（已完成）**

- 路线图：将"8.1-8.5 可与阶段 5-6 并行推进，仅需在阶段 7 集成验证时完成联调（8.6）"修正为明确区分内容编写并行和接口联调依赖关系，指出阶段 7 的 IC-03 模拟测试已为联调预检

**P3 — IC-03 字段约束表（已完成）**

- interface-contracts.md：为 IC-03 契约新增结构化字段约束表（请求参数 8 项约束 + 响应参数 8 项约束），包含字段名、类型、必填/可选、取值范围/枚举、降级行为/备注
- 接口契约版本从 v1.0 升级至 v1.1
- 契约变更日志新增 v1.1 条目

---

### 后续补充：飞书映射方案开放性重构（2026-06-12）

在第三方审核建议评估完成后，针对映射方案的架构审视发现了一个更深层的问题：飞书知识库为个人持有，而技能合集的目标是开放性应用，两者之间存在路由衔接的结构性风险。经讨论确认以"开放性"为基本准则，对映射方案进行了以下重构：

**标题与设计原则**：文件标题从"飞书规范标准 × 装配式装修技能"改为"规范标准知识管理 × 装配式装修技能"。新增"基本准则：开放性"声明，明确技能合集不依赖任何单一外部平台的账号权限、API 可用性或个人数据。

**映射架构总览**：新增"1.1 知识源架构（平台无关）"图，建立三级知识源体系——权威源头（国家标准全文公开系统等公开渠道）为正式来源，飞书个人云盘降级为可选入口，建仓标准库（shared/standards-pdf/）为仓库内自包含的正式资产。新增"运行时自包含原则"声明。

**数据流设计**：第三节全面重写。标准知识入库流从"飞书→技能的单向流"改为"权威源头→建仓标准库→技能文件"的平台无关流程。标准更新监控流从"监控飞书文件 modified_time"改为"监控国家标准全文公开系统发布公告"。新增"3.4 建仓标准库目录规划"，定义了 shared/standards-pdf/ 的分类目录结构和文件命名规则。

**归属层规则**：所有"飞书来源"表头统一改为"参考来源"，所有"飞书文件"表头改为"标准文件"。

**实施优先级**：去除"从飞书下载"的强依赖表述，改为多来源获取（国家标准全文公开系统、住建部官网、个人飞书云盘均可）。

**索引表**：从"飞书文件夹→技能归属"改为"标准分类→技能归属"。新增"建仓路径"列指向仓库内归档位置。folder_token 列降级为"个人参考"，标注为开发便利工具而非系统依赖项。增加版权合规说明。
